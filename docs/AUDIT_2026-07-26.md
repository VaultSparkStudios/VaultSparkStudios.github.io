<!-- generated-by: session S293 /audit -->
<!-- generated-at: 2026-07-26 -->
<!-- project: vaultsparkstudios-website · type=app · audience=public-live · SPARKED -->
<!-- rubric: product · lens: app-release-gate + web canon (011/041/047/048/045 + legal/IP) -->

# Audit — 2026-07-26 · Session 293

**Profile:** VaultSpark Studios website · `app` · `public-live` · SPARKED · product SIL rubric ·
direct-to-main (staging=hetzner verified first) · sanitize-before-push.

**Baseline (measured this session, not inherited):**

| Signal | Value | Source |
|---|---|---|
| Doctor | **15/15 passing**, blockingFailing 0 | `node scripts/ops.mjs doctor` |
| Build check | 226 steps (baseline run in flight at audit time) | `npm run build:check` |
| SIL | 999/1000 (S292) | `SELF_IMPROVEMENT_LOOP.md` |
| Worker route provenance | **`mismatch` · 0/5 matched · 5/5 reachable** | `api/worker-route-provenance.json` |
| Evidence graph | 10 nodes, 3 machine consumers, **0 human/agent projections** | `config/evidence-graph.json` |
| Genius list | 2 NOW · 8 deferred/gated | `docs/GENIUS_LIST.md` |

## Premise verification (no phantom items)

Every ranked item below was checked against **live code in this repo**, not against a prior
session's claim. Items whose premise failed verification are recorded in
[Rejected / not-real](#rejected--premise-failed-verification) — an honest rejection is a
recorded win, not a silent skip.

---

## Ranked plan

### NOW — unblocked, local, shippable this session

#### 1. [OBS/P1] Route-provenance history and incident duration — score 96

**Premise — VERIFIED.** `scripts/build-worker-route-provenance.mjs` derives a *point-in-time*
receipt (`deriveReceipt`) and `--check` re-derives that same committed snapshot against the
current Worker source hash. There is no history array, no transition record, and no duration
anywhere in `api/worker-route-provenance.json`. Confirmed by reading the full module and the
emitted feed.

**Why it matters.** Production has matched **0/5** expected Worker route semantics since the
2026-07-03 out-of-band clobber. Today that fact is a *level* — a snapshot that reads the same
on day 1 and day 23. An incident with no measured duration cannot create pressure, cannot be
ranked against other work, and cannot be honestly reported on a public trust surface. The
S275 forcing-function needs a clock, not another gauge.

**Ship.** An append-only, privacy-safe semantic-change ledger:
- Records **only state transitions** (per-route `matched` flips and overall state changes) —
  never one row per probe, so the ledger stays small and every row is meaningful.
- Computes **incident open/close and duration** per route and for the receipt as a whole.
- Retains **no response bodies, headers, cookies, identifiers, or URLs** — the existing
  `validatePrivacy` forbidden-field contract is extended to the ledger, not bypassed.
- `--self-test` + `--check` gated; wired into the evidence graph and the publish cascade so it
  cannot strand (the S291 cascade-drift class must not return through a new door).

**Done =** ledger artifact exists, self-test green, `--check` in sync, evidence-graph node
registered, `build:check` step count rises and stays EXIT 0.

---

#### 2. [AUTOMATION/P1] Evidence-graph human/agent projection — score 93

**Premise — VERIFIED.** `config/evidence-graph.json` holds 10 nodes and is consumed only by
`check-evidence-graph.mjs`, `check-publish-cascade-coverage.mjs`, and `pre-push-scan.mjs` —
all machine gates. `grep` for a rendered diagram returns nothing: no `docs/*.md` in this repo
contains a mermaid fence, and `.well-known/` exposes `entity-graph.json` but no evidence
relation view. The graph is the single declarative source that drives build order, pre-push
closure, and 27 publishers, and **no human or agent can read it without parsing JSON.**

**Why it matters.** CANON-048 is explicit: every surface is built for humans *and* AI agents.
The most load-bearing structural artifact in this repo is currently legible to neither. A
projection also makes the graph *reviewable* — cascade mistakes become visible before they
strand an artifact.

**Ship.** One generator, two projections, one validated source:
- **Human:** `docs/EVIDENCE_GRAPH.md` — a mermaid dependency diagram plus a node table
  (output · builder · cascade participation).
- **Agent:** `.well-known/evidence-graph.json` — an explicit relation view (`dependsOn` /
  `feeds` edges resolved, not raw glob sources) alongside the existing entity graph.
- Both derived **only after `validateEvidenceGraph()` passes** — never project an invalid graph.
- `--self-test` + `--check` gated; registered in the evidence graph itself (the graph describes
  its own projection).

**Done =** both projections generated from source, self-test green, `--check` in sync, adding a
node to `config/evidence-graph.json` provably changes both outputs.

---

### SECOND-ORDER — innovation candidates generated from items 1–2

#### 3. [OBS/P1] Edge-integrity panel on the public status surface — score 90

**Premise — VERIFIED.** `api/public-status.json` (schema 1.0, `build-public-status.mjs`) carries
`studio` + `nervousSystem` only: repo counts, sparked/forge/vaulted, last Ark broadcast, last
shipped session. It contains **zero edge/route health**. So the public `/status/` page tells a
visitor how many repos exist while saying nothing about the five edge routes that have been
serving wrong semantics for over three weeks.

**Why it matters.** This is an observability-honesty gap on a *public trust surface* — the
exact failure mode CANON-031 exists to prevent. It is only shippable **because** item 1 creates
the duration: a status panel that says "degraded" without a since-when is noise; one that says
"degraded, N days, this many routes, here is the source" is evidence. Second-order by
construction — item 1 is its input.

**Ship.** Extend `build-public-status.mjs` with an `edgeIntegrity` block derived from the
route-provenance receipt + the new history ledger (matched/total, state, open-incident duration,
`observedAt`), render it on `/status/`, and keep every value traceable to a committed feed. No
new hand-seeded numbers. Honest-dark when the ledger has no observations.

---

#### 4. [AUTOMATION/P2] Projection-coverage structural gate — score 84

**Premise — DERIVED (will verify at implementation).** Items 2 and 3 both add derived surfaces
fed by a source that a future session will extend. The S291 lesson is explicit: fix the class at
the producer with a *structural* gate, never an exception list.

**Why it matters.** Without it, the next node added to `config/evidence-graph.json` silently
fails to appear in the human diagram or the agent relation view, and the projections rot into
the "signal whose producer was never built" failure mode.

**Ship.** A structural check asserting every graph node appears in **both** projections (and
that neither projection carries a node absent from the validated source), self-testing in both
directions — it must fail when a node is dropped *and* when a phantom node is added.

---

### DEFERRED — gate verified genuine, not a silent skip

| Item | Gate | Verified this session |
|---|---|---|
| Restore production Worker `/v/rum` (P0 incident) | Founder — auth/security production deploy under an explicit fail-closed hold (CANON-019) | Yes. `cloudflare` deploy capability is READY in the gateway; the *only* gate is the founder hold. Not overridden. Item 1 makes the cost of the hold measurable, which is the correct agent-side action. |
| Candidate artifact Merkle manifest | Needs the next runtime candidate from Supabase/provider reconciliation | Yes — no new candidate exists |
| Privacy-safe provider ceremony trace compiler | Needs real provider access | Yes — `hashmark.supabase` READY but management/function authority for `fjnpzjjyhnpmunfoycrp` is not in the gateway |
| Forge devlog publication (×2) | Founder voice — never auto-published per AGENTS.md | Yes |
| TT-ENFORCE-REPROBE | Amber-soak; warm rows must age out | Yes |
| Social Dashboard bidirectional mirror | Cross-repo write — Ark cargo only (CANON-018) | Yes |
| Richer IGNIS public-safe layer | Founder public-safe decision | Yes |

### Rejected / premise failed verification

| Candidate | Why rejected |
|---|---|
| "Revenue signals stale ⛔" (startup brief signal) | **Phantom.** Doctor's live probe reads `Revenue signals freshness — 5d old ✓`. The brief's ⛔ is a renderer path miss against a portfolio-owned file, not project debt. Recorded, not "fixed" by touching data. |
| "Compliance 31/36 (86%) ⛔" | **Not self-debt.** Root cause diagnosed S291: `check-sitemap-compliance.mjs` (sibling-owned, studio-ops) false-negatives static `<page>/index.html` legal/contact pages that are present and deployed here. Ark cargo already shipped (`repo-question` `01JUDDNSAID43C1B5B481F0B03`). Editing a sibling tree to go green is forbidden. |
| Re-run/refresh existing observability reports | Refresh-a-report is not a root fix. Excluded by mission constraint. |

---

## Build order

1. **Item 1** — route-provenance history ledger (creates the duration everything downstream needs)
2. **Item 2** — evidence-graph projections (independent of 1; both register graph nodes)
3. **Item 3** — public status edge-integrity panel (consumes item 1's ledger)
4. **Item 4** — projection-coverage structural gate (locks 2 and 3 against silent rot)
5. Full `build:check` + doctor, direct exit codes, then closeout

## Non-negotiables carried into /implement

- No fabricated data to pass a gate. Honest-dark beats a green that lies.
- Root-fix at the producer; structural gates over exception lists.
- No sibling-repo tree edits — Ark cargo only.
- Every new derived artifact must be registered in the evidence graph *and* the publish cascade,
  or it will strand exactly like the four S291 artifacts did.
