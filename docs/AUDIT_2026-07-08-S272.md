# Audit — Session 272 — Startup Truth Saturation

Date: 2026-07-08  
Project: VaultSparkStudios.github.io  
Lens: public-live website · product rubric · startup/app-release truth

## Strategic Read

The primary Genius List is genuinely exhausted or gated: homepage Lighthouse 0.85 needs a focused trace-backed performance pass, Worker deploy is provider-token-scope gated, and founder/public-safe items remain founder-owned. The best local move is second-order startup truth: the one surface every session trusts was overstating context pressure and hiding context freshness.

## Ranked Plan

| Rank | Axis | Item | Premise verified against live code | Outcome |
|---:|---|---|---|---|
| 1 | Observability / Honesty | Startup context-meter percent truth | `context-meter --json` reported low single-digit usage while `STARTUP_BRIEF.md` rendered 6,622 / 1,000,000 tok as 70% used; `render-startup-brief.mjs` multiplied sub-1 percentages by 100. | Derive display percent from `usedTokens / limit`. |
| 2 | Process / Truth | Startup context-age fallback truth | `PROJECT_STATUS.lastUpdated` is current, but `CURRENT_STATE.md` has no `Last updated:` header, so the brief rendered `Context age ?d`. | Fallback to `PROJECT_STATUS.lastUpdated`, then latest SIL header. |
| 3 | Automation / Coverage | Startup meter percent regression gate | The existing freshness gate compared verdict/limit only, so a bad percent could pass if the verdict stayed `CONTINUE`. | Parse displayed `% used` and fail when it disagrees with token ratio. |

## Execution Log

| Item | Result | Evidence |
|---|---|---|
| Startup context-meter percent truth | Shipped. | Fresh `docs/STARTUP_BRIEF.md` now renders token counts as a ratio-derived percent (`12% used` for `117,132 / 1,000,000 tok` at S272 closeout), not a false high-pressure value. |
| Startup context-age fallback truth | Shipped. | Fresh `docs/STARTUP_BRIEF.md` now renders `Context age 0d` from `PROJECT_STATUS.lastUpdated`. |
| Startup meter percent regression gate | Shipped. | `node scripts/check-startup-meter-freshness.mjs --self-test` passes and includes a bad-percent fixture that fails against token ratio. |

## Honest Ledger

- Primary local Genius List items were not force-shipped; remaining items are founder, credential, provider, or field-soak gated.
- Homepage Lighthouse 0.85 remains evidence-gated.
- Worker deploy remains a Cloudflare token-scope repair, not local code failure.

## Verification

- `node --check scripts/render-startup-brief.mjs` — exit 0.
- `node --check scripts/check-startup-meter-freshness.mjs` — exit 0.
- `node scripts/check-startup-meter-freshness.mjs --self-test` — exit 0.
- `node scripts/render-startup-brief.mjs` — exit 0.
- `node scripts/validate-brief-format.mjs docs/STARTUP_BRIEF.md` — exit 0.
- `node scripts/check-startup-meter-freshness.mjs` — exit 0.
- `node scripts/smoke-startup-scripts.mjs` — exit 0, 40/40 checks passing.
