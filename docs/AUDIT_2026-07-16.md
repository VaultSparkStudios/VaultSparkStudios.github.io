<!-- generated-by: render-audit-md.mjs (R-H14) — DERIVED FROM AUDIT_2026-07-16.json; do not hand-edit -->
<!-- generated-at: 2026-07-16 -->
<!-- project: vaultsparkstudios-website · type: app -->

# Project Audit — VaultSparkStudios.github.io

> One ranked improvement plan covering 9 axes. Top items first.
> Source of truth: this file is derived from the JSON sidecar. Edit the JSON, then re-render.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 6
- Combined Priority: 174.70
- Top 3: public-ai-source-of-truth · genius-carry-classifier · oracle-public-feed-dedup

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov | Priority | Status | Item |
|---|:-:|---|---|:-:|:-:|:-:|:-:|---|
| 1 | 🔥 | aiIntegration | 2.5h | 10 | 8 | 36.9 | ✓ | **public-ai-source-of-truth** — Make public AI manifests derive from the committed public ecosystem. The dual-audience discovery layer currently depends on gitignored local IGNIS state, so local checks can disagree with the committed site while CI silently skips generation. The repo already has the correct public-safe source: api/ecosystem-state.json. **Recipe:** Switch both generators to api/ecosystem-state.json, fail closed if the committed public source is absent or malformed, keep deterministic checks active in CI, and add self-tests/contract coverage proving ignored IGNIS state cannot affect public output. |
| 2 | 🔥 | speedOrganization | 1.5h | 9 | 7 | 34.9 | ✓ | **genius-carry-classifier** — Stop the Genius List from suppressing ordinary tasks that mention a carry. The top verified S282 Lighthouse fix is absent from the queue because one explanatory sentence contains the word carry. A ranking system that deletes its best task on prose coincidence cannot be trusted. **Recipe:** Extract a precise consolidated-carry classifier that recognizes only carry metadata/titles, reuse it in the generator, add behavioral tests for true meta-carries and ordinary prose, then regenerate and assert the S282 Lighthouse item ranks. |
| 3 | 🔥 | ux | 2h | 8 | 7 | 28.0 | ✓ | **oracle-public-feed-dedup** — Make Oracle read the deployed public feeds once, without a 404 stampede. Oracle eventually renders from public fallbacks, but it first requests gitignored paths that cannot exist in production and repeats the same fetch family across two runtimes. Visitors pay in failed requests, console noise, and slower answers without receiving richer intelligence. **Recipe:** Default production code to the already-public /api feeds, introduce a shared promise cache so repeated consumers coalesce, preserve an explicit optional local-preview override rather than probing dead URLs, and add a browser/static contract that forbids production /ignis/output fetches. |
| 4 | 🔥 | speedOrganization | 2h | 9 | 6 | 27.0 | ✓ | **uptime-cron-precommit-contract** — Make the uptime publisher validate its staged truth before a skip-CI commit. The half-hour publisher can land generated data that no workflow validates, arming a failure for the next ordinary push. Its own publication boundary should prove the exact artifacts it stages. **Recipe:** Add a pre-commit validation block using the existing focused checks, make any red abort before git commit, and add a workflow structural test that fails if generation can reach a skip-CI commit without validation. |
| 5 | 🔥 | speedOrganization | 3h | 10 | 6 | 25.8 | ✓ | **lighthouse-volatility-single-source** — Give both Lighthouse gates one fail-closed volatility policy. Two blocking gates judge the same homepage performance signal differently. One correctly corroborates known lab noise; the other hard-fails a single runner dip, producing red CI on byte-identical code. **Recipe:** Extract the volatility decision into a shared pure module, make both gates consume it, preserve the 0.76 floor and >=2-of-5 slow-bleed tripwire, and expand self-tests for single noise, persistent regression, thin history, nonvolatile routes, and unproven corroborators. |
| 6 | ⚡ | speedOrganization | 1.5h | 8 | 5 | 22.1 | ✓ | **local-event-ledger-truth** — Stop closeout from claiming a sibling event mirror that never occurs. The local event ledger is intentionally the CI-readable project source, yet closeout says it mirrored Studio Ops while copying the file onto itself. That false message created a bogus 893-vs-1278 blocker and invites a CANON-018 cross-repo write fix that would be worse. **Recipe:** Replace the mirror step with local-ledger validation/counting, remove the post-commit self-copy, keep appendEvent local, and add a dry-run/self-test assertion that no path outside PROJECT_ROOT is written. |

## Depth Ladder (per item: L1 minimal · L2 solid · L3 genius)

> `/implement` selects the depth that fits remaining budget, and climbs the ladder
> on already-shipped items when the session still has budget (S175 saturation).

### public-ai-source-of-truth
- **L1 · minimal** _(~30m)_ — Change the two source paths to api/ecosystem-state.json.
- **L2 · solid** _(~1.5h)_ — Change sources, update comments/failure behavior, and verify both generators plus discovery contracts.
- **L3 · genius** _(~2.5h)_ — Also add a structural regression assertion that the public generators never reference ignis/output and prove build-check parity from a clean committed source.

### genius-carry-classifier
- **L1 · minimal** _(~15m)_ — Narrow the regex to carry tags only.
- **L2 · solid** _(~45m)_ — Extract the classifier and add positive/negative assertions to the existing startup smoke.
- **L3 · genius** _(~1.5h)_ — Add corpus-level proof against the live task board and ensure future queue generation fails when an actionable top carry is silently lost.

### oracle-public-feed-dedup
- **L1 · minimal** _(~30m)_ — Replace dead primary URLs with public API URLs.
- **L2 · solid** _(~1.25h)_ — Add shared promise caching and remove duplicated fallbacks across the two Oracle runtimes.
- **L3 · genius** _(~2h)_ — Also add a network contract/test proving no production Oracle request targets gitignored paths and every panel still hydrates.

### uptime-cron-precommit-contract
- **L1 · minimal** _(~30m)_ — Run focused --check commands before git add.
- **L2 · solid** _(~1.25h)_ — Add validation plus a workflow contract self-test.
- **L3 · genius** _(~2h)_ — Derive the staged-publisher contract from the workflow and prove missing/reordered validation fails the gate.

### lighthouse-volatility-single-source
- **L1 · minimal** _(~1h)_ — Teach check-lighthouse-trend the existing config flag and tripwire.
- **L2 · solid** _(~2h)_ — Add parity tests matching route-tier behavior without lowering any threshold.
- **L3 · genius** _(~3h)_ — Extract a shared policy module and prove both gates return the same classification for the same route/run/history corpus.

### local-event-ledger-truth
- **L1 · minimal** _(~30m)_ — Rename the step and remove the copy.
- **L2 · solid** _(~1h)_ — Validate local NDJSON and update post-commit reconcile comments/paths.
- **L3 · genius** _(~1.5h)_ — Also add a write-boundary test proving the closeout event path resolves inside PROJECT_ROOT.

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

- public-ai-source-of-truth
- genius-carry-classifier
- lighthouse-volatility-single-source

## Skipped (Priority < 1.5)

- utc-date-generator-normalization — Rejected on verification: build-agents-json derives dayOf from committed feed generatedAt values and the current check has no clock read. No reproducible midnight fixture exists; patching it now would be speculative.
- play-next-conversion-redesign — Correctly deferred: check-cta-readiness reports 20 more true-viewport post-epoch impressions are required; no trustworthy conversion denominator exists yet.
- homepage-critical-css-split — Correctly founder-device gated: applied LCP is already about 1.2s and the 47KB split is FOUC-risky on the brand anchor.
- trusted-types-enforcement — Correctly soak/founder-device gated: current TT readiness remains AMBER and live reports are nonzero.
- wishlist-momentum-proof — Credential premise is cleared, but the remaining low-count public-optics threshold is an explicit founder decision.
- rank-economy-expansion — Rejected without user evidence: membership/rank systems are load-bearing and no fresh behavioral signal justifies economy mutation.
- richer-ignis-public-layer — Founder public-safe exposure decision remains explicit; the Oracle de-noise item preserves current public data without expanding exposure.
- model-cost-optimization — Rejected: cost is flat-rate/notional and no measured API burn supports a quality-reducing optimization. The only real request waste is handled by Oracle feed dedup.

## Execution Log

### public-ai-source-of-truth
- `2026-07-16T05:23:41.488Z` · **shipped** · Both public discovery generators now fail closed on committed api/ecosystem-state.json publicSafe:true; 18 shards + agents.json regenerated; startup smoke pins the source contract.

### genius-carry-classifier
- `2026-07-16T05:23:41.488Z` · **shipped** · Extracted precise classifier with four behavioral cases; refreshed live queue now retains the S282 Lighthouse task while suppressing true carry meta rows.

### oracle-public-feed-dedup
- `2026-07-16T05:23:41.488Z` · **shipped** · Oracle now uses a shared promise cache over public API feeds, with explicit localhost-only private preview; production IGNIS fetch probes are structurally forbidden.

### uptime-cron-precommit-contract
- `2026-07-16T05:23:41.488Z` · **shipped** · Skip-CI publisher now runs five focused contracts before staging and masks no staged-artifact generator failure; 11/11 publisher/uptime self-tests pass.

### lighthouse-volatility-single-source
- `2026-07-16T05:23:41.488Z` · **shipped** · Both blocking Lighthouse gates consume one fail-closed policy; 16 route-tier and 21 trend tests cover noise, persistence, thin history, recurrence, and nonvolatile routes.

### local-event-ledger-truth
- `2026-07-16T05:23:41.488Z` · **shipped** · Closeout validates the project-local NDJSON source, removes both false self-mirrors, enforces the write boundary, and reports the SIL v3 denominator truthfully.

