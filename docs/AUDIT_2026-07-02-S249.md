<!-- generated-by: /arc /audit · Session 249 · 2026-07-02 -->
# AUDIT — Session 249 · 2026-07-02

Project: `VaultSparkStudios.github.io` (website · SPARKED · public-live)
Method: every item's premise verified against **LIVE code/data** before ranking. Phantom items rejected (recorded as WINs, not silent skips). Infrastructure rubric applied to the doctor/observability layer.

Prior SIL: 999/1000 (S248). Entry signals: 116/116 tests · doctor 11/15 (3 probes showing FAIL) · compliance 32/36. **The 3 doctor "failures" are the headline finding — all sibling-owned, mis-reported as this repo's blocking debt.**

---

## TIER 1 — Root-fixes (ship this session)

### 1. [INFRA/P0] Doctor S181 self-vs-sibling exit contract — observability-honesty
**Premise (VERIFIED):** `node scripts/ops.mjs doctor` shows 3 failing probes — Compliance validation (32 passed · 4 failed · 6 issues), Compliance velocity (32/36), Launch readiness (2 blockers). Traced every one to **sibling projects**, not this repo:
- validate-compliance → MindFrame · Hashmark · SHADOW · ATLAS (missing TRUTH_AUDIT "Overall status" line)
- launch-ready → `veilos` (liveUrl + Stripe)

This repo (`vaultsparkstudios-website`, self slug) passes clean. The canonical `../vaultspark-studio-ops/scripts/validate-compliance.mjs` already carries the **S181 exit contract** — `owner` classification + `exit 2 = self-FAIL · 1 = sibling-WARN · 0 = clean` — and its `run-doctor.mjs` `validate` probe parses `pass: code===0||code===1, warn: code===1`. This repo's **local copies are stale**: the validator `process.exit(1)` on ANY violation (incl. sibling), and the probe treats exit 1 as fail. Result: the board hard-fails on debt this repo must NOT fix (no-sibling-tree-edits). **Observability is lying.**

**Fix (root, this-repo tree only):** port the S181 self-vs-sibling classification into `scripts/validate-compliance.mjs` (self = `path.resolve(localPath) === path.resolve(root)`, case-insensitive on win32 — a robustness improvement over the canonical's exact `===`, given the registry stores `VaultSparkStudios.github.io` while cwd is lowercase). Sync the `validate` probe parse in `scripts/run-doctor.mjs`. Do **not** ship the Ark-nudge path into the doctor route (canonical gates it behind an explicit flag). NEVER edit the sibling trees.
**Outcome:** doctor blockingFailing → 0 honestly; sibling debt surfaces as WARN, not FAIL.

### 2. [ENGAGE/P1] play-next honest viewport-impression instrumentation
**Premise (VERIFIED):** `api/dead-ctas.json` → `deadCount:1`, play-next `shown:37 / click:0` (post-epoch `2026-06-18`). Read `assets/cross-game-play-next.js`: `play-next:shown` is emitted inside `reveal()` — which fires on the **engagement trigger** (scroll≥60% OR 45s dwell OR exit-intent), NOT on the card actually entering the viewport. The card `append`s to the **bottom of `main`**. So a 45s-dwell or exit-intent trigger emits an "impression" for a card the visitor never scrolled to. **The 37/0 denominator counts trigger-fires, not views** — the exact dishonest-metric class the genius item flags ("instrument viewport-impression vs click *before any redesign*").
**Fix (root):** decouple mount-animation from impression-emit. Keep the engagement-gated reveal (visual), but move `play-next:shown` + `cta:variant:play-next:N` into an `IntersectionObserver` that fires once when the card is genuinely ≥50% in the viewport. Bump the rollup family epoch (`scripts/rollup-rum-ux.mjs`) to `2026-07-02` so the honest-impression window starts fresh (the S209 epoch mechanism, purpose-built for a semantics change). No new event names → no allowlist change.
**Outcome:** the impression metric becomes a true viewport view; next session's 0-click read is interpretable (placement vs copy) instead of noise.

### 3. [POLISH/P1] VEILOS + Vorn hero cover art — S248 spotlight craft parity
**Premise (VERIFIED):** S248 recurated the hero spotlight to Call of Doodie · MindFrame · VEILOS · Vorn · VaultSpark Football GM. `build-hero-portfolio.mjs COVERS` + `assets/covers/` have doodie/footballgm/mindframe but **no veilos, no vorn** → 2 of 5 spotlit tiles render accent-gradient-only. Authentic metadata pulled from `api/public-intelligence.json`: VEILOS = Privacy Product, `#22d3ee`, veilos.io; Vorn = Social Agent Platform, `#a78bfa`, joinvorn.com.
**Fix:** add 2 bespoke specs to `scripts/build-game-covers.mjs` (the studio's one cover rasterizer), render png/webp/avif via sharp@0.34.5 (trusted devDep, zero new deps), update its self-test count, wire slug→key in `build-hero-portfolio.mjs COVERS`. Regenerate hero.
**Outcome:** all 5 spotlight tiles carry bespoke cover art.

## TIER 2 — Second-order innovation (generated, not on the list)

### 4. [META/P1] Phantom-carry suppressor
**Premise (VERIFIED):** genius item #3 "Forge Window naming propagation" (score 86) is a **decided phantom** — re-rejected S218 (D-S218.4), S221 (D-S221.5), S222 (D-S222.3): S185 renamed the public label to "Studio Pulse", the live site is correct, and `check-s151-contracts` enforces it. The generator keeps burning a **top-5 slot every session** because it reads TASK_BOARD/PROJECT_STATUS text, blind to the superseding DECISIONS entry. This is the recurring-waste root cause — a second-order fix, not on the list.
**Fix:** a phantom-carry suppression pass (registry of decided-phantom carry signatures with the superseding decision id) that the genius generator consults, so a re-surfaced phantom is filtered (or demoted with its rejection provenance) instead of ranked. Ship with a self-test.
**Outcome:** the hit list stops re-proposing settled rejections; a genuine item takes the reclaimed slot.

## TIER 3 — Verify + close stale carries

### 5. [VERIFY] Post-push CI + close 248-session-old S245/S246 carries
Latest `pages build and deployment` = success (18:29:29Z); CI Status Beacon green. Verify live at closeout; close the S245/S246 VERIFY carries that have polluted the hit list for 248 sessions.

---

## Honest deferrals — WINs to record (not silent skips)

| Carry | Score | Verdict | Evidence |
|---|---|---|---|
| Forge Window naming propagation | 86 | **PHANTOM** (4th rejection) | D-S218.4/221.5/222.3 — S185 → "Studio Pulse"; live site correct |
| Content-drift P1 cleanup | 84 | **RESOLVED** | `check-project-info-drift` → 0 P0 · 0 P1 · 0 P2 across 19 pages |
| INP root-fix when clean field data lands | 84 | **TIME-BLOCKED** | Externally exempt until 2026-07-09 (S247 filter deployed 07-02); today 07-02 |

## TIER 4 — If context remains
- [OPS/P2] Atlas registry freshness reconciliation (advisory — owning source or Ark)
- [HYGIENE/P2] TASK_BOARD size strategy (297KB, design a safe archival split)

## Recommended build order
1 (P0 infra honesty) → 2 (engagement instrument) → 3 (spotlight craft) → 4 (2nd-order meta) → 5 (verify/close) → Tier 4 as context allows.
