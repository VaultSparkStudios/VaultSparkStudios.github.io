<!-- generated-by: render-audit-md.mjs (R-H14) — DERIVED FROM AUDIT_2026-06-20-S213.json; do not hand-edit -->
<!-- generated-at: 2026-06-21 -->
<!-- project: unknown · type: unknown -->

# Project Audit — Unknown

> One ranked improvement plan covering 9 axes. Top items first.
> Source of truth: this file is derived from the JSON sidecar. Edit the JSON, then re-render.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 8
- Combined Priority: 0.00
- Top 3: —

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov | Priority | Status | Item |
|---|:-:|---|---|:-:|:-:|:-:|:-:|---|
|  | 🔥 | ux+ai | 1h | 9 | 9 | 89.4 | · | **ignis-starter-personalization** — Personalize IGNIS starters by vs_last_game signal. The 5 starter prompts are static for all visitors, but vs_last_game (set in S212 W3 by ambient-loader.js) already knows whether this person last played Call of Doodie, Football GM, or Forge. A returning CoD player who opens IGNIS and sees 'What makes Call of Doodie different?' converts the warm session signal into immediate relevance — no typing needed. This closes the S212 personalization loop: the game-visit tracking exists, the starters exist, but they never talk to each other. **Recipe:** Read localStorage.getItem('vs_last_game') at renderStarters() time; swap in 1–2 game-specific questions for the matching slug (cod/fgm/forge) while keeping 3 general starters. Add data-slug attr on each button for L3 analytics merge. |
|  | 🔥 | ux | 1h | 8 | 7 | 88.3 | · | **ignis-no-result-dynamic** — Replace static no-result fallback with dynamic cluster suggestions. When IGNIS finds no strong match it shows a hardcoded string: 'Try membership, latest ships, security, Oracle, or a game name.' These 4 suggestions were written months ago and may not reflect what's actually in the current 44,691-entry index. A dynamic fallback that pulls the top clusters from api/oracle-query-clusters.json stays current, surfaces what visitors actually search for, and emits an oracle:no_result RUM event — closing the only gap in the IGNIS observability loop (successes are measured, failures are invisible). **Recipe:** At the !result branch (line 302 ignis-answer-engine.js), load api/oracle-query-clusters.json and surface the top 3 cluster labels as tappable suggestion chips. Emit oracle:no_result RUM on each zero-result query. |
|  | 🔥 | ai+featureDepth | 30m | 9 | 7 | 49.7 | · | **ignis-starter-analytics** — Add slug suffix to oracle:starter_click RUM for per-starter attribution. oracle:starter_click fires for all 5 starters identically — the studio can count how many starter clicks happen but cannot tell which starter drives engagement. This matters because the S212 bet is that certain starters create a stronger IGNIS habit loop. Without per-starter attribution the personalization planned in ignis-starter-personalization has no feedback signal to prove which game-specific questions work best. **Recipe:** Change onClick to emit('oracle:starter_click:' + slugify(q)) where slugify converts the question to a lowercase-hyphen key. Add prefixAllowlist('oracle:starter_click', ...) to Worker. In the rollup, track per-slug counts in api/oracle-starter-insights.json. |
|  | 🔥 | featureDepth+newFeature | 2h | 8 | 9 | 54.0 | · | **push-game-context-store** — Store game interest context in push subscription KV for segmented dispatch. The first real push dispatch (next session's headline) will blast a single message to all subscribers regardless of their interests. A CoD player who gets a Football GM update notification has a high unsubscribe risk — and unsubscribing a push subscriber is permanent. Storing the visitor's page context at subscribe time (costs 0 extra bytes in the existing KV entry) makes every future dispatch richer without requiring the visitor to do anything different. **Recipe:** In push-subscribe.js POST body, append route: location.pathname and lastGame: localStorage.getItem('vs_last_game')||null alongside sub.toJSON(). In the Worker /v/push-subscribe handler, store these in the KV value. In notify-subscribers.mjs, accept --game flag to filter by stored lastGame. |
|  | ⚡ | featureDepth+feedbackLoop | 2h | 7 | 7 | 36.8 | · | **post-push-click-tracking** — Add service-worker push click → RUM beacon for delivery + open rate. The push notification pipeline can dispatch but has no visibility into whether pushes are received, opened, or clicked. The service worker receives push events and can respond to notificationclick — wiring a navigator.sendBeacon('/v/rum', ...) in the click handler closes the loop: the studio can measure open rate per campaign and compare push CTR against in-page CTAs. **Recipe:** In assets/service-worker.js (or sw.js), add a notificationclick handler that beacons push:clicked with the notification tag. Add push:clicked and push:received to Worker RUM_UX_EVENTS. Wire push:dispatched:{count} log in notify-subscribers.mjs. |
|  | 💡 | speed | 15m | 4 | 2 | 13.7 | · | **funnel-summary-refresh** — Regenerate funnel-summary.json from latest 6 days of RUM data. api/funnel-summary.json is dated 2026-06-14 — 6 days of engagement signal are invisible to any surface that reads this file. With the game quiz personalization (S212 W3) and IGNIS starters (S212 W4) now live, the first cohort of quiz:personalized + oracle:starter_click events should be visible in a fresh rollup. **Recipe:** Run node scripts/rollup-rum-ux.mjs to regenerate. Verify the new events from S212 appear in the families array. |
|  | 💡 | featureDepth | 4h | 5 | 4 | 11.6 | · | **nav-catalog-derive** — Derive Projects/Games nav dropdowns from catalog (carry D-S208). The nav dropdowns for Games and Projects are currently hardcoded arrays (NAV_GAMES, NAV_PROJECTS) in propagate-nav.mjs that can drift from the catalog. S210 closed the sync gate (check-nav-catalog-sync warns on drift) but the root fix — deriving the arrays from catalog data — was deferred due to the non-catalog-paged projects complication. This carry reduces the maintenance surface permanently. **Recipe:** Design the catalog∪extra-paged merge: maintain a EXTRA_NAV_PAGES constant for non-catalog paged entries (signal-log, vault-pipeline, etc.). Derive NAV_GAMES + NAV_PROJECTS by unioning catalog SPARKED/FORGE entries with EXTRA_NAV_PAGES. Propagate to 99+ pages. Update check-nav-catalog-sync to validate the derived list. |
|  | 💡 | security | 1h | 6 | 3 | 11.4 | · | **sibling-compliance-ark** — Ship Ark cargo to fix VOID/SHADOW/Hashmark protocol compliance gaps. Three sibling repos (VOID, SHADOW, Hashmark) have drifted from the v3.3 protocol template — missing TRUTH_AUDIT headers, stale session templates, missing PROJECT_STATUS fields. These 3 failures drag the studio compliance score from 100% to 91% for the third consecutive day. Ark cargo of type canon-update or agent-handoff to studio-ops is the only CANON-018-compliant path to fix these without direct cross-repo writes. **Recipe:** node ../vaultspark-studio-ops/scripts/ark.mjs ship --type agent-handoff --to studio-ops --payload with the specific gaps per repo. studio-ops propagation handles the template updates. |

## Depth Ladder (per item: L1 minimal · L2 solid · L3 genius)

> `/implement` selects the depth that fits remaining budget, and climbs the ladder
> on already-shipped items when the session still has budget (S175 saturation).

### ignis-starter-personalization
- **L1 · minimal** _(~30m)_ — Read vs_last_game at mount; prepend 1 game-specific starter before the 5 general ones. Minimal code delta, immediate personalization lift.
- **L2 · solid** _(~1h)_ — Full 3-game starter packs (3 game-specific + 2 general when vs_last_game set). Add data-slug attribute to each button for analytics merge (completes ignis-starter-analytics L2).
- **L3 · genius** _(~4h)_ — Topic-history analysis: parse vs_ignis_history query list for recurring terms (game names, features), weight matching starters higher. Requires a topic-extractor helper; no backend.

### ignis-no-result-dynamic
- **L1 · minimal** _(~30m)_ — Replace static string with random selection from the 5 existing STARTERS queries as fallback suggestions — no network request, instant improvement.
- **L2 · solid** _(~1h)_ — Fetch api/oracle-query-clusters.json (already on CDN), render top 3 cluster chip buttons. Add oracle:no_result to Worker RUM_UX_EVENTS so zero-result rate is measurable.
- **L3 · genius** _(~3h)_ — Intent-pivot: detect query entity type (name vs. question vs. keyword), offer reformulation hints ('Did you mean: ...?') using tokenized index title prefix matching. Surface most similar doc title as a rescue link.

### ignis-starter-analytics
- **L1 · minimal** _(~30m)_ — Emit oracle:starter_click:<slug> (5 bounded values), add prefixAllowlist entry in Worker. Rollup counts per slug appear in funnel-summary families.
- **L2 · solid** _(~1h)_ — Also track position (0–4) and whether the query produced a result: oracle:starter_click:<slug>:hit vs :miss. This closes the starter→answer→conversion funnel.
- **L3 · genius** _(~2h)_ — Build scripts/build-oracle-starter-insights.mjs → api/oracle-starter-insights.json (per-starter click rate + hit rate + downstream query continuation rate). Feed into the personalization decision for ignis-starter-personalization.

### push-game-context-store
- **L1 · minimal** _(~1h)_ — Store route: location.pathname in KV. dispatch --route /games/call-of-doodie/ filters to subscribers who subscribed on that page. Immediate segmentation by page context.
- **L2 · solid** _(~2h)_ — Store lastGame field from localStorage. notify-subscribers.mjs --game cod/fgm/forge filters by stored interest. Game-specific dispatch campaigns are now possible.
- **L3 · genius** _(~4h)_ — Multi-interest subscriber model: append gameSlug to existing interests[] array on each game page visit (requires a PATCH-style KV update on the Worker). Rich segmentation for newsletters, game updates, milestones.

### post-push-click-tracking
- **L1 · minimal** _(~30m)_ — Log dispatch count vs subscriber count to console + a .cache/last-dispatch-log.json file. Immediate visibility without a SW change.
- **L2 · solid** _(~2h)_ — SW notificationclick handler → sendBeacon push:clicked. Worker allowlist update. rollup push:* family in funnel-summary.
- **L3 · genius** _(~4h)_ — Full campaign attribution: include a campaign ID in notification data → SW preserves it in the click beacon → correlate open rate + downstream membership conversion per campaign.

### funnel-summary-refresh
- **L1 · minimal** _(~15m)_ — node scripts/rollup-rum-ux.mjs — regenerate and verify new event families appear.
- **L2 · solid** _(~1h)_ — Wire rollup-rum-ux into a daily GitHub Actions step so the summary never goes stale again.
- **L3 · genius** _(~2h)_ — Same as L2 plus a freshness check gate that fails build:check if funnel-summary is >3 days old (analogous to the perf-history freshness gate).

### nav-catalog-derive
- **L1 · minimal** _(~2h)_ — Document the EXTRA_NAV_PAGES list; prove which pages are non-catalog-paged. Design the merge algorithm before writing code.
- **L2 · solid** _(~4h)_ — Implement catalog union + EXTRA_NAV_PAGES constant in propagate-nav.mjs. Re-propagate 99 pages. Update sync gate to use the derived list.
- **L3 · genius** _(~6h)_ — Fully data-driven: remove EXTRA_NAV_PAGES by adding a navVisible:true field to each non-catalog-paged project's page meta. All nav items derive from one structure.

### sibling-compliance-ark
- **L1 · minimal** _(~30m)_ — Ship one Ark cargo per failing repo with the specific gap list. Studio-ops processes in next available slot.
- **L2 · solid** _(~1h)_ — L1 plus a targeted fix payload with the exact file edits needed so studio-ops can auto-apply rather than re-diagnose.
- **L3 · genius** _(~2h)_ — Propose a canon-update that adds a CI action to each repo that auto-validates against the v3.3 template on push — preventing future drift without manual checks.

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

- ignis-starter-personalization
- push-game-context-store
- post-push-click-tracking

## Skipped (Priority < 1.5)

- play-next-rotation — 0 impressions since epoch 2026-06-18 (only 2 days; data still accumulating). No verdict possible. Carry to next session when post-epoch RUM accrues.
- forge-devlog-publish — FOUNDER-VOICE-ONLY — never auto-published. Draft is ready in journal/_drafts/. Deferred permanently until founder publishes in their own voice.
- hero-glow-graduation — FOUNDER-DEVICE-GATED — mature surface requiring real-device verification before 100% flip. S164 carry unchanged.

