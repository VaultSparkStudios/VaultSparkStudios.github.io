<!-- generated-by: render-audit-md.mjs (R-H14) — DERIVED FROM AUDIT_2026-06-11-S188.json; do not hand-edit -->
<!-- generated-at: 2026-06-11 -->
<!-- project: unknown · type: unknown -->

# Project Audit — Unknown

> One ranked improvement plan covering 9 axes. Top items first.
> Source of truth: this file is derived from the JSON sidecar. Edit the JSON, then re-render.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 7
- Combined Priority: 0.00
- Top 3: —

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov | Priority | Status | Item |
|---|:-:|---|---|:-:|:-:|:-:|:-:|---|
|  | fire | ux |  | 9 | 5 | 34.1 | · | **sitewide-footer-dispatch** — . An email funnel that exists on the homepage only is a funnel for people who already found the homepage — it misses the visitor who lands on a game page from a search result and bounces. Because the honest-dark script is already sitewide, this is a pure markup-lift: add the dispatch column to the propagate-nav footer template and re-propagate. Outcome: the owned-list capture surface goes from 1 page to every page, with zero new JS and zero new vendor. **Recipe:** Lift the Studio Dispatch column (index.html lines 1874-1883: footer-col--dispatch + #footer-email-form + #footer-success) into buildFooter() in scripts/propagate-nav.mjs, placed before the closing footer-inner div. Run node scripts/propagate-nav.mjs to re-propagate to all pages (homepage's hand-placed column gets replaced by the canonical template one — single source). Verify the form renders on a non-home page and footer-dispatch.js binds to it. |
|  | fire | security |  | 7 | 8 | 31.0 | · | **rum-allowlist-integrity-gate** — . This is the bug that already bit the studio once and will again — instrumentation that looks shipped but measures nothing is worse than no instrumentation, because it manufactures false confidence in a conversion surface. A structural gate that diffs the two sets turns a silent runtime drop into a loud build-time failure. Outcome: no RUM event the team adds can ever be silently dropped or left dead, protecting every funnel measurement the last three sessions built. **Recipe:** New scripts/check-rum-allowlist.mjs: parse RUM_UX_EVENTS from cloudflare/security-headers-worker.js; grep assets/*.js for emitUx('name')/sendBeacon('/v/rum'...) literal event names; report (a) emitted-but-not-allowlisted = ERROR (silent drop), (b) allowlisted-but-never-emitted = WARN (dead config). Include a --self-test with in-memory fixtures. Wire into build:check as advisory-then-strict. |
|  | high | speed |  | 6 | 6 | 22.7 | · | **audit-freshness-in-plumbing** — . A discipline that depends on the agent remembering to run it is a discipline that erodes — the only durable version is one the build enforces. Making freshness structural means every future audit in this repo inherits the prior-art check for free, regardless of which agent or skill produced it. Outcome: the 'already-done audit item' failure mode is closed at the plumbing level, not the habit level. **Recipe:** Extend scripts/check-audit-staleness.mjs to auto-discover the newest docs/AUDIT_*.json, extract each item's distinctive phrase from its summary/recipe, and grep TASK_BOARD DONE + the asset corpus; emit an advisory warning listing any item whose phrase already appears as shipped. Add it to the build:check chain (advisory). Document the call as the canonical pre-score step at the top of the audit JSON's generatedBy provenance. |
|  | high | feedbackLoop |  | 6 | 4 | 18.2 | · | **proof-line-telemetry** — . A conversion surface with no telemetry can never be tuned or killed — it just accretes. Adding two allowlisted RUM names (shown + click) makes the microline's lift observable in the same /v/rum stream as the rest of the funnel, so a future session can A/B or retire it on evidence. Outcome: the proof microline joins the measured funnel instead of being a faith-based UI element. **Recipe:** In assets/proof-conversion-line.js add an emitUx helper (mirror footer-dispatch.js's sendBeacon to /v/rum) firing 'proof-line:shown' when the line mounts (only when a real fresh proof renders) and 'proof-line:click' on the CTA link. Add both names to RUM_UX_EVENTS in cloudflare/security-headers-worker.js with a privacy comment. The new rum-allowlist gate (rank 2) will then verify both ends stay in sync. |
|  | nice | ux |  | 7 | 5 | 13.5 | · | **flagship-product-storytelling** — . Routing more attention into a page that does not convert just moves the bounce downstream. A focused storytelling pass on the single most-trafficked flagship — narrative hero, one decisive CTA, SOUL-voice copy, kill the competing links — tests the pattern before spending design budget on key art. Outcome: the destination of the play-next funnel earns the click it now receives. **Recipe:** Pick the highest-traffic live game detail page; restructure to narrative hero (one-line promise in SOUL voice) + single primary CTA (play / notify) + supporting proof, demote secondary links below the fold. No new key art (that is a founder/design commission — see blockedFounderGated). Ship one page as the template; defer the rest to a follow-up if time-bound. |
|  | nice | engagement |  | 5 | 3 | 9.5 | · | **discord-to-nav** — . The cheapest retention surface a studio owns is its community, and burying it in the footer-social row signals it is an afterthought rather than a front door. Promoting Discord + Community Hub into the primary Studio nav dropdown puts the retention loop one click from every page. Outcome: the community funnel gets primary-nav prominence matching its retention value. **Recipe:** Add a 'Community' label group to the Studio nav dropdown in scripts/propagate-nav.mjs (around line 93) with /community/ (Community Hub) and the Discord invite (target=_blank rel=noreferrer). Re-propagate. Keep the footer-social Discord icon — this adds a path, it does not move one. |
|  | high | speed |  | 4 | 2 | 6.5 | · | **stale-board-hygiene** — . A phantom founder-action erodes trust in the whole action queue — if one item is stale, the founder learns to second-guess all of them, which is exactly how a real action later gets ignored. Reconciling the board so the only remaining membership orphans (membership-interview.js + vault-sdk.js, both still present and feature-bearing) are stated accurately keeps the founder's queue honest. Outcome: a founder-action list with zero phantom entries. **Recipe:** Edit context/TASK_BOARD.md Human Action Required: remove the vaultsparked-proof.js delete line (asset gone S186); correct the '3 membership/vaultsparked asset orphans' line to '2 orphans (membership-interview.js, vault-sdk.js)' since vaultsparked-proof.js no longer exists. Re-render the startup brief so FOUNDER UNLOCKS drops the phantom. Verify check-orphan-assets.mjs reflects 2, not 3. |

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

- rum-allowlist-integrity-gate
- audit-freshness-in-plumbing

## Skipped (Priority < 1.5)

- undefined — undefined
- undefined — undefined
- undefined — undefined
- undefined — undefined
- undefined — undefined

