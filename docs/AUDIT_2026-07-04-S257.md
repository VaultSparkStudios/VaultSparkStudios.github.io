<!-- generated-by: codex /arc S257 -->
<!-- generated-at: 2026-07-04 -->
<!-- project: VaultSparkStudios.github.io · type: website/public-live -->

# Project Audit — VaultSparkStudios.github.io — S257

> Live-verified S257 audit. Local project truth wins over the stale Studio Ops profiler: this repo is a public SPARKED website and uses the product/public-site lens.

## Ranked Plan

| # | Tier | Axis | Status | Item |
|---|:-:|---|---|---|
| 1 | P1 | engagement-truth | shipped | **CTA contract registry** — `scripts/lib/cta-contract-registry.mjs` is now the declarative source for tracked CTA families. `check-cta-impression-contracts.mjs` consumes it, so future families opt into source file, shown/click events, rollup family, epoch, and gated-helper checks without bespoke array edits in the checker. |
| 2 | P1 | observability | shipped | **Proof-surface substep diagnostics** — `check-proof-surface.mjs` now writes per-substep status and duration summaries to `api/proof-surface-diagnostics.json` and `docs/PROOF_SURFACE_DIAGNOSTICS.md`. The slowest build-check block is now explainable without scraping console output. |
| 3 | P2 | process-truth | verified-stale-carry | **GENERATOR-HEAD-CONTRACT-AUDIT** — verified already shipped in S255 as `scripts/check-generator-head-contracts.mjs`, wired into `build:check:steps` with self-test + live gate. Close the stale open task rather than duplicating the gate. |
| 4 | P2 | process-truth | verified-stale-carry | **ROTATE-TASKBOARD-CLOSEOUT-HOOK** — verified already shipped in S255: `prompts/closeout.md` runs `node scripts/rotate-taskboard.mjs --apply`, and `rotate-taskboard.mjs` has the S254 stale-session-heading support. Close the stale open task with evidence. |
| 5 | P2 | security | shipped-current-sink | **Trusted Types leaderboard current sink** — 2026-07-04 reprobe remains AMBER, but the fresh July 3 `/leaderboards/` fallback/skeleton `innerHTML` sink was root-fixed at source with DOM row helpers and propagated to all generated leaderboard subpages. |`n| 6 | P2 | evidence-gated | honest-deferral | **TT enforcement flip / play-next redesign / INP root-fix** — still gated. TT has nonzero fresh evidence; play-next/INP need enough clean post-2026-07-02 field data. Implementing before the evidence window matures would fabricate a verdict. |

## Second-Order Candidates

| # | Status | Item |
|---|---|---|
| A | shipped | **Proof-surface diagnostics contract** — publish proof-surface substep timings as committed public-safe artifacts, mirroring build-check diagnostics. |
| B | committed | **Proof-surface failure classifier** — next layer should classify failed substeps as self/sibling/freshness/flaky using the same owner-honesty pattern as doctor. |
| C | committed | **CTA registry rollup parity** — next layer should make `rollup-rum-ux.mjs` consume the CTA registry for family definitions, so denominator config has one source of truth. |`n| D | committed | **TT freshness lens** — split TT burndown by most-recent day so old high-volume clusters stop outranking current fresh sinks. |

## Honest Deferrals

- `play-next conversion redesign`: wait for enough clean post-2026-07-02 viewport-impression data, expected around 2026-07-09.
- `INP root-fix`: wait for clean post-filter route/phase evidence; do not ship a speculative performance "root fix."
- `Atlas registry freshness`: remains Studio Ops-owned until canonical Atlas data is enriched.
- Forge devlogs and richer IGNIS public exposure: founder-voice/public-safe decisions; do not publish autonomously.

