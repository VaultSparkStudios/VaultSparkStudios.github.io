<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-05-24 -->
<!-- project: vaultsparkstudios-github-io · type: platform -->

# Project Audit — VaultSparkStudios.github.io

> Session 161 · One ranked improvement plan covering 9 axes. Top items first.
> Run `/implement` to execute in optimal order.
> Axis weights: Security 2× · Speed 2× · UX 1.5× · Feedback 1.5× · AI/Feature/Gamification/Token 1×

## Top-line

- Total items: 17
- Combined Priority: 560.7
- Top 3: `lcp-regression-bisect-fix` · `rum-r2-activation` · `shell-hash-sw-warm-handoff`

> **S162 reconciliation (2026-05-24):** 7 of 17 items shipped during S161 and are now marked `shipped` in the JSON sidecar (verified on-disk):
> `lcp-regression-bisect-fix` · `progressive-membership-journey` · `vault-rank-ambient-progression` · `signed-in-state-centralize` · `oracle-query-personalization` · `compliance-30-32-fix` · `stale-shell-asset-sweep`.
> **10 open** (by priority): `rum-r2-activation` · `shell-hash-sw-warm-handoff` · `ambient-bundle-critical-split` · `perf-fix-recipe-autoloop` · `feedback-sentiment-aggregation` · `mobile-sheet-default-swap` (founder-gated) · `commit-to-site-forge-map` · `ignis-conduit-llm-narration` · `perf-budget-strict-flip` (depends on LCP fix) · `revenue-signals-automation`. These are the `/implement` target set for S162.

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | 🔥 | speed | 2h | 10 | 4 | **50.0** | **lcp-regression-bisect-fix** — Fix `/` desktop LCP regression (13-14s → <1.8s). Two consecutive prod traces show / desktop LCP at 13,060ms and 14,528ms — was 1,404ms in the S160 clean trace. FCP is also blown (11s+). Bisect: isolate nav-sheet.js from ambient bundle first; if still blown, audit account-chip.js for synchronous Supabase calls. **Recipe:** `node scripts/measure-page-performance.mjs --check --base=https://vaultsparkstudios.com` → remove nav-sheet.js from AMBIENT_SOURCES → rebuild → trace → narrow. |
| 2 | 🔥 | security | 1h | 8 | 5 | **50.6** | **rum-r2-activation** — Activate RUM R2 bucket (code production-ready from S154). Real-user vitals pipeline exists; only `vaultspark-rum` R2 binding is missing. Without RUM, every perf regression is diagnosed post-hoc via synthetic traces; with RUM, the S161 LCP spike would have been caught within minutes. **Recipe:** `wrangler r2 bucket create vaultspark-rum` → add `[[r2_buckets]]` binding to `wrangler.toml` → deploy Worker → verify `/v/rum` beacon POST. |
| 3 | 🔥 | speed | 4h | 8 | 9 | **43.4** | **shell-hash-sw-warm-handoff** — Service-worker warm handoff on shell hash rotation. Every hash rotation cold-forces all visitors to re-fetch — the exact failure class behind the S161 LCP spike. Extend the existing SW (VAPID/push already registered) to prefetch the new shell the moment a BroadcastChannel('vs-shell-update') fires. No other studio site does this; eliminates the recurring regression class entirely. **Recipe:** In `build-shell-assets.mjs`, write outgoing hash to `.cache/shell-manifest-prev.json`. Add SW `shell-prefetch` message handler to prefetch incoming hash before any navigation. |
| 4 | 🔥 | speed | 8h | 8 | 9 | **43.4** | **perf-fix-recipe-autoloop** — Connect `check-perf-budget.mjs` fix recipes to autonomous safe-fix loop. The fix-recipe data structure (S158) already knows WHAT to fix. Adding an applier for SAFE_AUTO_APPLY class (defer, lazy, preload) closes the LCP regression class autonomously. **Recipe:** New `scripts/auto-apply-perf-fixes.mjs` reads `.cache/perf-fix-recipes.json`, applies safe fixes idempotently, runs `build:check`, writes audit trail. |
| 5 | 🔥 | speed | 4h | 9 | 7 | **43.4** | **ambient-bundle-critical-split** — Split 130KB ambient bundle into critical-path (≤12KB) + enhancement layer loaded post-DCL. Enhancement sources (nav-sheet.js, adaptive-speculation.js, vault-atlas.js) are improvement-tier, not render-critical. Splitting cuts post-DCL parse budget ~85% for cold-cache new visitors. **Recipe:** Add `CRITICAL_SOURCES` to `build-ambient-bundle.mjs`. Build two outputs; inject enhancement bundle via inline DCL loader with `saveData`/`deviceMemory` gate. |
| 6 | 🔥 | ux | 8h | 9 | 9 | **36.5** | **progressive-membership-journey** — Adaptive `/membership/` narrative journey using existing trust-depth.js + intent-state.js signals. First-time visitor sees story + intrigue; returning free member sees 'what you're missing' delta; paid member sees value delivered. Zero URL change, three conversion experiences. **Recipe:** Wrap sections in `data-journey-stage=curious|interested|committed` attrs. `assets/membership-journey.js` reads `VS_TRUST_DEPTH` + `VS_SIGNED_IN` + visit-count, sets `body[data-journey-stage]`, shows/hides CSS. |
| 7 | ⚡ | gamification | 4h | 8 | 8 | **29.6** | **vault-rank-ambient-progression** — Ambient 2px rank progress bar at bottom of every page for signed-in members. Persistent micro-motivation; no studio website does this with this level of subtlety. Duolingo-style without the noise. **Recipe:** After session-ready, fetch `/api/member-rank-progress.json` (new Supabase RPC). Inject `<div id=vs-rank-bar>` fixed bottom:0 height:2px bg:var(--vs-gold) width:{pct}% with 1.2s transition. Store last-known in localStorage to prevent flash. |
| 8 | ⚡ | feedback | 4h | 7 | 7 | **28.5** | **feedback-sentiment-aggregation** — Monthly founder digest: aggregate `vault_feedback` + micro-feedback into themed signal surface. Buckets by theme (perf, content, nav, bug, compliment), shows sentiment trend, surfaces top 3 un-addressed asks. Closes the 'we heard → we shipped' loop with data. **Recipe:** `scripts/build-feedback-digest.mjs` queries last 30d, groups by category, writes `api/feedback-digest.json`. New `/feedback/digest/` page renders trend + top asks. |
| 9 | ⚡ | security | 2h | 7 | 4 | **28.0** | **signed-in-state-centralize** — Centralize `body[data-vs-signed-in]` into `assets/signed-in-state.js`. Multiple JS files independently query Supabase for session — 3-4 parallel auth calls per page load for signed-in users. Single source of truth + CustomEvent('vs:session-ready') unblocks progressive-membership-journey and oracle-personalization. **Recipe:** New `assets/signed-in-state.js` calls VSIdentity.getSession() once, writes body attrs, dispatches event. All other consumers switch to event listener. |
| 10 | ⚡ | ux | 1h | 7 | 3 | **26.6** | **mobile-sheet-default-swap** — Flip nav-sheet.js to default for mobile after founder iPhone verification. Already shipped behind `?nav=sheet` flag; one boolean change in `shouldActivate()`. Replaces the drawer that caused 3 regressions. **Recipe:** Founder verifies `vaultsparkstudios.com/?nav=sheet` on iPhone → flip predicate → log DECISIONS.md → build:check. _(Founder-gated)_ |
| 11 | ⚡ | feature | 6h | 7 | 9 | **22.7** | **commit-to-site-forge-map** — Live commit graph as Forge Window studio transparency surface. Builds from GitHub API at build time → `api/commit-map.json`. Turns commits into legible studio moments; combined with IGNIS conduit, each commit becomes a public artifact of creative labor. No comparable studio does this. **Recipe:** `scripts/build-commit-map.mjs` calls GitHub API `/commits?per_page=50`, writes `api/commit-map.json`. Render as scrollable timeline on `/studio-pulse/`. |
| 12 | ⚡ | ai | 2h | 7 | 6 | **21.0** | **ignis-conduit-llm-narration** — Upgrade IGNIS conduit from template narration to live IGNIS API sentences. `build-ignis-conduit.mjs` uses verb+subject template (cost-neutral, real, but stilted). Studio-ops `ignis.narrate` capability already exists. **Recipe:** In `build-ignis-conduit.mjs`, check `resolveCapability('ignis.narrate').ready`; if READY, call `ignis_narrate(rawEntries)`; else fall back to templateNarrate. |
| 13 | ⚡ | ai | 4h | 8 | 8 | **24.8** | **oracle-query-personalization** — Personalize `/oracle/` query suggestions by member tier. Anonymous → discovery; free member → exploration; sparked/eternal → strategic rank queries. Zero additional API calls — just serve a different JSON slice per tier from the existing `public-intelligence.js` cache. **Recipe:** New `api/oracle-queries.json` with 3 pools. `assets/oracle-interface.js` reads tier from `vs:session-ready` event, swaps suggestion pool. |
| 14 | ⚡ | speed | 30m | 6 | 2 | **18.2** | **perf-budget-strict-flip** — Promote `check-perf-budget.mjs` to `--strict` after LCP fix + 2 clean traces. One-line change in `package.json` that turns advisory perf check into a build-fail gate. **Recipe:** After item #1 is fixed and two traces confirm < 1800ms: change `--check-perf-budget` to `--check-perf-budget --strict` in `package.json`. _(Depends on: lcp-regression-bisect-fix)_ |
| 15 | ⚡ | security | 1h | 6 | 2 | **15.2** | **compliance-30-32-fix** — Fix 2 failing doctor probes (compliance validation + velocity). Leaving known failures unaddressed sets a precedent that gates are advisory. **Recipe:** `node scripts/ops.mjs doctor --verbose` → read failure output → apply canon-required changes → verify 13/13. |
| 16 | ⚡ | speed | 30m | 5 | 2 | **15.1** | **stale-shell-asset-sweep** — Purge 8 stale `ambient.shell-*.js` files; only the current hash is needed. They bloat the deployed surface and confuse orphan-asset checks. **Recipe:** `grep -r 'ambient.shell-' --include='*.html' . | grep src=` → identify live hash → `rm assets/ambient.shell-{other}.js` → `npm run build:check`. |
| 17 | 💡 | feedback | 2h | 6 | 3 | **13.5** | **revenue-signals-automation** — Automate `portfolio/REVENUE_SIGNALS.md` freshness. Currently hand-maintained → perpetually stale (⛔ in doctor). Stripe customer count + MRR exists in Supabase. **Recipe:** `scripts/generate-revenue-signals.mjs` queries Supabase member/subscriber counts, writes REVENUE_SIGNALS.md with current date. Wire into `npm run build`. |

## Innovation reserve (Innovation ≥ 8 — execute even when busy)

- `shell-hash-sw-warm-handoff` (9) — eliminates the recurring LCP regression class permanently
- `perf-fix-recipe-autoloop` (9) — autonomous safe-fix loop, closes the perf regression loop
- `ambient-bundle-critical-split` (7) — critical-path split; no studio of this type has done this
- `progressive-membership-journey` (9) — 3-stage conversion experience from one URL
- `vault-rank-ambient-progression` (8) — ambient rank bar; nobody does this with this subtlety
- `commit-to-site-forge-map` (9) — live commit graph as public studio transparency artifact
- `oracle-query-personalization` (8) — tier-aware Oracle with zero extra API cost

## Skipped (Priority < 1.5)

None deferred — all 17 items clear the Priority ≥ 1.5 threshold.

## Carries resolved in this audit

- `IGNIS conduit cron → LLM narration` — was scaffolded S160; now a ranked item (#12) with concrete recipe
- `Signed-in state centralization` — SIL brainstorm carry from S160; now ranked #9 with recipe + unblock chain
- `Compliance 30/32` — doctor failure surfaced; ranked #15 with recipe

## Obelisk-blocked items (not ranked — awaiting Obelisk Phase 2 reply in Ark inbox)

These 5 items remain blocked on Obelisk `repo-answer` cargo arriving in Ark inbox:
- `namespace-collapse-vault-and-membership` (S159 audit #1)
- `edge-personalized-html-via-worker` (S159 audit #2)
- `unified-intelligence-spine` (S159 audit #3)
- `vault-sso-contract` (S159 audit #16)
- `passkey-default` (S159 audit #17)

---

## Execution Log — Session 162 (2026-05-24)

10 open items addressed. **6 shipped/resolved, 1 verified-already-done, 3 deferred with evidence, 1 founder-gated.**

| Item | Result | Note |
|---|---|---|
| `revenue-signals-automation` | ✅ resolved-alt | Root cause was a brief-renderer false-negative, not missing automation. Fixed `render-startup-brief.mjs` sibling-fallback → signal ⛔→✓. Declined the literal recipe (committing MRR to a public repo violates the public-safe constraint). |
| `commit-to-site-forge-map` | ✅ shipped | `build-commit-map.mjs` (git-log, noise-filtered, self-test 5/5) → `api/commit-map.json`; "Forge Ledger" section + render on `/studio-pulse/`; wired into `build` + `build:check`. |
| `feedback-sentiment-aggregation` | ✅ shipped (slice) | Public-safe **theme bucketing** on `/feedback/insights/`, client-side from existing aggregate views (no raw feedback leaves the edge). Sentiment-trend + top-un-addressed-asks need a server-side categorization cron — scoped backend follow-up (studio-ops-owned). |
| `ignis-conduit-llm-narration` | ✅ shipped | Capability-gated (`resolveCapability('ignis.narrate')`, observability-only per CANON-022/015); respects cron-written `narrator:'ignis-llm'`; noise-filtered 7d window; sharpened type-aware, acronym-safe template. |
| `perf-fix-recipe-autoloop` | ✅ shipped | `auto-apply-perf-fixes.mjs` — additive/idempotent/reversible/opt-in/dry-run-default applier (`add-resource-hint`, `add-attr`); self-test 6/6; `--self-test` wired into `build:check`. Reported no-op on current cache by design (over-budget routes need human-reviewed structural fixes). |
| `rum-r2-activation` | ✅ verified-done | End-to-end verified: prod deploy `2026-05-25T00:13Z` (after S161 binding commit; CF validates R2 bindings at deploy), `/v/rum` live (202), beacon in ambient bundle. Completed S161. |
| `ambient-bundle-critical-split` | ⏸ deferred | Re-scoped: ambient bundle is **already `defer`red** — deferred scripts can't block FCP, so splitting won't move the home LCP/FCP headline (`/community/`+`/games/` measure 1.3–2.1s on the same bundle). High re-regression risk for no headline gain. Shipped `kit-fallback.js` defer as a safe adjacent micro-win. |
| `shell-hash-sw-warm-handoff` | ⏸ deferred | SW (312 lines) controls site-wide caching for the brand-anchor site; warm-handoff is additive but unvalidatable without a real browser this session. Deferred per the shell-rotation fragility memory. |
| `perf-budget-strict-flip` | ⏸ deferred | Dependency unmet — current `/` desktop trace = **14528ms** (>2500 budget) would hard-fail CI. The 14528ms is likely a synthetic-trace artifact (FCP==LCP exactly); now-live RUM is the better real-signal gate. |
| `mobile-sheet-default-swap` | 🔒 founder-gated | Flag readiness confirmed (`?nav=sheet` + `localStorage vs-nav-style`). One-line flip in `shouldActivate()` pending founder iPhone real-device verification. |

**Genius finding (carry to next session):** the `/` desktop "LCP regression" (14528ms) is almost certainly a **synthetic-trace artifact** — FCP==LCP exactly equal, while same-bundle pages measure 1.3–2.1s, and the render-blocking scripts are at end-of-body (can't block FCP). Real fix path: trust now-live RUM field data over the flaky synthetic trace; investigate whether the homepage hero animation confuses headless LCP detection. This unblocks `perf-budget-strict-flip` honestly.

---
*Generated by `/audit` v1.0 · Session 161 · 2026-05-24 · executed Session 162 · ready for `/implement` re-run*
