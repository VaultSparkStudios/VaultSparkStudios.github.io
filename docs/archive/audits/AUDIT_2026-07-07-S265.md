# Audit 2026-07-07 S265 — Arc Saturation

## Ranked Plan

1. **[OBS/P1] Startup active-age truth guard — shipped**
   - Premise verified: `docs/STARTUP_BRIEF.md` reported `Last active: 20640d` while latest closeout was current. `scripts/render-startup-brief.mjs` was sorting numeric `silLastSession` with ISO dates.
   - Fix: date candidates now accept only real `YYYY-MM-DD` strings; active age clamps invalid/negative values to `?`; startup smoke asserts active age is plausible and no greater than closeout age.
   - Verification: `node --check scripts\render-startup-brief.mjs`; `node --check scripts\smoke-startup-scripts.mjs`; `node scripts\render-startup-brief.mjs`; `node scripts\smoke-startup-scripts.mjs`.

2. **[AI/P1] AI discovery existing-route resolver — shipped**
   - Premise verified: route generation could prefer an external product URL when an on-site canonical page existed under the other category, and could miss `vaultspark-` slug directories.
   - Fix: `build-agents-json.mjs`, `build-llms-full-shards.mjs`, and `check-agents-json-coherence.mjs` now resolve real existing `games/` or `projects/` routes before heuristic fallback, with both original and stripped slug candidates.
   - Verification: `node --check` on all edited scripts; `node scripts\build-llms-full-shards.mjs`; `node scripts\build-agents-json.mjs`; `node scripts\build-agents-json.mjs --check`; `node scripts\build-llms-full-shards.mjs --check`; startup smoke. `agents.json` now advertises MindFrame at `/games/mindframe/` and Football GM at `/games/vaultspark-football-gm/`, each with committed `llms-full.txt` shards.

3. **[PERF/P2] Homepage Lighthouse floor advisory — investigated, deferred honestly**
   - Premise verified: `check-lighthouse-floor` still reports homepage lab performance around the warning floor (`median=0.77`, floor `0.78`), dominated by cold-start LCP around 5.5-5.8s in the trend ledger.
   - Decision: no page/runtime change shipped in this session. The session touched generated AI discovery/startup surfaces, not homepage rendering; the warning is advisory, not an error gate. A real performance change needs a focused LCP root-cause pass with fresh trace proof, not a cosmetic report refresh.

## Execution Log

| Item | Outcome | Evidence |
|---|---|---|
| Startup active-age truth guard | shipped | Startup brief now reports `Last active: 0d · Last closeout: 0d`; startup smoke includes `startup-active-age`. |
| AI discovery existing-route resolver | shipped | `agents.json` and `.well-known/llms-full.txt` regenerated; MindFrame and Football GM route to on-site pages with shards. |
| Lighthouse floor advisory | deferred | Existing advisory remains non-blocking; recorded as future focused perf work instead of fabricated closure. |

## Remaining Gated Work

- Founder/content gates: forge devlogs and richer public IGNIS exposure.
- Evidence gates: TT enforcement near-zero soak, play-next true-viewport sample threshold, Football GM INP post-deploy soak.
- Credential/provider gates: full Obelisk RP/provider flip and external receipt/browser checks.
