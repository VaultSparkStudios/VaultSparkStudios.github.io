<!-- generated-by: render-audit-md.mjs (R-H14) — DERIVED FROM AUDIT_2026-06-08.json; do not hand-edit -->
<!-- generated-at: 2026-06-08 -->
<!-- project: unknown · type: unknown -->

# Project Audit — Unknown

> One ranked improvement plan covering 9 axes. Top items first.
> Source of truth: this file is derived from the JSON sidecar. Edit the JSON, then re-render.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 4
- Combined Priority: 0.00
- Top 3: —

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov | Priority | Status | Item |
|---|:-:|---|---|:-:|:-:|:-:|:-:|---|
|  | fire | AI/SEO/FeatureDepth |  | 9 | 9 | 34.9 | · | **agents-json-spine** — . CANON-011's sitemap standard names /agents.json explicitly, and build-llms-full-shards.mjs already advertises a pairing that was never delivered. Info-finding outcome: ChatGPT/Claude/Perplexity and agentic crawlers discover the studio's canonical surfaces (sitemap, llms.txt, llms-full shards, primary CTA, projects, policy) from one well-known entrypoint and cite the studio accurately instead of guessing. The second-order win is integrity: a single contract gate keeps agents.json, llms.txt, the shards, and sitemap.xml mutually consistent so the AI spine can never silently drift as projects flip status. **Recipe:** Add scripts/build-agents-json.mjs that generates /agents.json from the project registry + existing public surfaces (declare: name, description, canonical URLs for sitemap/llms.txt/llms-full, per-SPARKED-project shard URLs, primary CTA /membership/, contact, policy /privacy + /terms, automation disclosure). Wire into npm run build. Add scripts/check-ai-discovery-spine.mjs (build:check gate + --self-test) asserting agents.json exists, is valid JSON, lists every SPARKED project that llms.txt lists, and has no dead internal URLs vs sitemap.xml. Public-safe only. |
|  | fire | SEO/UX |  | 8 | 4 | 16.0 | · | **meta-desc-backfill-gate** — . On a public studio site the meta description is the conversion copy that runs in search results before anyone clicks. Info-finding outcome: each affected page gets a unique 120-160 char description written in audience voice, so the SERP snippet reads as intentional and lifts click-through. The durable win is the gate: a structural build:check that fails on any indexable (non-noindex) page missing a description makes this class of drift impossible to reintroduce — the same subtractive-then-structural pattern that locked placeholder orphans (S176) and script relevance (S148). **Recipe:** Backfill unique meta descriptions on the 17 pages (audience voice, no internal vocabulary, name the page's value). Add scripts/check-meta-descriptions.mjs: crawl index.html pages, skip any with <meta name=robots ... noindex>, fail on missing/empty description or length outside ~80-170 chars; include --self-test. Wire into build:check. Where a page is propagator-owned, set the description at the template so re-propagation preserves it. |
|  | high | Speed |  | 6 | 5 | 15.0 | · | **ambient-split-wave2** — . Every KB in the always-parsed feature bundle is parse+compile cost paid by every first-time visitor on the cold-cache critical path, even though these scripts immediately bail when their DOM hook is absent. Speed outcome: moving proven single-surface scripts to ambient-loader predicate loading shrinks the cold-cache feature bundle so the homepage and every content page parse less JS to first interaction, mirroring the S178 genome-strip win. Conditional by design — only scripts proven to gate on a hook present on <=2 pages are split; anything ambiguous stays put (no silent regression on a 999 site). **Recipe:** For each feature-bundle candidate, read the script to find the REAL DOM hook it reads (not one it creates) and grep its presence across HTML. If present on <=2 pages, move the source out of AMBIENT_FEATURE_SOURCES in build-ambient-bundle.mjs and add a matching predicate to assets/ambient-loader.js (mirror the genome-strip entry). Re-run the ambient build + report-ambient-coverage + placement gates. If a candidate cannot be proven single-surface, leave it and log why in the closeout (honest, no forced split). |
|  | high | UX/A11y |  | 5 | 3 | 9.5 | · | **nav-aria-current** — . aria-current=page is the canonical signal for 'you are here' and costs almost nothing. UX/info-finding outcome: screen-reader and keyboard users get an explicit current-location cue while navigating, and the cue stays correct because it is emitted by the same propagator that already renders the nav. It also tightens the existing visual active state into a semantically correct one, closing a real WCAG gap on the brand-anchor site. **Recipe:** In scripts/propagate-nav.mjs, emit aria-current="page" on the nav link whose href matches the page's own path (alongside the existing active class), and re-propagate across all pages. Verify the homepage and a nested page render exactly one aria-current. Add a lightweight assertion to an existing nav/structure gate if cheap. |

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

- agents-json-spine

## Skipped (Priority < 1.5)

- undefined — undefined
- undefined — undefined
- undefined — undefined
- undefined — undefined


---

## Execution Log — Session 179 (`/implement`)

All 4 items SHIPPED in optimal order. `npm run build:check` green end-to-end (108-page crawl · 0 status failures · 0 blocking-script findings).

| # | Slug | Result | Notes |
|---|---|---|---|
| 1 | agents-json-spine | ✅ SHIPPED | `build-agents-json.mjs` → `/agents.json` (13 public projects); `check-ai-discovery-spine.mjs` gate (10-case self-test). Surfaced + fixed pre-existing phantom shards in llms.txt + stale call-of-doodie URL. Commit `f57c3853`. |
| 2 | meta-desc-backfill-gate | ✅ SHIPPED (re-scoped) | Audit's "17 missing pages" was a false premise (buggy `grep -Lq`); every indexable page already has a description. Real deliverable = the floor gate `check-meta-descriptions.mjs` (hard-fail missing/empty, advisory on length). Caught + fixed an apostrophe-truncation bug in the gate's own parser. Commit `910e4826`. |
| 3 | nav-aria-current | ✅ SHIPPED | `activeAttr()` helper in propagate-nav emits `aria-current="page"` with the active class; re-propagated 90 pages. CSP + extraction + coherency green. Commit `c0caf313`. |
| 4 | ambient-split-wave2 | ✅ SHIPPED | 4 route-scoped widgets (social-dashboard-public, security-posture, feedback-decision-board, rank-economy-simulator) moved to predicate loading; feature bundle 58.7KB→45.4KB (−23%). vault-atlas correctly NOT split (sitewide Resources dropdown). Commit `8710f830`. |

Build refresh: `f7309c23`.
