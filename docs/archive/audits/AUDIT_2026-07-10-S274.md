<!-- generated-by: render-audit-md.mjs (R-H14) — DERIVED FROM AUDIT_2026-07-10-S274.json; do not hand-edit -->
<!-- generated-at: 2026-07-10 -->
<!-- project: unknown · type: unknown -->

# Project Audit — Unknown

> One ranked improvement plan covering 9 axes. Top items first.
> Source of truth: this file is derived from the JSON sidecar. Edit the JSON, then re-render.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 6
- Combined Priority: 0.00
- Top 3: —

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov | Priority | Status | Item |
|---|:-:|---|---|:-:|:-:|:-:|:-:|---|
|  |  | ux | 2.5h | 9 | 6 | 0.0 | · | **mobile-drawer-elite-redesign** — .  **Recipe:** In assets/nav-toggle.js and the shared shell CSS: remove the redundant injected close button (keep the header X), tighten drawer item rhythm, give the drawer bottom padding-bottom that clears the cookie banner (or raise drawer z-index above the banner and let the drawer scroll independently with 100dvh + safe-area-inset-bottom), and verify with a 390x844 drawer-open screenshot that Sign In / Membership / socials are all reachable. |
|  |  | ux | 1.5h | 8 | 6 | 0.0 | · | **mobile-theme-parity-canon-047** — .  **Recipe:** Extend assets/theme-toggle.js to render a compact theme row (swatch grid or select) inside the mobile drawer beneath the nav sections, reusing the existing THEMES list, vs_theme persistence, and class application; keep touch targets >=44px and verify by switching themes from the drawer at 390px. |
|  |  | ux | 1h | 7 | 5 | 0.0 | · | **hero-reveal-stagger-compression** — .  **Recipe:** Compress the per-element --reveal-delay curve so the full hero lede (including .hero-actions) completes within ~0.8s of load on all viewports, keeping the existing reduced-motion opt-out and the shell-health 2.6s fallback; verify with a 900ms-settle mobile screenshot showing CTAs visible. |
|  |  | brand | 2.5h | 8 | 7 | 0.0 | ↷ | **premium-display-typography** — .  **Recipe:** Self-host a subset woff2 display serif (e.g. Fraunces or an equivalent open-license face passing package-trust) under assets/fonts/, add @font-face with font-display:swap plus preload on high-value pages, layer it in front of Georgia in the display font stack, keep the critical-shell fallback metrics-compatible (size-adjust) to avoid CLS, and gate with a before/after perf trace since ambient shell changes rotate the shell hash (cold-cache cost). |
|  |  | visual-polish | 45m | 4 | 3 | 0.0 | · | **genome-strip-glow-artifact** — .  **Recipe:** Root-cause the streaks in a headless probe (toggle vault-genome-strip.js and the ambient background layers independently, screenshot each); if the strip is the source, remove the bleed (isolation: isolate / contain: paint on the strip); if intentional aurora, tune opacity so it reads as design, not defect. |
|  |  | ux | 1h | 4 | 4 | 0.0 | · | **studio-hub-toast-batching** — .  **Recipe:** In the studio-hub trophy/toast module, batch multiple unlocks that occur within one load into a single summary toast ('4 trophies unlocked · +210 XP') with a details link, and cap concurrent toasts at 2; verify with a fresh-profile screenshot. |

