<!-- generated-by: render-audit-md.mjs (R-H14) — DERIVED FROM AUDIT_2026-06-07.json; do not hand-edit -->
<!-- generated-at: 2026-06-07 -->
<!-- project: unknown · type: unknown -->

# Project Audit — Unknown

> One ranked improvement plan covering 9 axes. Top items first.
> Source of truth: this file is derived from the JSON sidecar. Edit the JSON, then re-render.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 9
- Combined Priority: 0.00
- Top 3: —

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov | Priority | Status | Item |
|---|:-:|---|---|:-:|:-:|:-:|:-:|---|
|  | fire | UX |  | 9 | 3 | 20.4 | · | **now-playing-orphan-kill** — . A permanently-broken widget on the brand anchor's homepage erodes trust on first paint — visitors read 'Loading…' as 'this site is broken'. Conversion outcome: removes the first visual defect a prospective Vault member sees. Subtractive fix: the Studio Spine strip already carries live-status duty. **Recipe:** Delete the now-playing-bar div from index.html (lines ~1021-1026), remove its CSS rule + the vsx-086d2b9242 extracted class if orphaned, re-run build:check, verify homepage renders clean. |
|  | fire | Speed/Resilience |  | 8 | 7 | 35.4 | · | **worker-stale-on-5xx** — . The failover covers single-origin failure but a Pages platform blip takes out both primary and pages.dev simultaneously. Serving the last-known-good HTML from the edge cache turns a visible outage into invisible staleness. Info-finding outcome: every route keeps answering during platform incidents. **Recipe:** In originFetch (security-headers-worker.js:505), on double-failure for GET, try caches.default.match(request) before returning 502; on every healthy HTML 200 pass, ctx.waitUntil a cache.put copy keyed for disaster-recovery. Deploy with --env production (S175 lesson). Add a contract note in tests. |
|  | fire | Process/UX |  | 7 | 8 | 35.4 | · | **placeholder-sentinel-gate** — . The now-playing bug survived 170+ sessions because nothing watches for placeholder-forever states — the exact class memory says to gate structurally, not patch individually. Innovation 8: build-time dead-placeholder detection cross-referencing JS renderer reachability is something no static-site pipeline does. Conversion outcome: no visitor ever sees a permanently-dead widget again. **Recipe:** New scripts/check-placeholder-orphans.mjs: scan *.html for loading-placeholder text nodes, extract host element id/class, grep all shipped .js (including shell bundles' source list) for references; unreferenced → exit 1 with file:line. Self-test with a fixture. Wire into build:check. |
|  | fire | Product/Data |  | 8 | 5 | 30.3 | · | **field-verdict-refresh-readout** — . This is the S176 P1 runway item: the entire S175 speed arc is ungraded until the verdict engine sees post-deploy samples. Info-finding outcome: founder learns whether the CF Pages migration actually moved field LCP (pre-migration p75 was 9489ms on /). **Recipe:** node scripts/pull-rum-summary.mjs (or rum:pull chain) → node scripts/compare-rum-windows.mjs → read data/field-verdicts.json verdicts + api/geo-vitals.json non-US rows; record verdict in CURRENT_STATE + speed receipts. |
|  | high | Security |  | 9 | 6 | 27.0 | · | **tt-sink-burndown-wave2** — . The S174 burndown fixed clustered sinks but the founder's console proves six callers still violate report-only TT. Burning these down now means the 2026-06-12 re-probe (soak clock restarted by the env-fix) can return near-zero and unlock enforcement. Security outcome: DOM-XSS injection surface goes to zero on enforce. **Recipe:** theme-toggle.js:394 arrow.innerHTML='&#9660;' → textContent='▼'; recent-ships.js:42/88, trust-depth.js:207, related-content.js:256 → route through new assets/tt-policy.js (trustedTypes.createPolicy('vs-dom', {createHTML})) with non-TT-browser fallback; sentry-init.js script.src → createScriptURL via same policy; add policy name to TT_REPORT_CSP allowlist later when enforcing. Rebuild shell bundles. |
|  | high | Observability |  | 7 | 5 | 22.2 | · | **uptime-probe-firstparty** — . The founder's 503s were invisible to us — failed page loads never beacon RUM, and the uptimerobot credential is MISSING (free-build bias says build it first-party). Info-finding outcome: founder learns about outages from email, not from being the canary. **Recipe:** New .github/workflows/uptime-probe.yml (cron */30) → scripts/probe-uptime.mjs hits /, /games/, /vault-member/, /membership/, /api/founder-presence.json, /status/ with browser UA; writes api/uptime.json {route,status,ms,ts}; on any non-200 calls send-regression-alert.mjs path with uptime subject. Stay-in-free-band: Actions cron + Resend free tier. |
|  | high | Speed |  | 7 | 4 | 21.2 | · | **preload-prune** — . Preloading deferred scripts wastes visitor bandwidth on every page view, competes with the LCP image for connection priority, and sprays console warnings that mask real signals. Speed outcome: critical-path bytes drop and LCP fetch priority improves sitewide. **Recipe:** In scripts/build-early-hints-headers.mjs, restrict PRELOADS to render-blocking assets (shell CSS + ambient-core); drop ambient-feature/theme-toggle/nav-toggle (they load deferred). Regenerate _headers, redeploy, verify warnings gone. |
|  | nice | Process |  | 5 | 5 | 18.9 | · | **rum-pull-conflict-guard** — . Every future session that runs rum:pull locally while CI commits nightly will hit the same conflict, costing founder attention on a generated file. Process outcome: generated-data conflicts become structurally impossible. **Recipe:** pull-rum-summary.mjs: before writing, check git log -1 --format=%cI -- data/rum-summary.json; if a github-actions commit is <24h fresh, skip the local rewrite (print 'CI-fresh, skipping') unless --force. Document in script header. |
|  | nice | Process/Ecosystem |  | 6 | 4 | 15.2 | · | **sil-integrity-clamp** — . Cross-repo SIL aggregation chokes on out-of-range categories, and a score that disagrees with its own components undermines the studio's primary truth metric. Ecosystem outcome: studio-ops dashboards read this repo's SIL without sanitization shims. **Recipe:** Find the SIL writer (closeout path) and clamp every category to 0-100 + recompute total from categories; fix the two historical 101 entries (S173/S174) in SELF_IMPROVEMENT_LOOP.md; ship content-acked + answer cargo back to studio-ops via ark.mjs. |

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

- placeholder-sentinel-gate
- worker-stale-on-5xx

## Skipped (Priority < 1.5)

- tt-enforce-flip — Evidence-gated — soak clock restarted 2026-06-05 by the env-target fix; re-probe ~2026-06-12 per runway. Wave-2 sink fixes (item 5) are the prep.
- nav-sheet-default-swap — Founder device verify still gates it (SOUL #3); canary at 25% remains telemetry-watch only.
- vaultsparked-proof-delete — Founder unlock pending — 30-second yes/no in docs/MEMBERSHIP_ORPHAN_DECISION.md; not agent-deletable (founder-gated decision, not a phantom blocker).
- error-beacon-client — Next wave — window.onerror + resource-failure beacon piggybacked on RUM endpoint; uptime-probe (item 6) covers the availability half this session.

