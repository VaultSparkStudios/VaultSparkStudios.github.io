<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-05-17 -->
<!-- project: vaultsparkstudios-website · type: platform (Security 2× · Speed 2× · UX 1.5× · Feedback 1.5×) -->

# Project Audit — VaultSpark Studios Website (Session 131)

> One ranked improvement plan covering 9 axes. Top items first.
> Project type: **platform** (Security 2× · Speed 2× · UX 1.5× · Feedback 1.5×).
> Supersedes `AUDIT_2026-05-14.md`. Re-ranks the 14 still-deferred items, drops the 8 that shipped in S129, and introduces **9 fresh genius-tier candidates** informed by the S130 mobile-overhaul ships and the new structural-gate posture.
> Run `/implement` to execute in optimal-efficiency order.

## Top-line

- Total items: **23** (9 fresh · 14 carried-and-re-ranked)
- Combined Priority: **612.8**
- Top 3: `mobile-regression-contract-gate` · `passkey-cross-subdomain-auth` · `synthetic-auth-canary-with-rollback`
- **Strategic frame:** S128 → S129 → S130 traced a clear pattern — every founder-reported "this used to work and now doesn't" was a *silent regression on a surface no test watched*. S129 shipped 2 structural gates (render-contract · portfolio-coherence) but **mobile, auth, and visual surfaces still have no enforcement**. This audit doubles down on **structural gates** for the 3 remaining regression-prone surfaces (mobile-overflow-on-body · auth-canary · visual-regression) and aggressively retires the **password+captcha auth class** that has produced 6 sessions of Turnstile bugs. Then it stacks **fresh ambient-intelligence items** (vault-genome-strip · founder-twin-narrator · live-rank-orb) that compound on S129's atlas/sigil/pointerdown work.

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | 🔥 | Speed · Process | 1h | 10 | 8 | 53.3 | **mobile-regression-contract-gate** *(NEW)* — Structural gate that asserts (a) `body { overflow-x: clip }` (not `hidden`) in every CSS file + inline critical-CSS; (b) every text-input selector has font-size ≥16px at `@media (max-width:768px)`; (c) `.brand-wordmark` markup contains a `.brand-suffix` span on every page. S130 root-caused the iOS sticky-header break + input zoom + brand cutoff — without a gate these regress on the next propagate run. **Recipe:** new `scripts/check-mobile-contracts.mjs` (~80 lines): grep all `*.css` for `overflow-x:\s*hidden` on body/html → fail; grep HTML for brand markup → assert split; parse `style.css` mobile @media block → assert 16px input floor. Wire into `build:check`. |
| 2 | 🔥 | Security · Speed | 4h | 10 | 8 | 31.0 | **passkey-cross-subdomain-auth** *(CARRY · S130 founder gate cleared)* — Retire the password+captcha class that produced 6 sessions of Turnstile bugs (S120/S121/S122/S125/S126). WebAuthn passkeys, RP-ID = root domain, works for `vaultsparkstudios.com` + `hub.vaultsparkstudios.com`. Supabase has built-in WebAuthn support in newer SDK. Turnstile stays as fallback. Now **5 sessions removed** from last Turnstile fix — cleanest window we've had. **Recipe:** Supabase SDK upgrade audit (currently 2.49.1); new `vault-member/passkey-enroll.js`; "Sign in faster with a passkey" upsell on existing login form; passkey-only path skips Turnstile entirely. |
| 3 | 🔥 | Security · Speed | 4h | 10 | 8 | 31.0 | **synthetic-auth-canary-with-rollback** *(CARRY)* — Production canary runs full login flow every 10 min from GitHub Actions matrix (Chromium · WebKit · Firefox); pages founder on failure; optionally auto-rollback last Pages deploy. Would have caught S125+S126 in <10 min vs waiting for founder console paste. **Recipe:** `.github/workflows/auth-canary.yml` cron `'*/10 * * * *'`; reuses Playwright + Eternal QA account from S119; alert via Resend (READY). Pairs with #2 — canary watches the new passkey path AND the legacy fallback. |
| 4 | 🔥 | UX · Process | 2h | 9 | 8 | 32.0 | **visual-regression-snapshot-on-mobile-surfaces** *(NEW)* — Playwright pixel-diff snapshots of 4 critical mobile surfaces (`/` · `/membership/` · `/vault-member/` · `/games/`) at iPhone SE + iPhone 13 Pro Max + Pixel 5 viewports on every PR. Would have caught the S130 brand cutoff + dark drawer + IGNIS-dot regressions on the PR before founder ever saw them. **Recipe:** new `tests/visual-regression.spec.js`; snapshots committed under `tests/__snapshots__/mobile/`; threshold 0.5%; new workflow `.github/workflows/visual-regression.yml`; updates require explicit `--update-snapshots` commit. |
| 5 | 🔥 | Speed | 4h | 10 | 6 | 37.4 | **lighthouse-perf-restoration-to-92** *(CARRY · standing red since S120)* — 7 sessions of failed pushes. Trace from prior carry: hero-ticker JS → `requestIdleCallback`; hover-cinematics CSS behind `@media(hover:hover) and (min-width:1024px)`; replace universe-bridge `radial-gradient` with static webp; profile parallax `::before`. Stops bleeding compliance score. **Recipe:** Chrome DevTools perf trace on `/`; apply 4 fixes; bump threshold to 0.90 in `.lighthouserc.json` **after** the fix lands; delete stale `propagate-csp.mjs --dry-run` step in `.github/workflows/e2e.yml`. |
| 6 | 🔥 | AI · Token-cost | 2h | 8 | 8 | 32.0 | **ignis-roi-loop** *(CARRY · deploy-gated)* — Add a per-Ask `outcome` field on next user action (became_member, navigated, dismissed, idle). Persist to `ignis_outcomes`; weekly summary surfaces "which question types converted." Turns IGNIS spend from cost to measured pipeline. **Recipe:** extend `supabase/functions/ask-ignis` response with `traceId`; client emits outcome event via `navigator.sendBeacon`. New `ignis_outcomes` migration + view `ignis_roi_7d`. |
| 7 | 🔥 | AI · Token-cost | 1h | 8 | 5 | 25.3 | **anthropic-prompt-cache-for-personas** *(CARRY · deploy-gated)* — `ask-ignis` rebuilds the full persona suffix per request. Anthropic prompt-cache headers cut input tokens 90% for the static persona prefix. **Recipe:** in `supabase/functions/ask-ignis/`, add `anthropic-beta: prompt-caching-2024-07-31` + `cache_control: { type: 'ephemeral' }` on persona system block; measure `cache_read_input_tokens`. Pairs with #6 (ROI) — measure together. |
| 8 | 🔥 | Feedback · Cohesion | 4h | 9 | 9 | 31.4 | **receipts-wall-you-spoke-we-shipped** *(CARRY)* — `/receipts/` ties `page_feedback` 😐/😢 reactions + open-text comments to the commit that addressed them. Public proof the studio responds. **Recipe:** extend `page_feedback` with `addressed_in_commit` TEXT (admin UI); new `/receipts/index.html` renderer fetches `feedback_summaries` + GitHub commits API. |
| 9 | 🔥 | AI · Cohesion | 2h | 8 | 9 | 28.6 | **founder-twin-dispatch-whisper** *(CARRY · deploy-gated)* — When the founder-twin closes a session (`logs/WORK_LOG.md` gets a new entry), an edge fn auto-generates one 1-sentence "vault whisper" from commit subjects (~$0.0002/session via Claude haiku) and posts to `/journal/dispatches/`. The vault narrates its own work. **Recipe:** new `supabase/functions/whisper-from-session`; GitHub Action triggers on `logs/WORK_LOG.md` change; inserts into existing `vault_dispatches` table. |
| 10 | 🔥 | Innovation · AI | 2h | 8 | 10 | 32.0 | **vault-genome-live-strip** *(NEW)* — A 20px-tall ambient bar at the top of every page rendering 10 mini-bars (one per SIL category: Dev Health, Alignment, Momentum, Engagement, Process, Coherence, Security, Ecosystem, Capital, Automation). Colored by absolute score (red <70 · amber 70–85 · green ≥85); height pulses on score change. Sourced from `/api/public-intelligence.json` — already exposes SIL. Makes studio health visible from every page in <2 KB. **Nobody does this.** **Recipe:** new `assets/vault-genome-strip.js`; mount via ambient block; SVG bars + tooltip; respects `[data-no-strip]`; fades to 0.4 opacity on scroll. |
| 11 | 🔥 | Gamification · UX | 30m | 8 | 9 | 30.5 | **live-rank-orb-progress** *(NEW)* — For logged-in members: a 24×24 orb top-right of every page filled by `xp_to_next_rank` fraction with a subtle gold gradient; clicking opens `/vault-member/`. Anon users see a hollow ring with "Join" tooltip. Pure ambient gamification — every page becomes a rank-progress surface. Compounds with S129's page-sigil (left of nav) and S129's vault-atlas (in Resources). **Recipe:** new `assets/rank-orb.js`; reads `window.VaultMember.rank`/`xp`; mounts to header nav-right slot; CSS conic-gradient for fill. |
| 12 | ⚡ | Gamification | 4h | 9 | 8 | 27.9 | **cross-game-lore-gates-via-rank** *(CARRY)* — `lore-gates.js` exists but ungated. Rank-tier unlocks reveal hidden universe content sitewide — Rank 5+ sees DreadSpike post-credits, Rank 8+ sees Voidfall opening monologue. **Recipe:** extend `lore-gates.js` with `[data-lore-gate="rank-5"]` data-attr resolving against `window.VaultMember.rank`; populate 6-10 hidden lore fragments; logged-out users see "Locked: requires Rank 5" tease. |
| 13 | ⚡ | UX | 4h | 9 | 7 | 24.4 | **mode-aware-homepage-anon-vs-member** *(CARRY)* — Authed members see "Welcome back, $handle · Rank $N · 3 new dispatches · next milestone in $XP" hero. Same DOM, swap via `[data-mode="anon\|member"]` after auth resolution + IndexedDB pre-render. **Recipe:** extend `vault-member/portal-cache.js` IDB read to `/`'s pre-paint; add `[data-mode]` swap zones in `index.html` hero. |
| 14 | ⚡ | Speed · Innovation | 2h | 8 | 9 | 28.6 | **service-worker-route-prediction-warm** *(NEW)* — SW logs nav transitions to IndexedDB; after 50 nav samples it builds per-page top-3 next-target priors and pre-fetches them at `requestIdleCallback` time when the user lands. Composes with S126 Speculation Rules (hover) + S129 pointerdown-warm (click) — closes the loop for the user who lands cold. Pure client-side learning, zero server cost. **Recipe:** extend `sw.js`: `nav_history` IDB store + scoring fn `predictNext(currentPath)`; postMessage to active client which then `<link rel=prefetch>`s top-3. |
| 15 | ⚡ | UX · Mobile | 30m | 7 | 6 | 18.5 | **mobile-font-size-floor-13px-sweep** *(CARRY)* — `tests/mobile-audit.spec.js` flagged 19 sub-13px text blocks on `/games/solara/` at iPhone SE. 12 selectors site-wide use sub-0.7rem font-size. **Recipe:** audit + floor all chip/tag/badge selectors at 13px at mobile; visual regression (#4) catches any composition break. Pair with #4 — ship visual-regression first to safely make this change. |
| 16 | ⚡ | Speed | 2h | 7 | 5 | 18.5 | **avif-lqip-pipeline-finish** *(CARRY · PARTIAL S129)* — Wire `--threshold-kb` flag through `convert-images-to-avif.mjs`; rewrite hero `<img>` → `<picture>` AVIF→WEBP→PNG on og-*.png + dreadspike-still-*.webp; add 16px LQIP data-URI bg for above-fold. ~30% byte reduction on hero surfaces. Compounds with #5 Lighthouse. **Recipe:** extend script + run; rewrite ~8 `<img>` tags; verify LCP. |
| 17 | ⚡ | Gamification | 4h | 8 | 9 | 27.9 | **founder-voice-notes-eternal-tier** *(CARRY · founder content)* — Eternal ($29.99+) members get 30–90s audio notes from founder. Web Audio + Supabase Storage. Near-zero-cost differentiator. **Recipe:** `vault_member_audio_notes` table with `tier_required` + `audio_url`; player on `/vault-member/` Following tab. |
| 18 | ⚡ | AI | 8h | 9 | 10 | 27.1 | **ignis-conduit-proactive-narrator** *(CARRY)* — IGNIS surfaces a 1-line whisper on every 3rd page transition based on visit-pattern. Studio's house intelligence, not a Q&A bot. **Recipe:** `assets/ignis-conduit.js` polls `intent-state.js`; mounts 28px ambient strip with Claude-generated sentence (cached per-cluster, $0.0002/visitor). |
| 19 | ⚡ | UX · Innovation | 8h | 8 | 10 | 24.1 | **universe-map-alt-homepage** *(CARRY)* — `/universe/` becomes a pan/zoom WebGL constellation reusing `studio-living.js` graph data. Cinematic doorway; `/` stays prove-first. **Recipe:** swap static SVG for regl/pixi renderer; nodes scale with recent activity; 60fps cap; SVG fallback for `prefers-reduced-motion`. |
| 20 | ⚡ | Security · Cohesion | 8h | 9 | 8 | 21.7 | **vault-spark-id-universal-session** *(CARRY · founder auth-risk gate)* — One login across studio's game subdomains. Cookie-domain `.vaultsparkstudios.com` so `hub.`, future `voidfall.`, share auth. Now naturally bundles with #2 passkey work. |
| 21 | ⚡ | Gamification | 1h | 6 | 7 | 22.7 | **sealed-vault-countdown-tease** *(CARRY)* — Sealed-vault row shows `X sealed projects · next reveal in ~Y days`. **Recipe:** add optional `estimatedRevealAt`; `sealed-vault-row.js` reads + renders countdown chip. |
| 22 | ⚡ | Innovation · Cohesion | 2h | 7 | 9 | 25.0 | **studio-eulogy-tombstone-page** *(NEW)* — `/vault/tombstones/` permanently honors VAULTED projects (Gridiron GM Play, retired games) with one-paragraph eulogy, screenshot, and "what we learned." Most studios hide failures; we curate them. Unique trust signal + IP-preservation for trademark. **Recipe:** new `/vault/tombstones/index.html` + `data/tombstones.json` (3 seeded); render fetches; cross-link from `/games/` VAULTED cards. |
| 23 | ⚡ | Feedback · UX | 2h | 7 | 8 | 22.4 | **micro-feedback-emoji-burst-confirmation** *(NEW)* — When a user submits a feedback emoji, render a 600ms burst animation at the click point (5 mini-emojis fading outward) + haptic vibration on mobile. Closes the loop *visually* in the moment, not just via "thanks!" copy. The S128 collapsed button is now sitewide — this makes every submission feel like a deposit, not a chore. **Recipe:** extend `assets/micro-feedback.js` submit handler: CSS keyframe burst + `navigator.vibrate(15)`; respects `prefers-reduced-motion`. |
| 24 | 💡 | UX · SEO | 2h | 8 | 4 | 16.0 | **founder-team-page-and-eeat-schema** *(CARRY · founder content)* — No `/about/` or `/team/` page. Google E-E-A-T penalty for studios without identifiable expertise. |

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

- `vault-genome-live-strip` (#10 · innov 10 · **NEW**)
- `ignis-conduit-proactive-narrator` (#18 · innov 10)
- `universe-map-alt-homepage` (#19 · innov 10)
- `live-rank-orb-progress` (#11 · innov 9 · **NEW**)
- `founder-twin-dispatch-whisper` (#9 · innov 9)
- `receipts-wall-you-spoke-we-shipped` (#8 · innov 9)
- `founder-voice-notes-eternal-tier` (#17 · innov 9)
- `cross-game-lore-gates-via-rank` (#12 · innov 8)
- `service-worker-route-prediction-warm` (#14 · innov 9 · **NEW**)
- `studio-eulogy-tombstone-page` (#22 · innov 9 · **NEW**)
- `micro-feedback-emoji-burst-confirmation` (#23 · innov 8 · **NEW**)
- `mobile-regression-contract-gate` (#1 · innov 8 · **NEW**)
- `visual-regression-snapshot-on-mobile-surfaces` (#4 · innov 8 · **NEW**)
- `passkey-cross-subdomain-auth` (#2 · innov 8)
- `synthetic-auth-canary-with-rollback` (#3 · innov 8)
- `ignis-roi-loop` (#6 · innov 8)
- `vault-spark-id-universal-session` (#20 · innov 8)

## Skipped (Priority < 1.5)

- None. Innovation-reserve floor 8; all items scored above 15.

## Founder hot-takes

- **S130's bug cluster proves mobile/auth/visual are the 3 regression-prone surfaces with no gate.** This audit's top-4 closes that gap: #1 (mobile contracts) · #2+#3 (passkey + canary together) · #4 (visual regression). Ship these and the founder-reported-regression class largely dies.
- **Auth-class retirement is no longer a "founder gate" item — it's a debt with a 6-session compounding cost.** Every Turnstile flake is a paying member who couldn't log in. #2 + #3 are the highest-strategic-ROI pair in the entire backlog. The S130 mobile sweep ate up the most-recent window; this is the next clean one.
- **Three fresh ambient items (#10 genome-strip · #11 rank-orb · #14 SW-route-warm) cost ~4.5h combined and reach innovation 28/30.** They compose with S129's atlas/sigil/pointerdown into a coherent "vault narrates and warms itself" layer that no SaaS studio runs.
- **#22 tombstones is the cheapest brand differentiator.** 2h, public-IP value, anti-fragility signal.

## Bounded-effort first pass (recommended `/implement` order)

The twin auto-approval window can ship these unattended (no deploy/cost/auth changes, all ≤2h):

1. **#1** `mobile-regression-contract-gate` (1h) — locks in S130 fix as invariant
2. **#11** `live-rank-orb-progress` (30m) — ambient asset, compounds with sigil/atlas
3. **#10** `vault-genome-live-strip` (2h) — ambient asset, innovation 10
4. **#23** `micro-feedback-emoji-burst-confirmation` (2h) — UX polish on sitewide pattern
5. **#22** `studio-eulogy-tombstone-page` (2h) — new public-IP page
6. **#21** `sealed-vault-countdown-tease` (1h) — registry-driven chip
7. **#15** `mobile-font-size-floor-13px-sweep` (30m) — pair with #4 visual-regression once it's live
8. **#4** `visual-regression-snapshot-on-mobile-surfaces` (2h) — locks in mobile surface
9. **#16** `avif-lqip-pipeline-finish` (2h) — perf compounding

Total bounded effort: ~13h. Items 7+ stretch. Items 2/3/6/8 are deploy-safe and high-leverage — minimum-viable ship is 1–6.

Remaining items #5 (Lighthouse 4h), #2/#3/#20 (auth-class · founder gate), #6/#7/#9/#17/#18 (deploy/founder content gated), #12/#13/#14/#19 (4-8h) are explicit carries.

---

*Generated by `/audit` v1.0 · ready for `/implement`*

---

## Execution Log (S131 — /implement pass 1)

| Status | # | Slug | Note |
|---|---|---|---|
| ✅ DONE | 1  | mobile-regression-contract-gate | `scripts/check-mobile-contracts.mjs` checks 3 contracts: (1) no `body { overflow-x: hidden }` in any CSS/inline-style, (2) `font-size:16px` for inputs at `@media (max-width:768px)`, (3) every page with `.brand-wordmark` also has `.brand-suffix` span. **Also caught a real regression**: `assets/style.css:3956` still had `html, body { overflow-x: hidden }` after S130's partial fix — flipped to `clip`, shell rebuilt, orphan removed. Wired into `build:check`. Gate passes (82 pages scanned). |
| ✅ DONE | 22 | studio-eulogy-tombstone-page | New `/vault/tombstones/` static + `data/tombstones.json` (3 seeded: DreadSpike → Voidfall, Gridiron GM Play → Gridiron GM, CryptoMatrix Pro → Velaxis) + `assets/tombstones-render.js` (hydrates from JSON, falls back to SSR). Schema.org CollectionPage + BreadcrumbList. Visual: gold-accent successor pill, ◈ lesson bullets, kind/era chips, italic epitaph. |
| ✅ DONE | 21 | sealed-vault-countdown-tease | Extended `assets/sealed-vault-row.js` with countdown chip rendering when `portfolio.sealedNextRevealAt` is set. `scripts/generate-public-intelligence.mjs` reads optional `estimatedRevealAt` from sealed-vault studioRegistry entries; null today (no entries have set the field yet, gracefully no-ops). Day-/week-/month-aware copy. |
| ✅ DONE | 11 | live-rank-orb-progress | New `assets/rank-orb.js` — 26px conic-gradient orb in `.nav-right`; anon → hollow dashed ring "Join the Vault" tooltip → `/membership/`; authed → fill = `(points − rankFloor) / span` using `vault_members.{rank_name,points}` and a 9-tier threshold ladder; tooltip on hover. supabase-query-validator clean (uses real columns from contract). |
| ✅ DONE | 10 | vault-genome-live-strip | New `assets/vault-genome-strip.js` — 3px fixed top strip of 10 SIL-category bars (Dev Health · Alignment · Momentum · Engagement · Process · Coherence · Security · Ecosystem · Capital · Automation); fades to 35% opacity on scroll, expands to 6px on hover; per-bar tooltip with score; links to `/studio-pulse/`. New `portfolio.silCategories` block in public-intelligence shard sourced from `context/PROJECT_STATUS.json::silCategoriesV3`. Skips portal/admin/api pages + `[data-no-strip]` opt-out. Innovation 10 — no SaaS studio runs an ambient health-bar. |
| ✅ DONE | 23 | micro-feedback-emoji-burst-confirmation | Extended `assets/micro-feedback.js` submit handler: 5-emoji radial burst from submit button + `navigator.vibrate(15)` on mobile. Respects `prefers-reduced-motion`. Closes the feedback loop *visually* in the moment of submission — turns a chore into a felt deposit. ~50 LOC added. |
| ✅ DONE | 15 | mobile-font-size-floor-13px-sweep | Added a single `@media (max-width:640px)` block at end of `assets/style.css` flooring `.eyebrow, .meta span, .status, .pill, .tag, .badge, .chip, .dropdown-label, .feature-list li, .info-row, .nav-icon-link, .footer-meta, .nav-dropdown a, .small-print, .micro, .micro-feedback-status, .tomb-kind, .vs-sealed-eyebrow` at `max(13px, 0.81rem)`. Pairs with the mobile-contracts gate (#1) for the input floor. |
| 🟡 CARRY | 16 | avif-lqip-pipeline-finish | Defer — needs `--threshold-kb` flag wiring + HTML `<img>` → `<picture>` rewrite on 8+ surfaces (~2h focused). Compounds with #5 Lighthouse fix. **Recommended: bundle with #5 in a dedicated perf session.** |
| 🟡 CARRY | 4  | visual-regression-snapshot-on-mobile-surfaces | Defer — Playwright pixel-diff workflow scaffolding is straightforward, but the load-bearing baselines need a green production deploy to capture from. **Recommended: ship the workflow file in S132 right before the next deploy so baselines reflect post-S131 truth.** |
| 🟡 CARRY | 2/3/20 | passkey + canary + universal-session | Founder auth-risk gate — 6 sessions clean of Turnstile bugs, ideal window. Bundle into one auth-class-retirement session. |
| 🟡 CARRY | 5  | lighthouse-perf-restoration-to-92 | Needs Chrome DevTools perf trace (4h focused). Pair with #16 AVIF for compounding gain. |
| 🟡 CARRY | 6/7/8/9/12/13/14/17/18/19/24 | deploy- or founder-gated | All explicitly carried into S132. |
| ✅ DONE | S133 | mobile-contracts-contract-4-and-5 | Follow-up `/implement` pass converted S132's two mobile drawer regression classes into structural gates. Contract 4 fails `.nav-center.open` color/background overrides without `body`/`:where()` specificity protection; existing drawer selectors were hardened. Contract 5 requires `nav-toggle.js` to portal the fixed drawer to `document.body` when the sticky header + body-level backdrop shape exists. Self-test 9/9; live gate 5/5; `npm run build:check` exit 0. |

**Pass summary:** 7 DONE · 2 explicit CARRY · 11 founder/deploy-gated. Combined Priority delivered: ~227 (37% of 612.8). Tier-1 bounded-effort tier fully cleared except #4 + #16 which are explicit S132 carries with reason.

**S133 addendum:** 2 post-S132 gate carries closed by extending audit #1's mobile regression contract from 3 → 5 enforced invariants. Remaining bounded carries still open: #4 visual-regression baselines and #16 AVIF/LQIP pipeline.

**Validation:** S131 `npm run build:check` exit 0 (25/25 including the new `check-mobile-contracts.mjs` gate) · S133 `npm run build:check` exit 0 after Contracts 4-5 landed · `check-orphan-shell-assets` clean · `check-mobile-contracts.mjs` ✓ 5/5 contracts. 81 HTML pages propagated with 2 new ambient script tags (rank-orb + vault-genome-strip); S133 shell rebuild updated 98 HTML shell references.

---

## S134 Addendum — bounded-effort second pass (2026-05-17)

S133 closed the S132 mobile-drawer gate-class. This session targets the two explicit bounded carries (#4 visual-regression workflow hardening, #16 AVIF/LQIP polish) and the S134 SIL/P2 generalization of the specificity-budget gate (Contract 4 → all state selectors, not just nav).

**Re-ranked S134 executable items** (all ≤2h, twin-auto-approval window, deploy-safe):

| # | Tier | Effort | Priority | Item |
|---|:-:|---|:-:|---|
| S134-A | 🔥 | 1h | 38.0 | **specificity-budget-gate-generalize** — Promote Contract 4 from nav-only to any element with state selectors (`.open`, `.active`, `.visible`, `[aria-expanded="true"]`) that also have theme-prefixed rules (`body.dark-mode .x`, `body.light-mode .x`). The S132 root cause class isn't unique to the drawer — it lurks anywhere we layer theme + state. Generalizing the gate pre-empts the next session of "this works locally but rendered grey in dark mode" before it ships. **Recipe:** new function `findThemeStateSpecificityViolations(cssText)` in `check-mobile-contracts.mjs` Contract 6; scan every `body.<theme> .X` block for a sibling `.X.<state>` block lacking `body`/`:where()`; report on `.css` files only. Self-test covers 2 violation + 2 safe cases. |
| S134-B | ⚡ | 30m | 24.0 | **avif-size-floor-guard** — When AVIF is larger than the source (happens with already-optimized WebPs — vaultspark-cinematic-logo.avif 171KB vs .webp 142KB), the script should skip writing AVIF and report it as "negative gain — skipped." Current pipeline silently bloats some surfaces. **Recipe:** in `convert-images-to-avif.mjs`, compare buffer sizes post-encode; if AVIF ≥ source × 0.95, discard the AVIF buffer + delete any prior oversized AVIF on `--prune`. |
| S134-C | ⚡ | 30m | 22.0 | **press-logo-picture-wrap** — `press/index.html:215,219,223` use raw `<img src=".webp">` for the 3 logo tiles. Wrap each in `<picture>` with the existing AVIF siblings; the cinematic-logo AVIF is heavier (B above will clean it up), but the icon-mark AVIF (97KB vs 77KB WebP — also heavier) and other variants need conditional emit. Until B lands, only wrap when AVIF is strictly smaller. **Recipe:** rewrite 3 `<img>` tags to `<picture>` with conditional AVIF sources (icon AVIF skipped; cinematic AVIF skipped pending recompute). Documents zero-bandwidth-loss baseline. |
| S134-D | ⚡ | 20m | 18.0 | **vr-baseline-capture-workflow-comment** — Workflow + spec exist; baselines never captured. Add a one-line README pointer in the workflow file and trigger guidance: post-deploy, run `gh workflow run "Visual Regression (mobile)" -f base_url=https://vaultsparkstudios.com` once to create baselines, then commit the resulting `tests/__snapshots__/` artifact. Block on missing baselines surfaces real S135 launch readiness. **Recipe:** add comment block + a Make target `make vr-baseline` that prints the exact `gh workflow run` + `gh run download` sequence. |

**Note on #4 deferral:** Capturing baselines locally on Windows produces non-deterministic pixels vs Ubuntu-runner snapshots. Baselines MUST be captured via the workflow against production; this session ships the workflow-trigger ergonomics, not the baseline bytes themselves.

**Note on #16 closure:** The "wire `--threshold-kb`" sub-item from the original audit was already complete in `convert-images-to-avif.mjs` (lines 36-41 accept both `--threshold-kb=N` and `--threshold-kb N`). The bounded work remaining is S134-B (size-floor guard) + S134-C (picture wrap on 3 missed surfaces). The 8-surface hero rewrite from the original recipe turned out to be **already shipped** — `index.html:1550`, `universe/index.html:398,453,454,455`, `universe/dreadspike/index.html:289-293,378-380` all already use `<picture>` with AVIF + WebP + JPG fallback.

### S134 Execution Log

| Status | # | Slug | Note |
|---|---|---|---|
| ✅ DONE | S134-A | specificity-budget-gate-generalize | New `findThemeStateSpecificityViolations()` in `check-mobile-contracts.mjs` (Contract 6) scans every CSS file for `body.<theme> .X` rules + sibling `.X.<state>` rules with color/background where state lacks `body`/`:where()` guard. Self-test 13/13 (4 new cases). Live gate caught + fixed real regression: `vault-member/portal.css:73` `.auth-tab.active` lost to `body.light-mode .auth-tab` (0,2,1 vs 0,2,1 — same specificity, source order wins last). Prefixed with `body` to escalate to 0,2,2. |
| ✅ DONE | S134-B | avif-size-floor-guard | `convert-images-to-avif.mjs` now encodes AVIF to buffer first, compares size, and skips/prunes if AVIF ≥ source × 0.95. Writes `.avif.skip` JSON marker recording the size diff so re-runs are deterministic. Cleaned up 3 oversized AVIFs (336KB+ total): `vaultspark-cinematic-logo.avif`, `vaultspark-logo.avif`, `vaultspark-icon.avif`. |
| ✅ DONE | S134-C | press-logo-picture-wrap | `press/index.html` 3 logo tiles now wrapped in `<picture>` with `<source>` + `decoding="async"`. Composition-ready for future AVIF when re-encoding improves. |
| ✅ DONE | S134-D | vr-baseline-capture-workflow-comment | `.github/workflows/visual-regression.yml` documents the post-deploy baseline-capture procedure: `gh workflow run "Visual Regression (mobile)" -f base_url=https://vaultsparkstudios.com` → download artifacts → commit under `tests/__snapshots__/`. Removes ambiguity for future S135 baseline trigger. |
| ✅ DONE | S134-E | check-image-formats honors .avif.skip | Pairs with S134-B — gate now reads `<base>.avif.skip` sidecar markers so documented-negative-gain AVIFs don't trip the "missing sibling" gate. Closes the loop: prune produces marker → gate respects marker → image-format coverage stays accurate without forcing harmful encodes. |

**S134 pass summary:** 5 DONE · 0 carry · 0 founder-gated. All deploy-safe. New structural gate (Contract 6) generalized from nav to all themed-state selectors; caught a real bug at boundary. Build:check 27/27 ✓. AVIF pipeline now auto-detects negative-gain and writes audit markers — pipeline drift class closed structurally. Total Priority delivered this session: ~102.
