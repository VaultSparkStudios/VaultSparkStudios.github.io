# Audit — Session 253

Date: 2026-07-03
Project: VaultSparkStudios.github.io
Mode: /arc audit

## Summary

The fresh arc audit re-verified the generated hit list before acting. Several high-scoring items remain correctly gated rather than implementable:

- `play-next conversion redesign` still needs a trustworthy post-2026-07-02 viewport-impression window.
- `INP root-fix` still needs enough clean post-filter field data, despite build:check surfacing field over-budget game routes.
- forge devlogs remain founder-voice gated.
- Atlas registry freshness remains studio-ops-owned until the canonical project entry carries a real description.

The best local item was `TT-ENFORCE-REPROBE`: it directly supports SOUL #3 security, had current evidence available through the Cloudflare KV probe, and exposed active first-party Trusted Types sinks.

## Shipped

- Reprobed Trusted Types soak evidence with `scripts/probe-tt-soak.mjs`: 449 violations across 28 counter days in the last 30 days, so enforcement remains AMBER / not ready.
- Rebuilt the violation cluster report with `scripts/analyze-tt-violations.mjs`: current top clusters are now recorded in `docs/TT_BURNDOWN_2026-07-03.md`.
- Removed default-policy dependence from active first-party sinks:
  - `assets/home-dynamic-hero.js` now builds the dynamic spotlight with DOM nodes instead of `innerHTML`.
  - `assets/vault-pulse.js` now builds ticker rows and footer link with DOM nodes instead of `innerHTML`.
  - `assets/membership-idle-loader.js` now uses a narrow `vs-membership-idle-loader` TrustedScriptURL policy for same-origin `/assets/` scripts.
  - `assets/turnstile.js` now uses a narrow `vs-turnstile` TrustedScriptURL policy for the Cloudflare Turnstile API URL.
- Regenerated tracked public/generated artifacts with `npm run build`, including ambient shell bundles.\n- Corrected VEILOS from a thin “privacy product” label to its actual public identity: a D1-backed public Cognitive Civilization OS with Sovereign Dashboard, Chain Verification, proprietary IP, Collaborate Exchange, onboarding ceremony, status/changelog/legal surfaces, and `veilos.world` redirect live.

## Evidence

- `node scripts/analyze-tt-violations.mjs --self-test` passed 5/5.
- `node --check` passed for all four edited JS files.
- `node scripts/lint-repo.mjs` passed clean.
- `npm run build` exited 0.
- `npm run build:check` exited 0.
- `node scripts/run-doctor.mjs --json` exited 0 with `overallPass:true` and `blockingFailing:0`.
- `node ../vaultspark-studio-ops/scripts/check-cost-gate.mjs --project vaultsparkstudios-website` allowed the project (`cost-neutral`).

## Carries

- Keep `TT-ENFORCE-REPROBE` open: enforcement is not ready until fresh trailing soak counters and clusters reach near-zero, then founder real-device verification approves any flip.
- Cross-repo football-gm Trusted Types sinks remain outside this repo's write boundary.
- `schema-injector` and older shell-hash clusters may be stale or generated from prior deployed assets; verify after this deployment accrues new reports before further source changes.