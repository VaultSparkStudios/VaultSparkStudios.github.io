# Audit 2026-07-07 S266 — Arc Saturation

## Ranked Plan

1. **[ENGAGEMENT/P1] Membership value calculator runtime restored — shipped**
   - Premise verified: `/membership-value/` still had the `data-membership-value-calculator` mount and the calculator CSS, but no page script loaded `assets/membership-value-calculator.js`; `check-orphan-assets` reported that browser-shipped runtime as an orphan.
   - Fix: restored the calculator script include on `/membership-value/` so the personalized value calculator renders again.
   - Verification: `node scripts\check-page-script-relevance.mjs`; `node scripts\check-orphan-assets.mjs --json`; `node scripts\check-orphan-assets.mjs --strict`.

2. **[VERIFY/P1] Required runtime contract for calculator mount — shipped**
   - Premise verified: the previous build only surfaced the missing calculator runtime as advisory orphan output, so a user-facing feature could be blank while `build:check` still passed.
   - Fix: `scripts/check-page-script-relevance.mjs` now has a required-runtime rule: any page with `data-membership-value-calculator` must load `membership-value-calculator.js`. Self-test covers both allowed and violation cases.
   - Verification: `node --check scripts\check-page-script-relevance.mjs`; `node scripts\check-page-script-relevance.mjs --self-test`; `node scripts\check-page-script-relevance.mjs`.

3. **[VERIFY/P2] Browser asset orphan gate promoted to strict — shipped**
   - Premise verified: after restoring the calculator, the browser-asset orphan baseline is zero. That makes strict gating safe for `assets/*.js` while preserving advisory script-orphan behavior for manual/cross-repo tools.
   - Fix: `package.json` build-check steps now run `node scripts/check-orphan-assets.mjs --strict`.
   - Verification: `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8')); console.log('package json ok')"`; `node scripts\check-orphan-assets.mjs --strict`.

4. **[PROCESS/P2] Task board stale-block rotation — shipped**
   - Premise verified: `npm run build:check` reported `TASK_BOARD.md` had 2 rotatable blocks past the 3-session window; `rotate-taskboard --dry-run` confirmed sessions `< 262` would move.
   - Fix: ran the existing rotation script, moving S260/S261 outcome blocks to `context/archive/TASK_BOARD_ARCHIVE.md` and restoring the board to the active window.
   - Verification: `node scripts\rotate-taskboard.mjs --self-test`; `node scripts\rotate-taskboard.mjs --check-size`.

5. **[PERF/P1] Field LCP/INP advisory — deferred honestly**
   - Premise verified: build-check refreshed `.cache/perf-fix-recipes.json` with `/` field p75 LCP over budget and `/games/vaultspark-football-gm/` field p75 INP over budget.
   - Decision: no performance root fix claimed in this session. `analyze-home-lcp --check` reports a local 192ms image path, while field data says the live issue is broader/persistent and lacks a good-to-bad transition window. A real fix needs focused trace/waterfall evidence and, for Football GM, likely cross-repo handling through Ark rather than editing a sibling tree.

## Execution Log

| Item | Outcome | Evidence |
|---|---|---|
| Membership value calculator runtime restored | shipped | `/membership-value/` loads `/assets/membership-value-calculator.js`; browser asset orphan count is 0. |
| Required runtime contract | shipped | `check-page-script-relevance` self-test now covers missing calculator runtime and live scan passes. |
| Browser asset orphan strict gate | shipped | `check-orphan-assets --strict` passes and `package.json` build-check steps use `--strict`. |
| Task board rotation | shipped | `rotate-taskboard` moved 2 stale blocks; `--check-size` reports ok. |
| Field LCP/INP advisory | deferred | Recorded as evidence-backed carry; no fabricated root fix. |

## Remaining Gated Work

- Founder/content gates: forge devlogs and richer public IGNIS exposure.
- Evidence gates: TT enforcement near-zero soak, play-next true-viewport sample threshold, homepage field LCP root-cause trace, Football GM INP post-deploy soak.
- Credential/provider gates: full Obelisk RP/provider flip and external receipt/browser checks.