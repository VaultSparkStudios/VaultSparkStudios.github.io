<!-- generated-by: /audit skill v1.0 (S156 addendum) -->
<!-- generated-at: 2026-05-22 -->
<!-- project: vaultsparkstudios-website · type: website (platform-weighted) -->

# Project Audit — VaultSparkStudios.github.io · S156 Addendum

> Builds on `docs/AUDIT_2026-05-22.md` (S154, 22 items) and `docs/AUDIT_2026-05-22-S155.md`
> (S155, +8 items). S156 personalizes against today's exact surface: S155 shipped #1
> prod-LCP CSS quick-wins + #30 ambient vault-pulse favicon, but #18 (Contract 7),
> #29 (stale-while-revalidate), and the perf-history regression validation remain
> open and shippable today without founder unlock. R2 capability is still founder-blocked
> (R2:Edit token scope), so we skip #23/#10/#25 and route Trusted Types reporting
> through a Worker KV path instead — eliminating the dependency entirely.
>
> Weights: Security 2× · Speed 2× · UX 1.5× · Feedback 1.5×. Carry items retain
> their audit-doc ID. New S156 items are 31–34.

## Top-line

- Ranked rows: 6
- New additive items: 4 (#31, #32, #33, #34)
- Carry-forward items re-ranked: 2 (#18, #29)
- Combined Priority (all rows): 128.2
- New Priority delta: +86.0
- Top 3 by score: **perf-budget-guardian (#31)** · **trusted-types-report-only-via-kv-rotation (#32)** · **ignis-edge-stale-while-revalidate (#29)**

## S156 Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 31 | 🔥 | Speed · Reliability · Observability | 1h | 9 | 9 | 26.7 | **perf-budget-guardian** — a structural gate that fails `build:check` when the rolling-3-sample median LCP/CLS for any tracked (route × profile) in `data/perf-history.ndjson` exceeds budget. Catches regressions *before* the next audit instead of *during*; turns S153's `--detect-regressions` into a closeout-blocking contract. No studio enforces a perf budget against a longitudinal timeseries — most use single-shot Lighthouse. **Recipe:** `scripts/check-perf-budget.mjs --strict` reads ndjson, groups by route+profile, computes median of last 3 prod samples, fails if `>budget[route].lcp` (default 2500ms desktop / 3000ms mobile) or median CLS > 0.1. Wire into `npm run build:check` as advisory until 2 sessions of clean runs, then `--strict`. Self-test injects synthetic regression and asserts detection. |
| 18 | ⚡ | Mobile · UX | 1h | 7 | 6 | 19.4 | **mobile-contract-7-safe-area-gate** — extend `check-mobile-contracts.mjs` with Contract 7: any `position: fixed` rule targeting `top:0` / `bottom:0` / `inset-block-*: 0` must include `env(safe-area-inset-*)` padding (or be allowlisted with a `/* allow: <reason> */` marker). Prevents iPhone notch/home-indicator overlap regressions before they reach Safari. **Recipe:** parse CSS blocks via existing `parseCssBlocks`, find `position: fixed` with top/bottom-0, check body for `env(safe-area-inset-`. Self-test with 3 cases (violation / safe / allowlisted). |
| 29 | ⚡ | Speed · Token cost · Innovation | 45m | 8 | 8 | 22.8 | **ignis-edge-stale-while-revalidate** — wrap `/api/public-intelligence.json` (and `/api/heartbeat.json`, `/api/founder-presence.json`) in Worker-level cache with `stale-while-revalidate=300, max-age=60`. The JSON hot-path is currently served straight from GitHub Pages with a fixed 2h TTL (line 55 of Worker), which makes data stale-for-the-many while invalidating-for-the-one. SWR keeps every visitor instant while a background fetch refreshes. **Recipe:** new `JSON_SWR_PATHS` allowlist in `cloudflare/security-headers-worker.js`, intercept before the static cache rule, set `Cache-Control: public, max-age=60, stale-while-revalidate=300`. |
| 32 | 🔥 | Innovation · Trust · Security | 1h | 8 | 9 | 24.0 | **trusted-types-report-only-via-kv-rotation** — start the S155 #10 soak *without* the R2 bucket dependency. Use existing `RATE_LIMIT` KV namespace (already bound) as a rolling-1000-entry sink with TTL=86400 and a key prefix `tt:`. The Worker `/v/tt-report` route accepts CSP violation reports, applies a 0.5% sample, writes to KV with TTL. Reads via Worker debug route gated by founder cookie. Eliminates 2-week founder unlock dependency. **Recipe:** add `Content-Security-Policy-Report-Only: require-trusted-types-for 'script'; report-to vs-tt` header + `Reporting-Endpoints` header pointing at `/v/tt-report`, plus the KV-backed handler. |
| 33 | ⚡ | Innovation · UX · Cross-tab | 45m | 6 | 9 | 19.0 | **broadcastchannel-presence-mirror** — S155's `assets/favicon-pulse.js` polls `/api/founder-presence.json` per tab. Add a `BroadcastChannel('vault-presence')` so the first tab to fetch broadcasts to all sibling tabs in the same browser — every additional tab cuts polling to zero. Edge-pattern: presence is browser-scoped truth, not tab-scoped. **Recipe:** `assets/favicon-pulse.js` adds `bc = new BroadcastChannel('vault-presence'); bc.onmessage = (e) => apply(e.data); bc.postMessage(state)` after every fetch; leader-election by lowest `crypto.randomUUID()` per refresh window. |
| 34 | ⚡ | Verify · Observability | 15m | 7 | 4 | 16.3 | **prod-lcp-fix-history-validate** — re-run `node scripts/measure-page-performance.mjs --check --base=https://vaultsparkstudios.com/` for `/` + `/membership/`, append to perf-history, run `--detect-regressions` against the rolling baseline. Closes S155 #1 verification loop and gives perf-budget-guardian (#31) its first 3rd data point. **Recipe:** measure → `append-perf-history.mjs` → detect-regressions. Skip if Pages deploy not yet propagated (parity check). |

## Optimal-efficiency execution order (S156 sprint)

1. **#18 Mobile Contract 7** — 1h, hot gate, smallest cognitive surface
2. **#31 perf-budget-guardian** — 1h, ships fastest, unblocks #34 enforcement loop
3. **#29 ignis-edge-stale-while-revalidate** — 45m, Worker edit, atomic deploy
4. **#33 broadcastchannel-presence-mirror** — 45m, polish on S155 #30
5. **#32 trusted-types-report-only-via-kv** — 1h, defer-able to next session if budget tight
6. **#34 prod-lcp-fix-history-validate** — 15m, gated on Pages deploy propagation; advisory

## Carry-from-S155

| # | Slug | Status entering S156 | This-session expectation |
|---|---|---|---|
| 1 | prod-lcp-regression-rootcause | FIX SHIPPED locally · validation pending | **Re-validate via perf-history** (#34) |
| 18 | mobile-contract-7-safe-area-gate | not started | **Ship Contract 7** |
| 29 | ignis-edge-stale-while-revalidate | not started | **Ship SWR JSON cache** |
| 10 | csp-trusted-types-strict-mode | blocked on R2 | **Reroute via KV** (#32) |
| 30 | ambient-vault-pulse-favicon | SHIPPED | **Add BroadcastChannel** (#33) |

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

- #31 perf-budget-guardian (Innov 9)
- #32 trusted-types-report-only-via-kv-rotation (Innov 9)
- #33 broadcastchannel-presence-mirror (Innov 9)
- #29 ignis-edge-stale-while-revalidate (Innov 8)

## Skipped (Priority < 1.5)

- 0 items skipped. The low-value/blocked S155 candidates remain excluded until they have fresh evidence or an available capability path.

## Execution Log

| Item | Outcome | Evidence |
|---|---|---|
| #18 mobile-contract-7-safe-area-gate | Already shipped before this implement pass; kept as DONE. | `scripts/check-mobile-contracts.mjs` contains Contract 7 parser and report block; fixed CSS selectors include safe-area padding. Focused self-test escalation was rejected after a Windows sandbox spawn error. |
| #31 perf-budget-guardian | Already shipped before this implement pass; kept as DONE. | `scripts/check-perf-budget.mjs` exists and `package.json` wires self-test + advisory gate into `build:check`. Focused self-test escalation was rejected after a Windows sandbox spawn error. |
| #29 ignis-edge-stale-while-revalidate | Already shipped before this implement pass; kept as DONE. | `cloudflare/security-headers-worker.js` includes `JSON_SWR_PATHS` and SWR cache-control handling for public JSON hot paths. |
| #33 broadcastchannel-presence-mirror | Already shipped before this implement pass; kept as DONE. | `assets/favicon-pulse.js` uses `BroadcastChannel('vault-presence')` and leader-election state to reduce duplicate polling. |
| #32 trusted-types-report-only-via-kv-rotation | SHIPPED in this implement pass. | `cloudflare/security-headers-worker.js` now emits Trusted Types report-only headers and accepts sampled `/v/tt-report` posts into a privacy-minimized `RATE_LIMIT` KV ring buffer. `node --check cloudflare/security-headers-worker.js` passed. |
| #34 prod-lcp-fix-history-validate | Deferred. | Requires fresh production deploy parity and external browser trace after this closeout lands. |

---
*Generated by `/audit` v1.0 · S156 addendum · ready for `/implement`*
