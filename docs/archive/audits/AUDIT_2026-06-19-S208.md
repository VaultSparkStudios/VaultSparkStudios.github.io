# AUDIT — Session 208 · VaultSparkStudios.github.io

Generated: 2026-06-19 · Mode: BUILDER (single-repo) · Rubric: website/public-live
Method: every item's premise verified against LIVE code before ranking. Phantom items rejected. Honest deferrals recorded as wins.

> **Frame:** Founder flagged that a prior session was *cut off mid-work (terminal froze)*. Forensics (git reflog, stashes, working tree, push-contract verifier) found **no orphaned commit or deliverable** — S207 is fully committed/pushed and the web-push feature is complete + verified. BUT premise-verification surfaced the real unfinished work: **S207's handoff claimed "SEALED retired + purged sitewide" — and that claim is false.** The retired status still renders on 89 pages. That, plus the S207-scoped "premium roadmap" deferred to "next session," IS the cut-off work. This audit finishes it.

---

## TIER 1 — Finish the cut-off / falsely-claimed-done work  *(P0)*

### 1. [BRAND·HONESTY] Complete the SEALED→VAULTED purge — `score 99`
**Premise (VERIFIED):** S207 handoff + CDR claim "SEALED retired + purged sitewide." Reality: the footer `vault-status-legend` still renders `⬡ SEALED — Vault sealed` on **89 pages**. Source of truth = `scripts/propagate-nav.mjs:247`.
- Violates the Vault Lifecycle canon (D-S207.9: SEALED retired, folds into VAULTED).
- Violates the "observability must not lie" non-negotiable (a closeout claimed done-work that wasn't).
- The SEALED line is also redundant — it duplicates `VAULTED — Paused`.
**Root-fix:** remove the `legend-status-sealed` span from `propagate-nav.mjs` template → re-propagate to all 89 pages. Also audit the `legend-status-sparked` gloss `SPARKED — Live` against the founder's explicit "SPARKED not Live" coinage; tighten to honor it without losing the first-visitor definition. Sweep remaining user-facing `vault-sealed` / "Sealed silhouette" prose.
**Verify:** `grep -rl 'legend-status-sealed' --include=*.html` → 0 ; build:check EXIT 0.

---

## TIER 2 — Premium roadmap (S207-scoped, deferred to this session)

### 2. [PERF] Kill the perf-budget phantom + ship a real CWV win — `score 96`
**Premise (VERIFIED):** `check-perf-budget` reports `/ (desktop) LCP 13060ms > 2500ms`. Traced: the rolling-3-sample median is dragged by **two 3-week-old S161 LCP-incident artifacts** (13060ms + 14528ms, 2026-05-24) — a regression already root-caused and FIXED (the 2026-06-03 sample is 676ms; real field RUM p75 on 2026-06-16 is **976ms**). The window has no recency bound, so resolved-incident samples linger forever and lie.
**Two-part root-fix:**
  - (a) **Recency-aware / RUM-first gate** — make `check-perf-budget` prefer real field RUM p75 when sufficient samples exist, and/or bound the synthetic window by recency so a closed incident can't drag it. Honest data only — no deletion-to-go-green.
  - (b) **Real speed win** — the 8 cover PNGs (`assets/covers/*.png`, ~920KB total) have NO AVIF/WebP variants. Generate modern formats via the existing `sharp` devDep (no new deps, per S196 precedent) + responsive `<picture>`/`srcset` where covers render.
**Verify:** perf-budget no longer flags `/` on a stale basis; cover payload measurably smaller; build:check EXIT 0.

### 3. [POLISH] Bespoke OG card for Atlas (+ generic-og pages) — `score 88`
**Premise (VERIFIED):** `atlas/index.html` og:image = generic `og-home.png` (used on 2 pages). Atlas is the flagship ecosystem map and deserves a bespoke social card.
**Root-fix:** generate a bespoke per-page OG via the existing sharp SVG→PNG generator (no new deps). Atlas first; extend to any other generic-og page.

### 4. [DEPTH] Atlas v2 — covers + "moving this week" strip — `score 87`
**Premise (VERIFIED):** `atlas/index.html` has 0 per-project cover thumbnails and 0 live "moving this week" strip. `build-atlas.mjs` server-renders rows from the live catalog.
**Root-fix:** add per-project cover thumbnails to atlas rows (reuse `assets/covers/*` + new modern formats from #2) and a "moving this week" live strip derived from a source-of-truth feed (commit velocity / public-intelligence) — honest, self-validating, honest-dark when no recent motion.

### 5. [COHESION] Graduate the elite hero treatment site-wide — `score 84`
**Premise (PARTIAL — confirm during implement):** S207 redesigned the homepage hero (`hero-chamber`, `hero-glow`, `hero-stage`, `hero-showcase`). Genius list asks to graduate gradient-glass/accent-glow/premium-easing into `/games/`, `/membership/`, `/studio/`, Atlas rows. Confirm which treatment tokens are homepage-only before porting.

---

## TIER 3 — Naming / content coherence

### 6. [BRAND] Forge Window naming propagation — `score 80`
**Premise (confirm scope):** finish propagating Forge Window vocabulary across generated copy/shared surfaces while preserving `/studio-pulse/` URL for SEO (`scripts/propagate-nav.mjs`). Verify residual drift before acting.

---

## HONEST DEFERRALS  *(recorded as wins, not silent skips)*

| Item | Why deferred | Disposition |
|---|---|---|
| Publish forge devlog | FOUNDER-voice essay; the draft tool never auto-publishes by design (memory-confirmed) | DEFER → founder |
| Hero v2 real-device review | Founder-gated visual judgment | DEFER → founder |
| Obelisk Passport auth flow | Founder's parallel session owns auth (302/no-store guardrail D-S207.8) | DEFER → founder |
| HERO V2 GRADUATION | Already shipped as default in S207 (`?hero=classic` kill-switch) | CLOSE — done |
| Measurement-watch: play-next rotation | Needs post-S207 visit data to accrue; insufficient samples | DEFER — data-gated |
| Sibling compliance drift (Syntha/VOID/SHADOW/MindFrame) + VEILOS launch blocker | Cross-repo; doctor probe is portfolio-wide. NEVER edit sibling trees to go green | Ark cargo → studio-ops |
| IGNIS 7d staleness | `rescore --stale` reports all fresh; brief warning is a display artifact | NO-OP |

---

## SECOND-ORDER INNOVATION  *(generate + implement after primary list — saturate directive)*
To be expanded once Tier 1–3 ship; candidates seeded during implement (e.g. a closeout-claim self-validator that would have caught the false "purged sitewide" claim — directly preventing this class of observability lie).

---

## RECOMMENDED BUILD ORDER
1 (SEALED purge — root-fix + re-propagate) → 2 (perf gate + cover formats) → 3 (Atlas OG) → 4 (Atlas v2) → 5 (hero cohesion) → 6 (naming) → second-order pass → closeout.
