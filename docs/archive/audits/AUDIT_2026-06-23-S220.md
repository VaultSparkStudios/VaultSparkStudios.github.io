# AUDIT — Session 220 · VaultSparkStudios.github.io

Generated: 2026-06-23 · Mode: continuous arc (/start → /audit → /implement → /closeout)
Profile: **website · public-live · SPARKED** · product SIL rubric · website/public lens (CANON-011/041/047/048/045)
SIL at audit: 960/1000 · Doctor: blockingFailing **0** (3 advisory, all sibling/portfolio scope) · Tests 116/116 · sitemap 117 · dead-CTAs 0

> Every item below has had its premise **verified against live code** this session. The S219 audit (`AUDIT_2026-06-23.md`) is fully consumed; this is a fresh frontier. An Explore fan-out surfaced 6 candidates; 3 were rejected/deferred on inspection (recorded with evidence — a deferral with evidence is a WIN, not a skip).

---

## START-STATE TRUTH (verified this session)

- `git pull --rebase` fast-forwarded `d999948f..10f40d0d` — the only incoming commits were hourly CI-beacon `api/ci-status.json` data. Tree clean except one untracked orphan.
- `scripts/lib/obelisk-broker.mjs` is **byte-identical** (`diff` → IDENTICAL) to the canonical `../vaultspark-studio-ops/scripts/lib/obelisk-broker.mjs`. Untracked here, imports `./secrets.mjs` (absent in this repo) + references `portfolio/` (studio-ops path). Already Ark-shipped S219. → pure debris in the website tree.
- `check-orphan-libs` EXIT 0: flags it as allowlisted-but-untracked (never committed → absent on fresh clone/CI).
- Hero portfolio JSON-LD (`scripts/build-hero-portfolio.mjs:186-198`) emits a bare 4-property schema (`@type`, `name`, `url`, `creativeWorkStatus`) — no `image`, `description`, `genre`, or game-specific fields, and never surfaces the live product domain.
- Site hygiene green: `api/dead-ctas.json` deadCount 0; `agents.json` + `.well-known/llms.txt` regenerated today; sitemap 117 URLs.

---

## RANKED PLAN

### TIER 1 — Hygiene root-fix (verified, immediate)

**A1 · Resolve the `obelisk-broker.mjs` orphan — remove the debris** — score 85
Premise VERIFIED LIVE: the website's untracked copy is byte-identical to the authoritative studio-ops copy, lives on studio-ops paths, was preserved via Ark in S219, and has zero consumers here. The long-standing "disposition pending" carry (S183 → S219 B2) is now cleanly closeable: **delete it from the website working tree** (the canonical copy is safe in its proper home), prune its now-stale `check-orphan-libs` allowlist entry, and record the disposition in DECISIONS. This removes ~40 sessions of latent untracked debris without losing any work.

### TIER 2 — Real site + agent value (verified) — FLAGSHIP

**A2 · Enrich the hero-portfolio ItemList JSON-LD** — score 88
Premise VERIFIED LIVE (`build-hero-portfolio.mjs:186-198`): the homepage hero — explicitly "the first interface for BOTH humans and agents" — ships a minimal 4-prop schema per tile. Search engines and AI agents that read the ItemList get no image, no description, no genre, and no link to the actual live product. Enrich `renderJsonLd` (all data already on the catalog item — zero new build inputs):
- `image` — absolute cover URL from the `COVERS` map (PNG) when present.
- `description` — `item.note` ‖ `item.category` (truthful, already on the feed).
- `genre` / category — `item.category`.
- For `VideoGame` items: `applicationCategory: "GameApplication"`, `gamePlatform: "Web browser"`, `operatingSystem: "Any (browser)"`.
- `sameAs` — the live product domain (via the existing `liveHref`/`deployedUrl`) for SPARKED products on external domains (VEILOS → veilos.io, Vorn), so the JSON-LD links the studio page to the real destination instead of dead-ending at a generic landing.
Gate-protected: extend the `--self-test` with assertions on the new fields; `--check` stays deterministic (pure function of the committed feed). Re-inject into `index.html` and verify `build:check` green.

### TIER 3 — Second-order innovation (saturation mandate) — STRETCH

**A3 · IGNIS returning-visitor re-entry starter** — score 76 (ship only if A1+A2 land clean + budget)
Premise VERIFIED LIVE (`ignis-answer-engine.js:76-110, 182-215`): the prefix-cache (S206 #15) stores a returning visitor's last query+excerpt for 24h but only surfaces it *reactively* (cache-hint mid-answer, offline retry rows). The curated starter prompts (S212 W4) only fire for *first-time / no-history* visitors. Returning visitors with a stored query see nothing inviting them back in. Reuse the existing starter-chip render path (CSS classes only — style-contract safe, NO `.style.cssText`) to surface a single "Pick up where you left off — '{last query}'" chip when prefix-cache history exists. Converts already-stored-but-invisible state into a visible engagement hook. Gate: `check-intelligence-style-contract --strict` + a new self-test branch.

---

## REJECTED / DEFERRED (premise checked — evidence recorded)

- **agents.json `llmsFull` for shard-less projects** → DEFER (by-design). The 4 projects without a shard (MindFrame, The Living Protocol, SparkFunnel, VEILOS) are all **external-domain** products with no on-site canonical page; `build-agents-json.mjs` only mints a shard for on-site pages. Minting thin llms-full content for externally-hosted products would be low-value and keyword-stuffing-adjacent. Real (minor) follow-up noted: MindFrame *does* have an on-disk page (`games/mindframe/`) yet agents.json points to the external `usemindframe.com` with no shard — generator inconsistency, future item.
- **Light-mode hero CTA contrast** → REJECT (premise false). The flagged buttons use dark navy `#03101f` text on a blue gradient over cream `#f6efe5` — the Explore pass computed ~11:1 itself. No WCAG risk; no override needed.
- **MindFrame FORGE → SPARKED re-rating** → DEFER (founder-gated). A project's lifecycle status is a public promise (CANON escalation: public promises). Not an autonomous change.
- **Themes black-on-black / mobile nav a11y** → CLOSE (verified healthy: all theme palettes pass; `nav-toggle.js` has full ARIA + scroll-lock + portal).

---

## EXECUTION ORDER

1. **A1** — remove orphan + prune allowlist + DECISIONS (fast, unblocks a clean tree).
2. **A2** — hero JSON-LD enrichment + self-test + rebuild + `build:check` (flagship; deployable site value).
3. **A3** — IGNIS re-entry starter (stretch; only if budget remains after A2 verifies green).
4. **/closeout** — full write-back, SIL, visuals; **commit + push directly to `main`**; verify CF Pages deploy.
