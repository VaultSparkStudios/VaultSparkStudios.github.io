<!-- generated-by: render-audit-md.mjs (R-H14) — DERIVED FROM AUDIT_2026-06-12.json; do not hand-edit -->
<!-- generated-at: 2026-06-12 -->
<!-- project: vaultsparkstudios-website · type: website -->

# Project Audit — VaultSpark Studios

> One ranked improvement plan covering 9 axes. Top items first.
> Source of truth: this file is derived from the JSON sidecar. Edit the JSON, then re-render.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 10
- Combined Priority: 419.80
- Top 3: funnel-waterfall-pedagogical · session-velocity-trust-badge · progressive-membership-unlock

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov | Priority | Status | Item |
|---|:-:|---|---|:-:|:-:|:-:|:-:|---|
| 1 | transformative | ux | 1h | 6 | 8 | 75.9 | · | **funnel-waterfall-pedagogical** — Funnel waterfall — teach the journey, don't just say 'warming'. The /status/ Conversion funnel tile shows '1 / 20 events measured' and nothing else. Even in the honest-dark state, the tile can show the five journey stages (Visit → Proof seen → Dispatch shown → Subscribe → Membership) as a horizontal waterfall with 'awaiting data' placeholders. This transforms a passive wait indicator into active transparency — visitors see exactly what the studio measures and why. The waterfall fills in with real rates when minSamples is reached. **Recipe:** In status/index.html extend the funnel tile render: when honestDark=true, render 5 labeled stages with '—' counts in a .funnel-waterfall container; when live, fill in real rates. Stages: Visit, proof-line:shown, studio-dispatch:subscribe (terminal), oracle-chip:click, Membership. CSS: flex row with connecting arrows via ::before pseudo, gold highlight on populated stages. |
| 2 | transformative | ux | 2h | 7 | 8 | 70.0 | · | **session-velocity-trust-badge** — Session velocity badge — animate the 189-session proof. The honest-traction scoreboard shows sessions as a static number. The velocity is the story: 189 sessions in ~189 days is a rare solo-studio cadence. Framing it as 'avg 1 per day' with an animated count-up on first scroll + 'Last session: today' turns a static fact into a felt signal. Also fixes the 1-session staleness: public-intelligence.json sessionsCompleted=188, not 189. **Recipe:** Update public-intelligence.json sync to pull sessionsCompleted from context/SELF_IMPROVEMENT_LOOP.md Rolling Status header at build time. In honest-traction-scoreboard.js: (1) add IntersectionObserver-triggered count-up animation (requestAnimationFrame, 800ms); (2) add 'Last session: N days ago' meta label from public-intelligence.json updatedAt; (3) add 'avg 1/day' framing when sessions/days > 0.7. No new external deps. |
| 3 | transformative | ux | 4h | 9 | 8 | 69.8 | · | **progressive-membership-unlock** — Progressive membership unlock — stage-matched nudges from real visit signals. The membership page shows identical copy to a cold first-timer and a fifth-visit returning visitor. returning-visitor-digest.js already tracks visit count in localStorage; proof-conversion-line.js fires a beacon on proof interaction. These are the two strongest pre-purchase signals on the site. A progressive disclosure system that reads those signals and adjusts the membership card copy — Stage 1 (cold), Stage 2 (returning), Stage 3 (proof-engaged), Stage 4 (dispatch subscriber) — turns a static pitch into a natural escalating conversation. **Recipe:** Create assets/membership-unlock.js (deferred, CSP-safe): read vs_visit_count from localStorage + vs_proof_seen flag + footer-dispatch cookie. Set data-vs-unlock-stage=[1-4] on the #mem-interview-mount container. CSS attribute selectors show stage-matched elements. Wire allowlisted 'membership-unlock:stage-N' beacons (check-rum-allowlist both ends). |
| 4 | high | ux | 1h | 8 | 5 | 63.3 | · | **forge-devlog-soul-voice** — Forge devlog — rewrite draft generator to produce publishable SOUL-voice prose. The current journal/_drafts/forge-week-2026-06-11.md contains verbatim task-board entries like '[S186][UX/P0] PROOF-TO-CONVERSION-BRIDGE — DONE.' — internal shorthand, not studio voice. check-content-freshness.mjs warns on 82+ day journal staleness every session. Improving draft-weekly-forge.mjs to translate Done-item slugs into plain-English SOUL-voice sentences means the founder can publish with a read-through, not a full rewrite. The journal is a direct engagement surface for Vault Members. **Recipe:** In scripts/draft-weekly-forge.mjs: add slug-to-sentence mapping that strips [S###][AXIS/P#] prefix and converts to first-person SOUL voice ('We built X' / 'X now does Y'). Add a 2-paragraph narrative intro from the top 3 Done items. List forbidden internal terms at top. Keep the raw board entries as an appendix for founder reference only. Generate a cleaner draft + update the existing journal/_drafts/ file. |
| 5 | high | speed | 1h | 6 | 6 | 45.6 | · | **changelog-auto-derive** — Auto-derive changelog from TASK_BOARD Done — kill the 59-day staleness. The /changelog/ page hasn't been updated in 59 days. check-content-freshness.mjs warns every session but the fix is manual. The TASK_BOARD Done section has every shipped item in structured format. A script that reads the most recent 30-60 days of Done entries and generates a structured changelog section (grouped by session, cleaned of internal shorthand) makes content freshness a solved problem, not a recurring founder-action. Changelog freshness is a direct SEO and trust signal. **Recipe:** Create scripts/generate-changelog-entry.mjs: (1) parse TASK_BOARD.md Done entries from last 60 days using ## Done (Session N...) header pattern; (2) strip [S###][AXIS/P#] and BLOCKED annotations; (3) write a dated entry to changelog/index.html. --dry-run shows output. Self-test: 4 cases. Wire into npm run content:refresh. |
| 6 | high | featureDepth | 2h | 6 | 9 | 40.5 | · | **proof-embed-snippet** — Proof embed card — let the status proof travel outside the site. api/status-proof.json publishes trustScore, LCP, uptime, and deploy field-wins as machine-readable proof used exclusively within the site. A copyable embed snippet (<script src='/assets/proof-card.js'></script>) gives the studio a trust-distribution surface: the card can embed in third-party pages, press kit emails, Discord descriptions. It reads from the deployed JSON, renders in ~40 lines of vanilla JS. No indie studio does this. **Recipe:** Create assets/proof-card.js (standalone, no deps): reads /api/status-proof.json, renders a 240px inline card (LCP, uptime %, session count, trust tier) with VS branding link. Self-contained CSS, respects prefers-color-scheme. On /status/ add a 'Share this proof' toggle section showing the embed code + a preview render. Wire proof-card:embed beacon (check-rum-allowlist). |
| 7 | high | ai | 2h | 7 | 8 | 28.0 | · | **oracle-adaptive-chips** — Oracle seed chips — adapt to helpful-feedback signal instead of staying static. The 3 cold-start chips on the Oracle page (S186) are drawn from static oracle-insights.json clusters. The oracle-answer:helpful/unhelpful feedback loop (S189) now fires signals identifying which query-class resonates. Re-ranking chips by recency-weighted helpful-rate turns the cold-start into a self-improving surface. Even re-ranking once per session (reading oracle-insights.json sorted by recent helpful signals) is a meaningful improvement over static order. **Recipe:** In the chip-rendering path of ignis-engine.js: after loading oracle-insights.json, sort clusters by recency-weighted helpful-rate proxy (join data/rum-ux-history.ndjson oracle-answer:helpful entries by approximate time window; fallback to static order). Expose data-vs-chip-rank on each chip. Wire oracle-chip:adaptive-shown beacon (add to Worker RUM_UX_EVENTS + check-rum-allowlist). |
| 8 | nice-to-have | ai | 2h | 6 | 7 | 21.0 | · | **oracle-corpus-feedback** — Oracle quality loop — flag low-helpful clusters for next IGNIS rescore. The 👍/👎 oracle-answer feedback fires to /v/rum but never completes the loop — IGNIS re-scoring doesn't read the helpful-rate. When a query cluster consistently generates unhelpful answers, it should flag for the next rescore cycle. A lightweight data/oracle-feedback.ndjson appended by rollup-rum-ux.mjs gives IGNIS a quality signal it currently lacks. **Recipe:** In scripts/rollup-rum-ux.mjs: when processing oracle-answer family, if unhelpful count > 2 within the window, append an entry to data/oracle-feedback.ndjson with {date, unhelpfulCount, helpfulCount, flagged: true}. Add check-funnel-contract validation: oracle-feedback.ndjson entries must have required fields. Self-test case added. |
| 9 | nice-to-have | speed | 30m | 5 | 2 | 15.2 | · | **public-intelligence-session-sync** — Sync sessionsCompleted in public-intelligence.json at build time. api/public-intelligence.json shows sessionsCompleted=188 but S189 is complete. The traction scoreboard reads this value, showing a 1-session-stale count. The sessions counter is a featured trust signal — staleness undermines it. Fix: read session number from context/SELF_IMPROVEMENT_LOOP.md Rolling Status header at build time and write to the JSON. **Recipe:** In the script that regenerates public-intelligence.json (sanitize-public-oracle-feed.mjs or vault-narrative): read 'Session N' from context/SELF_IMPROVEMENT_LOOP.md rolling-status block via regex. Write N to stats.sessionsCompleted. Add to build:check: sessionsCompleted should match latest SIL session number. Verify with npm run build:check. |
| 10 | nice-to-have | security | 2h | 7 | 3 | 10.5 | · | **tt-default-policy-finish** — TT named policy — finish the last 2 default-policy sinks before the ~2026-06-18 reprobe. The TT reprobe is scheduled ~2026-06-18 (6 days). home-idle-loader.js:16 and schema-injector.js:23 still use the default TrustTokens policy. The S185 named-policy wave covered 4 modules; these 2 remain. Adding named policies now means the reprobe finds a clean first-party surface. **Recipe:** Open home-idle-loader.js and schema-injector.js. Add named TT policies at the flagged lines (pattern from S185: vs-home-idle and vs-schema-injector). Run node scripts/lint-tt-policies.mjs to confirm gate passes. Then npm run build:check. Reprobe on 2026-06-18 per TT_ENFORCE_READINESS doc. |

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

- session-velocity-trust-badge
- funnel-waterfall-pedagogical
- proof-embed-snippet
- oracle-adaptive-chips
- progressive-membership-unlock
- oracle-corpus-feedback

## Skipped (Priority < 1.5)

- per-game-waitlist-subscribe — Already done — all 4 FORGE game pages already have notify-me forms via scripts/inject-early-signal.mjs. Grepped and confirmed before scoring.
- membership-social-proof-wiring — Already done — live-proof.js is correctly loaded on /membership/ and populates #proof-members + #proof-sparked from Supabase. Shows '—' because there are currently 0 members, not a wiring bug.
- funnel-visualization-basic — Already done — S189 added the Conversion funnel tile to /status/. The enhancement (funnel-waterfall-pedagogical) is item #1.

