<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-06-05 -->
<!-- project: vaultsparkstudios-website · type: website · session: 174 -->

# Project Audit — VaultSparkStudios.github.io

> One ranked improvement plan covering 9 axes. Top items first.
> Run `/implement` to execute in optimal order.
> **Personalized to S173 closeout:** the homepage critical path is evidence-backed, the RUM ladder is honest, the TT soak finally accumulates volume, and staging parity is measured yellow. S174 should make every one of those loops self-feeding instead of session-fed.

## Top-line

- Total items: 10
- Combined Priority: 204.9
- Top 3: `rum-autopull-ci` · `field-verdict-engine` · `tt-intake-forensics-fix`
- Headline: S173 built the instruments; S174 should wire them to run without us. Field evidence accrues on a cron instead of a founder session, deploys grade themselves against subsequent field data, and the TT soak stops being blind to its own reports.

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | 🔥 | Data/Automation | 2h | 9 | 8 | 36.0 | **rum-autopull-ci** — Daily GitHub Actions cron accrues field RUM history passively so the strict ladder flips on evidence cadence, not founder-session cadence. The ladder needs 36 more `/` samples and accrual currently only happens when a session manually runs `npm run rum:pull` — weeks of sessions babysitting a data pipe. **Recipe:** Create `.github/workflows/rum-pull.yml` (daily cron) running `npm run rum:pull` and committing `data/rum-history.ndjson` + `data/rum-summary.json` deltas with [skip ci]; set R2 read creds as Actions secrets via `gh secret set` from the secrets gateway (CANON-019 agent work — both capabilities READY). |
| 2 | 🔥 | Perf/Feature-depth | 4h | 9 | 9 | 31.3 | **field-verdict-engine** — Deploy-annotated field-performance verdicts: every ship receipt gets auto-graded by subsequent RUM data (pending → improved/regressed/neutral), surfaced publicly. S173's "field-verify" is currently a human squinting at two JSON files; a pre/post-deploy window comparator turns each deploy into a falsifiable experiment, and public deploy-annotated perf provenance is something no studio site does. **Recipe:** Create `scripts/compare-rum-windows.mjs --boundary 2026-06-05` emitting per-route median/p75 deltas with honest confidence tiers; write `data/field-verdicts.json`; extend the /studio-pulse/ Field Performance strip with deploy markers; auto-grade ship receipts with a `fieldVerdict` field on every `rum:pull`. |
| 3 | 🔥 | Security | 2h | 8 | 7 | 28.0 | **tt-intake-forensics-fix** — Fix the Trusted Types intake parser dropping Reporting-API body fields (80/81 reports are all-null) and add sink clustering so burndown is evidence-driven. The Worker intake only normalizes the legacy `csp-report` shape, not the Reporting API `{type, body:{...}}` shape modern Chrome sends — you cannot burn down sinks you cannot see. **Recipe:** Normalize both report shapes in `cloudflare/security-headers-worker.js`; add `scripts/analyze-tt-violations.mjs` clustering by sourceFile:line into `docs/TT_BURNDOWN_<date>.md`; deploy worker, verify intake 204 + a parsed sample. |
| 4 | 🔥 | Security | 2h | 8 | 6 | 24.0 | **tt-gtag-policy-fix** — Eliminate the dominant sitewide TT sink: the gtag idle-boot inline script's `createElement('script').src` assignment (index.html line 15 col 126 — fires on every page view, plausibly the 78-violation day). **Recipe:** Add a narrow `trustedTypes.createPolicy('vs-loader', {createScriptURL: allowlist(googletagmanager.com, js.stripe.com, *.supabase.co)})` with graceful no-op when trustedTypes is absent; route the gtag `s.src` through it; propagate sitewide; rerun `node scripts/probe-tt-soak.mjs` after a soak interval. |
| 5 | ⚡ | UX | 1h | 7 | 5 | 22.1 | **nav-sheet-canary-readout** — Read the 5% mobile nav-sheet canary telemetry and emit a graduate/hold/silent verdict instead of letting the canary run unobserved forever. A canary nobody reads is just risk with extra steps; silence is itself a finding. **Recipe:** Create `scripts/check-nav-sheet-canary.mjs` reading `api/nav-sheet-stats.json` + nav-sheet events; emit `data/nav-sheet-verdict.json` (graduate-ready | hold | telemetry-silent) with evidence counts. |
| 6 | 💡 | Token-cost | 30m | 5 | 4 | 15.1 | **handoff-cache-ttl** — Hash-key the compact-handoff LLM cache so unchanged handoffs skip the haiku call entirely instead of expiring hourly. This session burned ~3K haiku tokens re-summarizing an unchanged file. Measurement: context-meter ledger byScript shows compact-handoff 3053 tokens today; expected 0 on unchanged-handoff sessions. **Recipe:** Key the cache on sha256(LATEST_HANDOFF.md); skip the LLM call on hash match regardless of age; keep a 7d hard ceiling. |
| 7 | ⚡ | Ops/Security | 2h | 7 | 4 | 14.0 | **staging-header-parity-fix** — Turn CANON-007 staging parity green: staging serves a pre-S173 shell on 2/3 routes and is missing the CSP/HSTS/XCTO/Referrer-Policy quartet everywhere — it cannot catch the exact drift class it exists to catch. **Recipe:** Curl staging headers to confirm the diff; sync the current build to the Hetzner staging origin; add the security-header quartet at the staging edge; rerun `node scripts/check-staging-parity.mjs` and require green. |
| 8 | ⚡ | Process/Efficiency | 30m | 6 | 3 | 13.6 | **protocol-shim-completion** — Close the MODULE_NOT_FOUND class: `lib/skill-profile.mjs`, `sample-codebase.mjs`, and `render-audit-md.mjs` all failed this session; the S172 heal allowlist missed them. **Recipe:** Add all three to the `check-protocol-scripts.mjs` heal manifest; run `--heal`; verify each resolves (delegating to studio-ops siblings or emitting a clean no-op). |
| 9 | 💡 | Ecosystem | 30m | 5 | 3 | 11.4 | **ark-sig-repair-ship** — Ship the Ark signature-failure dossier upstream as repo-question cargo so studio-ops actually sees the 4 accumulated verification failures (up from 3 — the failure class is live). **Recipe:** `node scripts/ark.mjs ship --type repo-question --to studio-ops` with the dossier path, the failing cargo IDs, and replyTo this slug; log the cargo ID in the handoff. |
| 10 | 💡 | Process/Truth | 1h | 5 | 3 | 9.5 | **brief-signal-plumbing** — Restore truthful startup-brief signals: Tests shows `?/?`, Context age shows `⛔ ?d`, Genome shows `?` — three sources the renderer can no longer parse, in the only instrument panel the session gets. **Recipe:** Trace `render-startup-brief.mjs` reads for tests count, CURRENT_STATE `Last updated:` parse, and genome snapshot; fix source or parser; re-render and require real values. |

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

- `rum-autopull-ci` (8) — evidence loop becomes self-feeding
- `field-verdict-engine` (9) — deploys grade themselves; public perf provenance

## Skipped (gated, not low-value)

- `membership-proof-device-verify` — founder-gated (real device in hand)
- `vaultsparked-proof-delete` — founder yes/no pending, evidence-complete
- `tt-enforce-canary` — canon-gated until sink burndown + near-zero soak
- `rum-strict-flip` — canon-gated until `/` clears the 50-sample floor

---
*Generated by `/audit` v1.0 · ready for `/implement`*

## Execution Log (S174 — same session)

| # | Slug | Result |
|---|---|---|
| 1 | rum-autopull-ci | ✅ SHIPPED — `.github/workflows/rum-pull.yml` daily cron; R2 creds set via `gh secret set`; dispatch verify after push |
| 2 | field-verdict-engine | ✅ SHIPPED — `compare-rum-windows.mjs` (7/7 self-test) · S173 boundary registered (honest PENDING, 38 pre / 0 post) · speed receipts carry `fieldVerdict` · studio-pulse deploy-verdict line |
| 3 | tt-intake-forensics-fix | ✅ SHIPPED + DEPLOYED (f4c0d0c7) — Reporting-API array shape parsed, `sample` captured; `analyze-tt-violations.mjs` (5/5) clustered 80 KV bodies |
| 4 | tt-gtag-policy-fix → tt-top-sink-burndown | ✅ SHIPPED (scope corrected by evidence) — real top sink was `dispatches:364` innerHTML (30×), NOT gtag (1×). Fixed: dispatches DOM API · page-sigil createElementNS · sealed-vault-row DOM API · palette-loader btn · nav-toggle ✕ · vs-speculation/vs-idle-loader/vs-ambient-loader TT policies. gtag (1×/30d) deferred with recipe |
| 5 | nav-sheet-canary-readout | ✅ SHIPPED — verdict TELEMETRY-SILENT (0 events / 116 raw files, intake live-verified); acted on it: canary 5%→25% |
| 6 | handoff-cache-ttl | ✅ SHIPPED — content-hash cache, 7d ceiling; second run = 0 LLM tokens |
| 7 | staging-header-parity-fix | ✅ SHIPPED — parity GREEN 3/3 (first time): `try_files` lacked `{path}index.html` (staging served homepage for every subdir!), header quartet synced via `sync-staging-headers.mjs`, nonce-normalized compare |
| 8 | protocol-shim-completion | ✅ SHIPPED — 3 shims healed (incl. lib/ subpath support); sentinel 19/4/0 |
| 9 | ark-sig-repair-ship | ✅ SHIPPED — cargo `01JQARTIQ4F428A7E440BFE7D6` to studio-ops (dossier + try_files learning for setup-staging.sh) |
| 10 | brief-signal-plumbing | ✅ SHIPPED — Tests 116/116 (update-test-signal.mjs) · Context age 0d · Genome from history |

**10/10 shipped · Priority sum 204.9 · 0 blocked · 0 deferred** (gtag micro-fix folded into TT burndown doc as known-remaining, 1 report/30d)
