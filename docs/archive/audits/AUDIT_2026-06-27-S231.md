<!-- generated-by: arc /audit (S231) — hand-authored, live-verified -->
<!-- generated-at: 2026-06-27 · project: vaultsparkstudios.github.io · type: website/public · rubric: product -->

# Project Audit — VaultSpark Studios Website (S231)

> Ranked plan. Every item verified against LIVE code + LIVE CI state (`gh run list`), not the cached genius list.
> Headline: S230 closeout claimed `build:check EXIT 0` but **CI has been RED on every push** — two distinct
> root causes, both the "green-locally / red-in-CI" determinism class. This audit root-fixes both + hardens the class.

## Live signal (verified this session)

- `gh run list`: **Lighthouse CI = failure** (last 3 pushes) · **E2E Test Suite = failure** (last 3 pushes) · Accessibility = success · Pages deploy = success.
- E2E *test job* itself PASSES; the failing job is **compliance → "Public intelligence sync check"** (`build:check`) → exit 1.
- Lighthouse *audits* PASS; the failing step is **trend-ledger `git push` → 403 → exit 128**.
- doctor: blockingFailing 0 · 3 advisory failures all **portfolio/sibling scope** (VEILOS liveUrl, Stripe across siblings, compliance velocity) → Ark cargo, not tree edits.

## Ranked Plan

| # | Wave | Tier | Axis | Item | Premise verified |
|---|------|:-:|---|---|---|
| 1 | W1 | 🔥P0 | CI/infra | Orphan shell + `clean-stale-shells` determinism root-fix | ✅ `bff2141eb7.js` committed, 0 tracked-HTML refs; masked locally by gitignored `lighthouse-results/*.html` |
| 2 | W1 | 🔥P0 | CI/infra | Lighthouse CI trend-push 403 root-fix | ✅ `lighthouse.yml` has no `permissions:` block → read-only token → push 403 → exit 128 |
| 3 | W2 | ⚡P2 | infra/trust | Generalize `blockDays` trust-ceiling to status-proof + uptime feeds | ✅ pattern exists in `check-content-freshness.mjs`; status-proof seed-rot is WARN-only; guards dead-cron class (S221/S222) |
| 4 | W3 | ⚡P2 | speed | INP proactive passive-listener pass (data-independent slice) | ⚠ data-driven fix premature (0 `inp:slow_interaction` samples); ship the safe slice, defer attribution |

## Honest deferrals (WINS — recorded, not silently skipped)

- **INP data-driven attribution fix** — telemetry shipped S229 *same day*; needs 2–3 days of field samples (currently 0). Ship only the passive-listener slice now.
- **Changelog publish** — DONE in S230 (`check-content-freshness` changelog 75d→0d). Phantom now; close it.
- **Forge Window naming propagation** — real work but **founder-gated**: renames public vocabulary on 108 pages ("Studio Pulse" → "Forge Window"). CANON escalation (public vocabulary). Needs founder sign-off on the public name first.
- **First real push notification** — FOUNDER go-ahead required (0 subs).
- **`ark.hmac.seed` provision** — FOUNDER credential mint (CANON-019 reserved).
- **Sibling compliance (VEILOS/SHADOW/ATLAS/Stripe)** — cross-repo; CANON-018 → Ark cargo, never tree edits.

## Second-order innovation candidates (implement after primary list, budget permitting)

- **`check-tracked-html-only` lint** — generalize the root-fix: any gate that walks the FS for `*.html`/asset refs and would diverge between local (gitignored artifacts present) and CI (clean) should scan `git ls-files` instead. Audit sibling gates for the same bug class.
- **CI-truth beacon in startup brief** — the brief showed tests ✓ while CI was red 3 pushes running. Surface real `gh run list` conclusions in the brief so a closeout can never again claim green over a red main.
