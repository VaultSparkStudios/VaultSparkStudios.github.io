<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-05-27 -->
<!-- project: vaultsparkstudios-website · type: website -->

# Project Audit — VaultSparkStudios.github.io

> One ranked improvement plan covering the current highest-leverage local surfaces.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 4
- Combined Priority: 147.9
- Top 3: generated-drift-preflight · rum-anomaly-canary · ambient-split-candidate-json

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | transformative | speed/organization/efficiency | 45m | 9 | 8 | 42.1 | **generated-drift-preflight** — Add a fast preflight that runs only drift-prone generated-output checks and tells the agent whether a full build refresh is needed. Session 165 caught stale public-intelligence artifacts only inside the full build gate; this catches the same class near start/closeout. **Recipe:** Create `scripts/check-generated-drift-preflight.mjs` with self-test and `--json`, run generated-output checks, and wire it near the front of `build:check`. |
| 2 | high | security | 30m | 8 | 7 | 36.1 | **ci-status-freshness-contract** — Public CI health is trust material, so stale or malformed `api/ci-status.json` should fail locally before the site implies operational confidence. **Recipe:** Create `scripts/check-ci-status-freshness.mjs` with `--max-age-hours` and `--self-test`, validate the artifact, and wire it into `build:check`. |
| 3 | transformative | AI/intelligence integration | 1h | 9 | 8 | 36.0 | **rum-anomaly-canary** — Add a week-over-week RUM canary that compares the latest field window to the previous field window and warns before the perf budget hard-fails. The RUM bridge is wired; the next-order value is early drift detection. **Recipe:** Create `scripts/check-rum-anomaly-canary.mjs`, emit `.cache/rum-anomaly-canary.json`, support `--check/--self-test`, and wire check mode into `build:check` as advisory. |
| 4 | high | token/API consumption reduction | 45m | 8 | 8 | 33.7 | **ambient-split-candidate-json** — Turn ambient coverage Markdown into machine-readable split candidates with proof commands and risk notes. Future audits can choose the next bundle split without re-reading the whole report. **Recipe:** Extend `scripts/report-ambient-coverage.mjs` to write `.cache/ambient-split-candidates.json` with ranked guarded modules and recommended first proof step. |

## Innovation reserve

- generated-drift-preflight
- rum-anomaly-canary
- ambient-split-candidate-json

## Skipped

- RUM strict flip remains correctly data-gated until `/` reaches 50 field samples.
- Mobile nav default swap remains evidence/founder gated while `api/nav-sheet-stats.json` reports zero opens.

## Execution Log

| Item | Status | Evidence |
|---|---|---|
| generated-drift-preflight | shipped | Added `scripts/check-generated-drift-preflight.mjs`; wired first in `build:check`; caught stale `llms-full-shards` during implementation; final preflight green. |
| ci-status-freshness-contract | shipped | Added `scripts/check-ci-status-freshness.mjs`; self-test green; `--max-age-hours=96` gate green against `api/ci-status.json`. |
| rum-anomaly-canary | shipped | Added `scripts/check-rum-anomaly-canary.mjs`; self-test green; check mode emits empty `.cache/rum-anomaly-canary.json` until RUM history exists. |
| ambient-split-candidate-json | shipped | Extended `scripts/report-ambient-coverage.mjs`; check mode green and writes `.cache/ambient-split-candidates.json` with 11 candidates. |

Validation: `npm run build:check` passed end-to-end on 2026-05-27.

---
*Generated by `/audit` v1.0 · ready for `/implement`*
