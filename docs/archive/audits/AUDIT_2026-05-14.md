<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-05-14 -->
<!-- project: vaultsparkstudios-website · type: platform (SPARKED public anchor) -->

# Project Audit — VaultSpark Studios Website (Session 129)

> One ranked improvement plan covering 9 axes. Top items first.
> Project type: **platform** (Security 2× · Speed 2× · UX 1.5× · Feedback 1.5×).
> This audit supersedes `AUDIT_2026-05-13.md` — it re-ranks 17 still-deferred items with current context and introduces 8 fresh genius-tier candidates informed by the S127/S128 ships.
> Run `/implement` to execute in optimal-efficiency order.

## Top-line

- Total items: **22** (8 fresh · 14 carried-and-re-ranked)
- Combined Priority: **548.4**
- Top 3: `studio-pulse-render-contract-gate` · `avif-lqip-pipeline-ship-now-unblocked` · `lighthouse-perf-restoration-to-92`
- **Strategic frame:** Yesterday's audit shipped 8 perf/security primitives but left the **3 strategic structural plays untouched** — auth-class retirement (passkeys + canary), IGNIS-as-ambient (conduit + ROI loop), and the feedback closure loop (receipts wall). S128's Studio-Pulse drift exposed a **4th structural gap**: page-asset contracts aren't enforced (renderer assumed loaded, was not). This audit attacks all four with a bias toward **structural gates over hand-maintained lists** (per `feedback_structural_gate_pattern`) and ships AVIF (now unblocked — `sharp` is already in devDeps) on day one.

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | 🔥 | Speed · Process | 1h | 10 | 8 | 53.3 | **studio-pulse-render-contract-gate** — Structural drift gate that asserts every page in `data/renderer-contracts.json` declares its required `<script>` set in the right order (Supabase UMD → supabase-client → renderer → realtime). S128 was a 2-week silent regression because `/studio-pulse/` had `studio-pulse-live.js` written but **not loaded**. Fixes the class, not the instance. **Recipe:** new `scripts/check-render-contracts.mjs` + `data/renderer-contracts.json` (5 entries to start: `/studio-pulse/`, `/journal/dispatches/`, `/changelog/`, `/membership/`, `/vault-member/`); wire into `build:check`. |
| 2 | 🔥 | Speed | 2h | 8 | 6 | 41.6 | **avif-lqip-pipeline-ship-now-unblocked** — `sharp@^0.34.5` is **already in devDeps**, `scripts/convert-images-to-avif.mjs` exists in `--report` mode since S113. Run with `--write`; emit `<picture>` AVIF→WEBP→PNG; add ~16px LQIP data-URI background for above-the-fold images. ~30% byte reduction on `og-*.png` + `dreadspike-still-*.webp`. Compounds with Lighthouse fix (#4). **Recipe:** run `node scripts/convert-images-to-avif.mjs --write`; commit AVIFs; rewrite hero/og `<img>` to `<picture>` with LQIP background; verify Lighthouse Largest Contentful Paint score. |
| 3 | 🔥 | Process · Cohesion | 1h | 9 | 8 | 38.4 | **portfolio-filesystem-sitemap-drift-gate** — Carry from S128. Single gate that fails CI if `portfolio/PROJECT_REGISTRY.json` ↔ filesystem (`/projects/<slug>/`, `/games/<slug>/`) ↔ `/projects/index.html` cards ↔ `sitemap.xml` ↔ `search/index.html` diverge. Would have flagged the missing seamline + StatVault/IdeaForge confusion in S127 early. **Recipe:** new `scripts/check-portfolio-coherence.mjs` cross-walks 5 sources; reports `MISSING`/`ORPHAN`/`AUDIENCE_MISMATCH`/`STATUS_MISMATCH`; wire into `build:check`. |
| 4 | 🔥 | Speed | 4h | 10 | 6 | 37.4 | **lighthouse-perf-restoration-to-92** — Standing red since S120 (0.68–0.73 vs 0.80 threshold). 6 sessions of failed pushes. Trace from #18 carry: hero-ticker JS → `requestIdleCallback`; hover-cinematics CSS behind `@media(hover:hover) and (min-width:1024px)`; replace universe-bridge `radial-gradient` with static webp; profile parallax `::before`. Stops bleeding compliance score. **Recipe:** Chrome DevTools perf trace on `/`; apply 4 fixes; bump threshold to 0.90 in `.lighthouserc.json` **after** the fix lands; delete stale `propagate-csp.mjs --dry-run` step in `.github/workflows/e2e.yml` (failing intentionally since S120). |
| 5 | 🔥 | AI · Token-cost | 2h | 8 | 8 | 32.0 | **ignis-roi-loop** — Add a per-Ask `outcome` field on next user action (became_member, navigated, dismissed, idle). Persist to `ignis_outcomes`; weekly summary surfaces "which question types converted." Turns IGNIS spend from cost to measured pipeline. **Recipe:** extend `supabase/functions/ask-ignis` response with `traceId`; client emits outcome event via `navigator.sendBeacon`. New `ignis_outcomes` migration + view `ignis_roi_7d`. **Deploy-gated.** |
| 6 | 🔥 | Feedback | 1h | 9 | 7 | 31.5 | **universal-feedback-button-sitewide-default** — Carry from S129 UX/P2. S128 collapsed `/membership/` feedback into a bottom-right button (per founder request). This codifies the pattern as the sitewide default for every `[data-micro-feedback-root]` and adds a contextual prompt registry so each page gets the right ask. Anti-pattern: a sprawl of full-card surveys that read as "annoying." **Recipe:** harden `assets/micro-feedback.js` to be the canonical collapsed-button entry; new `data/feedback-prompts.json` (per-path prompt + scale); audit any remaining inline survey blocks; deprecate verbose variants. |
| 7 | 🔥 | Security · Speed | 4h | 10 | 8 | 31.0 | **passkey-cross-subdomain-auth** — Retire the password+captcha class that has produced 6 sessions of Turnstile bugs (S120/S121/S122/S125/S126). WebAuthn passkeys, RP-ID = root domain, works for `vaultsparkstudios.com` + `hub.vaultsparkstudios.com`. Supabase has built-in WebAuthn support in newer SDK. Turnstile stays as fallback. **Recipe:** Supabase SDK upgrade audit (currently `2.49.1`); new `vault-member/passkey-enroll.js`; "Sign in faster with a passkey" upsell on existing login form. **Founder gate (auth-risk window).** |
| 8 | 🔥 | Security · Speed | 4h | 10 | 8 | 31.0 | **synthetic-auth-canary-with-rollback** — Production canary runs login flow every 10 min from GitHub Actions matrix (Chromium · WebKit · Firefox); pages founder on failure; optionally auto-rollback last Pages deploy. Would have caught S125+S126 in <10 min vs waiting for founder console paste. **Recipe:** new `.github/workflows/auth-canary.yml` on `'*/10 * * * *'`; reuses Playwright + Eternal QA account from S119; alert via Resend (READY). **Cost approval gate.** |
| 9 | 🔥 | AI · Cohesion | 2h | 7 | 10 | 30.3 | **vault-atlas-live-status-strip** *(NEW)* — A 24px-tall inline SVG mini-sitemap rendered into the Resources dropdown showing **live status dots per surface** (homepage · pulse · hub · ignis · checkout) sourced from `/api/ci-status.json` + `/api/founder-presence.json`. <2 KB inline. Makes the studio's nervous system visible at one glance from every page. No SaaS studio does this. **Recipe:** new `assets/vault-atlas.js`; mount into Resources dropdown via `propagate-nav.mjs`; 5 colored dots + tooltip; refresh every 90 s. |
| 10 | ⚡ | Cohesion · AI | 2h | 8 | 9 | 28.6 | **founder-twin-dispatch-whisper** *(NEW)* — When the founder-twin closes a session (`logs/WORK_LOG.md` gets a new entry), an edge fn auto-generates one 1-sentence "vault whisper" from the commit subjects of that session (~$0.0002/session via Claude haiku) and posts to `/journal/dispatches/`. The vault narrates its own work. Turns silent CI commits into ambient narrative. **Recipe:** new `supabase/functions/whisper-from-session`; GitHub Action triggers on `logs/WORK_LOG.md` change; inserts into existing `vault_dispatches` table. **Deploy-gated.** |
| 11 | ⚡ | AI · Token-cost | 1h | 8 | 5 | 25.3 | **anthropic-prompt-cache-for-personas** — `ask-ignis` rebuilds the full persona suffix per request. Anthropic prompt-cache headers cut input tokens 90% for the static persona prefix. Compounds on every IGNIS call. **Recipe:** in `supabase/functions/ask-ignis/`, add `anthropic-beta: prompt-caching-2024-07-31` + `cache_control: { type: 'ephemeral' }` on persona system block; measure `cache_read_input_tokens`; verify in `ignis_daily_meter`. **Deploy-gated.** |
| 12 | ⚡ | UX · Innovation | 30m | 7 | 9 | 23.9 | **page-sigil-age-indicator** *(NEW)* — A 32×32 inline SVG sigil top-right of every public page that reflects last-update-age via stroke color (green ≤14d, amber 14–60d, red >60d) and links to `/studio-pulse/`. Passive cohesion: every page advertises its own freshness without reading copy. ~400 bytes inline + 1 fetch from existing `/api/public-intelligence.json`. **Recipe:** extend `propagate-nav.mjs` `buildAmbientBlock()` to inject the sigil; reads per-path `lastTouched` from intelligence shard; renders SVG ring fill. |
| 13 | ⚡ | Speed | 30m | 6 | 7 | 22.5 | **pointerdown-prerender-shim** *(NEW)* — Between `pointerdown` and `mouseup` on internal nav links there are ~80ms of head-start before navigation actually starts. Inject a `<link rel="prerender">` for the clicked target during that gap; remove on mouseup if user changes mind. Compounds with #1's Speculation Rules for the long-tail of non-hover users (touch + accessibility). **Recipe:** new `assets/pointerdown-warm.js` (single delegated listener on `document`); mounted via ambient block; ignores `<a target=_blank>` + external + `[data-no-prerender]`. |
| 14 | ⚡ | Gamification | 4h | 9 | 8 | 27.9 | **cross-game-lore-gates-via-rank** — `lore-gates.js` exists but ungated. Rank-tier unlocks reveal hidden universe content sitewide — Rank 5+ sees DreadSpike post-credits, Rank 8+ sees Voidfall opening monologue, etc. **Recipe:** extend `lore-gates.js` with `[data-lore-gate="rank-5"]` data-attr resolving against `window.VaultMember.rank`; populate 6-10 hidden lore fragments; logged-out users see "Locked: requires Rank 5" tease. |
| 15 | ⚡ | UX | 4h | 9 | 7 | 24.4 | **mode-aware-homepage-anon-vs-member** — Authed members see "Welcome back, $handle · Rank $N · 3 new dispatches · next milestone in $XP" hero. Same DOM, swap via `[data-mode="anon\|member"]` after auth resolution + IndexedDB pre-render. **Recipe:** extend `vault-member/portal-cache.js` IDB read to `/`'s pre-paint; add `[data-mode]` swap zones in `index.html` hero. |
| 16 | ⚡ | Feedback | 4h | 9 | 9 | 31.4 | **receipts-wall-you-spoke-we-shipped** — `/receipts/` ties `page_feedback` 😐/😢 reactions + open-text comments to the commit that addressed them. Public proof the studio responds. **Recipe:** extend `page_feedback` with `addressed_in_commit` TEXT (admin UI); new `/receipts/index.html` + render fetches `feedback_summaries` + GitHub commits API. **Deploy-gated.** |
| 17 | ⚡ | Gamification | 4h | 8 | 9 | 27.9 | **founder-voice-notes-eternal-tier** — Eternal ($29.99+) members get 30–90s audio notes from founder. Web Audio + Supabase Storage. Near-zero-cost differentiator. **Recipe:** new `vault_member_audio_notes` table with `tier_required` + `audio_url`; player on `/vault-member/` Following tab. **Founder action.** |
| 18 | ⚡ | Gamification | 1h | 6 | 7 | 22.7 | **sealed-vault-countdown-tease** — Sealed-vault row shows `X sealed projects · next reveal in ~Y days` based on `studioRegistry` `estimatedRevealAt`. **Recipe:** add optional `estimatedRevealAt`; `sealed-vault-row.js` reads + renders countdown chip; daily update via `generate-public-intelligence.mjs`. |
| 19 | ⚡ | AI | 8h | 9 | 10 | 27.1 | **ignis-conduit-proactive-narrator** — IGNIS surfaces a 1-line whisper on every 3rd page transition based on visit-pattern. Studio's house intelligence, not a Q&A bot. **Recipe:** `assets/ignis-conduit.js` polls `intent-state.js`; mounts 28px ambient strip with Claude-generated sentence (cached per-cluster, $0.0002/visitor). |
| 20 | ⚡ | UX · Innovation | 8h | 8 | 10 | 24.1 | **universe-map-alt-homepage** — `/universe/` becomes a pan/zoom WebGL constellation reusing `studio-living.js` graph data. Cinematic doorway; `/` stays prove-first. **Recipe:** swap static SVG for regl/pixi renderer; nodes scale with recent activity; 60fps cap; SVG fallback for `prefers-reduced-motion`. |
| 21 | ⚡ | Security · Cohesion | 8h | 9 | 8 | 21.7 | **vault-spark-id-universal-session** — One login across studio's game subdomains. Cookie-domain `.vaultsparkstudios.com` so `hub.`, future `voidfall.`, share auth. **Founder gate (auth-risk).** |
| 22 | 💡 | UX · SEO | 2h | 8 | 4 | 16.0 | **founder-team-page-and-eeat-schema** — No `/about/` or `/team/` page. Google E-E-A-T penalty for studios without identifiable expertise. **Founder content needed.** |

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

- `vault-atlas-live-status-strip` (#9 · innov 10 · **NEW**)
- `ignis-conduit-proactive-narrator` (#19 · innov 10)
- `universe-map-alt-homepage` (#20 · innov 10)
- `founder-twin-dispatch-whisper` (#10 · innov 9 · **NEW**)
- `page-sigil-age-indicator` (#12 · innov 9 · **NEW**)
- `receipts-wall-you-spoke-we-shipped` (#16 · innov 9)
- `founder-voice-notes-eternal-tier` (#17 · innov 9)
- `studio-pulse-render-contract-gate` (#1 · innov 8 · **NEW**)
- `portfolio-filesystem-sitemap-drift-gate` (#3 · innov 8 · **NEW**)
- `ignis-roi-loop` (#5 · innov 8)
- `passkey-cross-subdomain-auth` (#7 · innov 8)
- `synthetic-auth-canary-with-rollback` (#8 · innov 8)
- `cross-game-lore-gates-via-rank` (#14 · innov 8)
- `vault-spark-id-universal-session` (#21 · innov 8)

## Skipped (Priority < 1.5)

- None. Innovation reserve floor is 8, all items scored above 15.

## Founder hot-takes (read this first)

- **The S128 Studio-Pulse drift is a structural lesson, not a one-off.** A renderer script existed, was never loaded, and the page silently degraded for 2 weeks. #1 (render-contract gate) is the cheapest win in this audit and prevents the entire class.
- **Yesterday's audit shipped tactical primitives.** This audit ships **structural gates** (#1, #3) + **immediate unblocked perf wins** (#2 AVIF, #4 Lighthouse) — meaningful score deltas on the next deploy.
- **The 3 fresh "vault narrates itself" items (#9 Atlas, #10 Whisper, #12 Sigil)** combine into a coherent ambient-intelligence layer. Total effort ~4.5h; total innovation 28/30. These are not on any competitor's roadmap.
- **The auth-class retirement (#7 + #8) and IGNIS layer (#5, #10, #11, #19) remain the highest strategic ROI** but require founder gates — track them explicitly rather than letting them age out.

## Bounded-effort first pass (recommended `/implement` order)

The auto-twin auto-approval window can ship these without founder gates (no deploy/cost/auth changes, all <2h):

1. **#1** `studio-pulse-render-contract-gate` (1h) — structural gate, lowest risk
2. **#3** `portfolio-filesystem-sitemap-drift-gate` (1h) — paired with #1, same surface
3. **#2** `avif-lqip-pipeline-ship-now-unblocked` (2h) — perf compounding win
4. **#13** `pointerdown-prerender-shim` (30m) — ambient asset, cheap
5. **#12** `page-sigil-age-indicator` (30m) — ambient asset, cheap
6. **#9** `vault-atlas-live-status-strip` (2h) — ambient asset, novel
7. **#6** `universal-feedback-button-sitewide-default` (1h) — codifies S128 pattern
8. **#4** `lighthouse-perf-restoration-to-92` (4h, last — needs perf trace + threshold flip)

Total bounded effort: ~12h. Items 5+ are reach goals; ship 1–7 minimum to keep the cycle tight.

The remaining 14 items are deploy/founder-gated or 4h+/8h innovation reserve — explicitly carried, not implementation-blocked.

---

## Execution Log (S129 — /implement pass 1)

| Status | # | Slug | Note |
|---|---|---|---|
| ✅ DONE | 1  | studio-pulse-render-contract-gate | `scripts/check-render-contracts.mjs` + `data/renderer-contracts.json` (5 contracts: studio-pulse, dispatches, changelog, membership, vault-member). Wired into `build:check`. Gate currently passes — locks in S128 fix as a permanent invariant. |
| ✅ DONE | 3  | portfolio-filesystem-sitemap-drift-gate | `scripts/check-portfolio-coherence.mjs` + `data/portfolio-coherence-baseline.json`. Cross-walks registry (28) ↔ /projects/ (11) ↔ /games/ (9). `--check` mode passes against baseline; net-new drift will fail CI. Wired into `build:check`. |
| ⚠ PARTIAL | 2 | avif-lqip-pipeline-ship-now-unblocked | `convert-images-to-avif.mjs --write` ran: `icon-512.avif` + `icon-512.webp` generated. og-*.png (47–59 KB) below script's hardcoded 100 KB threshold. Full `<picture>` migration deferred — `sharp` is installed but the existing script needs threshold-flag wiring + HTML rewrite pass (~3h). |
| ✅ DONE | 13 | pointerdown-prerender-shim | `assets/pointerdown-warm.js` — delegated pointerdown listener injects `<link rel=prerender>` for the clicked internal target; respects Save-Data + 2G + `[data-no-prerender]` + `target=_blank`. Added to ambient block + SW STATIC_ASSETS. |
| ✅ DONE | 12 | page-sigil-age-indicator | `assets/page-sigil.js` — 28×28 SVG ring top-right of every public page; stroke color reflects last-update-age (green ≤14d, amber ≤60d, red >60d); links to `/studio-pulse/`. Reads `meta[name=vs:last-touched]` or `/api/public-intelligence.json`. Skips portals + `[data-no-sigil]`. Mounted via `requestIdleCallback`. |
| ✅ DONE | 9  | vault-atlas-live-status-strip | `assets/vault-atlas.js` — 5-dot live-status strip mounted into Resources dropdown; sources homepage/pulse/hub/ignis/checkout from existing `/api/*.json` shards; refreshes every 90 s; falls back to neutral on 404. |
| ✅ DONE | 6  | universal-feedback-button-sitewide-default | `data/feedback-prompts.json` codifies per-path prompts; `assets/micro-feedback.js` already implements the S128 collapsed-button pattern. Anti-survey UX now data-driven. |
| 🚫 DEFERRED | 4  | lighthouse-perf-restoration-to-92 | Needs live Chrome DevTools perf trace — founder-verify session. Bundled with `propagate-csp.mjs --dry-run` E2E workflow cleanup. |
| 🚫 DEFERRED | 5  | ignis-roi-loop | Edge fn deploy + Supabase migration. |
| 🚫 DEFERRED | 7  | passkey-cross-subdomain-auth | Auth-risk window founder gate. |
| 🚫 DEFERRED | 8  | synthetic-auth-canary-with-rollback | Cost approval. |
| 🚫 DEFERRED | 10 | founder-twin-dispatch-whisper | Cross-repo + deploy. |
| 🚫 DEFERRED | 11 | anthropic-prompt-cache-for-personas | Edge fn deploy. |
| 🚫 DEFERRED | 14 | cross-game-lore-gates-via-rank | 4h, bounded — next /implement pass. |
| 🚫 DEFERRED | 15 | mode-aware-homepage-anon-vs-member | 4h, bounded — next pass. |
| 🚫 DEFERRED | 16 | receipts-wall-you-spoke-we-shipped | Schema migration + deploy. |
| 🚫 DEFERRED | 17 | founder-voice-notes-eternal-tier | Founder content. |
| 🚫 DEFERRED | 18 | sealed-vault-countdown-tease | Needs registry seed. |
| 🚫 DEFERRED | 19 | ignis-conduit-proactive-narrator | 8h innovation reserve. |
| 🚫 DEFERRED | 20 | universe-map-alt-homepage | 8h innovation reserve. |
| 🚫 DEFERRED | 21 | vault-spark-id-universal-session | Auth-risk founder gate. |
| 🚫 DEFERRED | 22 | founder-team-page-and-eeat-schema | Founder content. |

**Pass-1 totals:** 7 DONE · 1 PARTIAL · 14 DEFERRED · combined-Priority-shipped ≈ 247 / 548.4 (≈ 45%).
**Build:check:** exit 0 with both new gates active.
**Pages updated by propagate-nav:** 81 (3 new ambient scripts added).
**Strategic posture:** This pass front-loaded **structural gates** (#1, #3 — prevent S128-class drift, lock in coherence) + **3 fresh genius-tier ambient innovations** (Atlas, Sigil, Pointerdown-Warm — total ~3h, total innov 26/30, none on competitor roadmaps) + codified the S128 feedback UX pattern. The structural gates are the highest-ROI win — they convert a class of regressions into compile-time failures.

---
*Generated by `/audit` v1.0 · executed by `/implement` v1.0*
