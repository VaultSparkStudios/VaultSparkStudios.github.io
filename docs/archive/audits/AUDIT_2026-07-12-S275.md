<!-- generated-by: render-audit-md.mjs (R-H14) — DERIVED FROM AUDIT_2026-07-12-S275.json; do not hand-edit -->
<!-- generated-at: 2026-07-12 -->
<!-- project: unknown · type: unknown -->

# Project Audit — Unknown

> One ranked improvement plan covering 9 axes. Top items first.
> Source of truth: this file is derived from the JSON sidecar. Edit the JSON, then re-render.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 20
- Combined Priority: 0.00
- Top 3: —

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov | Priority | Status | Item |
|---|:-:|---|---|:-:|:-:|:-:|:-:|---|
|  |  | dual-audience | 30m | 9 | 6 | 0.0 | · | **robots-wellknown-allow** — .  **Recipe:** Insert a targeted Allow block for the four public files (llms.txt, llms-full.txt, entity-graph.json, security.txt) above the broad Disallow — crawlers honor most-specific match — keeping the rest of .well-known private. Add a build:check contract asserting every /.well-known/ URL referenced by agents.json + llms.txt is robots-Allowed, so the two surfaces can never contradict again (cross-surface coherence gate pattern). |
|  |  | ux | 1.5h | 7 | 4 | 0.0 | · | **hero-cta-conversion-hierarchy** — .  **Recipe:** Keep Play as the gold primary (games are the soul), promote Join The Vault to the accent-filled second slot, demote Map the Studio to secondary third. Track via existing data-track-event funnel counters (home_hero_member_click vs home_hero_atlas_click) in funnel-summary. |
|  |  | speed | 5h | 7 | 7 | 0.0 | · | **fgm-hover-inp-presentation** — .  **Recipe:** Statically identify the hover-reactive ambient layer in ambient-core (pointerenter/mousemove listeners driving large-area repaint); add CSS containment (contain: paint / content-visibility) or throttle-gate the effect so hover no longer invalidates a large layer; verify with the INP soak verdicts feed after field data accumulates. Memory: INP Event Timing needs interactionId filter — confirm the beacon filters correctly while in there. |
|  |  | organization | 2h | 7 | 9 | 0.0 | · | **ark-sig-fail-noise-and-root-bug** — .  **Recipe:** Local root-fix: cap/rotate the sig-fail ledger at write time in the drain path, untrack the volatile ndjson pair (gitignore .cache/ark-sig-fails.ndjson + ark-drained.ndjson) so noise stops riding commits, keep the dossier builder reading the live file. Cross-repo root bug (port-online cargo signed with a key this repo cannot verify) is studio-ops-owned: ship a repo-question Ark cargo with the dossier evidence — never edit the sibling tree. |
|  |  | speed | 4h | 8 | 5 | 0.0 | · | **cls-late-injection-strips** — .  **Recipe:** Reserve space at the injection sites: min-height/skeleton sized to typical injected content on .cl-timeline, the oracle live list, and the games catalog dynamic strips; where content length varies, use content-visibility with contain-intrinsic-size. Verify with a local Lighthouse/Playwright pass on /games/ + /changelog/. |
|  |  | speed | 6h | 8 | 5 | 0.0 | · | **homepage-field-lcp-critical-path** — .  **Recipe:** Split the inline <style>: keep above-the-fold shell + hero rules inline, move below-the-fold homepage section styles into the already-async external stylesheet path. Measure before/after with local Lighthouse; ship only if lab LCP improves and no FOUC on the fold. Shell-hash rotation cost is expected — pre-push perf trace per memory. |
|  |  | organization | 3h | 7 | 8 | 0.0 | · | **orphan-scripts-gate** — .  **Recipe:** Ship scripts/check-orphan-scripts.mjs modeled on check-orphan-libs with a reference corpus of package.json scripts + workflows + git hooks + cross-script imports + HTML; wire into build:check. Triage the 7: wire validate-task-ids + check-canon-044-waves + validate-skill-yaml into the gate chain, allowlist hook-invoked pre-push-scan, resolve the dead producers (see studio-feed item). |
|  |  | organization | 3h | 7 | 8 | 0.0 | · | **ledger-rotation-generalize** — .  **Recipe:** Generalize into rotate-ledger.mjs with a target manifest (file → byte cap → archive shard pattern context/archive/<NAME>_<year>Q<q>.md); rotate oldest entries out of SELF_IMPROVEMENT_LOOP/WORK_LOG/HANDOFF_ARCHIVE preserving the Rolling Status header + recent entries; wire --check-size advisory into build:check next to rotate-taskboard. |
|  |  | ux | 1h | 5 | 3 | 0.0 | · | **forge-count-single-source** — .  **Recipe:** Derive both from the same catalog source that feeds build-hero-portfolio.mjs: wrap the stat-mini count in build markers, and make propagate-nav.mjs compute the forge count from the registry/catalog at generation time. Extend the cross-surface coherence gate to cover forge-count agreement so it can never drift again (derive-dont-hardcode memory pattern). |
|  |  | speed | 3h | 9 | 3 | 0.0 | · | **rum-summary-restore** — .  **Recipe:** Root-fix the export path, not the report: find why the Worker RUM export into rum-history stopped on 07-03 (workflow schedule, R2/KV read, credential), repair it, re-export the missing window, re-run rum:summary until sufficientRoutes > 0. Then add a freshness ceiling on rum-history age (public-trust blockDays pattern) so a stalled export fails loud within days, not weeks. |
|  |  | organization | 1.5h | 5 | 8 | 0.0 | · | **studio-feed-dead-contract** — .  **Recipe:** Resolve the contradiction at the ledger level: remove fetch-studio-feed.mjs + the stale expectation, or wire producer + consumer end-to-end if the feed is wanted. Fold the verdict into the orphan-scripts triage (rank 7). |
|  |  | token-cost | 30m | 4 | 7 | 0.0 | · | **buildcheck-duplicate-steps** — .  **Recipe:** Remove the later occurrence of each duplicate; add a self-check in run-build-check (or a build:check contract) that fails on duplicate identical steps so the chain can't silently re-accumulate them. |
|  |  | speed | 5h | 6 | 5 | 0.0 | · | **ambient-loader-split** — .  **Recipe:** Move the rank-1 candidate out of the always-on bundle behind its existing gating conditions per the split-candidates recipe; remember the 3 contracts a website asset change trips (ambient-loader registration, style contract, rum allowlist) and that shell-hash rotation costs a cold cache — run the Playwright smoke + perf trace before ship. |
|  |  | token-cost | 2h | 6 | 9 | 0.0 | · | **skill-cost-full-instrumentation** — .  **Recipe:** Hook recordSkillCost() into the phase-boundary scripts the heavier skills already run (set-active-skill transition points + closeout writer), estimating tokens from the context-meter delta between phases. Measurement plan: after one full arc, aggregate actual.tokens per skill and rank; target the top-2 for trimming next session. |
|  |  | security | 1.5h | 7 | 5 | 0.0 | · | **verify-jwt-pin-all-functions** — .  **Recipe:** Determine each function's intended posture from its in-code auth (getUser/authHeader/public+rate-limit) and declare all 13 explicitly in config.toml so git is the single source of truth. |
|  |  | security | 30m | 6 | 4 | 0.0 | · | **portal-gate-nostore** — .  **Recipe:** Rebuild the gate response with explicit Cache-Control: no-store (+ Vary: Cookie) headers; extend tests/redirects.spec.js (or the worker unit spec) to assert no-store on the gate path. |
|  |  | ux | 30m | 3 | 3 | 0.0 | · | **nav-sheet-home-link** — .  **Recipe:** Prepend a Home entry in buildBody() cloned from navMenu.querySelector(':scope > a'); keep it cohort-consistent with the drawer ordering. |
|  |  | security | 1h | 5 | 4 | 0.0 | · | **redirect-spec-worker-coverage** — .  **Recipe:** Extend the table-driven spec with the missing [from,to] pairs (hash targets asserted via toContain); Worker Layer-0c is canonical for these per memory, so the spec is the right home. |
|  |  | security | 30m | 5 | 3 | 0.0 | · | **obeliskgate-csp-allowlist** — .  **Recipe:** Add https://obeliskgate.com to script-src + connect-src in csp-policy.mjs (single source feeds worker CSP); note in DECISIONS that this un-blocks the page without flipping the provider decision. |
|  |  | dual-audience | 45m | 4 | 3 | 0.0 | · | **sitemap-drop-disallowed** — .  **Recipe:** Make the sitemap generator skip robots-Disallowed prefixes (keyed off the same Disallow set — one source of truth), regenerate, and let the rank-1 robots coherence contract cover this direction too. |

