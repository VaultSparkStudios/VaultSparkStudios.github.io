<!-- generated-by: render-audit-md.mjs (R-H14) — DERIVED FROM AUDIT_2026-06-18.json; do not hand-edit -->
<!-- generated-at: 2026-06-18 -->
<!-- project: vaultsparkstudios-github-io · type: website -->

# Project Audit — VaultSpark Studios Website

> One ranked improvement plan covering 9 axes. Top items first.
> Source of truth: this file is derived from the JSON sidecar. Edit the JSON, then re-render.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 16
- Combined Priority: 0.00
- Top 3: —

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov | Priority | Status | Item |
|---|:-:|---|---|:-:|:-:|:-:|:-:|---|
| 1 | 🔥 | ux | 1h | 7 | 8 | 88.3 | · | **adaptive-oracle-intro** — Adaptive Oracle intro: personalize the IGNIS prompt from localStorage query history. The Oracle/IGNIS page greets every visitor identically: 'Ask anything about VaultSpark Studios.' But S199 already stores up to 10 {query, ts} objects in localStorage (ignis_history). For returning visitors, swapping this static line to 'Welcome back — last time you asked about [topic]. Continue?' eliminates the blank-slate cold-start that causes most users to never type a first question. Outcome: returning visitor → IGNIS query → deeper site engagement and membership consideration. **Recipe:** Predicate-load a tiny <1KB script (ignis-personalize-intro.js) on /oracle/ and /ignis/ pages. Read localStorage ignis_history; if ≥1 entry, swap .vs-ask-ignis form placeholder + surrounding h2 text with 'Continue your [topic] research' + pre-populate the input with the last query. Animated cross-fade transition. RUM: emit 'oracle:personalized_intro_shown'. No backend needed. |
| 2 | 🔥 | ux | 2h | 8 | 7 | 70.0 | · | **play-next-redesign** — Dead CTA fix: redesign cross-game play-next card (18 shown, 0 clicks). api/dead-ctas.json confirms: the 'play-next' CTA family has been shown 18 times with 0 clicks — a 0% click-through rate on the studio's only cross-product routing surface. The current card appends after main content with generic copy. It's being seen and ignored. The fix is content + placement, not removal: a personalized 'You haven't tried [game] yet' headline with game cover art, positioned inside the hero area where attention already lives. Outcome: game page → second game play (audience compounding). **Recipe:** Update cross-game-play-next.js: (1) Position the card inside .game-hero or top of main, not appended after it. (2) Replace generic headline with 'Haven't tried [slug] yet?' using SOUL-voice. (3) Add cover art (data/game-registry.json mediaReady flag) or a styled gradient per game's brand colors. (4) Ensure [data-play-next] attr is present in game hero HTML. RUM: play-next:click is already wired; also emit play-next:position to track placement experiment. |
| 3 | 🔥 | ux | 1h | 7 | 6 | 66.3 | · | **vault-momentum-social-proof** — Vault Momentum social proof strip on /membership/. build-vault-momentum.mjs generates api/vault-momentum-score.json (total points moved this week) and this data already powers the homepage chip for signed-in members (S205). But /membership/ — the conversion page — shows no social signal. Adding a strip ('Studio momentum: +X points this week · N members active') makes the purchase decision feel social, not solo. This is the oldest conversion psychology trick in SaaS — social proof at the moment of decision. Outcome: /membership/ scroll → subscription CTA click. **Recipe:** Add a <div class='vs-momentum-strip'> after the /membership/ hero. Client-side fetch api/vault-momentum-score.json (same pattern as homepage chip in vault-momentum-score.js). Render 3 data points: total_points_this_week, active_members, and a 7-day sparkline if present. Honest-dark when api returns 0 or error. Predicate-load membership-social-proof.js only on /membership/. |
| 4 | 🔥 | featureDepth | 4h | 9 | 8 | 41.7 | · | **progressive-membership-unlock** — Progressive membership reveal (Wave 5 deferred from S185): free tier first, premium stagger on scroll, Eternal on 3rd visit. S185 deferred this as Wave 5 (8h estimate then, refined to 4h now with membership-unlock.js already providing the localStorage signal infrastructure). The /membership/ page currently dumps all 6 tiers at once — visitors who see $99.99/mo first and bounce never encounter the free Forge tier. SaaS studies show progressive reveals increase free-tier sign-ups by 25-40% when the free option is anchored first. The behavioral tier (Eternal reveals on 3rd visit) rewards return visitors with the top offer. Outcome: /membership/ → free-tier join → upsell ladder. **Recipe:** 1) Reorder tier cards in HTML: Forge (free) as hero card, VaultSparked group below, Eternal last. 2) Add CSS animation-play-state:paused + data-reveal-tier to premium tier cards. 3) IntersectionObserver in membership-unlock.js (already exists with visit-count logic) triggers the stagger. 4) Eternal tier opacity:0 until visit_count >= 3 in localStorage. 5) 'See all options' expand link below the initial view. No backend changes. |
| 5 | ⚡ | speed | 1h | 6 | 5 | 37.9 | · | **check-identity-coherence** — Build gate: check-identity-coherence.mjs prevents 'game studio' identity drift. S204 rewrote 7 surfaces to the canonical 'creative studio building games, cinematic worlds, creative tools, and AI-native intelligence' identity. Without a gate, any future session — or an AI context carry — can silently narrow this back to 'game studio' on a public marketing surface. check-mission-statement-coherence.mjs (already wired in check-proof-surface.mjs) proves the pattern works. The identity coherence gate is the exact same shape and ~45m of work. Outcome: identity drift caught at build time, not at audit time 3 sessions later. **Recipe:** Create scripts/check-identity-coherence.mjs modeled on check-mission-statement-coherence.mjs. Banned phrase: 'game studio' standalone in 6 mission surfaces (index.html, studio/index.html, press/index.html, join/index.html). Allowlist: privacy.html, terms.html, meta[name=keywords], investor-portal/ (legal/SEO contexts). Exit 0 = clean, warns to stderr. Wire into check-proof-surface.mjs as a spawnSync step. |
| 6 | ⚡ | featureDepth | 2h | 8 | 6 | 36.0 | · | **acquisition-source-breakdown** — Acquisition source breakdown: referrer family in /v/rum → api/funnel-summary.json.sources. api/funnel-summary.json.sources exists (S194 rollup) but the Worker /v/rum POST body doesn't currently receive the referrer — analytics.js captures it but the beacon doesn't include it, so the sources field is always empty. A traffic-starved studio's single most important intelligence question is 'which channel should I double?' Without a real sources breakdown, every marketing decision is blind. Outcome: funnel dashboard shows search vs social vs direct → one channel gets doubled-down on. **Recipe:** 1) In ambient.js or analytics.js, add referrer: document.referrer?.slice(0,100) to the /v/rum POST body. 2) In Worker worker.mjs, extract referrer and classify into family: 'search'|'social'|'direct'|'referral'|'unknown' using URL hostname matching. 3) Store family in KV alongside ux events. 4) rollup-rum-ux.mjs gains sources aggregation block → api/funnel-summary.json.sources. 5) check-rum-allowlist.mjs: add referrer handling (not an event, so no allowlist entry needed). |
| 7 | 🔥 | gamification | 3h | 9 | 9 | 34.9 | · | **smart-trial-offer** — Behavioral trial offer: 50% off first month for high-intent visitors (5min+ total site time). The highest-intent visitors — those who have spent 5+ cumulative minutes across sessions reading game pages, lore, and membership info — never receive a friction-reducing nudge to convert. A time-limited offer (24h countdown, localStorage-gated to show exactly once per device) at the exact moment of peak interest mimics the proven 'exit intent' conversion technique but triggers on demonstrated value consumption, not rage-quit. Stripe promo code API is already credentialed. Outcome: high-intent visitor → paid member on first contact. **Recipe:** 1) In membership-unlock.js (already tracking visits), compute total_session_minutes from localStorage vs_visit_timestamps. 2) When total >= 5min AND !localStorage.vs_trial_offered, inject a bottom-anchored panel: 'Your first month, half price — offer expires in 24h.' 3) Link to /join/?promo=TRIAL50 which pre-applies a Stripe promo code (create via Stripe dashboard). 4) localStorage.vs_trial_offered = Date.now(). 5) RUM: emit funnel:trial_offer_shown / funnel:trial_offer_clicked. 6) check-rum-allowlist: add funnel:trial_offer_* to Worker EVENTS. |
| 8 | 🔥 | gamification | 2h | 8 | 8 | 32.0 | · | **adaptive-pricing-reveal** — Adaptive pricing reveal: highlight the tier that matches each visitor's behavior. A visitor who has spent time on 3 game pages is a player — not a casual browser. A visitor who just read 5 universe lore pages is an immersion seeker. Yet /membership/ shows every visitor the same generic tier cards in the same order. Using localStorage visit-page tracking (vs_visit_count + game page URL history) to highlight the tier most relevant to their behavior turns a generic pricing page into a personalized recommendation. Outcome: /membership/ → relevant tier click → conversion. **Recipe:** 1) Extend membership-unlock.js (already reads vs_visit_count) to track page-type visit tallies (game_visits, lore_visits, portal_visits) in localStorage. 2) On /membership/, classify visitor into player|explorer|builder|engaged using the tallies. 3) Add data-profile to the matching tier card. 4) CSS: [data-highlighted='true'] gets a golden border, a '★ Recommended for you' label, and a subtle entrance animation. 5) Copy overlay: 'Based on your interest in [top game], this tier unlocks [key perk].' No backend. |
| 9 | 🔥 | ai | 3h | 8 | 9 | 31.0 | · | **oracle-query-insights-feed** — Oracle 'What the community is exploring' feed: IGNIS becomes publicly self-documenting. IGNIS answers questions but says nothing about how it's being used. Adding a build-time synthesis of top queries and entities makes the AI surface self-documenting and socially credible: '354 questions answered · top topic this week: membership perks.' This turns a solo AI tool into a community signal. Visitors who see 'others are asking about this' are more likely to engage. Outcome: /oracle/ → IGNIS query → engagement depth → membership consideration. **Recipe:** 1) Create scripts/build-oracle-query-insights.mjs: reads data/rum-ux-history.ndjson (already committed), extracts oracle-answer:* and oracle-followup:* events, computes top 5 topics (by query frequency), most-clicked entity chips, week-over-week trend. 2) Writes api/oracle-query-insights.json (generatedAt = latest data day, honest-dark when < 10 events). 3) Oracle page fetches and renders: 'This week: N questions · Top topics: [A, B, C]' in the existing Studio Intelligence nav suite. Wire into build + check-proof-surface. |
| 10 | ⚡ | gamification | 2h | 7 | 8 | 28.0 | · | **constellation-public-feed** — Constellation Challenge public feed: anonymous aggregate drives FOMO → membership. S205 shipped 5 hidden constellation badges (constellation-tracker.js). The achievements exist but nobody knows others are unlocking them. An anonymous aggregate ('3 members unlocked the Spark Seeker challenge this week') on the homepage or /vault-member/ page creates social FOMO without exposing individual users. The knowledge that real people are completing challenges turns a solo game into a community race. Outcome: anonymous visitor sees badge activity → join CTA click → membership. **Recipe:** 1) Extend scripts/build-constellation-state.mjs (or create if needed): read api/funnel-summary.json constellation events, aggregate by challenge_id, write api/constellation-activity.json {challengeId, count, week}. Honest-dark when count=0. 2) Add a 'Community Activity' module to /vault-member/ (signed-out view) and homepage intel strip. 3) Render: 'N explorers found the [challenge] badge this week.' SOUL-voice. 4) Wire into build + check-proof-surface. |
| 11 | 🔥 | newFeatures | 8h | 9 | 10 | 27.1 | · | **vault-passport** — Vault Passport: shareable member achievement card (Spotify Wrapped for Vault membership). Members have rank, tenure, constellation badges, and visit history — yet there's no surface that makes this achievement feel worth sharing. A Vault Passport at /vault-member/passport/ renders a visual achievement card (rank arc, top game, badges earned, membership tenure) and offers a one-tap share. Every share is an acquisition event: friend sees card, visits, joins. This is the studio's only potential viral loop outside of game pages. The entire data layer already exists. Outcome: signed-in member → shares passport → new visitor acquisition. **Recipe:** 1) New page /vault-member/passport/index.html. 2) JS fetches signed-in member data from Supabase (rank_points, created_at, constellation achievements). 3) Renders a dark-theme 'card' HTML element: rank badge arc, top game visited (localStorage), tenure in days, badge icons. 4) share-game.js pattern: Web Share API + canvas-to-PNG fallback for social cards. 5) og:image points to a static branded passport template PNG. 6) RUM: passport:shared, passport:viewed. |
| 12 | ⚡ | speed | 3h | 6 | 5 | 25.8 | · | **build-parallelization** — Build pipeline parallelization: fan out independent generators (~45s → ~20s). npm run build chains all generators sequentially even though build-og-cards, build-velocity-series, build-rank-climbers, build-constellation-state, and build-changelog-narrative are fully independent (no shared output files). Running these in parallel via child_process.spawn fans reduces end-to-end build time from ~45s to ~20s. Shorter builds mean faster iteration, faster CI green, and the context-meter at startup doesn't flag build slowness as a friction signal. Outcome: developer experience → iteration speed → more experiments shipped per session. **Recipe:** 1) Create scripts/build.mjs orchestrator (or extend npm run build via a parallel runner). 2) Phase 1 (parallel): build-og-cards, build-velocity-series, build-rank-climbers, build-changelog-narrative, build-constellation-state. 3) Phase 2 (serial, after phase 1): build-ignis-search-index (needs data from phase 1). 4) Phase 3 (serial): shell extraction + ambient bundle (needs phase 2). Use node:child_process.spawn with stdio:inherit, await all phase 1 spawns via Promise.all. Wire into package.json 'build' script. |
| 13 | ⚡ | feedbackLoop | 2h | 7 | 7 | 24.5 | · | **oracle-feedback-close** — Oracle feedback close loop: capture 'nothing found' queries to drive IGNIS content improvements. When IGNIS returns <3 docs for a query, the visitor gets a thin answer with no recourse. There is no signal from this failure to the content team. A 'Was this helpful? / What were you looking for?' mini-form on low-result queries routes unmet need directly into api/ignis-feedback.json, which becomes the input for expanding the knowledge base. This closes the IGNIS self-improvement loop that has been open since the Oracle shipped. Outcome: low-result query → feedback captured → IGNIS knowledge base expanded → next visitor gets a better answer. **Recipe:** 1) In ignis-answer-engine.js, detect when answer.sources.length < 3. 2) After the answer block, inject a feedback row: '👍 Helpful? 👎 Not quite — [What were you looking for? text input]'. 3) On thumbs-down + text submit, POST to a new edge function /functions/v1/ignis-feedback (Supabase), or write to api/ignis-feedback.json via a Worker PUT endpoint. 4) RUM: emit oracle:feedback_shown / oracle:feedback_submitted. 5) check-rum-allowlist: add oracle:feedback_* to Worker events. |
| 14 | 💡 | speed | 30m | 5 | 3 | 22.7 | · | **public-note-freshness** — Build gate: check-public-note-freshness.mjs prevents dev jargon in visitor-facing copy. PROJECT_STATUS.json's publicNote and publicNextStep fields feed visitor-facing intelligence surfaces (Nervous System, Studio Hub tile). When these fields contain session-code patterns (S206, [VERIFY/P0]) or dev jargon, real visitors see internal planning language instead of plain-English copy. A build gate that fails if publicNote is missing or contains session codes is a 30m investment that prevents a recurring copy problem. Outcome: visitor-facing intelligence surfaces always show plain English. **Recipe:** Create scripts/check-public-note-freshness.mjs: read context/PROJECT_STATUS.json, test publicNote + publicNextStep for (1) missing/empty fields, (2) session-code pattern /S\d{2,3}/i, (3) bracket-notation patterns /\[\w+\//. Wire into check-proof-surface.mjs spawnSync (non-fatal warn on missing, ERROR on session codes in public fields). Matches S202 TASK_BOARD: '[S202][STRUCT/P3] Add check-public-note-freshness.mjs gate.' |
| 15 | ⚡ | tokenCost | 2h | 6 | 7 | 21.0 | · | **ignis-query-cache-expansion** — IGNIS query cache: prefix-sharing reduces API calls ~30% on related-query sessions. ignis-answer-engine.js has a simple localStorage exact-match cache from S185. But related queries like 'membership' and 'membership benefits' each make full API calls. Extending the cache to store prefix → answer excerpt mappings means a visitor who asks 5 related questions in a session hits the API only for the first unique prefix. For a site running on the Claude Max Plan (CANON-015), reducing per-query API calls is direct cost-reduction. Outcome: 30% fewer Claude API calls per multi-query session without any answer quality loss. **Recipe:** In ignis-answer-engine.js, extend the existing localStorage cache: (1) When a query Q answers successfully, also store prefix(Q, 5 words) → {excerpt: answer.slice(0,200), ts}. (2) On query, check exact match first, then prefix match. (3) LRU eviction: cap at 30 prefix entries, evict oldest on overflow. (4) Staleness: TTL 24h. (5) Prefix match shows 'Continuing from your earlier search — [excerpt]' with a 'See full answer' link that triggers the real API call if the visitor wants depth. |
| 16 | ⚡ | security | 2h | 8 | 4 | 16.0 | · | **tt-enforce-reprobe** — Trusted Types enforce-flip: reprobe today (scheduled 2026-06-18), fix 2 remaining sinks. TT-ENFORCE-REPROBE was scheduled for ~2026-06-18 (today) in TASK_BOARD. S185 wired named policies for most sinks. S199 confirmed CSP violation reporting is live in Worker KV. Two sinks remain without named policies: home-idle-loader.js and schema-injector.js. With those two named and the first-party surface confirmed CLEAN, flipping Content-Security-Policy to require-trusted-types-for 'script' is the final step toward a class of XSS that can no longer exist on this site. Outcome: Trusted Types enforce-mode live → XSS script-injection class blocked at browser level. **Recipe:** 1) Run node scripts/ops.mjs tt-audit (or grep for TrustedTypes in worker.mjs / config/csp-policy.mjs) to confirm current posture. 2) Inspect home-idle-loader.js + schema-injector.js for innerHTML/eval/script-creation sinks. 3) Create named policies for each sink via TrustedTypePolicyFactory. 4) Update config/csp-policy.mjs: require-trusted-types-for 'script'. 5) Run npm run build:check. 6) Deploy with wrangler deploy --env production. |

## Depth Ladder (per item: L1 minimal · L2 solid · L3 genius)

> `/implement` selects the depth that fits remaining budget, and climbs the ladder
> on already-shipped items when the session still has budget (S175 saturation).

### adaptive-oracle-intro
- **L1 · minimal** _(~30m)_ — Swap /oracle/ h2 subtitle text and input placeholder if ≥1 localStorage history entry. No animation, no RUM.
- **L2 · solid** _(~1h)_ — Full intro swap + animated cross-fade + 'Continue' pre-fill button + RUM oracle:personalized_intro_shown. Predicate-loaded on /oracle/ and /ignis/.
- **L3 · genius** _(~3h)_ — Cross-page topic aggregation: track which game/membership/universe pages the visitor touched, not just IGNIS queries. Adapt intro to their dominant interest area ('You've been reading about Voidfall — ask IGNIS about the lore?').

### play-next-redesign
- **L1 · minimal** _(~30m)_ — Move card insertion point to after .game-hero (above main content). Change headline from generic to 'Haven't tried [slug]?'. Test predicate still fires.
- **L2 · solid** _(~2h)_ — Full redesign: hero-positioned card + cover art from game-registry.json + personalized copy + visit-history filter (don't recommend a game they already visited).
- **L3 · genius** _(~4h)_ — Behavioral rotation: recommendation refreshes each session; 3 possible candidates ranked by affinity score from data/game-affinity.json. A/B card vs inline CTA design via localStorage variant assignment.

### vault-momentum-social-proof
- **L1 · minimal** _(~30m)_ — Fetch api/vault-momentum-score.json on /membership/. Show a single 'Studio momentum: +X pts this week' line below the hero headline.
- **L2 · solid** _(~1h)_ — Full 3-data-point strip: points, active members, 7-day sparkline. Honest-dark. Predicate-loaded. SOUL-voice copy.
- **L3 · genius** _(~3h)_ — Animated momentum pulse: a real-time style waveform of the last 7 days built from build-vault-momentum.mjs data. Similar to the Studio Pulse ship-waveform (S185 wave3b). Shows 'the vault is alive' at the moment of purchase decision.

### progressive-membership-unlock
- **L1 · minimal** _(~1h)_ — Reorder tier cards: Forge first. CSS opacity: VaultSparked cards start at 0.2, animate to 1 on IntersectionObserver. 'See all' expand link.
- **L2 · solid** _(~4h)_ — Full 3-stage reveal: free immediate, paid scroll-triggered with stagger, Eternal reveals on 3rd visit (localStorage). 'N members sparked this week' social counter per tier.
- **L3 · genius** _(~8h)_ — Behavioral personalization: which tier highlights first depends on visit signals (game visitor → player-benefit tier first; universe reader → lore-benefit tier first; returning-member → upgrade path). Driven from data/membership-tiers.json.

### check-identity-coherence
- **L1 · minimal** _(~30m)_ — Grep 6 HTML surfaces for 'game studio' (case-insensitive, word-boundary), warn on match, exit 0. Not yet wired into build.
- **L2 · solid** _(~1h)_ — Full gate with allowlist (legal, meta keywords, investor), wired into check-proof-surface.mjs spawnSync. 5-case self-test.
- **L3 · genius** _(~2h)_ — Auto-fix mode: suggest replacement text inline ('game studio' → 'creative studio') when a violation is found. Add --fix flag that applies the suggestion directly.

### acquisition-source-breakdown
- **L1 · minimal** _(~30m)_ — Add referrer to /v/rum POST body in ambient.js. Confirm it reaches the Worker (check KV via dashboard). Sources classification deferred.
- **L2 · solid** _(~2h)_ — Full pipeline: beacon includes referrer → Worker classifies into 5 families → rollup includes sources → api/funnel-summary.json.sources populated. check-funnel-contract.mjs updated.
- **L3 · genius** _(~4h)_ — Channel optimization dashboard: api/channel-insights.json (top channel this week, week-over-week trend, recommended next action from a tiny decision tree). Surface on /oracle/ Studio Intelligence suite.

### smart-trial-offer
- **L1 · minimal** _(~1h)_ — localStorage-gated banner after 5min: links to /join/ page with no Stripe code. Proves the pattern and starts measuring click-through.
- **L2 · solid** _(~3h)_ — Full implementation: Stripe promo TRIAL50 created, countdown timer (24h from first show), UTM link, RUM funnel:trial_offer_* events wired.
- **L3 · genius** _(~6h)_ — Behavioral A/B: 50% see the offer at 3min, 50% at 7min. Record which cohort converts better. Winning threshold auto-adopts after 50 events. No backend — pure localStorage + RUM.

### adaptive-pricing-reveal
- **L1 · minimal** _(~30m)_ — If game_visits >= 3 in localStorage, add data-highlighted='true' to VaultSparked tier card. CSS: golden border + '★ Recommended' label. No copy change.
- **L2 · solid** _(~2h)_ — Full 3-profile system: player|explorer|engaged each highlights a different tier + SOUL-voice 'Based on your interest in [area]' copy overlay. Predicate-load on /membership/.
- **L3 · genius** _(~4h)_ — Complete match engine: which tier to highlight depends on visit-pattern (games → player tier, universe → lore tier, portal-returns → upgrade path). Behavioral quiz fallback ('Which best describes you?') for cold visitors.

### oracle-query-insights-feed
- **L1 · minimal** _(~1h)_ — Count unique query strings in rum-ux-history.ndjson with oracle events. Write top 3 to api/oracle-query-insights.json. No UI — data only.
- **L2 · solid** _(~3h)_ — Full synthesis: top 5 topics, trending entities, total questions answered, week-over-week. Rendered on /oracle/ in the Studio Intelligence suite. Honest-dark < 10 events.
- **L3 · genius** _(~6h)_ — Weekly digest email to opted-in members via Resend (credentialed READY): 'This week the community explored X, Y, Z.' Driven from oracle-query-insights.json. Honest-dark while < 50 events.

### constellation-public-feed
- **L1 · minimal** _(~30m)_ — Aggregate constellation unlock events from rum-ux-history.ndjson. Write api/constellation-activity.json. Data only — no UI yet.
- **L2 · solid** _(~2h)_ — Full render: 'N members unlocked [challenge] this week' module on /vault-member/ (signed-out view). Honest-dark when count=0. SOUL-voice copy.
- **L3 · genius** _(~5h)_ — Full challenge leaderboard at /constellation/: all 5 challenges with live progress bars, unlock counts, recent-unlock feed (anonymous). Drives a new discovery page in the nav.

### vault-passport
- **L1 · minimal** _(~2h)_ — Static /vault-member/passport/ page. Render rank badge + tenure from Supabase. Share button copies link to clipboard. No canvas, no badge art.
- **L2 · solid** _(~8h)_ — Full Wrapped-style card: rank arc, top game (localStorage), constellation badges, tenure. Web Share API + canvas-to-PNG. SOUL-voice copy. RUM passport:shared.
- **L3 · genius** _(~16h)_ — Dynamic generation with multiple card themes (Forge, Sparked, Eternal). Annual 'Your Vault Year' summary auto-generated on membership anniversary. Scheduled Resend email with card PNG attachment.

### build-parallelization
- **L1 · minimal** _(~1h)_ — Identify 3 independent generators and add a parallel phase to package.json using 'node -e "require(...)" & sleep 0' pattern. Rough but proves the win.
- **L2 · solid** _(~3h)_ — Full build.mjs orchestrator: Phase 1 parallel (5 generators), Phase 2 serial. Error handling: one failing generator doesn't kill the build. Timing report.
- **L3 · genius** _(~6h)_ — Incremental build: hash-stamp each generator's input files. Skip re-run if inputs unchanged. Extends build-cache.mjs. Reduces CI cold build to <5s for no-change commits.

### oracle-feedback-close
- **L1 · minimal** _(~30m)_ — Add a text input to the existing thumbs-down feedback path. On submit, console.log the value + emit RUM oracle:feedback_submitted. No storage yet.
- **L2 · solid** _(~2h)_ — Full pipeline: text captured + stored in api/ignis-feedback.json (Worker PUT or Supabase edge fn). Honest-dark render on /oracle/ when < 5 entries. Oracle:feedback_* wired to RUM allowlist.
- **L3 · genius** _(~4h)_ — Feedback-driven tuning: build-ignis-search-index.mjs includes a review-queue pass for low-result query terms. If a term appears 3+ times in feedback, it becomes a signal to add a doc targeting that query.

### public-note-freshness
- **L1 · minimal** _(~30m)_ — Check publicNote exists + no session-code pattern. Log warning. Wire into check-proof-surface. Non-fatal.
- **L2 · solid** _(~30m)_ — Same as L1 but also checks publicNextStep. ERROR (not warn) on session codes found in either field.
- **L3 · genius** _(~1h)_ — Auto-generate publicNote from currentFocus via stripDevTalk() + suggest replacement in error message. Closes the loop so the fix is actionable.

### ignis-query-cache-expansion
- **L1 · minimal** _(~30m)_ — Store first 3 words of query as prefix key → answer excerpt in localStorage alongside existing history. Simple prefix match on query start.
- **L2 · solid** _(~2h)_ — Full LRU prefix cache (30 entries, 24h TTL). Prefix match shows 'Continuing from your earlier search' + excerpt + 'See full answer' link for deep queries.
- **L3 · genius** _(~4h)_ — Server-side warm cache: scripts/build-ignis-cache-warmup.mjs pre-populates answers for the top 10 oracle queries (from oracle-query-insights.json) at build time as api/ignis-cache.json. ignis-answer-engine.js checks static cache first.

### tt-enforce-reprobe
- **L1 · minimal** _(~30m)_ — Audit current TT posture: grep sinks in home-idle-loader.js + schema-injector.js. Report — no code changes.
- **L2 · solid** _(~2h)_ — Named policies for both remaining sinks. Flip CSP to require-trusted-types-for 'script'. Deploy. Verify via CSP KV in dashboard.
- **L3 · genius** _(~4h)_ — Full TT named-policy suite for all sinks with a self-test that runs each policy path under test input. Ark cargo to football-gm sibling for the same TT pattern.

## Skipped (Priority < 1.5)

- check-mission-statement-coherence — Already shipped and wired in check-proof-surface.mjs — TASK_BOARD item stale
- share-game — Already shipped S194 — assets/share-game.js exists + wired in ambient-loader.js:124
- ignis-proactive-hint — Already shipped S185 — .vs-ignis-proactive in ignis-answer-engine.js:471
- progressive-membership-reveal-s205-audit — S205 shipped sticky nav + Worker 301s (L1); Wave 5 progressive reveal is item #4 in this audit

