<!-- generated-by: render-audit-md.mjs (R-H14) — DERIVED FROM AUDIT_2026-06-15.json; do not hand-edit -->
<!-- generated-at: 2026-06-15 -->
<!-- project: unknown · type: unknown -->

# Project Audit — Unknown

> One ranked improvement plan covering 9 axes. Top items first.
> Source of truth: this file is derived from the JSON sidecar. Edit the JSON, then re-render.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 24
- Combined Priority: 0.00
- Top 3: —

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov | Priority | Status | Item |
|---|:-:|---|---|:-:|:-:|:-:|:-:|---|
| 1 | transformative | ux | 1h | 9 | 8 | 113.6 | · | **ignis-membership-advisor** — IGNIS tier advisor on /membership/ — 'Not sure? Ask IGNIS'. The /membership/ conversion page has no intelligent decision aid — visitors who don't know which tier fits them bounce rather than ask. A 'Which tier is right for me?' input box beneath the tier grid routes to /ignis/ with a pre-seeded context string, turning IGNIS into the studio's conversion assistant. This is a one-hour ship with session-long payoff. **Recipe:** Add an `.ignis-tier-advisor` section below `.mem-tiers-grid` in membership/index.html: form with input + 'Ask IGNIS →' button. On submit, redirect to `/ignis/?q=Which+tier+fits+me%3F+` + encodeURIComponent(userInput). Wire `funnel:membership_ignis_query` to /v/rum + add to RUM_UX_EVENTS in src/worker.js. No backend changes. |
| 2 | transformative | ux | 2h | 9 | 7 | 78.8 | · | **membership-intent-filter** — Progressive disclosure on /membership/ — 3-intent toggle (Play / Create / Support). The membership page shows all three tiers simultaneously — a cognitive load problem when a visitor just wants to know 'what do I get for free?' vs 'what does Sparked unlock?'. A three-intent toggle at the top lets visitors self-select into the relevant tier narrative and reduces scroll depth needed before a conversion decision. **Recipe:** Add `.mem-intent-tabs` with 3 buttons (Play / Create / Support) above `.mem-tiers-grid` in membership/index.html. Each tab shows/hides tier cards and rewrites the sub-heading. Pure vanilla JS + CSS. Wire `funnel:membership_intent_tab` to /v/rum. ~2h. |
| 3 | transformative | ux | 2h | 8 | 5 | 50.0 | · | **faq-data-driven-search** — FAQ data-driven + search + category tabs. The /faq/ page is 461 lines of static HTML — adding a FAQ entry requires editing raw markup, and visitors must scan visually with no search or category filtering. Moving entries to data/faq.json and rendering client-side with search + category pills makes the page maintainable and measurably improves answer-finding. FAQPage JSON-LD stays accurate because it derives from the same source. **Recipe:** Create `data/faq.json` (array of {question, answer, category}). Replace static `<details>` entries in faq/index.html with `<div id='faq-root'></div>` + inline renderer. Add `<input id='faq-search'>` + category pill row. FAQPage JSON-LD generated by `scripts/inject-faq-schema.mjs` from the same JSON (wire into build). ~2h. |
| 4 | transformative | ux | 4h | 7 | 7 | 47.4 | · | **shareable-rank-progress-card** — In-browser SVG rank-progress card — shareable via Web Share API. The 9-tier rank system is load-bearing for community buy-in (SOUL), but members have no way to share their progress externally. An in-browser SVG card generated from the signed-in member's rank/XP data, shareable via Web Share API (mobile) or clipboard fallback, creates organic word-of-mouth acquisition and reinforces rank as a real identity. No new deps — sharp and Web Share API are both available. **Recipe:** In `assets/vault-rank-bar.js`, add 'Share My Rank' button after velocity chip. On click, draw SVG card (rank name, XP bar, VaultSpark Studios footer), create object URL, call navigator.share({url, title, files:[svgFile]}) with clipboard fallback. Wire `funnel:rank_card_shared` to /v/rum + Worker RUM_UX_EVENTS. ~4h. |
| 5 | high | ux | 2h | 6 | 5 | 37.5 | · | **theme-cross-device-sync** — Persist theme choice to Supabase profile — syncs across devices for signed-in members. Theme selection lives in localStorage only — a member who chooses 'Lava' on desktop gets the default theme on mobile. For a membership product where identity is central, having your aesthetic follow you is a small but memorable detail. Supabase already tracks member preferences; adding a `theme` column is a trivial migration. **Recipe:** Apply migration: `ALTER TABLE profiles ADD COLUMN IF NOT EXISTS theme TEXT`. In `assets/theme.js`, after a theme change, call `supabase.from('profiles').update({theme}).eq('id', user.id)` if session exists. On page load (after auth check in identity.js bootstrap), read `profile.theme` and apply before CSS paints to avoid FOUC. ~2h. |
| 6 | high | featureDepth | 4h | 8 | 8 | 37.1 | · | **lore-gated-dispatches** — Rank-gated 'Classified' journal dispatches — Forge+ exclusive content. Journal dispatches are fully public — every visitor reads the same content as a Forge-rank member. Adding a 'CLASSIFIED' gate on select dispatches (excerpt visible to all, full content locked to Forge+) creates genuine upgrade incentive via content exclusivity without changing the content itself. Auth check uses existing Supabase session. No studio in this category gates narrative dispatches. **Recipe:** Add `classified: true, minRank: 'forge'` frontmatter to select dispatch entries. In dispatch template, if `classified && userRank < minRank`, replace body with a styled 'CLASSIFIED — Forge Clearance Required' block + upgrade CTA. Auth check uses existing `identity.js` session. Wire `funnel:dispatch_gate_hit` to /v/rum + RUM_UX_EVENTS. ~4h. |
| 7 | high | redundancy/ux | 3h | 8 | 4 | 34.5 | · | **merge-pathways-pages** — Collapse 6 byte-identical pathways/* pages into one filterable /pathways/. pathways/players, builders, investors, supporters, press, lore are each EXACTLY 165 lines (verified) — identical templates differing only in audience copy — sitting behind 6 separate URLs. One filterable /pathways/?audience= hub with Worker Layer 0c 301s collapses the crawl surface and consolidates SEO authority. **Recipe:** Extract audience-specific content into `data/pathways.json`. Build `pathways/index.html` with audience pill filters (vanilla JS show/hide). Wire 6 Worker 301s in `src/worker.js` Layer 0c block + `tests/redirects.spec.js` cases. Archive 6 sub-folder index.html files. ~3h. |
| 8 | high | speed | 30m | 7 | 3 | 31.8 | · | **wire-derive-into-build** — Wire derive-game-nav + derive-game-index into npm run build (drift prevention). derive-game-nav.mjs and derive-game-index.mjs are wired as CI gates (--check fails on drift) but NOT in npm run build. Any nav or index change requires a manual derive pass before build:check passes — a silent drift source that costs time every session. 30-minute fix. **Recipe:** In package.json `build` script, prepend `node scripts/derive-game-nav.mjs --apply && node scripts/derive-game-index.mjs --apply &&` before the first existing step. Run `npm run build` to verify. ~30m. |
| 9 | high | ai | 4h | 8 | 9 | 27.9 | · | **ignis-synthesis-mode** — IGNIS session synthesis — after 3+ questions, offer to synthesize the inquiry. After a multi-turn IGNIS session, users have explored a topic in depth but insights vanish on page close. A 'Synthesize this inquiry' button (after 3+ Q&A pairs) sends session history to IGNIS asking for a 1-paragraph synthesis. The output is copyable and shareable, turning IGNIS browsing into a tangible deliverable. No studio intelligence product offers this. **Recipe:** In `assets/ignis-answer-engine.js`, after the 3rd Q&A pair renders, inject a `.ignis-synthesize-btn` chip. On click, call IGNIS endpoint with session history prefixed: 'Synthesize this research session into one paragraph:' + history.map(h=>Q:+h.query+A:+h.answer).join(). Stream synthesis into `.ignis-synthesis-panel`. Wire `oracle-followup:synthesis_requested` to /v/rum (already in DYNAMIC prefix family). ~4h. |
| 10 | high | feedbackLoop | 2h | 7 | 5 | 26.3 | · | **acquisition-source-in-funnel** — Wire referrer family → /v/rum beacon + rollup into funnel-summary.json sources block. funnel-summary.json already has a `sources` key but it stays empty because document.referrer is captured in analytics.js but never added to the /v/rum beacon. On a traffic-starved site, knowing whether visitors arrive from Discord, search, or direct links is the single most actionable growth signal — closes the third end of the funnel (emit + allowlist + rollup). **Recipe:** In `assets/analytics.js`, add referrer bucketing: `const family = src.includes('discord')?'discord':src.includes('google')?'search':src?'referral':'direct'`. Add `src: family` to the existing RUM beacon payload. In `rollup-rum.mjs`, aggregate the `src` field into `sources` map. Wire into `api/analytics-summary.json`. ~2h. |
| 11 | high | gamification | 2h | 7 | 7 | 24.5 | · | **vault-climbers-monthly-digest** — Month's Top Climbers strip on homepage — community pulse via Supabase build-time query. The rank system is active but invisible to newcomers — no social proof that real people are progressing. A 'Top Climbers This Month' strip (5 anonymized members: rank icon + rank name + XP gained, NO usernames) derived from Supabase rank history gives the alive-signal that turns the rank system from a promise into a proof. **Recipe:** Create `scripts/build-rank-climbers.mjs`: query Supabase `rank_history` for members with highest XP delta this month, take top 5 anonymized rows (rank_name + xp_gained). Emit `api/rank-climbers.json`. Add `.rank-climbers` section to index.html below Heartbeat. Wire into `npm run build` + `build:check`. ~2h. |
| 12 | transformative | visual/data-viz | 4h | 8 | 7 | 21.7 | · | **render-dormant-intelligence-visuals** — Render the dormant intelligence visuals (heatmap + constellation + insights) instead of 'Loading...'. oracle/ and studio-pulse/ ship the markup for a 60-day signal heatmap, a project-constellation network graph, and an auto-insights list, but on the public deploy they sit at 'Reading the data...' / 'Loading 60-day grid...' placeholders. A studio whose whole pitch is 'we are alive and measurable' that shows perpetual loading spinners reads as broken (CANON-031). api/velocity-series.json (S198) exists so wiring cost is low; visual payoff is the single most premium-dashboard upgrade available. **Recipe:** In studio-pulse-live.js + oracle-insights-compute.js: render heatmap as CSS-grid (7 rows × ~9 weeks) bound to api/velocity-series.json; render constellation as inline SVG node graph (nodes = projects, edges = shared-universe links); populate #oracle-insights with 3-5 derived bullets (peak day, trend %, top mover). Add CSS shimmer skeleton so slow fetch never shows blank 'Loading' text. |
| 13 | transformative | ux/visual | 4h | 8 | 7 | 21.7 | · | **portal-member-first-load-premium** — Make the signed-in portal first-load premium + personal (the experience currently INVERTS). The anonymous funnel pages glow with radial gradients and animated orbs, but the signed-in /vault-member/ renders on a flat dark panel — the reward for joining is a downgrade in polish, directly undercutting SOUL non-negotiable #2 ('membership must feel like entry into something'). rank-orb.js already works on ranks/ so the reuse path is proven. **Recipe:** Above the fold in vault-member/index.html: tier card with current pts + next-tier + animated progress arc (reuse rank-orb.js), login-streak badge (streak>=3), 'this week's challenge' micro-card. Style dashboard header with tier-colored gradient + left-border accent (gold Sparked / purple Eternal / silver Free). Gate on session presence (account-chip pattern). |
| 14 | transformative | visual | 4h | 9 | 6 | 20.9 | · | **game-cover-art-system** — Give every game a real cover tile (cards are bare radial gradients on the #1 CTA path). The games grid is the most-trafficked conversion surface and every card is a colored radial-gradient blur behind text — no cover art, no distinctive imagery. This reads uniform and impersonal. sharp@0.34.5 is already a trusted devDep (S196) so bespoke SVG→PNG cover tiles can be generated build-time with zero new deps. **Recipe:** Author one bespoke SVG cover per game (title lockup + genre motif + brand-consistent palette), rasterize to PNG via the existing sharp pipeline, render as card hero image with gradient overlay for text legibility. Apply across games/index.html cards AND each game page hero. Keep radial gradient as loading fallback. Wire into build:check so a game without a cover fails the gate. |
| 15 | high | featureDepth/immersion | 8h | 7 | 9 | 19.0 | · | **universe-depth-map** — Interactive Universe Depth Map — node graph of game/project/lore connections. The site repeatedly hints at a shared universe but exposes it only as prose. An interactive node-graph showing how titles and lore connect — with a few public edges and more hinted behind membership — turns flat copy into explorable depth. No indie-studio site in this category has explorable lore-as-data. Founder-verify edges before publish (lore canon must be founder-reviewed). **Recipe:** Build an inline-SVG/canvas force-ish node graph (no heavy dep — reuse the constellation renderer). Seed nodes/edges from public `universe-graph.json`: games, projects, lore entities. Public visitors see 2-3 confirmed edges + locked/blurred nodes; signed-in members see more. Place on new /universe/ + embed a teaser on homepage. |
| 16 | high | visual/immersion | 3h | 7 | 6 | 18.1 | · | **hero-immersion-parallax** — Tighten homepage hero reveal timing + add scroll parallax depth. The hero forge-wordmark animation is cinematic but a ~1.4s reveal-chain delay creates dead-air before the tagline/copy land, and the three background glows are static radial gradients with opacity-only animation — no depth on scroll. First-impression immersion is left on the table on the studio's most important page. **Recipe:** Overlap the tagline reveal into the wordmark's final animation frame in home-dynamic-hero.js; add subtle scroll-driven parallax (~40px translate) on hero glows; add breathing micro-glow on hero-center synced to spark-burst rhythm. Keep prefers-reduced-motion honored. |
| 17 | high | featureDepth/immersion | 3h | 6 | 7 | 18.1 | · | **homepage-live-velocity-meter** — Replace the static '27 initiatives' teaser with a live studio velocity meter. The homepage states '27 initiatives. One vault.' as flat promo copy with no live count or drill-down, while the Heartbeat/Pulse data needed to make it real is already fetched elsewhere on the page. A live counter ('4 live / 9 in forge / 12 sealed') with inline expand makes the 'alive studio' claim self-proving instead of asserted. **Recipe:** Bind the teaser to the existing Heartbeat/public-intelligence feed: render live counts by status, each clickable to expand a brief manifest inline before linking to /studio-pulse/. Add a tiny velocity sparkline (ships/week) using velocity-series data. |
| 18 | high | redundancy/ux | 3h | 7 | 5 | 15.1 | · | **homepage-merge-activity-feeds** — Merge the three homepage activity feeds into one Vault Pulse strip. Recent Ships, Portfolio Heartbeat, and Vault Signal are three separate feed sections (~900px of mid-page scroll) all hydrated from backend JSON and all signalling 'studio momentum' — overlapping content that lengthens the page and dilutes impact. One consolidated, auto-rotating strip is tighter and more premium. **Recipe:** Combine into a single ~320px 'Vault Pulse' strip with tabs (Heartbeat / Ships / Signal) or auto-rotating carousel; default first tab by visitor type (Heartbeat for new, Activity for returning). Remove two now-redundant sections. |
| 19 | high | redundancy/ux | 4h | 7 | 5 | 13.5 | · | **differentiate-intelligence-surfaces** — Differentiate (or unify) oracle / studio-pulse / nervous-system — three near-identical pages. All three render the same ecosystem snapshot and a visitor can't tell why they exist separately. This is the largest conceptual redundancy on the site. Either give each a distinct, signposted job or unify them into one dashboard with modes. **Recipe:** Assign distinct roles: Pulse = live heartbeat (cards + current focus), Oracle = deep read (velocity + forecasts + insights), Nervous System = signal digest (tiles + decisions). Add shared sub-nav across the three. De-duplicate the shared project-card block. L3 = collapse to /studio-pulse/ with mode tabs + 301 the other two. |
| 20 | high | visual | 1.5h | 6 | 4 | 13.3 | · | **light-mode-visual-quality** — Light-mode first-class pass (hero glows nearly invisible at current opacity). SOUL quality bar requires light mode to be first-class, but the hero glows use identical blur/low-opacity values across themes — the gold glow (~0.09) nearly vanishes in light mode and the hero looks flat/unfinished for light-mode visitors. **Recipe:** Add theme-aware glow intensities via CSS custom props keyed off theme attribute: dark ~0.15/blur90, light ~0.22/blur60, ambient ~0.18. Spot-check contrast on 3-4 top pages in light mode. |
| 21 | high | ux | 1h | 7 | 3 | 13.2 | · | **games-grid-honesty-and-cta** — Remove dead-end Gridiron-GM-Play card + standardize CTA text + fix state honesty. The games grid carries a vaulted 'Gridiron GM Play' card whose primary button reads 'Play Football GM Instead' (a dead-end), the SPARKED section contains a vaulted game, and CTA text varies ('Play Now' / 'Play Beta' / 'Play Football GM Instead'). This is the exact surface-vs-truth class check-game-playability-coherence exists to catch. **Recipe:** Remove gridiron-gm-play card from the grid (keep /games/gridiron-gm-play/ as 301 target only); standardize CTA copy to Play Now (live) / Play Beta (beta) / Join Waitlist (pre-release); ensure SPARKED section contains only SPARKED titles; re-run check-game-playability-coherence.mjs. |
| 22 | high | redundancy/ux | 3h | 6 | 4 | 10.3 | · | **member-surface-ia-cleanup** — Untangle member / members / vault-member / vault-portal naming maze. Four similarly-named surfaces confuse navigation: /vault-portal/ is a door-chooser router, /vault-member/ is the real portal, /members/ is the directory, /member/ is a near-orphan profile page. The naming overlap makes it unclear where a member 'goes home', and /members/ isn't linked in nav. **Recipe:** Make /vault-member/ the portal home; make /vault-portal/ auth-conditional OR 301 it away; add /members/ to nav as 'Member Directory'; retire/redirect /member/ after usage check. Add cross-links from portal to directory. |
| 23 | nice-to-have | redundancy | 4h | 6 | 4 | 9.3 | · | **merge-membership-value** — Consolidate /membership/ and /membership-value/ (overlapping pitch, two mental models). Both pages present tier cards + benefits + value estimates; a visitor toggling between them gets the same pitch twice. Two large pages (1473 + 1022 lines) carrying one message splits conversion attention. **Recipe:** Make /membership/ canonical. Fold unique value-comparison/market bars into a collapsible '#value' section. 301 /membership-value/ → /membership/#value (Worker Layer 0c). Keep tier CTAs deep-linking to the upgrade flow. |
| 24 | nice-to-have | redundancy | 2h | 5 | 3 | 7.5 | · | **merge-brand-into-press** — Merge /brand/ into /press/ (one media kit, not two). /press/ (press kit: logos, bio, facts) and /brand/ (logos, palette, code) serve the same 'give me assets + usage' need and share ~40% content. One canonical resource is clearer and halves maintenance. **Recipe:** Merge /brand/ content into /press/ as tabbed 'Brand Assets' section. 301 /brand/ → /press/#brand-assets (Worker Layer 0c + redirects.spec.js). Consolidate near-identical .asset-card / .press-card CSS. |

## Depth Ladder (per item: L1 minimal · L2 solid · L3 genius)

> `/implement` selects the depth that fits remaining budget, and climbs the ladder
> on already-shipped items when the session still has budget (S175 saturation).

### ignis-membership-advisor
- **L1 · minimal** _(~30m)_ — Static 'Ask IGNIS →' link below tier grid with pre-seeded URL: /ignis/?q=Help+me+choose+my+Vault+tier
- **L2 · solid** _(~1h)_ — Input form with custom prompt prefix + RUM event. User's text appended to pre-seeded context string.
- **L3 · genius** _(~4h)_ — Embedded IGNIS widget (fetch-based) that answers inline without leaving the page. Supabase session injected so signed-in members get personalized advice.

### membership-intent-filter
- **L1 · minimal** _(~30m)_ — 3 anchor links that scroll to the relevant tier card with :target highlight.
- **L2 · solid** _(~2h)_ — Intent toggle: show/hide tier cards + rewrite section heading. RUM event.
- **L3 · genius** _(~6h)_ — URL param ?for=player/creator/supporter persists tab, OG image differs per intent, IGNIS advisor pre-seeded with intent context.

### faq-data-driven-search
- **L1 · minimal** _(~1h)_ — Extract FAQ entries to data/faq.json and generate static HTML from it via a build script. No search.
- **L2 · solid** _(~2h)_ — Client-side search + category pills rendered from JSON. FAQPage schema auto-derived.
- **L3 · genius** _(~5h)_ — L2 + 'No results? Ask IGNIS →' fallback when search term matches no entry.

### shareable-rank-progress-card
- **L1 · minimal** _(~30m)_ — Copy-to-clipboard share URL: vaultsparkstudios.com/ranks/?member=<slug>. No image.
- **L2 · solid** _(~4h)_ — SVG card generator + Web Share API + clipboard fallback. RUM event.
- **L3 · genius** _(~8h)_ — Server-rendered PNG via Supabase Edge Function (sharp) so shared URL shows real OG image on social.

### theme-cross-device-sync
- **L1 · minimal** _(~30m)_ — Read theme from Supabase on load only (no write-back). Reads stored preference.
- **L2 · solid** _(~2h)_ — Full bidirectional sync: read on load + write on change. Migration applied via Supabase Management API.
- **L3 · genius** _(~4h)_ — Theme sync + per-game theme override stored as JSON preferences map.

### lore-gated-dispatches
- **L1 · minimal** _(~1h)_ — Static 'CLASSIFIED' visual badge on 2 existing dispatches — teases exclusivity, no actual gate.
- **L2 · solid** _(~4h)_ — Client-side auth gate: hide body, show redacted excerpt + Forge upgrade CTA. RUM event.
- **L3 · genius** _(~8h)_ — Server-side gate via Supabase Edge Function: classified body never sent to non-Forge client.

### merge-pathways-pages
- **L1 · minimal** _(~1.5h)_ — Data-drive the 6 pages from one source to kill copy drift; defer deletion.
- **L2 · solid** _(~3h)_ — Collapse to /pathways/?audience= with filters + 301 the 6 subfolders (Worker Layer 0c).
- **L3 · genius** _(~4h)_ — L2 + audience auto-detect (referrer/UTM) that pre-selects the right pathway on arrival.

### wire-derive-into-build
- **L1 · minimal** _(~30m)_ — Add both derive scripts to npm run build.
- **L2 · solid** _(~30m)_ — Same as L1.
- **L3 · genius** _(~2h)_ — Content-hash-aware: skip derive when game-registry.json unchanged. Speeds up full builds.

### ignis-synthesis-mode
- **L1 · minimal** _(~1h)_ — Static 'Copy session log' button that copies all Q&A pairs to clipboard as plain text.
- **L2 · solid** _(~4h)_ — AI synthesis button: sends history to IGNIS, streams synthesis, copyable output. RUM event.
- **L3 · genius** _(~8h)_ — Synthesis saved to Supabase member profile as a 'Research Note', accessible from vault-member/ as a library.

### acquisition-source-in-funnel
- **L1 · minimal** _(~1h)_ — Add referrer family to beacon payload only (no rollup). Manual R2 inspection possible.
- **L2 · solid** _(~2h)_ — Beacon + rollup + analytics-summary sources block.
- **L3 · genius** _(~4h)_ — UTM parameter tracking (utm_source, utm_medium, utm_campaign) + sessionStorage attribution across multiple pageviews.

### vault-climbers-monthly-digest
- **L1 · minimal** _(~30m)_ — Static placeholder strip: '5 members ranked up this month' — honest-dark until data available.
- **L2 · solid** _(~2h)_ — Build-time Supabase query → api/rank-climbers.json → homepage strip with rank icons + XP gains. Anonymized.
- **L3 · genius** _(~5h)_ — Worker: Supabase query on edge, refreshed hourly via R2 cache. Eliminates build-time data staleness.

### render-dormant-intelligence-visuals
- **L1 · minimal** _(~1h)_ — Replace every 'Loading...' literal with a shimmer skeleton; wire heatmap to velocity-series.json only.
- **L2 · solid** _(~4h)_ — L1 + SVG constellation graph + derived insights bullet list + hover tooltips.
- **L3 · genius** _(~8h)_ — L2 + animated draw-in on scroll, click-to-drill on constellation nodes, freshness pill bound to feed timestamp.

### portal-member-first-load-premium
- **L1 · minimal** _(~1h)_ — Tier-colored gradient header + 'N pts to <next tier>' line + progress bar.
- **L2 · solid** _(~4h)_ — L1 + rank-orb progress arc, streak badge, this-week challenge micro-card; emit engagement:portal_personalized RUM event.
- **L3 · genius** _(~8h)_ — L2 + 'your focus this session' smart suggestion, recently-unlocked achievement reel, Browse-Members CTA.

### game-cover-art-system
- **L1 · minimal** _(~1.5h)_ — Polish existing gradients: add genre glyph/wordmark watermark + consistent overlay so cards stop looking identical.
- **L2 · solid** _(~4h)_ — Bespoke SVG→PNG cover tile per game via sharp; image + overlay on cards and game-page heroes.
- **L3 · genius** _(~8h)_ — L2 + subtle parallax/tilt on hover, animated cover variants for SPARKED titles, OG-image reuse.

### universe-depth-map
- **L1 · minimal** _(~2h)_ — Static SVG constellation of public titles with hover labels, no interactivity, homepage teaser only.
- **L2 · solid** _(~8h)_ — Full interactive /universe/ graph with locked/unlocked nodes, click-to-open, founder-verified edges.
- **L3 · genius** _(~16h)_ — L2 + animated reveal of new edges as titles ship, lore-gate tie-in, member-only deep edges with 'you unlocked' moment.

### hero-immersion-parallax
- **L1 · minimal** _(~30m)_ — Remove serial reveal delay so tagline+copy land with the wordmark.
- **L2 · solid** _(~3h)_ — L1 + scroll parallax on glows + breathing center glow, reduced-motion guarded.
- **L3 · genius** _(~6h)_ — L2 + pointer-reactive glow drift and a one-time first-visit cinematic that self-disables.

### homepage-live-velocity-meter
- **L1 · minimal** _(~1h)_ — Bind the count to the live feed (no hardcoded 27); show live/forge/sealed split.
- **L2 · solid** _(~3h)_ — L1 + inline expand per status + ships/week sparkline.
- **L3 · genius** _(~6h)_ — L2 + a 'vault pressure gauge' animated meter that visibly reacts as counts/velocity cross thresholds.

### homepage-merge-activity-feeds
- **L1 · minimal** _(~1h)_ — Tabbed container around the three existing feeds (no carousel), reclaim the scroll.
- **L2 · solid** _(~3h)_ — Auto-rotating strip with visitor-type default tab + reduced-motion pause.
- **L3 · genius** _(~5h)_ — L2 + glitch-on-new-signal micro-animation and click-to-pin a feed.

### differentiate-intelligence-surfaces
- **L1 · minimal** _(~1.5h)_ — Add one-line purpose banner + shared sub-nav to each so the distinction is explicit; cross-link.
- **L2 · solid** _(~4h)_ — L1 + extract duplicated project-card block to one shared renderer + specialize unique content.
- **L3 · genius** _(~8h)_ — Unify into /studio-pulse/?mode= with tabs; 301 oracle/ and nervous-system/ via Worker Layer 0c.

### light-mode-visual-quality
- **L1 · minimal** _(~30m)_ — Bump light-mode glow opacity/blur on homepage hero only.
- **L2 · solid** _(~1.5h)_ — Theme-aware glow tokens applied sitewide via custom props.
- **L3 · genius** _(~3h)_ — L2 + light-mode QA sweep of the top 10 pages fixing any low-contrast surfaces found.

### games-grid-honesty-and-cta
- **L1 · minimal** _(~1h)_ — Remove the dead card, standardize the three CTA labels, fix section membership.
- **L2 · solid** _(~2h)_ — L1 + data-driven status→CTA-label map so cards can't drift.
- **L3 · genius** _(~3h)_ — L2 + add status→label rule to check-game-playability-coherence so wrong CTA fails build:check.

### member-surface-ia-cleanup
- **L1 · minimal** _(~1h)_ — Add /members/ to nav + cross-link from portal; rename labels so four surfaces read distinctly.
- **L2 · solid** _(~3h)_ — L1 + make /vault-portal/ auth-conditional OR 301 it; retire/redirect /member/.
- **L3 · genius** _(~5h)_ — L2 + single coherent member-area sub-nav shared across portal/directory/ranks.

### merge-membership-value
- **L1 · minimal** _(~1.5h)_ — Cross-link the two + dedupe the most blatant repeated tier table; defer the merge.
- **L2 · solid** _(~4h)_ — Fold unique value content into /membership/#value and 301 /membership-value/.
- **L3 · genius** _(~6h)_ — L2 + interactive tier comparator (toggle Free/Sparked/Eternal) replacing static tables.

### merge-brand-into-press
- **L1 · minimal** _(~30m)_ — Cross-link the two pages; remove duplicate brand link from Resources nav dropdown.
- **L2 · solid** _(~2h)_ — Merge into /press/ tabs + 301 /brand/ + consolidate CSS.
- **L3 · genius** _(~3h)_ — L2 + one-click 'download full press+brand kit' zip.

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

- [object Object]
- [object Object]
- [object Object]
- [object Object]
- [object Object]
- [object Object]
- [object Object]

## Skipped (Priority < 1.5)

- wire-videogame-schema-gate — PREMISE FALSE — check-videogame-schema.mjs is already wired in check-proof-surface.mjs lines 49-50. Not a new gate needed.
- rank-orb-on-ranks — PREMISE FALSE — ranks/index.html ALREADY references rank-orb/rank-projector/rank-quest. Gap is portal-member-first-load-premium (item 13).
- oracle-velocity-chart-build — PARTIALLY DONE — build-velocity-series.mjs (S198) already generates api/velocity-series.json. Remaining gap is render-dormant-intelligence-visuals (item 12).
- mobile-sheet-default-swap — DATA-GATED — defaultSwapReady=false (1/50 opens). Requires 50 real mobile opens.
- merge-legal-pages — REJECTED BY CANON — privacy/cookies/data-deletion/accessibility must remain individually addressable for app-store + GDPR review.

