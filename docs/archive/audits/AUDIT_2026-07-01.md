<!-- generated-by: render-audit-md.mjs (R-H14) — DERIVED FROM AUDIT_2026-07-01.json; do not hand-edit -->
<!-- generated-at: 2026-07-01 -->
<!-- project: unknown · type: unknown -->

# Project Audit — Unknown

> One ranked improvement plan covering 9 axes. Top items first.
> Source of truth: this file is derived from the JSON sidecar. Edit the JSON, then re-render.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 5
- Combined Priority: 0.00
- Top 3: —

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov | Priority | Status | Item |
|---|:-:|---|---|:-:|:-:|:-:|:-:|---|
| S246-A1 | 🔥 | observability |  | 10 | 8 | 12.6 | · | **startup-session-regression-guard** — Prevent startup brief from regressing PROJECT_STATUS.currentSession. The renderer rewrote currentSession from 245 to 244 because the detailed SIL entry lagged while handoff, task board, current state, and status already proved S245. The fix must reconcile multiple completed-session sources and heal forward only. **Recipe:** Update render-startup-brief.mjs session reconciliation to use freshest completed-session evidence and refuse backward self-heals; add focused smoke coverage. |
| S246-A2 | ⚡ | processQuality |  | 8 | 6 | 8.0 | · | **human-pressure-empty-state** — Render an honest HUMAN PRESSURE empty state. validate-brief-format warns that the recommended HUMAN PRESSURE block is missing when there are no pressure items. An explicit empty state keeps the canonical brief shape stable without inventing founder pressure. **Recipe:** Render HUMAN PRESSURE every time, with none / 0 / continue agent-owned work when the compiled pressure file has no items. |
| S246-A3 | ⚡ | organization |  | 7 | 6 | 7.0 | · | **router-preflight-shim** — Restore local router preflight shim. /start attempts to run scripts/router.mjs, but the file is absent locally and throws MODULE_NOT_FOUND. A thin delegation shim keeps preflight reliable without copying Studio Ops routing logic. **Recipe:** Add scripts/router.mjs delegation shim and register it in check-protocol-scripts healing metadata. |
| S246-A4 | ⚡ | automationCoverage |  | 8 | 5 | 6.3 | · | **closeout-brief-behavior-fixture** — Add closeout brief renderer behavioral fixture. S245 restored the closeout renderer and import smoke, but task board still calls for behavior coverage. The fixture should prove bad voice is rejected and archive writing works before closeout depends on it. **Recipe:** Extend smoke-startup-scripts with a valid closeout fixture archive run and an invalid insight rejection check. |
| S246-A5 | 💡 | processQuality |  | 7 | 4 | 7.0 | · | **audit-implement-contract-shim** — Stabilize the local audit to implement contract. The implement skill expects scripts/lib/audit-sidecar.mjs, but this repo lacks that local shim even though Studio Ops has the canonical file. A thin shim keeps future implement passes from falling back to brittle markdown parsing. **Recipe:** Add a scripts/lib/audit-sidecar.mjs delegation/re-export shim to the sibling canonical module when reachable. |

## Depth Ladder (per item: L1 minimal · L2 solid · L3 genius)

> `/implement` selects the depth that fits remaining budget, and climbs the ladder
> on already-shipped items when the session still has budget (S175 saturation).

### startup-session-regression-guard
- **L1 · minimal** _(~30m)_ — Change self-heal to forward-only using status and SIL.
- **L2 · solid** _(~1h)_ — Reconcile SIL, status, handoff, task board, and current state, then validate the rendered brief.
- **L3 · genius** _(~2h)_ — Add a reusable session-evidence parser shared by startup, closeout, and doctor.

### human-pressure-empty-state
- **L1 · minimal** _(~15m)_ — Always render the empty block.
- **L2 · solid** _(~30m)_ — Always render the empty block and validate it through the startup brief gate.
- **L3 · genius** _(~1h)_ — Also emit structured pressure metadata for downstream status boards.

### router-preflight-shim
- **L1 · minimal** _(~15m)_ — Add the shim.
- **L2 · solid** _(~30m)_ — Add the shim plus protocol-heal metadata.
- **L3 · genius** _(~1h)_ — Add a protocol-script smoke assertion for expected shims.

### closeout-brief-behavior-fixture
- **L1 · minimal** _(~30m)_ — Test render() rejects a bad insight.
- **L2 · solid** _(~1h)_ — Test bad insight rejection plus archive creation from a fixture.
- **L3 · genius** _(~2h)_ — Split the fixture into a dedicated closeout renderer unit suite.

### audit-implement-contract-shim
- **L1 · minimal** _(~30m)_ — Add the local re-export shim.
- **L2 · solid** _(~1h)_ — Add shim plus smoke-startup export check.
- **L3 · genius** _(~2h)_ — Add a tiny implement-plan reader fixture against the audit JSON sidecar.

## Skipped (Priority < 1.5)

- inp-root-fix — Rejected on verification: data/inp-breakdown.json has no route samples, so a root-cause fix would be fabricated.
- lighthouse-floor-tuning — Deferred honestly: current signal is advisory and should not be tuned from one synthetic runner without field or production corroboration.


## Execution Log

| Item | Outcome | Verification |
|---|---|---|
| S246-A1 `startup-session-regression-guard` | shipped | Startup brief now renders Session 246 from completed S245 evidence and validates coherent. |
| S246-A2 `human-pressure-empty-state` | shipped | `validate-brief-format` no longer reports missing HUMAN PRESSURE. |
| S246-A3 `router-preflight-shim` | shipped | Router shim returns Studio Ops route suggestions. |
| S246-A4 `closeout-brief-behavior-fixture` | shipped | Startup smoke verifies voice rejection and archive creation. |
| S246-A5 `audit-implement-contract-shim` | shipped | Startup smoke imports `scripts/lib/audit-sidecar.mjs`. |
| S246-I1 `startup-session-coherence-gate` | shipped | Startup smoke reports `completed S245 -> brief S246`. |
| S246-I2 `protocol-shim-completion` | shipped | Protocol sentinel reports `0 unexpected-absent`. |
| S246-I3 `homepage-audit-fallbacks` | shipped | Homepage proof fallbacks are non-dash, Studio Spine fallback copy has no crawlable loading text, `Project ???` is gone, and Gridiron GM legacy copy names the active successor. |
| S246-I4 `homepage-audit-regression-guard` | shipped | `node scripts\check-home-audit-regressions.mjs` passes and is wired into `npm run build:check`. |
| S246-I5 `nav-vault-pipeline-collision` | shipped | `/roadmap/` label is `Studio Roadmap`; `/projects/vault-pipeline/` remains `Vault Pipeline`. |
| S246-I6 `project-schema-build-alignment` | shipped | `npm run build` runs `scripts/enrich-projects-schema.mjs`; `check-proof-surface` passes with project schema present. |
### Rejected / Deferred on Verification

- `inp-root-fix` — still data-blocked; 0 route samples.
- `lighthouse-floor-tuning` — no current floor failure or rolling-median regression.
