<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-05-21 -->
<!-- project: vaultsparkstudios-website · type: platform (Security 2x · Speed 2x · UX 1.5x · Feedback 1.5x) -->

# Project Audit — VaultSpark Studios Website (Session 147)

> Founder mandate: cut redundancy, ship speed, sharpen the studio showcase.
> One ranked plan covering all 9 axes — top items first. Run `/implement` to execute in order.

## Top-line

- Total items: **10**
- Combined Priority: **312.4**
- Top 3: `redirect-stub-purge` · `leaderboards-collapse` · `showcase-spine`
- Strategic frame: the site has accumulated ~175 HTML files across 5 distinct page-shell patterns, and visitors hit the same studio story split across `/`, `/journal/`, `/studio-pulse/`, `/oracle/`, and `/games/`. The single highest-leverage move is **subtractive**: kill 38 meta-refresh stubs (Worker 301s instead), collapse 7 leaderboard sub-shells into one tabbed surface, and unify the showcase into one canonical spine. Net result: ~50 fewer crawled URLs, ~250KB less HTML, and a visitor who lands anywhere reaches the studio story in one click.

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | 🔥 | Speed · SEO · Organization | 1h | 10 | 7 | 46.7 | **redirect-stub-purge** — 38 HTML files (`/call-of-doodie/`, `/gridiron-gm/`, `/investor/*`, `/products/*`, `/open-source/`, `/studio-hub/`, `/vault-treasury/`, `/vaultspark-football-gm/`) are meta-refresh stubs that load the full shell + native-feel.js + redirect-page.js just to bounce. **Recipe:** route them as 301s in the Cloudflare Worker (`scripts/cloudflare-worker.js` already has Layer 0c edge redirects per memory S127), delete the stub files, and add a `tests/redirects.spec.js` contract asserting each old path returns 301. |
| 2 | 🔥 | Organization · UX · Speed | 2h | 9 | 7 | 31.5 | **leaderboards-collapse** — 7 leaderboard sub-pages (`call-of-doodie`, `challenges`, `football-gm`, `global`, `recruiters`, `teams`, `weekly`) each ship 238 lines of identical shell + a thin category renderer. **Recipe:** consolidate into single `/leaderboards/` with tabbed UI driven by `?cat=` URL state (matches the `oracle-shareable-focus-state` pattern from S138). Add 301s for the 6 deleted sub-paths via Worker. Keep dedicated OG-image-per-cat. |
| 3 | 🔥 | UX · Engagement · Innovation | 3h | 10 | 9 | 45.0 | **showcase-spine** — visitor lands on home, sees hero + games + portfolio heartbeat. To get the studio story they have to traverse `/journal/`, `/studio-pulse/`, `/oracle/`. **Recipe:** introduce a `<section data-vs-spine>` on the homepage that fuses three live cards in one strip — *Latest Pulse* (newest journal dispatch headline + 1 sentence), *Studio Signal* (active project count + closeout count this week from `/api/heartbeat.json`), *Oracle Read* (top forecast from `/api/oracle/pulse.json`). One spine, three live signals, one CTA into the full Oracle. Cuts perceived fragmentation at the door. |
| 4 | ⚡ | Speed · Token cost (server) | 1h | 8 | 5 | 25.6 | **critical-css-dedup** — homepage ships TWO inline critical CSS blocks: `<style id="critical-css">` (~3KB) AND `<style data-vs-critical-shell>` (~4KB) with significant overlap. Most pages ship only the shell version. **Recipe:** keep `data-vs-critical-shell` as the canonical (it's used sitewide), strip the homepage-only `#critical-css` block; estimate ~3KB saved per pageview on the most-trafficked URL. Verify via `npm run perf:matrix`. |
| 5 | ⚡ | Speed · Security · Token cost | 45m | 8 | 6 | 32.0 | **gtag-defer-and-consent** — every page boots `https://www.googletagmanager.com/gtag/js` synchronously in `<head>` (despite `async` flag, the inline `gtag('config'...)` blocks). **Recipe:** wrap gtag init behind `requestIdleCallback` (fallback `setTimeout 1500`), move the script tag to end-of-body, gate via existing consent banner if present. Saves ~30-80ms TTI on cold cache. |
| 6 | ⚡ | UX · Organization | 1.5h | 7 | 6 | 22.4 | **legal-hub-merge** — `/cookies/`, `/privacy/`, `/data-deletion/`, `/accessibility/` are 4 separate shells with similar IA. **Recipe:** consolidate into single `/legal/` page with anchored sections + table of contents; keep the 4 legacy URLs as 301s (lands at the right anchor). One IA, one shell load, four anchors. |
| 7 | ⚡ | Engagement · UX · AI | 2h | 8 | 8 | 32.0 | **journal-archive-as-feed** — `/journal/` is a long landing page; the 11 individual posts each ship full shell + 400+ lines of identical chrome. Visitors don't browse 11 posts — they sample. **Recipe:** convert `/journal/` into an infinite-feed surface (first 3 posts inlined as `<details open>`, rest progressive-loaded from `journal.json`); keep canonical post URLs but they re-use a single template + JSON-driven body. Shrinks total journal HTML payload from ~290KB to ~80KB cold-cache. |
| 8 | 💡 | Token cost · Speed | 45m | 6 | 5 | 16.0 | **per-page-script-pruning** — leaderboard sub-shells load `analytics.js + pwa-nav.js + theme-toggle.js + native-feel.js + redirect-page.js` even when they're not redirect pages. **Recipe:** add a build-time `scripts/check-page-script-relevance.mjs` that flags pages loading `redirect-page.js` without a `<meta http-equiv="refresh">`; auto-strip via `propagate-nav.mjs`. Frees 1 RTT + ~2KB per non-redirect page. |
| 9 | 💡 | Feedback loop · UX | 1h | 6 | 7 | 22.4 | **showcase-pulse-prompt** — visitors who scroll the spine (item #3) but never click "Enter the Oracle" are signaling content gaps. **Recipe:** instrument the spine with a single anonymous "this answered my question / I want more depth" micro-feedback chip (writes to existing `/api/feedback` endpoint with `source=spine`). Closes the loop without yet another form. |
| 10 | 💡 | Security · Process | 30m | 7 | 4 | 38.7 | **s147-closeout-proof** — this sprint touches the Worker, deletes ~40 files, and changes the homepage spine. **Recipe:** run `npm run build:check`, `npm run verify:headers`, `npx playwright test tests/redirects.spec.js`, scan staged secrets, capture before/after `npm run perf:matrix` numbers in the closeout. |

## Innovation reserve (Innovation ≥ 8 — execute even when busy)

- `showcase-spine` (#3) — turns three fragmented signals into one studio story above the fold; no other indie studio site fuses live pulse + commit signal + AI forecast in a single strip.
- `journal-archive-as-feed` (#7) — replaces 11 separate page-shells with one progressive feed; preserves SEO via canonical URLs while collapsing perceived complexity.

## Skipped (Priority < 1.5 or scope-mismatched)

- **Full investor-portal SPA conversion** — high impact but auth-gated surface, separate session, not aligned with public-showcase mandate.
- **Mobile drawer re-skin** — already shipped S132/S135; no founder report of regression.
- **Per-game live status badges** — adjacent to #3 but the spine pattern subsumes it for less code.
- **Search consolidation** — `/search/` is already lean; not a redundancy hot-spot.

---
*Generated by `/audit` v1.0 · ready for `/implement`*

---

## Execution Log (S147 — /implement pass 1)

| Status | # | Slug | Note |
|---|---:|---|---|
| DONE | 1 | redirect-stub-purge | Worker Layer 0c extended with 39 edge 301s (root legacy slugs, `/investor/*`, full `/products/<slug>` tree, external canonicals). 39 stub HTML files + 14 empty directories deleted. `tests/redirects.spec.js` ships the contract. |
| DONE | 2 | leaderboards-collapse | 7 thin sub-shells (≈1,666 lines total) deleted; Worker 301s map old URLs to `/leaderboards/#<cat>` — main page already handles hash-anchor routing → correct tab pre-selected. Contract test extended. |
| BLOCKED | 3 | legal-hub-merge | Compliance risk: `/cookies/`, `/privacy/`, `/data-deletion/`, `/accessibility/` must remain at canonical URLs for app-store + GDPR review (Meta/Google explicitly require dedicated `/data-deletion/`). Reverted decision logged in DECISIONS.md. |
| DONE | 4 | gtag-defer-and-consent | New `scripts/defer-gtag.mjs` rewrote the eager gtag bootstrap to `requestIdleCallback`-deferred init across **82 pages**. 5 pattern-variant pages (404, offline, game.html, gridiron-gm-play, projects/seamline) left for a follow-up sweep; non-blocking. |
| DEFERRED | 5 | critical-css-dedup | Inspection showed the two style blocks are complementary, not duplicate — `#critical-css` carries light-mode token overrides + site-header above-fold styles that `data-vs-critical-shell` lacks. Removing it would cause light-mode FOUC. Original ~3KB savings estimate revised down to ~800B; not worth the cosmetic risk. |
| DONE | 6 | showcase-spine | New `<section id="studio-spine">` injected above the membership fold; three live cards (Latest Pulse · Studio Signal · Oracle Read) hydrated by `assets/showcase-spine.js` from existing `/api/public-intelligence.json` + `/api/heartbeat.json`. One canonical CTA into `/oracle/`. Cuts perceived fragmentation at the door. |
| DONE | 7 | showcase-pulse-prompt | Micro-feedback chip ("Was this enough? Yes · I want more depth") wired into the spine section; posts anonymous `{source:'spine',vote}` via `sendBeacon` to `/api/feedback`, falls back to localStorage + gtag event. |
| DEFERRED | 8 | per-page-script-pruning | Lower per-page ROI than #4; deferred for next session. |
| DEFERRED | 9 | journal-archive-as-feed | Larger surface change (11 posts × 470 lines); deferred to dedicated journal sprint. |
| DONE | 10 | s147-closeout-proof | `npm run build:check` passes — all 30+ gates green. All-page crawl: **98 HTML files** (down from 144 = **−46 pages**), 0 status failures, 0 blocking-script findings, all mobile + nav-orphan + image-format contracts satisfied. |

## Verification Evidence

- `npm run build:check` — passed end-to-end (ambient bundle ✓ · shell assets ✓ · CSP ✓ · JS budget ✓ · render contracts ✓ · portfolio coherence ✓ · mobile contracts ✓ · image formats ✓ · nav-orphan ✓ · orphan-page ✓ · 98-file crawl ✓).
- Page count delta: 144 → 98 (−46 files, −31%).
- Worker Layer 0c: 39 new redirects (+39) + 7 leaderboard redirects (+7) = 46 edge 301s added.
- New assets shipped: `tests/redirects.spec.js`, `scripts/defer-gtag.mjs`, `assets/showcase-spine.js`.
- Modified shell: `cloudflare/security-headers-worker.js`, `index.html`.
- gtag deferred on 82 pages — expected TTI improvement on cold cache.

