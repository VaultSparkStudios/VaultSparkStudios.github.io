# AUDIT — VaultSparkStudios.github.io · Session 222 (arc)

Generated: 2026-06-25 · Mode: BUILDER · SIL baseline 967/1000
Premise-verification: every item below was checked against LIVE code/CI before ranking. Phantoms are recorded as rejection-WINs, not silent skips (CANON-019 honesty).

## Headline finding

S221 got three CI workflows to *run* again (the `npm ci` → `npm install` root-fix). Running exposed **two latent test-code failures that now go red every single CI run** — and the studio had no local probe to notice (the "CI-failure blindness" gap). The arc's real work is rooting out those two reds and gating the blindness class.

A second, subtler finding: the prior closeout (D-S218.4) asserted "the live site uses **Studio Pulse** everywhere; *Forge Window* survives only in historical data." **That was incomplete** — the `/studio-pulse/` page H1 (the single most prominent on-page label) still literally reads "The Forge Window," and the smoke gate still *asserts* "Forge Window." `check-s151-contracts.mjs` couldn't catch it because it only checks `<title>` + nav-link labels, never the page body — the exact D-S208.1 anti-pattern ("a gate that strips the surface where the truth lives can't catch the lie").

---

## RANKED PLAN

### P0 — CI red, fails every run (real session work)

**A1 · Visual-Regression structural bug — `defaultBrowserType` in a describe group**
`tests/visual-regression.spec.js:47` spreads whole Playwright `devices[...]` configs into `test.use({ ...vp.config })` *inside* a `test.describe()`. Device configs carry `defaultBrowserType` (webkit for iPhone, chromium for Pixel); Playwright forbids setting it in a describe ("forces a new worker") → the suite errors at collection, every Visual Regression (mobile) run fails. Root-fix: strip `defaultBrowserType` (and `browserName`) from the per-describe `use()` spread; the CI project already pins chromium and snapshot names already encode `vp.name`.

**A2 · `/studio-pulse/` label migration — finish the S185 rename (honors binding D-S221.5)**
The E2E/smoke red is `✗ /studio-pulse/ · missing: "Forge Window"`. The binding decisions (D-S185 → D-S218.4 → **D-S221.5**) rule "Forge Window" a phantom; the product label is **"Studio Pulse"**. So the fix is NOT to propagate the phantom — it's to complete the rename the page body + smoke gate never received:
- `studio-pulse/index.html:287` H1 `The Forge<br>Window` → `Studio<br>Pulse`.
- `scripts/smoke-http.mjs:72` `contains: ['Forge Window']` → `['Studio Pulse']`.
This greens E2E *correctly*, makes D-S218.4's claim actually true, and removes the last user-facing "Forge Window" remnant.

### P1 — observability: gate the CI-blindness class

**A3 · Scheduled-workflow staleness beacon** (genius #NEXT-3, score 83 — verified unbuilt)
Record per-workflow last-conclusion in `api/ci-status.json`; add a read-only doctor probe that flags any *scheduled* workflow red for ≥2 consecutive runs. This is the gap that let A1 sit broken and (per S221 memory) og-images stay broken 3 months silently. Goes in `run-doctor.mjs`, NOT build:check (build:check is at 7986 chars, over the Windows cmd.exe safe threshold).

### P2 — defensive generalization

**A5 · Workflow cache-dependency lint generalization** (genius #NOW-1, score 96)
`check-workflow-install-consistency.mjs:72` only flags literal `cache: 'npm'`/`cache: npm`. Generalize to flag any `actions/setup-node` `cache:` value (yarn/pnpm) when no committed lockfile exists — keeps the class closed if the stack ever changes. In-place edit to an existing gate (no build:check length change).

### SECOND-ORDER INNOVATION

**SI-2 · Harden `check-s151-contracts.mjs` to scan the page body, not just title+nav**
The root reason A2's bug hid for 30+ sessions: the contract gate polices `<title>` and nav-link labels but never the `<h1>`/body of `/studio-pulse/`. Extend it to assert the page body contains no standalone "Forge Window" *label* (precise pattern, not the `.forge-*` CSS class names or "forge" verb/metaphor prose). Self-test must prove both detection AND non-false-positives on legitimate "forge" metaphor copy ("Live from the forge", "Get closer to the forge"). This closes the gate-gap that allowed the lie — the D-S208.1 "police where the truth lives" pattern applied to the exact hole just found.

---

## REJECTIONS — verified phantoms / already-shipped (recorded as WINs)

- **R1 · Forge Window naming propagation** (genius 86) — phantom per D-S221.5 (a standing decision). A2 moves the *opposite* direction (removes the last remnant). Re-confirmed.
- **R2 · orphan-lib allowlist-rot gate** (genius 87) — ALREADY shipped S221 (`check-orphan-libs.mjs:161` `// allowlist-rot (S221)` + `auditAllowlist()`). Done.
- **R3 · CANON_ADOPTION freshness in smoke runner** (genius 90) — ALREADY wired S221 (`smoke-startup-scripts.mjs:251`). Done.

## OUT-OF-SCOPE — sibling drift (Ark cargo only, never edit sibling trees)

- **Doctor: Compliance validation + Compliance velocity 32/36** — driven entirely by sibling repos (Hashmark, VOID, SHADOW, ATLAS: templates not at v3.3, TRUTH_AUDIT headers). `VaultSparkStudios.github.io` itself PASSES (✓). 
- **Doctor: Launch readiness — 2 SPARKED blockers** — VEILOS (liveUrl + Stripe). Sibling. This repo clean.
- Action: ship one Ark `pattern-share` cargo to studio-ops noting the cross-repo template/TRUTH_AUDIT drift cluster so the propagation lane can address it. No sibling-tree edits.

---

## Build order
A2 → SI-2 → A1 → A3 → A5 → build:check + smoke + doctor verify (exit codes direct) → Ark cargo → closeout.
