<!-- generated-by: render-audit-md.mjs (R-H14) — DERIVED FROM AUDIT_2026-07-28.json; do not hand-edit -->
<!-- generated-at: 2026-07-29 -->
<!-- project: vaultsparkstudios-website · type: app -->

# Project Audit — VaultSparkStudios.github.io

> One ranked improvement plan covering 9 axes. Top items first.
> Source of truth: this file is derived from the JSON sidecar. Edit the JSON, then re-render.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 3
- Combined Priority: 120.10
- Top 3: diagnostic-discovery-contract-registry · atomic-staging-deploy-attestation · protocol-propagation-truth-dossier
- Sampled: 9/443 files · 29964 tokens

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov | Priority | Status | Item |
|---|:-:|---|---|:-:|:-:|:-:|:-:|---|
| 1 | 🔥 | aiIntegration | 2h | 10 | 9 | 45.0 | · | **diagnostic-discovery-contract-registry** — Make agent discovery validate typed diagnostic receipts and explain every honest-dark omission. build-agents-json.mjs curates build-check and proof-surface diagnostics whenever their paths exist. A truncated, stale-plan, integrity-mutated, partial, or structurally malformed receipt is still advertised as authoritative verification, and agents.json has no channel for explaining why a feed was withheld. **Recipe:** Extract proof receipt validation into an import-safe library, add a small typed public-feed contract registry used by build-agents-json, validate complete build coverage and complete proof enforcement coverage, emit stable discovery.omissions entries for expected-but-invalid feeds, add malformed/partial/integrity-mutated fixtures, and verify generated agents.json plus the discovery spine. |
| 2 | 🔥 | speedOrganization | 3h | 10 | 9 | 38.8 | · | **atomic-staging-deploy-attestation** — Turn every Hetzner staging deploy into one atomic, content-bound, remotely verifiable attestation. deploy-staging.mjs prints a file count, archive size, and rollback path to transient stdout, then discards them. staging-health separately proves candidate SHA and critical-byte parity, but release-proof cannot prove which deploy created that state, which source fingerprint was archived, whether the remote file count matched the bounded manifest, or which rollback snapshot corresponds to the candidate. **Recipe:** Add a pure staging-deploy receipt contract with content ID and atomic JSON writes; make deploy-staging compute the source fingerprint and archive digest, record the safe manifest/file count plus rollback id, run a fresh parity probe after rsync, write the final receipt locally and atomically install it on staging, fetch it back for equality validation, fold it into release-proof and the evidence graph, and cover malformed roots, count mismatch, parity failure, replay, and secret-redaction fixtures. |
| 3 | ⚡ | speedOrganization | 30m | 8 | 6 | 36.3 | · | **protocol-propagation-truth-dossier** — Ship a reproducible Ark dossier for the protocol-currency false green. The local propagated docs/SESSION_PROTOCOL.md points /audit and /implement at sections that do not exist, while the authoritative studio-ops copy contains §2B and §2C. Startup applied pending propagation and Doctor still reported the propagated document current, so agent-neutral command fidelity can silently degrade while observability says green. **Recipe:** Ship a signed repo-question to studio-ops with local/canonical section inventories and exact acceptance tests: propagated copies include every canonical command heading, command-table targets resolve, apply-pending repairs the copy, and Doctor cannot report propagated-doc green on semantic drift. Request a receipt; do not edit the sibling or local canonical copy manually. |

## Depth Ladder (per item: L1 minimal · L2 solid · L3 genius)

> `/implement` selects the depth that fits remaining budget, and climbs the ladder
> on already-shipped items when the session still has budget (S175 saturation).

### diagnostic-discovery-contract-registry
- **L1 · minimal** _(~30m)_ — Validate the two diagnostic JSON shapes before inclusion.
- **L2 · solid** _(~1h)_ — Extract import-safe validators and add invalid fixtures.
- **L3 · genius** _(~2h)_ — Create a reusable typed feed registry, emit honest-dark omission reasons, cover integrity/coverage drift, and prove discovery output remains deterministic.

### atomic-staging-deploy-attestation
- **L1 · minimal** _(~45m)_ — Persist the current console values in a public-safe JSON receipt.
- **L2 · solid** _(~1.5h)_ — Bind candidate SHA/root/source fingerprint and consume the receipt from release proof.
- **L3 · genius** _(~3h)_ — Add atomic local/remote installation, archive digest, remote equality verification, replay-resistant identity, parity evidence, evidence-graph reachability, and full negative fixtures.

### protocol-propagation-truth-dossier
- **L1 · minimal** _(~12m)_ — Send the missing-heading evidence.
- **L2 · solid** _(~30m)_ — Include hashes, command-table target mismatch, and executable acceptance tests.
- **L3 · genius** _(~1h)_ — Add semantic propagation-coverage design, backward compatibility expectations, and a signed repair receipt request.

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

- receipt-chain-replay-resistance
- remote-deploy-receipt-self-verification
- typed-public-feed-contract-registry

## Execution Log

### diagnostic-discovery-contract-registry
- `2026-07-29T05:31:42.068Z` · **shipped-l3** · Extracted import-safe proof diagnostics and a typed public-feed contract registry. Agent discovery now advertises a diagnostic URL only when its complete coverage, current build plan, row counts, enforcement classes, and content receipt validate; otherwise discovery points are null and a public-safe omission reason is emitted. Focused contracts 20/20 plus proof 15/15 and discovery-spine green.

### atomic-staging-deploy-attestation
- `2026-07-29T05:31:42.068Z` · **shipped-l3-plus** · Deployer now SHA-256 verifies the transferred archive, measures the bounded remote file count, binds source fingerprint + candidate root + rollback snapshot + parity, atomically installs the receipt on staging twice (pending then verified), fetches exact bytes back, and fails on mismatch. The canonical served receipt attests 4,294 files, an exact SHA/root candidate, atomic rollback identity, and an append-only verified history; release proof and the 16-node evidence graph consume it.

### protocol-propagation-truth-dossier
- `2026-07-29T05:31:42.068Z` · **shipped-l3** · Shipped a signed semantic-currency dossier to studio-ops through Studio Ark as cargo 01JULCLFE32881AA71DA10278F, including local/canonical heading evidence and four executable acceptance tests. No sibling tree was edited.
