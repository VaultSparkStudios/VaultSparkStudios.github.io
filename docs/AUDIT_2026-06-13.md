<!-- generated-by: render-audit-md.mjs (R-H14) — DERIVED FROM AUDIT_2026-06-13.json; do not hand-edit -->
<!-- generated-at: 2026-06-13 -->
<!-- project: unknown · type: unknown -->

# Project Audit — Unknown

> One ranked improvement plan covering 9 axes. Top items first.
> Source of truth: this file is derived from the JSON sidecar. Edit the JSON, then re-render.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 13
- Combined Priority: 0.00
- Top 3: —

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov | Priority | Status | Item |
|---|:-:|---|---|:-:|:-:|:-:|:-:|---|
| 1 | transformative | ai | 3h | 9 | 8 | 31.0 | · | **ignis-conversational-thread** — Make IGNIS conversational without spending a cent — client-side multi-turn retrieval with follow-up intent. Ask IGNIS is a one-shot lookup today: it retrieves the top cluster from /data/ignis-search-index.json and forgets everything the moment it answers. A visitor who types 'tell me more' or 'what about the free tier' starts from zero — the single biggest gap between this and a product that feels intelligent. The genius constraint: it must stay cost-neutral under CANON-029, so the whole thing runs client-side over the index already shipped — no new edge-LLM calls, zero added API cost, and as a bonus it keeps the privacy win S194 just earned. **Recipe:** In ignis-answer-engine.js keep the last N (3) turns + the prior answer's top entities in memory; on a new query, detect follow-up shape ('more', pronouns, bare noun) and bias the retrieval toward the prior cluster; render 3 dynamic follow-up chips derived from the returned cluster's sibling docs. |
| 2 | transformative | ux | 4h | 8 | 9 | 27.9 | · | **forge-immersion-layer** — Give the hero real motion — a capability-gated ember/forge field that mounts AFTER LCP so it can never cost a millisecond of paint. The shell is CSS-and-SVG flat: rich gradients but no living motion, while the games embed their own canvases and leave the brand anchor looking static by comparison. The reason no one has added it is the LCP discipline this repo has bled over for a dozen sessions — so the move is a layer that mounts only after LCP fires, gated on prefers-reduced-motion, save-data, and deviceMemory, and self-throttles on FPS. Done right it reads as a forge that's actually lit, with zero regression to the perf budget that defines this site. **Recipe:** Add forge-immersion.js to the ambient-feature bundle; on requestIdleCallback after LCP, if !reduced-motion && !save-data && deviceMemory>=4, mount a 2D canvas ember field behind the hero with an FPS cap and an IntersectionObserver pause when scrolled away. |
| 3 | high | feature-depth | 3h | 8 | 8 | 27.6 | · | **studio-now-live-surface** — Make the studio's pulse visibly LIVE — a 'Studio Now' surface joining founder-presence + ship-receipts + heartbeat. Every feed on the site is a snapshot frozen at deploy, yet the raw material for a living signal is already published — founder-presence.json, ship-receipts.json, heartbeat.json. A visitor has no way to feel that this is an active studio building right now versus an abandoned landing page, and that felt-aliveness is the single strongest trust signal a one-founder studio can show. This is also the deepest cohesion win to Studio Ops: the site becomes a window onto the actual nervous system, not a brochure beside it. **Recipe:** Add a 'Studio Now' pill/strip fed by founder-presence.json (live/idle) + the most recent ship-receipts.json entry ('shipped X · 4h ago') + today's heartbeat pulse count; reuse the favicon-pulse leader-election poller so it costs one fetch per browser. |
| 4 | high | feedback-loop | 3h | 7 | 8 | 24.1 | · | **you-asked-we-shipped-loop** — Visibly close the feedback loop — link every shipped change back to the signal that asked for it. The site captures feedback (feedback-decision-board) and records ships (ship-receipts) but never draws the line between them in front of the visitor, so the loop is invisible exactly where it would build the most trust. A public 'you asked → we decided → we shipped' join turns the changelog from a list of changes into proof that this studio listens. It compounds with the now-live S194 funnel: the moment a requested thing ships, the asker has a reason to come back. **Recipe:** Join feedback-decision-board.json to ship-receipts.json by a shared tag/id; render a 'You asked → shipped' badge on matching changelog entries linking back to the originating feedback item. |
| 5 | high | ai | 3h | 7 | 8 | 24.1 | · | **command-palette-ask-and-act** — Turn Cmd+K from a nav menu into an ask-and-act surface — IGNIS answers inline next to commands. The command palette already indexes navigation, and IGNIS already answers questions — but they're two separate doors, so a power user has to know which one they want before they start typing. Fusing them means one keystroke handles both 'take me to membership' and 'what does membership cost', with the answer rendered inline. It's the kind of keyboard-first intelligence that signals a product built by people who live in keyboards, and it reuses the cost-neutral client retrieval from item 1. **Recipe:** In command-palette.js, detect question-shaped input (ends in ?, starts with what/how/why/can) and render the IGNIS top-answer inline in the results list above the command matches; share the retrieval module with ignis-conversational-thread. |
| 6 | high | gamification | 4h | 8 | 7 | 21.7 | · | **member-quest-progression** — Give ranks teeth — turn the thin /ranks/ and /pathways/ pages into real progression with quests and badges. The four-tier rank system (Vaulted→Forge→Sparked→Ignited) is named everywhere but /ranks/ (472 lines) and /pathways/ (194 lines) barely render more than the labels, so a free member has no visible reason to climb. A progress bar plus a short quest chain converts an abstract tier into a goal the member can see closing, which is the single most reliable retention mechanic a free community has. It also feeds the very funnel S194 just re-lit — every quest step is a conversion event worth measuring. **Recipe:** On /ranks/, render a progress bar to the next tier ('3 actions to Forge') from the member's Supabase profile; add a 3-step quest checklist (join → react → ask IGNIS) persisted per member. |
| 7 | high | ux | 3h | 6 | 7 | 18.1 | · | **theme-as-rank-identity** — Make the theme system mean something — unlock ambient themes by rank so personalization signals progress. The ambient theme engine (dark/lava/cool/warm/high-contrast) is genuinely sophisticated but currently decorative — a setting nobody is rewarded for finding. Tying one or two premium themes to rank turns a cosmetic toggle into a visible badge of belonging that members carry on every page they load. It's near-zero marginal cost (the themes already exist) and it makes the gamification of item 6 felt, not just tracked. **Recipe:** Persist theme choice already; gate 1-2 existing themes (e.g. 'lava') behind a rank check via body[data-vs-rank]; show a subtle 'unlocked at Forge' hint on locked themes in the picker. |
| 8 | high | security | 3h | 6 | 7 | 18.1 | · | **obelisk-trust-passport** — Make the Obelisk passport a live trust proof — render real security posture instead of a placeholder. The /obelisk-passport/ page exists as a brand gesture but doesn't yet show anything a skeptic could verify, while the site quietly runs a genuinely strong stack (nonce CSP, Worker headers, supply-chain scanning, uptime). Surfacing security-posture.json + the headers verdict turns an empty promise into a public proof surface — exactly the 'show, don't claim' posture CANON-021 demands ('post-quantum migration-ready', never 'unhackable'). It advances the project's Obelisk adoption phase while giving every visitor a reason to trust the auth flow. **Recipe:** On /obelisk-passport/, fetch security-posture.json + verify-headers output and render a live posture card (CSP strict ✓, headers ✓, supply-chain clean ✓, uptime %); link the signed-ish status-proof manifest. |
| 9 | high | ux | 3h | 7 | 6 | 18.1 | · | **first-visit-onboarding-arc** — Orient first-time visitors in 30 seconds — a skippable arc pointing at membership, games, and IGNIS. S194 re-lit a conversion funnel that had been dead for eight sessions, but a re-instrumented funnel still needs something to convert — and a first-time visitor lands on a 14-section narrative with no guided path. A short, dismissible coachmark arc (membership → games → ask IGNIS) gives the funnel a spine to measure and the visitor a reason to take the first action. It directly feeds the now-live funnel events, so impact is measurable the day it ships. **Recipe:** Extend ignis-tour.js into a 3-step first-visit arc (localStorage-gated to once); each step emits a funnel: event so the arc's own conversion is measurable. |
| 10 | high | ux | 2h | 7 | 5 | 17.5 | · | **nav-sheet-graduate** — Graduate the bottom-sheet mobile nav from flag to default — it has been ?nav=sheet-gated and yellow since S178. The mobile bottom-sheet nav is built, shipped behind ?nav=sheet / localStorage, and has sat at 'yellow' in PROJECT_STATUS for sixteen sessions waiting on real-device confirmation — meaning every mobile visitor still gets the older drawer while a better surface gathers dust. Graduating it (after an iPhone + Android pass) is pure realized value from work already done. Keep the classic drawer as a one-line kill-switch so the flip is reversible forever. **Recipe:** Founder verifies on a real iPhone + Android; then flip nav-sheet.js default-on for <=768px viewports with a localStorage kill-switch ('vs-nav-style=classic') retained as fallback. |
| 11 | nice-to-have | speed | 2h | 6 | 5 | 15.0 | · | **field-inp-budget** — Extend the field perf budget from LCP-only to INP — measure the interaction lag the RUM gate is blind to. The RUM perf budget guards LCP (load), but Core Web Vitals graded INP (interaction responsiveness) since 2024 and the site has no field signal for it — a long task in the ambient-feature bundle could be hurting the score invisibly. Capturing INP in the existing /v/rum beacon costs one added field and turns a blind spot into a gated number. It's the natural next rung on the perf-budget ladder the repo has been climbing since S153. **Recipe:** Add INP capture (PerformanceObserver 'event' / web-vitals onINP) to the /v/rum beacon; surface it in rollup-rum and add an INP threshold to check-perf-budget (warn-only until >=50 samples, per the LCP precedent). |
| 12 | nice-to-have | seo-branding | 4h | 6 | 6 | 13.9 | · | **og-per-title-rasterizer** — Give every game its own share card — the per-title PNG rasterizer S194 flagged as the OG L3 carry. S194 fixed the blank-SVG share-card bug by repointing to a single PNG, but every page still shares the same generic card, so a posted game link looks identical to a posted legal page. A build-time per-title rasterizer gives each game and major page a bespoke card with its own title and art — the difference between a link that gets clicked and one that gets scrolled past. It's the explicit next rung named in the S194 nextMilestone. **Recipe:** Add a build step using satori + resvg to render per-title 1200x630 PNGs from an HTML template (title + tagline + sigil); wire check-og-images to require a per-title card for SPARKED games. |
| 13 | nice-to-have | seo-branding | 2h | 6 | 4 | 12.0 | · | **sitewide-jsonld-completion** — Finish the structured-data coverage — BreadcrumbList on the ~57 pages missing it + an Article schema + a coverage gate. Structured data is rich here (FAQPage, VideoGame, BlogPosting, Organization) but uneven: BreadcrumbList lands on only 57 of 114 pages, so half the site forfeits breadcrumb rich-results in search. Closing the gap with the existing propagate tooling plus Article schema on journal/dispatch entries is cheap SEO yield, and a coverage gate stops the drift from ever reopening. Low innovation, but it's a clean compounding branding win. **Recipe:** Run the nav/breadcrumb propagate script across the 57 missing pages to inject BreadcrumbList; add Article/BlogPosting to journal + dispatch entries; add check-jsonld-coverage to build:check. |

## Depth Ladder (per item: L1 minimal · L2 solid · L3 genius)

> `/implement` selects the depth that fits remaining budget, and climbs the ladder
> on already-shipped items when the session still has budget (S175 saturation).

### ignis-conversational-thread
- **L1 · minimal** _(~30m)_ — Render 3 dynamic follow-up chips from the returned cluster's sibling docs (no memory yet) — instantly makes a one-shot answer feel like a thread.
- **L2 · solid** _(~3h)_ — Add a client-side 3-turn context window + follow-up intent detection (more / pronoun / bare-noun) that biases retrieval toward the prior cluster; persist nothing server-side.
- **L3 · genius** _(~8h)_ — Add a lightweight intent classifier + answer-synthesis that stitches 2-3 cluster docs into one narrative answer with inline citations — still 100% client-side, still zero API cost.

### forge-immersion-layer
- **L1 · minimal** _(~1h)_ — Pure CSS/SVG ember particle field behind the hero (no canvas) — drifting gold motes on a keyframe, reduced-motion disables.
- **L2 · solid** _(~4h)_ — Lazy 2D-canvas ember/forge-spark system mounted post-LCP via requestIdleCallback, FPS-capped, IntersectionObserver-paused, capability-gated.
- **L3 · genius** _(~8h)_ — A <30KB WebGL heat-haze shader (gold forge glow) gated on GPU tier + interaction-only, with a static SVG fallback for every excluded device.

### studio-now-live-surface
- **L1 · minimal** _(~30m)_ — A header pill: 'Studio live · last shipped 4h ago' from founder-presence + ship-receipts top entry.
- **L2 · solid** _(~3h)_ — A dedicated 'Studio Now' section animating recent pulses + current focus + live/idle state, polled once-per-browser via the existing BroadcastChannel leader.
- **L3 · genius** _(~8h)_ — Near-real-time stream via a Worker SSE/edge-poll endpoint with a 'LIVE' badge and an activity waterfall of the last 10 studio events.

### you-asked-we-shipped-loop
- **L1 · minimal** _(~1h)_ — Add a 'You asked for this' badge on changelog entries that carry a feedback-origin id.
- **L2 · solid** _(~3h)_ — A decision-board view that threads feedback → decision → ship-receipt as one timeline per request.
- **L3 · genius** _(~8h)_ — Opt-in: notify voters via Brevo when their requested item ships, closing the loop all the way to the inbox.

### command-palette-ask-and-act
- **L1 · minimal** _(~1h)_ — Add an 'Ask IGNIS: <query>' result row that deep-links to /ignis/ with the query prefilled.
- **L2 · solid** _(~3h)_ — Render the IGNIS top-answer inline in the palette for question-shaped queries, reusing the item-1 retrieval module.
- **L3 · genius** _(~6h)_ — Add agentic shortcuts (join, share, switch theme, jump-to-game) with fuzzy intent ranking so the palette can act, not just answer.

### member-quest-progression
- **L1 · minimal** _(~1h)_ — Progress bar to next rank + 'X actions to <tier>' on /ranks/ from existing member score.
- **L2 · solid** _(~4h)_ — Achievement badge set + a persisted quest checklist on the member profile, surfaced on /ranks/ and /vault-member/.
- **L3 · genius** _(~8h)_ — A seasonal challenge ladder tied into /leaderboards/ with shareable badge cards (feeds OG item 12).

### theme-as-rank-identity
- **L1 · minimal** _(~1h)_ — Persist theme choice across sessions + a 'theme unlocked at rank X' hint in the picker.
- **L2 · solid** _(~3h)_ — Gate 1-2 premium themes behind rank/membership via a server-trusted flag (CANON-029 server-side gate).
- **L3 · genius** _(~6h)_ — Seasonal/event themes + a theme gallery with live preview and a shareable 'my vault look' card.

### obelisk-trust-passport
- **L1 · minimal** _(~1h)_ — Render live security-posture.json + headers verdict as a posture card on the passport page.
- **L2 · solid** _(~3h)_ — Add supply-chain scan result + uptime + a link to the status-proof manifest, with honest-dark states while pending.
- **L3 · genius** _(~6h)_ — Per-visit 'your connection' verify + Obelisk phase badge + a shareable trust card.

### first-visit-onboarding-arc
- **L1 · minimal** _(~1h)_ — A dismissible first-visit spotlight on the primary hero CTA + the Ask-IGNIS box.
- **L2 · solid** _(~3h)_ — A 3-step coachmark arc (membership → games → IGNIS) with a funnel: event per step.
- **L3 · genius** _(~6h)_ — Adaptive arc that branches on referrer/source bucket from the S194 acquisition-source breakdown.

### nav-sheet-graduate
- **L1 · minimal** _(~1h)_ — Real-device verify (iPhone + Android), fix any safe-area/tap issues found.
- **L2 · solid** _(~2h)_ — Flip default-on for <=768px with a localStorage kill-switch; classic drawer stays as fallback.
- **L3 · genius** _(~4h)_ — Add haptic-style micro-feedback + safe-area-inset polish + edge-swipe-to-sheet handoff.

### field-inp-budget
- **L1 · minimal** _(~1h)_ — Capture INP in the /v/rum beacon and surface it in the rollup summary.
- **L2 · solid** _(~2h)_ — Add an INP threshold to check-perf-budget (warn-only until field samples accrue) + identify the worst interaction.
- **L3 · genius** _(~4h)_ — Yield-to-main (scheduler.yield / setTimeout-chunk) the worst handler in the ambient-feature bundle.

### og-per-title-rasterizer
- **L1 · minimal** _(~1h)_ — Hand-make bespoke PNG cards for the 3 SPARKED titles + the homepage.
- **L2 · solid** _(~4h)_ — Build-time satori/resvg rasterizer generating per-title cards from one template.
- **L3 · genius** _(~8h)_ — A Worker dynamic-OG endpoint rendering per-URL cards on demand with edge cache.

### sitewide-jsonld-completion
- **L1 · minimal** _(~1h)_ — Inject BreadcrumbList into the ~57 missing pages via the propagate script.
- **L2 · solid** _(~2h)_ — Add Article schema to journal/dispatch + a check-jsonld-coverage gate in build:check.
- **L3 · genius** _(~4h)_ — Add speakable + sitewide rich-result validation (schema lint) in CI.

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

- forge-immersion-layer
- ignis-conversational-thread
- studio-now-live-surface
- you-asked-we-shipped-loop
- command-palette-ask-and-act

## Skipped (Priority < 1.5)

- status-proof-seed-rot-fix — FALSE PREMISE (verified-reject). The Explore pass flagged status-proof.json health metrics as hand-seeded rot, but S192 already shipped build-security-posture.mjs + check-proof-feed-generators.mjs which derive the feed and GATE against hand-seeds. Fixing it would re-introduce the very pattern the gate forbids.
- paid-llm-ignis-chat — CANON-029 violation. A studio-paid conversational LLM on the free public surface is forbidden pre-revenue. The cost-neutral client-side version (item 1) delivers the same felt experience with zero API cost.

