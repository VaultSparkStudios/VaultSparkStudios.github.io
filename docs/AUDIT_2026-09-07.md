<!-- generated-by: render-audit-md.mjs (R-H14) — DERIVED FROM AUDIT_2026-09-07.json; do not hand-edit -->
<!-- generated-at: 2026-09-07 -->
<!-- project: vaultsparkstudios-website · type: website -->

# Project Audit — VaultSparkStudios.github.io

> One ranked improvement plan covering 9 axes. Top items first.
> Source of truth: this file is derived from the JSON sidecar. Edit the JSON, then re-render.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 5
- Combined Priority: 150.60
- Top 3: skipped-step-guards-read-as-success · homepage-publishes-ci-jargon-to-returning-visitors · member-newsletter-has-never-sent
- Sampled: undefined/undefined files · undefined tokens

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov | Priority | Status | Item |
|---|:-:|---|---|:-:|:-:|:-:|:-:|---|
| 1 | 🔥 | security | 2h | 10 | 9 | 45.0 | ✓ | **skipped-step-guards-read-as-success** — Every downstream guard in the Desk publisher passes when its upstream step was SKIPPED - an unattended commit path is held shut only by two accidents. PRE-VERIFIED IN RUN 34063581495. news-publish.yml chains its steps with `if: steps.<X>.outputs.status == '0'`. A skipped step writes no output, so the expression compares the empty string to '0' and GitHub Actions evaluates that TRUE. Observed, not theorised: in that run `prepare` exited 1 and `author` was correctly skipped - and then `generate-news-art`, `Rebuild the Desk and render pages`, `Desk gates - the decision surface`, and `Cascade the derived public feeds` ALL RAN, four guards deep, on a slot that had drafted nothing. author-news-edition.mjs never appears in the log; its dependents ran anyway. The guard cannot tell 'succeeded' from 'never ran'. Two accidents - not one gate - are what stopped this run from reaching `git commit` and publish-push.sh: an unhandled ENOENT crash in generate-news-art.mjs (item 2) and the cadence gate failing one step earlier. Both look exactly like bugs a maintainer would fix in isolation, and fixing EITHER one alone opens an unattended publish of a non-edition to production. Outcome for a reader: the public Desk can be rebuilt, gated and cascaded - and on the next such slot, committed - from an empty draft directory. **Recipe:** Stop comparing a step output to a numeric literal, because an empty string impersonates one. Have each tolerant step emit a non-numeric sentinel (ok=yes / ok=no) alongside the existing status, and guard every dependent on steps.<X>.outputs.ok == 'yes' - a value the empty string cannot coerce into. Keep status for the slot summary. Then make the class unrepeatable: add a gate that parses .github/workflows/*.yml and fails on any `if:` that compares a steps.*.outputs.* to a numeric literal, with a negative control proving it still catches a reintroduced == '0'. |
| 2 | 🔥 | devHealth | 30m | 7 | 4 | 21.0 | ✓ | **news-art-crashes-on-the-normal-dropped-slot** — generate-news-art.mjs throws an unhandled ENOENT on a dropped slot - the normal outcome, printed as a stack trace. PRE-VERIFIED. scripts/generate-news-art.mjs:90 calls fs.readdirSync(DRAFT_DIR) unguarded. When a slot drafts nothing, .cache/news-drafts does not exist and the script dies with `ENOENT: no such file or directory, scandir .../.cache/news-drafts` plus a six-line stack - visible in run 34063581495. A dropped slot is a documented normal state ('A rejected or unauthorable edition is dropped for that slot'), so the publisher's most common non-publishing outcome is reported as a crash. That noise is what hides a REAL art failure. Critically, this defect is currently load-bearing: per item 1 it is one of only two things preventing an unattended commit of a non-edition, so it MUST be fixed in the same change as item 1 and never on its own. **Recipe:** Treat an absent draft directory as the no-drafts case the code already handles one line later: if DRAFT_DIR does not exist, take the same path as `no drafts for <date>`. Keep a genuine art/pixel-inspection failure loud. Ship together with item 1. |
| 3 | 🔥 | engagement | 2h | 9 | 5 | 28.1 | ⛔ | **member-newsletter-has-never-sent** — The Monthly Member Newsletter has never sent - the function is undeployed and the Action's bearer token is empty. PRE-VERIFIED, TWO INDEPENDENT FAULTS. Run 33618640518 (2026-09-02) and the two before it are identical: `HTTP status: 404`, `{"code":"NOT_FOUND","message":"Requested function was not found"}`. supabase/functions/send-member-newsletter/index.ts EXISTS in this repo - it was written and never deployed. Separately, the run log renders the header as `-H "Authorization: Bearer "` with nothing after it, while the URL is masked as ***: the secret NEWSLETTER_SECRET is absent, confirmed against `gh secret list` (DIGEST_SECRET is present; NEWSLETTER_SECRET is not) and the function reads it at index.ts:20. So even once deployed the call would be rejected. Six consecutive monthly failures. Outcome for a reader: a Vault Member who joined for the studio's monthly dispatch has never received one, against SOUL non-negotiable 2 ('membership must feel real - never ship half-baked portal features'). **Recipe:** Fix both faults or neither helps. Deploy the function via the Supabase management API using SUPABASE_ACCESS_TOKEN (the project-scoped agent deploy path; note the service-role slot is the known-mismatched one from D-S344.1 and is NOT the credential to use here). Mint a NEWSLETTER_SECRET, set it on both sides - `gh secret set NEWSLETTER_SECRET` and the function's env - then prove it end to end with a real workflow_dispatch, reading the response body rather than the workflow conclusion. If either half cannot be completed by an agent path, say which and leave the cron honestly red rather than green. |
| 4 | ⚡ | ux | 2h | 7 | 8 | 35.0 | ✓ | **homepage-publishes-ci-jargon-to-returning-visitors** — The first sentence a returning visitor reads under the studio name is CI jargon about our own build race. PRE-VERIFIED ON THE LIVE FEED. api/changelog-narrative.json carries "Refined resync after publisher race (attempt 1)." three times, and assets/returning-signal-strip.js renders that feed on the homepage for every visitor with vs_visit_count >= 2. scripts/build-changelog-narrative.mjs has a jargon strip, but it is TOKEN-level: STRIP_RE removes session markers (S340, SIL 981, 981/1000) and MOVE_VERB drops chore/test. This sentence passes both - its move is `Tended` (mapped to `Refined`) and its summary contains no session token. The filter has a notion of internal VOCABULARY and no notion of internal SUBJECT MATTER, so a commit that changed nothing a visitor can see still earns a public sentence. The leaked retry counter `(attempt 1)` is the second tell. Outcome for a reader: a returning visitor's welcome-back line is a status report on our CI plumbing. **Recipe:** Classify structurally, not by blocklist - a word list would need a new entry for every future leak. A commit that touches no visitor-facing path has no public sentence, whatever its move type. Compute that once in the producer (build-commit-map.mjs writes a per-entry surface classification from the commit's changed paths) and filter on it in build-changelog-narrative.mjs. Ship BOTH ends in the same change: a reader keyed on a field its producer never writes silently publishes everything. Since the map is regenerated wholesale from git each run, an absent field means the producer did not run - so the reader must treat it as not-publishable rather than defaulting open. Cover the leaked retry counter in the token strip as well, and pin this exact sentence in the self-test. |
| 5 | ⚡ | featureDepth | 3h | 8 | 6 | 21.5 | ↷ | **desk-queue-starves-on-a-single-blocked-host** — The Desk has published nothing since 2026-09-04 because a 14-day novelty hold plus one 403 empties the queue. PRE-VERIFIED IN THE RUN LOG. `novelty: 13 story/stories published in the last 14 days are held against the queue`, then `skipped research-acceleration-the-view-inside-openai - no readable source: HTTP 403`, then `no draftable topic yielded a readable source after 1 attempt(s) - refusing to draft from nothing`. The attempt budget was not exhausted - only ONE candidate survived the novelty hold, and its single source was blocked. The refusal itself is correct behaviour (drafting from nothing is the worse outcome), and the cadence gate reporting this as red is honest. But the queue is one blocked host wide, so the public cadence has degraded to `periodic` and the site's daily promise is unmet for a third day. Outcome for a reader: The Desk, the studio's public editorial surface, is visibly stale. **Recipe:** Widen the eligible pool rather than loosening the refusal. Diagnose whether the radar is queueing too few topics or the novelty window is too wide for the current intake rate, and fix the binding one - do not lower the novelty bar to manufacture a publish. Verify by a real dispatch that drafts from a source that is actually readable. |

## Depth Ladder (per item: L1 minimal · L2 solid · L3 genius)

> `/implement` selects the depth that fits remaining budget, and climbs the ladder
> on already-shipped items when the session still has budget (S175 saturation).

### skipped-step-guards-read-as-success
- **L1 · minimal** _(~30m)_ — Switch the five guards in news-publish.yml to the ok sentinel.
- **L2 · solid** _(~1.5h)_ — Also ship the structural gate over every workflow, wired into build:check.
- **L3 · genius** _(~3h)_ — Audit every other workflow for the same coercion and fix what it finds.

### news-art-crashes-on-the-normal-dropped-slot
- **L1 · minimal** _(~15m)_ — Guard the readdirSync.
- **L2 · solid** _(~30m)_ — Distinguish no-drafts (benign) from art-failed (loud) in the exit code, and assert it in the self-test.
- **L3 · genius** _(~1h)_ — Sweep every scheduled script for unguarded readdirSync on a cache path.

### member-newsletter-has-never-sent
- **L1 · minimal** _(~30m)_ — Deploy the function; confirm the 404 becomes a 401.
- **L2 · solid** _(~2h)_ — Also set the secret on both sides and prove a 200 with a real dispatch.
- **L3 · genius** _(~4h)_ — Add a monthly canary that verifies the endpoint answers before the send window, so the next breakage surfaces in hours not a month.

### homepage-publishes-ci-jargon-to-returning-visitors
- **L1 · minimal** _(~30m)_ — Strip (attempt N) and drop Tended from the public verb map.
- **L2 · solid** _(~2h)_ — Ship the structural visitor-facing classification across producer and reader, with the live sentence pinned as a regression test.
- **L3 · genius** _(~4h)_ — Apply the same classification to every public narrative feed, not just the changelog.

### desk-queue-starves-on-a-single-blocked-host
- **L1 · minimal** _(~1h)_ — Measure the queue: how many topics survive novelty, and how many have a readable source.
- **L2 · solid** _(~3h)_ — Fix the binding constraint and prove a real edition publishes.
- **L3 · genius** _(~6h)_ — Make queue depth a published health metric so starvation is visible before the cadence breaks.

## Skipped (Priority < 1.5)

- cadence-gate-fires-false-red-on-the-first-slot — DISPROVED before inclusion, and the disproof is the point. The candidate was that build-news-freshness.mjs --check --require-daily runs on all four slots and must fail on the 06:00 slot of every healthy day, because today's edition does not exist yet. Reading scripts/build-news-freshness.mjs shows deriveDeskFreshness treats a ONE-DAY-OLD edition as state `daily` ('one day old remains inside the daily evidence window', line 22), so the morning slot passes on any day whose predecessor published. The gate is correctly shaped and its four reds are TRUE reds. Had this shipped, it would have weakened the one alarm honestly reporting item 5 - the S325 property that stops a no-op publisher reporting green for nine days. Left untouched deliberately.

## Execution Log

### skipped-step-guards-read-as-success
- `2026-09-07` · **?** · Shipped S344. 7 guards moved to a non-numeric ok=yes/no sentinel; check-workflow-step-guards.mjs added (10/10 self-test, wired into build:check, 29 workflows clean) and run against the pre-fix file as a negative control — caught all 7 historical guards.

### news-art-crashes-on-the-normal-dropped-slot
- `2026-09-07` · **?** · Shipped S344 in the SAME change as item 1, never alone. readdirSync guarded; exit code deliberately unchanged so promotion stays gated.

### member-newsletter-has-never-sent
- `2026-09-07` · **?** · BLOCKED S344 — not by a credential. Tooling shipped and self-tested (scripts/deploy-member-newsletter.mjs, 5/5), both faults confirmed live against a project-scoped 200. The deploy/secret calls were refused by the sandbox permission classifier; escalated to the founder rather than worked around.

### homepage-publishes-ci-jargon-to-returning-visitors
- `2026-09-07` · **?** · Shipped S344. Found a second, deeper bug than the audit predicted: the chore:null filter was dead code (13/24 live sentences were chore commits) because its own self-test used a fixture shape the producer never emits. Precedence fixed + structural visitorFacing classification across producer and reader + (attempt N) stripped + the live sentence pinned. Feed 24 -> 8.

### desk-queue-starves-on-a-single-blocked-host
- `2026-09-07` · **?** · DEFERRED S344 with the measurement attached: radar queues 4 vs 211 rejected; 4 slots/day x 14-day novelty needs ~56 stories. Retuning cadence or the novelty window changes a published promise (AGENTS.md -> escalate), so boarded for the founder rather than decided by a gate.

---
*Every premise below was verified against a live run log or live code before inclusion. One candidate was generated and then DISPROVED - see skipped[0] - which is why the cadence gate is untouched by this plan.*
