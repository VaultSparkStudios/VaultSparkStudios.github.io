<!-- generated-by: /audit skill v1.0 (S185 goal-chain — merged) -->
<!-- generated-at: 2026-06-10 -->
<!-- project: vaultsparkstudios-github-io · type: website -->

# Project Audit — VaultSparkStudios.github.io

> One ranked improvement plan covering 9 axes. Top items first.
> S185 items are new this session. S184 items (all SHIPPED) are preserved at the bottom.
> Run `/implement` to execute S185 items in optimal order.

## Top-line (S185)

- Total new items: 12
- Combined Priority: 502.5 (axis-weighted: UX×2.5, Speed×2.0, FeatureDepth×1.5)
- Top 3: `oracle-proactive-contextual` · `returning-visitor-achievement-nudge` · `vault-kinesis-ship-pulse`
- Theme: S184 closed the 'built but unproven' gap. S185 asks: what converts visitors into members? The site has 64 views/7d, a confirmed −46% LCP win, a working Oracle, and 0 organic Oracle queries. Three conversion levers are fully agent-buildable today.

## Ranked Plan — S185

| # | Tier | Axis | Effort | Impact | Innov | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | 🔥 | ux | 4h | 8 | 9 | 69.8 | **oracle-proactive-contextual** — Surface a dismissible IGNIS prompt after 20s dwell on game/project cards. Passive readers convert to Oracle question-askers (detectable via palette telemetry). Zero extra API cost; reuses the existing edge fn. **Recipe:** Add `data-ignis-hint` to `.game-card`+`.project-card` during `propagate-nav.mjs`; in `ignis-answer-engine.js` add IntersectionObserver + 20s timer → inject `.vs-ignis-hint` strip with pre-seeded query via named TT policy. |
| 2 | 🔥 | ux | 2h | 7 | 7 | 61.3 | **returning-visitor-achievement-nudge** — Extend `returning-visitor-digest.js` to append a membership ask on 3rd+ visit. 3rd+ visit renders the nudge; /membership/ CTA click is measurable via RUM. **Recipe:** After `visitCount > 2` branch, append a 'membership nudge' row with entry rank + one concrete benefit from `api/public-intelligence.json`. Guard with `body:not([data-vs-signed-in])`. |
| 3 | 🔥 | ux | 4h | 7 | 9 | 61.0 | **vault-kinesis-ship-pulse** — Animated SVG waveform on /studio-pulse/ that peaks at each ship event in the last 30 days. Scroll depth + dwell increase; anonymous visitors who see active ship activity convert at higher rate. **Recipe:** New `assets/vault-kinesis.js`: load `api/commit-map.json` + `api/ship-receipts.json`; build 30-bucket daily timeline; render SVG waveform; mount via ambient-loader on `/studio-pulse/` predicate. Respects `prefers-reduced-motion`. |
| 4 | 🔥 | gamification | 8h | 8 | 8 | 48.2 | **progressive-membership-unlock** — 5-stage localStorage progression: anonymous → Shadow Member → Intelligence clearance (3 queries) → locked benefit preview (return visit) → signup with rank preview. Anonymous visitors at stage 3 click /membership/ at measurably higher rate. **Recipe:** New `assets/vault-progression.js`: manage `vs_progression_stage` (0–4); advance on behavioral triggers; render ambient badge in nav. All cosmetic/aspirational — no membership features until auth (SOUL #2 compliance). |
| 5 | ⚡ | speed | 1h | 6 | 6 | 45.6 | **command-palette-query-cache** — localStorage 15-min cache keyed by normalized query hash in `command-palette.js aiSearch()`. Repeat Oracle queries return <50ms vs ~800ms; encourages multi-query exploration and reduces Supabase edge fn token spend. **Recipe:** `hash = btoa(query.toLowerCase().trim()).slice(0,16)`; check `vs_ignis_q_<hash>` in localStorage; return stale+indicator on hit; fetch+cache on miss. |
| 6 | ⚡ | ux | 1h | 5 | 5 | 39.6 | **forge-window-naming** — Propagate 'Studio Pulse' vocabulary sitewide; add a grep lint gate to prevent regression. Vocabulary coherence reduces exit-from-confusion. **Recipe:** `grep -rn 'Forge Window' --include='*.html'`; update `propagate-nav.mjs` copy; add `check-vocabulary-consistency.mjs` gate (fail on match) to `build:check`. |
| 7 | ⚡ | ecosystem | 30m | 8 | 6 | 36.4 | **ark-deploy-strand-pattern-share** — Ship the [skip ci] tip strand fix as Ark pattern-share to all CF-Pages sibling repos. Fleet-wide prevention of a confirmed silent-deploy failure class. **Recipe:** `node scripts/ark.mjs ship --type pattern-share --to '*' --payload '{...}'` |
| 8 | ⚡ | speed | 2h | 5 | 7 | 35.0 | **geo-vitals-sample-accelerator** — Extend `probe-uptime.mjs` to measure TTFB from a EU Cloudflare colo; write synthetic EU bucket to `api/geo-vitals.json`. Non-US LCP verdict either confirms global win or surfaces a CDN gap. **Recipe:** Add EU colo probe leg; write `syntheticEuTtfbP75` separately from real-user data; clear labeling as synthetic. |
| 9 | ⚡ | featureDepth | 1h | 5 | 7 | 33.2 | **status-proof-in-agents-json** — Add `statusProof: "/api/status-proof.json"` to agents.json discovery block; annotate llms.txt. AI agents discover the trust manifest without scraping /status/. **Recipe:** `scripts/build-agents-json.mjs` + llms.txt template update. |
| 10 | 🔥 | security | 4h | 9 | 7 | 24.4 | **tt-named-policy-wave** — Migrate 4 first-party TT sinks (journal/dispatches:364, home-idle-loader.js:16, schema-injector.js:23, ambient.shell:337/367) to named TT policies; add lint gate; ship football-gm sinks as Ark cargo. Violations 148/30d → near-zero; enforce-flip becomes viable. **Recipe:** `grep -n 'innerHTML\|insertAdjacentHTML' ...` → wrap each with `trustedTypes.createPolicy('vs-[name]-policy',{createHTML:s=>s}).createHTML(...)`; add `check-tt-named-policy.mjs` gate to `build:check`. |
| 11 | ⚡ | speed | 2h | 6 | 4 | 24.0 | **ambient-split-wave4** — Move 4 split-candidates from `.cache/ambient-ledger.json` to predicate loading. Feature bundle 52KB→~42KB; cold-cache FCP improves for first-time visitors not hitting a split-candidate route. **Recipe:** For each ledger `split-candidate`: add predicate to `ambient-loader.js`; move from unconditional list; run `check-ambient-placement.mjs` + `build:check`. Shell-rotate if needed. |
| 12 | ⚡ | ai | 2h | 6 | 8 | 24.0 | **oracle-query-learning-loop** — New `scripts/build-oracle-query-clusters.mjs`: cluster `api/oracle-queries.json` into 5 intent categories; write `api/oracle-insights.json`; `build-ignis-search-index.mjs` consumes as ranking-weight overlay. Oracle answers improve for real visitor question patterns over time. |

## Innovation reserve (Innovation ≥ 8 — execute even when busy)

- `oracle-proactive-contextual` (9) — dwell-triggered contextual AI
- `vault-kinesis-ship-pulse` (9) — live studio momentum waveform
- `progressive-membership-unlock` (8) — anonymous progression hook
- `oracle-query-learning-loop` (8) — self-improving AI from real queries

## Skipped (Priority < 1.5 or blocked)

- `geo-vitals-watch-passive` — merged into geo-vitals-sample-accelerator (active probe > passive wait)
- `mobile-sheet-default-swap` — data-gated: 0/50 required mobile opens; not agent-executable yet
- `richer-ignis-layer-public-safe` — FOUNDER CALL: public-safe boundary decision needed before deploying cross-project IGNIS intelligence
- `tt-enforce-flip` — FOUNDER-DEVICE-GATED per SOUL #3: flip itself requires founder real-device verify; agent does the burn-down
- `vaultsparked-proof-delete` — FOUNDER 30-second yes/no: evidence-complete but requires explicit confirmation

---

## S184 Items (All SHIPPED — preserved for executionLog)

| # | Status | Slug | Commit |
|---|:-:|---|---|
| S184-1 | ✅ DONE | status-proof-index | `build-status-proof` |
| S184-2 | ✅ DONE | tt-enforce-reprobe | AMBER verdict, readiness doc written |
| S184-3 | ✅ DONE | workflow-rebase-race-guard | `cc9b434d` |
| S184-4 | ✅ DONE | dr-cache-smoke | 21/21 worker tests |
| S184-5 | ✅ DONE | ambient-candidate-ledger | 21 sources classified, 4 split-candidates |
| S184-6 | ✅ DONE+UPGRADED | field-win-tile-verify-reconcile | deploy-strand root-cause + check-deploy-tip.mjs |

---
*Generated by `/audit` v1.0 · S185 · ready for `/implement`*
