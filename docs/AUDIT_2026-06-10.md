<!-- generated-by: render-audit-md.mjs (R-H14) — DERIVED FROM AUDIT_2026-06-10.json; do not hand-edit -->
<!-- generated-at: 2026-06-10 -->
<!-- project: unknown · type: unknown -->

# Project Audit — Unknown

> One ranked improvement plan covering 9 axes. Top items first.
> Source of truth: this file is derived from the JSON sidecar. Edit the JSON, then re-render.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 6
- Combined Priority: 0.00
- Top 3: —

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov | Priority | Status | Item |
|---|:-:|---|---|:-:|:-:|:-:|:-:|---|
|  | fire | speed |  | 9 | 8 | 31.0 | · | **status-proof-index** — . Ten parallel fetches collapse to one RTT, and the manifest becomes a trust instrument instead of a directory of files. Self-grading is the genius layer: each embedded proof carries its source generatedAt plus a computed staleness flag, and the manifest exposes a top-level worstStale + trustScore, so /status/ can render a per-signal freshness clock (green/amber/red) that no studio site does. Info-finding outcome: a visitor or AI agent verifies the studio's entire live posture in one fetch. **Recipe:** Add scripts/build-status-proof.mjs that reads api/{uptime,field-win,ai-discovery-health,ci-status,site-health,staging-health,geo-vitals,field-verdicts,public-status,security-posture}.json, stamps each with freshnessSeconds + stale boolean (vs its own generatedAt), computes top-level worstStale/trustScore, writes api/status-proof.json (publicSafe:true, sealed-vault respected). Wire into the existing proof-generation workflow + build:check drift gate; point status/index.html at the single file with graceful per-key fallback. Add a <link rel='alternate' type='application/json' href='/api/status-proof.json'> + /agents.json cross-link so agents discover it. |
|  | fire | security |  | 8 | 6 | 30.3 | · | **tt-enforce-reprobe** — . Security posture is only real when it is measured on a cadence, not assumed between sessions. Doing the probe + analysis now (the agent-attemptable 90%) leaves only the SOUL #3 device-verified enforce-flip for the founder. Info-finding outcome: a committed readiness doc tells the next session exactly whether enforcement is safe. **Recipe:** Run: node scripts/probe-tt-soak.mjs && node scripts/analyze-tt-violations.mjs. Capture the cluster count + top sinks into docs/TT_ENFORCE_READINESS_2026-06-10.md with a clear GREEN/AMBER verdict and the exact flip command. Do NOT flip enforcement (founder-device gated per SOUL #3) — update the TASK_BOARD carry with the dated probe result. |
|  | fire | speed |  | 8 | 5 | 25.2 | · | **workflow-rebase-race-guard** — . Avoidable CI failure noise that self-heals but erodes trust in the green dashboard and can drop a generated artifact for a cycle. Fixing the class (not the one observed instance) is the durable move. Info-finding outcome: scheduled proof refreshes stop failing on benign races. **Recipe:** In each of the 7 NO-REBASE workflows (ci-status-beacon, leaderboard-api, member-seo, og-images, rum-pull, uptime-probe, vault-narrative), add 'git pull --rebase --autostash origin main' immediately before the push step (guard with || true only where a detached/no-op state is expected). Validate every edited YAML with: npx --yes js-yaml <file> >/dev/null (per the validate-workflow-yaml memory) before committing. |
|  | high | security |  | 7 | 7 | 24.5 | · | **dr-cache-smoke** — . An untested failover is a hope, not a guarantee — the only time you find out it's broken is during the outage it was built for. A deterministic test promotes the DR layer from 'assumed' to 'proven' and guards against silent regression. Info-finding outcome: CI proves the site degrades gracefully instead of hanging. **Recipe:** Add tests/dr-cache.spec.js (Node/worker-harness style matching existing cloudflare tests) that stubs originFetch to return two consecutive 5xx, invokes the worker-lib handler, and asserts response is the cached body + header X-VS-Disaster-Recovery: stale. Wire into the test suite run by build:check. Reference: cloudflare/worker-lib.mjs (X-VS-Disaster-Recovery). |
|  | high | speed |  | 6 | 6 | 22.7 | · | **ambient-candidate-ledger** — . Each ambient-split wave currently re-litigates which engines belong in the cold path; a committed ledger with reason codes makes the next wave a lookup, not an investigation, and documents why each engine is where it is. Info-finding outcome: the cold-path budget has an auditable provenance trail. **Recipe:** Add scripts/build-ambient-ledger.mjs that reads the candidate cache + current ambient block composition and writes docs/AMBIENT_LEDGER.md (or context/ambient-ledger.json) with {asset, placement, reasonCode, lastReviewed}. Seed reason codes from the existing S147/S180 split decisions. Optionally gate drift in build:check. |
|  | high | feedback |  | 7 | 4 | 21.2 | · | **field-win-tile-verify-reconcile** — . This is the exact honest-deduction holding Engagement at 99 across S180/S181 — the field-win tile was honest-dark because nothing had confirmed. It has now confirmed, so the truth surface should light and the board should stop re-surfacing a resolved item. Info-finding outcome: the site's flagship 'measured deploy win' proof goes live and the runway shrinks by one phantom carry. **Recipe:** Confirm status/index.html renders topWin from field-win.json (route '/', boundary 2026-06-05) and the tile is no longer dark. If the renderer still gates on the old PENDING shape, fix the conditional. Update TASK_BOARD: mark ORIGIN-MIGRATION-FIELD-VERDICT DONE with the confirmed verdict; keep GEO-VITALS-WATCH open (global samples still thin: US:107 GB:3). |

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

- status-proof-index
- dr-cache-smoke

## Skipped (Priority < 1.5)

- richer-ignis-layer-public-safe — FOUNDER-GATED — deploying aggregated cross-project/sealed IGNIS intelligence needs a public-safe-boundary decision the agent cannot make unilaterally (CANON public-safe). Stays a founder call.
- tt-enforce-flip — FOUNDER-DEVICE-GATED per SOUL #3 — the enforcement flip itself requires founder real-device verification. The agent does the reprobe + readiness doc (item #2); the flip is not agent scope.
- vaultsparked-proof-delete — FOUNDER yes/no — evidence-complete deletion candidate but a 30-second founder confirmation, not agent-attemptable without explicit go.
- nav-sheet-device-verify — FOUNDER-DEVICE — mobile bottom-sheet default-swap needs real-device verification before graduating the flag (flag-gated UX swap pattern).
- geo-vitals-watch — OBSERVATIONAL / DATA-GATED — non-US LCP confirmation needs more samples to accrue (US:107 GB:3); nothing to ship until the data grows. Left open as a watch, not an action.

