<!-- generated-by: render-audit-md.mjs (R-H14) — DERIVED FROM AUDIT_2026-09-10.json; do not hand-edit -->
<!-- generated-at: 2026-09-10 -->
<!-- project: vaultsparkstudios-website · type: website -->

# Project Audit — VaultSparkStudios.github.io

> One ranked improvement plan covering 9 axes. Top items first.
> Source of truth: this file is derived from the JSON sidecar. Edit the JSON, then re-render.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 3
- Combined Priority: 92.29
- Top 3: uptime-challenge-aware-api-legs · worker-scheduled-uptime-sampler · check-secrets-consults-probe-status

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov | Priority | Status | Item |
|---|:-:|---|---|:-:|:-:|:-:|:-:|---|
| 1 | 🔥 | security | 3h | 10 | 8 | 34.5 | · | **uptime-challenge-aware-api-legs** — Stop publishing a Cloudflare bot challenge as an outage on /status/. The public availability surface has reported edge-degraded for 100% of the 1488 retained samples since 2026-07-13. Visitors reading /status/ are told our own edge is degraded while it is serving them fine, which is a CANON-031 observability-honesty failure on a public trust surface. **Recipe:** Give the liveness, workerIngest and rumIngestPost legs the same challenge-shape classification the login leg already has: a CF challenge is UNOBSERVABLE from CI, not an outage. Never convert unobservable to ok:true — publish observable:false, exclude unobserved rows from rollup denominators, and let overall report edge-unobservable rather than edge-degraded or a fake up. |
| 2 | 🔥 | infrastructure | 4h | 9 | 9 | 31.3 | · | **worker-scheduled-uptime-sampler** — Observe the edge from inside the edge: Worker scheduled() sampler with bounded KV. Edge liveness is structurally unobservable from GitHub Actions because every datacenter request to the apex is challenged. A Cloudflare cron runs inside Cloudflare, so it can observe apex health without passing a challenge - it is the only way to make item 1 report a real green instead of an honest unobservable. **Recipe:** Add scheduled() to the production worker behind UPTIME_SAMPLER_ENABLED (default 0, so a deploy changes nothing), write bounded fixed-key KV samples with TTL, and add a drain script that folds samples into the unchanged uptime history contract. Rollback is a flag flip, not a redeploy. |
| 3 | ⚡ | security | 1h | 7 | 6 | 26.5 | · | **check-secrets-consults-probe-status** — check-secrets must consult the disproof it already holds. The gateway prints READY for a capability whose own CAPABILITY_MAP entry records lastProbeStatus auth-error. This is the surface an agent checks before declaring itself blocked (CANON-019), so a false READY sends the next session down a dead path. **Recipe:** Read lastProbeStatus alongside presence and report a third state - present-but-failing - instead of collapsing it into READY. Presence is not scope. |

## Depth Ladder (per item: L1 minimal · L2 solid · L3 genius)

> `/implement` selects the depth that fits remaining budget, and climbs the ladder
> on already-shipped items when the session still has budget (S175 saturation).

### uptime-challenge-aware-api-legs
- **L1 · minimal** _(~2h)_ — Give the liveness, workerIngest and rumIngestPost legs the same challenge-shape classification the login leg already has: a CF challenge is UNOBSERVABLE from CI, not an outage. Never convert unobservable to ok:true — publish observable:false, exclude unobserved rows from rollup denominators, and let overall report edge-unobservable rather than edge-degraded or a fake up.
- **L2 · solid** _(~3h)_ — Give the liveness, workerIngest and rumIngestPost legs the same challenge-shape classification the login leg already has: a CF challenge is UNOBSERVABLE from CI, not an outage. Never convert unobservable to ok:true — publish observable:false, exclude unobserved rows from rollup denominators, and let overall report edge-unobservable rather than edge-degraded or a fake up. Add self-tests covering the new states.
- **L3 · genius** _(~5h)_ — Give the liveness, workerIngest and rumIngestPost legs the same challenge-shape classification the login leg already has: a CF challenge is UNOBSERVABLE from CI, not an outage. Never convert unobservable to ok:true — publish observable:false, exclude unobserved rows from rollup denominators, and let overall report edge-unobservable rather than edge-degraded or a fake up. Add self-tests plus a negative control that fails if the new state is never exercised.

### worker-scheduled-uptime-sampler
- **L1 · minimal** _(~3h)_ — Add scheduled() to the production worker behind UPTIME_SAMPLER_ENABLED (default 0, so a deploy changes nothing), write bounded fixed-key KV samples with TTL, and add a drain script that folds samples into the unchanged uptime history contract. Rollback is a flag flip, not a redeploy.
- **L2 · solid** _(~4h)_ — Add scheduled() to the production worker behind UPTIME_SAMPLER_ENABLED (default 0, so a deploy changes nothing), write bounded fixed-key KV samples with TTL, and add a drain script that folds samples into the unchanged uptime history contract. Rollback is a flag flip, not a redeploy. Add self-tests covering the new states.
- **L3 · genius** _(~6h)_ — Add scheduled() to the production worker behind UPTIME_SAMPLER_ENABLED (default 0, so a deploy changes nothing), write bounded fixed-key KV samples with TTL, and add a drain script that folds samples into the unchanged uptime history contract. Rollback is a flag flip, not a redeploy. Add self-tests plus a negative control that fails if the new state is never exercised.

### check-secrets-consults-probe-status
- **L1 · minimal** _(~1h)_ — Read lastProbeStatus alongside presence and report a third state - present-but-failing - instead of collapsing it into READY. Presence is not scope.
- **L2 · solid** _(~1h)_ — Read lastProbeStatus alongside presence and report a third state - present-but-failing - instead of collapsing it into READY. Presence is not scope. Add self-tests covering the new states.
- **L3 · genius** _(~3h)_ — Read lastProbeStatus alongside presence and report a third state - present-but-failing - instead of collapsing it into READY. Presence is not scope. Add self-tests plus a negative control that fails if the new state is never exercised.

