<!-- generated-by: codex /audit live-code verification -->
<!-- generated-at: 2026-07-06 -->

# Audit 2026-07-06 S260 — VaultSparkStudios.github.io

## Verdict

Live-code verification narrowed this arc to one high-value local implementation item: burn down the current active Trusted Types sinks that still exist in this repo. The product-looking items in the hit list are still gated by evidence, credentials, sibling ownership, or founder voice and are recorded below rather than forced through.

## Ranked Plan

| Rank | Item | Tier | Category | Effort | Impact | Innovation | Outcome |
|---:|---|---|---|---:|---:|---:|---|
| 1 | S260-TT-ACTIVE-SINK-BURNDOWN | P1 | Security / public UX | 2h | 9 | 7 | Leaderboards, homepage hero ticker, and vaulted game live surfaces keep their information-finding value while reducing fresh Trusted Types violation noise before enforcement. |

### S260-TT-ACTIVE-SINK-BURNDOWN

Evidence:
- `docs/TT_BURNDOWN_2026-07-05.md` names active 3-day rows for `/leaderboards/`, `assets/hero-ticker.js`, and `/games/gridiron-gm/`.
- `rg` confirmed matching local `innerHTML` writers in `assets/hero-ticker.js`, `games/gridiron-gm/index.html`, and `leaderboards/index.html`.
- `assets/home-dynamic-hero.js` was rejected as already fixed locally; it now builds DOM nodes and the July 2 row is stale live evidence.

Recipe:
- Replace active sink table/card/ticker renderers with DOM construction while preserving the existing CSS hooks.
- Add a static regression gate for these freshness-ranked sinks.
- Verify with syntax checks, targeted page specs, `npm run build:check`, and doctor.

## Honest Deferrals

- `play-next conversion redesign`: data-gated. `api/funnel-summary.json` reports `play-next` shown/click as `0/0` since the 2026-07-02 honest viewport epoch.
- `INP root-fix`: time/data-gated until the clean post-2026-07-02 field window matures.
- `Full Obelisk provider/data-plane flip`: credential-gated; `obelisk.identity.verify` lacks RP keys.
- `Atlas registry freshness`: Studio Ops-owned; this repo must use Ark cargo, not sibling-tree edits.
- `Forge devlog`: founder-voice gated.

## Execution Log

- Shipped `S260-TT-ACTIVE-SINK-BURNDOWN`: replaced active `innerHTML` writers with DOM construction in the hero ticker, Gridiron GM live stream/rating UI, and leaderboards including generated SEO subpages.
- Added `scripts/check-active-tt-sinks.mjs` and wired it into `build:check:steps` after the Trusted Types policy analyzer.
- Verified with `node --check`, `node scripts/check-active-tt-sinks.mjs`, local Chromium coverage for homepage hero + games + leaderboards, and full `npm run build:check` (171/171 steps passed).
- Shipped second-order hygiene from the gate: `node scripts/rotate-taskboard.mjs` archived one stale task-board block; `--check-size` now passes.
