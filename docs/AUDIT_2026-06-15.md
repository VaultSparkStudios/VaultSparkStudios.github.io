<!-- generated-by: render-audit-md.mjs (R-H14) — DERIVED FROM AUDIT_2026-06-15.json; do not hand-edit -->
<!-- generated-at: 2026-06-15 -->
<!-- project: unknown · type: unknown -->

# Project Audit — Unknown

> One ranked improvement plan covering 9 axes. Top items first.
> Source of truth: this file is derived from the JSON sidecar. Edit the JSON, then re-render.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 15
- Combined Priority: 0.00
- Top 3: —

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov | Priority | Status | Item |
|---|:-:|---|---|:-:|:-:|:-:|:-:|---|
| 1 | transformative | visual/data-viz | 4h | 8 | 7 | 21.7 | · | **render-dormant-intelligence-visuals** — Render the dormant intelligence visuals (heatmap + constellation + insights) instead of 'Loading...'. oracle/ and studio-pulse/ ship the markup for a 60-day signal heatmap, a project-constellation network graph, and an auto-insights list, but on the public deploy they sit at 'Reading the data...' / 'Loading 60-day grid...' placeholders. A studio whose whole pitch is 'we are alive and measurable' that shows perpetual loading spinners reads as broken (CANON-031 honesty). The velocity series data already exists (api/velocity-series.json, shipped S198) so the wiring cost is low; the visual payoff is the single most premium-dashboard upgrade available. **Recipe:** In studio-pulse-live.js + oracle-insights-compute.js: render the heatmap as a CSS-grid (7 rows x ~9 weeks) with intensity bound to api/velocity-series.json daily counts + hover tooltip (date + count); render the constellation as an inline SVG node graph (nodes = projects from public-intelligence.json, edges = shared-universe links) with hover labels; populate #oracle-insights with 3-5 derived bullets (peak day, trend %, top mover). Add a CSS shimmer skeleton so a slow fetch never shows blank/raw 'Loading' text. |
| 2 | transformative | ux/visual | 4h | 8 | 7 | 21.7 | · | **portal-member-first-load-premium** — Make the signed-in portal first-load premium + personal (the experience currently INVERTS). The anonymous funnel pages (join/, invite/, membership/) glow with radial gradients, animated orbs and gradient panels, but the signed-in /vault-member/ dashboard renders on a flat var(--page-bg) dark panel with a static placeholder below the profile card. The reward for joining is a downgrade in polish, which directly undercuts SOUL non-negotiable #2 ('membership must feel real, like entry into something'). There is no 'you're N pts from <next tier>', no streak, no this-week's-challenge moment, so the portal feels like a settings page, not a vault. **Recipe:** Above the fold in vault-member/index.html: add a personalization cluster bound to session — (a) tier card with current pts + next-tier + animated progress arc (reuse rank-orb.js already proven on ranks/), (b) login-streak badge when streak>=3, (c) 'this week's challenge' micro-card with XP reward. Style the dashboard header with a tier-colored gradient + left-border accent (gold Sparked / purple Eternal / silver Free) and a 'member since' / founding-member badge. Gate everything on session presence (account-chip presence-first pattern). |
| 3 | transformative | visual | 4h | 9 | 6 | 20.9 | · | **game-cover-art-system** — Give every game a real cover tile (cards are bare radial gradients on the #1 CTA path). The games grid is the most-trafficked conversion surface and every card is a colored radial-gradient blur behind text — no cover art, no distinctive imagery. It reads uniform and impersonal, the opposite of 'studio-owned, not generic' (SOUL anti-goal). This is the highest single-issue visual lift on the site because it's the first thing a player evaluates. sharp@0.34.5 is already a trusted devDep (S196) so bespoke SVG->PNG cover tiles can be generated build-time with zero new deps. **Recipe:** Author one bespoke SVG cover per game (title lockup + genre motif + brand-consistent palette), rasterize to PNG via the existing sharp pipeline, and render as a card hero image with a gradient overlay for text legibility. Apply uniformly across games/index.html cards AND each game page hero. Keep the radial gradient as the loading fallback. Wire into build:check so a game without a cover fails the gate. |
| 4 | high | featureDepth/immersion | 8h | 7 | 9 | 19.0 | · | **universe-depth-map** — Interactive Universe Depth Map — node graph of game/project/lore connections. The site repeatedly hints at a shared universe (Voidfall, DreadSpike, Solara, lore gates) but exposes it only as prose. An interactive node-graph that shows how titles and lore connect — with a few public edges visible and more hinted behind membership — turns flat copy into explorable depth and is a genuine immersion differentiator no indie-studio site in this category has. It also creates a natural membership pull (unlock hidden connections). **Recipe:** Build an inline-SVG / canvas force-ish node graph (no heavy dep — reuse the constellation renderer from item 1). Seed nodes/edges from a public data file (universe-graph.json): games, projects, lore entities. Public visitors see 2-3 confirmed edges + locked/blurred nodes; signed-in members see more. Place on a new /universe/ surface and embed a teaser on the homepage. Founder-verify edges before publish (lore canon must be founder-reviewed). |
| 5 | high | visual/immersion | 3h | 7 | 6 | 18.1 | · | **hero-immersion-parallax** — Tighten homepage hero reveal timing + add scroll parallax depth. The hero forge-wordmark animation is cinematic but a ~1.4s reveal-chain delay creates dead-air before the tagline/copy land, and the three background glows are static radial gradients with opacity-only animation — no depth on scroll. First-impression immersion is left on the table on the studio's most important page. will-change + contain:paint are already applied so parallax is low-jank. **Recipe:** Overlap the tagline reveal into the wordmark's final animation frame (remove the serial delay) in home-dynamic-hero.js; add a subtle scroll-driven parallax (~40px translate) on the hero glows; add a breathing micro-glow on hero-center synced to the spark-burst rhythm. Keep prefers-reduced-motion honored. |
| 6 | high | featureDepth/immersion | 3h | 6 | 7 | 18.1 | · | **homepage-live-velocity-meter** — Replace the static '27 initiatives' teaser with a live studio velocity meter. The homepage states '27 initiatives. One vault.' as flat promo copy with no live count or drill-down, while the Heartbeat/Pulse data needed to make it real is already fetched elsewhere on the page. A live counter ('4 live / 9 in forge / 12 sealed') with inline expand makes the 'alive studio' claim self-proving instead of asserted, reinforcing CANON-031 honesty. **Recipe:** Bind the teaser to the existing Heartbeat/public-intelligence feed: render live counts by status, each clickable to expand a brief manifest inline before linking to /studio-pulse/. Add a tiny velocity sparkline (ships/week) using the velocity-series data from item 1. |
| 7 | high | redundancy/ux | 3h | 7 | 5 | 15.1 | · | **homepage-merge-activity-feeds** — Merge the three homepage activity feeds into one Vault Pulse strip. Recent Ships, Portfolio Heartbeat, and Vault Signal are three separate feed sections (~900px of mid-page scroll) all hydrated from backend JSON and all signalling 'studio momentum' — overlapping content that lengthens the page and dilutes impact. One consolidated, auto-rotating strip is tighter and more premium. **Recipe:** Combine into a single ~320px 'Vault Pulse' strip with tabs (Heartbeat / Ships / Signal) or an auto-rotating carousel; default the first tab by visitor type (Heartbeat for new, Activity for returning, using the existing returning-visitor signal). Remove the two now-redundant sections. |
| 8 | high | redundancy/ux | 4h | 7 | 5 | 13.5 | · | **differentiate-intelligence-surfaces** — Differentiate (or unify) oracle / studio-pulse / nervous-system — three near-identical 'intelligence' pages. All three render the same ecosystem snapshot (project cards, health, focus) and a public visitor can't tell why they exist separately. This is the largest conceptual redundancy on the site. Either give each a distinct, signposted job or unify them into one dashboard with modes — but the current three-near-duplicate-landings state is the worst option. **Recipe:** Decision-first: assign distinct roles — Pulse = live heartbeat (cards + current focus), Oracle = deep read (velocity, forecasts, insights from item 1), Nervous System = signal digest (tiles + decisions). Add a shared sub-nav across the three so they read as one suite, de-duplicate the shared project-card block to a single include, and cross-link. L3 = collapse to one /studio-pulse/ with mode tabs and 301 the other two (Worker Layer 0c). |
| 9 | high | visual | 1.5h | 6 | 4 | 13.3 | · | **light-mode-visual-quality** — Light-mode first-class pass (hero glows nearly invisible at current opacity). SOUL quality bar requires light mode to be a first-class experience, but the hero glows use identical blur(80px)/low-opacity values across themes so the gold glow (~0.09) nearly vanishes in light mode and the hero looks flat/unfinished for light-mode visitors. **Recipe:** Add theme-aware glow intensities + blur (dark ~0.15/blur90, light ~0.22/blur60, ambient ~0.18) via CSS custom props keyed off the theme attribute; spot-check contrast on 3-4 top pages in light mode. |
| 10 | high | ux | 1h | 7 | 3 | 13.2 | · | **games-grid-honesty-and-cta** — Remove the dead-end Gridiron-GM-Play card + standardize CTA text + fix state honesty. The games grid carries a vaulted 'Gridiron GM Play' card whose primary button reads 'Play Football GM Instead' (a dead-end that wastes grid space and confidence), the 'Sparked - Live & Playable' filter section contains a vaulted game, and Play CTAs vary ('Play Now' / 'Play Beta' / 'Play Football GM Instead'). This is exactly the surface-vs-truth class the check-game-playability-coherence gate (CANON-031) exists to catch. **Recipe:** Remove the gridiron-gm-play card from the grid (keep /games/gridiron-gm-play/ as a 301/redirect target only); standardize CTA copy to Play Now (live) / Play Beta (beta) / Join Waitlist (pre-release); ensure the SPARKED section contains only SPARKED titles; re-run check-game-playability-coherence.mjs. |
| 11 | high | redundancy | 3h | 7 | 4 | 12.1 | · | **merge-pathways-pages** — Collapse 6 byte-identical pathways/* pages into one filterable /pathways/. pathways/players, builders, investors, supporters, press, lore are each EXACTLY 165 lines (verified) — identical templates with only audience-specific copy swapped — sitting behind 6 separate URLs while the parent /pathways/ already has a JSON-driven grid. This is dead surface area that dilutes IA and crawl budget for zero unique value. **Recipe:** Move the per-audience content into pathways data (JSON) and render via filter/anchor on /pathways/?audience=players (the grid pattern already exists). Delete the 6 subfolders and 301 them to the anchored route via Worker Layer 0c + a redirects.spec.js case. Verify no inbound links break. |
| 12 | high | redundancy/ux | 3h | 6 | 4 | 10.3 | · | **member-surface-ia-cleanup** — Untangle member / members / vault-member / vault-portal naming maze. Four similarly-named surfaces confuse navigation: /vault-portal/ (422 lines) is really just a door-chooser router, /vault-member/ (1740) is the real portal, /members/ (307) is the directory, and /member/ (835) is a near-orphan single-profile page. The naming overlap makes it unclear where a member 'goes home', and /members/ isn't even linked in nav. **Recipe:** Decide canonical homes: make /vault-member/ the portal home; make /vault-portal/ either an auth-conditional portal (members -> dashboard, anon -> door chooser) or 301 it away; add /members/ to nav as 'Member Directory'; rename or retire /member/ (verify usage first). Add cross-links from the portal to the directory. |
| 13 | nice-to-have | ux/text-org | 2h | 5 | 4 | 10.0 | · | **faq-data-driven-search** — Make FAQ data-driven with category tabs + search. FAQ Q&A are inline HTML with no categorization or search, so a visitor hunting one answer must eyeball the whole accordion. A data-driven FAQ (the same JSON+render pattern used elsewhere) adds search + category filters and keeps FAQPage schema intact. **Recipe:** Move FAQ entries to data/faq.json with category tags; render the accordion client-side; add a search input + category tabs (Games / Membership / Account / Technical); keep FAQPage structured data generated from the same JSON. |
| 14 | nice-to-have | redundancy/text-org | 4h | 6 | 4 | 9.3 | · | **merge-membership-value** — Consolidate /membership/ and /membership-value/ (overlapping pitch, two mental models). Both pages present tier cards + benefits + value estimates; a visitor toggling between them gets the same pitch twice with no new insight beyond /membership-value/'s market-comparison bars. Two large pages (1473 + 1022 lines) carrying one message splits conversion attention. NOTE: both are substantial, so this is genuine consolidation work, not a trivial delete. **Recipe:** Make /membership/ canonical (narrative + voices + philosophy + tier cards). Fold the unique value-comparison/market bars into a collapsible '#value' section on /membership/ and 301 /membership-value/ -> /membership/#value (Worker Layer 0c). Keep tier CTAs deep-linking to the upgrade flow. |
| 15 | nice-to-have | redundancy | 2h | 5 | 3 | 7.5 | · | **merge-brand-into-press** — Merge /brand/ into /press/ (one media kit, not two). /press/ (press kit: logos, bio, facts, contact) and /brand/ (logos, palette, code) serve the same 'give me assets + usage' need and share ~40% content; press and partners don't distinguish 'press kit' from 'brand kit'. One canonical resource is clearer and halves maintenance. **Recipe:** Merge /brand/ content into /press/ as a tabbed 'Brand Assets' section; 301 /brand/ -> /press/#brand-assets (Worker Layer 0c + redirects.spec.js); consolidate the near-identical .asset-card / .press-card CSS. |

## Depth Ladder (per item: L1 minimal · L2 solid · L3 genius)

> `/implement` selects the depth that fits remaining budget, and climbs the ladder
> on already-shipped items when the session still has budget (S175 saturation).

### render-dormant-intelligence-visuals
- **L1 · minimal** _(~1h)_ — Replace every 'Loading...' literal with a shimmer skeleton block so nothing reads as broken; wire the heatmap to existing velocity-series.json only.
- **L2 · solid** _(~4h)_ — L1 + render heatmap with hover tooltips, the SVG constellation graph, and the derived insights bullet list.
- **L3 · genius** _(~8h)_ — L2 + animated draw-in on scroll, click-to-drill on constellation nodes (open the project), and a 'last updated' freshness pill bound to the feed timestamp.

### portal-member-first-load-premium
- **L1 · minimal** _(~1h)_ — Tier-colored gradient header + 'N pts to <next tier>' line + progress bar. No new feeds.
- **L2 · solid** _(~4h)_ — L1 + rank-orb progress arc, streak badge, this-week challenge micro-card; emit engagement:portal_personalized RUM event.
- **L3 · genius** _(~8h)_ — L2 + 'your focus this session' smart suggestion, recently-unlocked achievement reel, and a Browse-Members CTA into /members/.

### game-cover-art-system
- **L1 · minimal** _(~1.5h)_ — Polish the existing gradients: add a genre glyph/wordmark watermark + consistent overlay so cards stop looking identical.
- **L2 · solid** _(~4h)_ — Bespoke SVG->PNG cover tile per game via sharp; image + overlay on cards and game-page heroes; fallback gradient on load.
- **L3 · genius** _(~8h)_ — L2 + subtle parallax/tilt on hover, animated cover variants for SPARKED titles, and an OG-image reuse so social shares carry the same cover.

### universe-depth-map
- **L1 · minimal** _(~2h)_ — Static SVG constellation of public titles with hover labels, no interactivity, on homepage teaser only.
- **L2 · solid** _(~8h)_ — Full interactive /universe/ graph with locked/unlocked nodes by session, click-to-open, founder-verified edges.
- **L3 · genius** _(~16h)_ — L2 + animated reveal of new edges as titles ship, lore-gate tie-in, and member-only deep edges with a 'you unlocked' moment.

### hero-immersion-parallax
- **L1 · minimal** _(~30m)_ — Remove the serial reveal delay so tagline+copy land with the wordmark; no parallax.
- **L2 · solid** _(~3h)_ — L1 + scroll parallax on glows + breathing center glow, reduced-motion guarded.
- **L3 · genius** _(~6h)_ — L2 + pointer-reactive glow drift and a one-time first-visit cinematic that self-disables after first view.

### homepage-live-velocity-meter
- **L1 · minimal** _(~1h)_ — Bind the count to the live feed (no hardcoded 27); show live/forge/sealed split.
- **L2 · solid** _(~3h)_ — L1 + inline expand per status + ships/week sparkline.
- **L3 · genius** _(~6h)_ — L2 + a 'vault pressure gauge' animated meter that visibly reacts as counts/velocity cross thresholds.

### homepage-merge-activity-feeds
- **L1 · minimal** _(~1h)_ — Tabbed container around the three existing feeds (no carousel), reclaim the scroll.
- **L2 · solid** _(~3h)_ — Auto-rotating strip with visitor-type default tab + reduced-motion pause.
- **L3 · genius** _(~5h)_ — L2 + glitch-on-new-signal micro-animation and click-to-pin a feed.

### differentiate-intelligence-surfaces
- **L1 · minimal** _(~1.5h)_ — Add a one-line purpose banner + shared sub-nav to each so the distinction is explicit; cross-link.
- **L2 · solid** _(~4h)_ — L1 + extract the duplicated project-card block to one shared renderer and specialize each page's unique content.
- **L3 · genius** _(~8h)_ — Unify into /studio-pulse/?mode= with tabs; 301 oracle/ and nervous-system/ via Worker Layer 0c + redirects.spec.js.

### light-mode-visual-quality
- **L1 · minimal** _(~30m)_ — Bump light-mode glow opacity/blur on the homepage hero only.
- **L2 · solid** _(~1.5h)_ — Theme-aware glow tokens applied sitewide via custom props.
- **L3 · genius** _(~3h)_ — L2 + a light-mode QA sweep of the top 10 pages fixing any low-contrast surfaces found.

### games-grid-honesty-and-cta
- **L1 · minimal** _(~1h)_ — Remove the dead card, standardize the three CTA labels, fix the section membership.
- **L2 · solid** _(~2h)_ — L1 + a data-driven status->CTA-label map so cards can never drift again.
- **L3 · genius** _(~3h)_ — L2 + add the status->label rule to check-game-playability-coherence so a wrong CTA fails build:check.

### merge-pathways-pages
- **L1 · minimal** _(~1.5h)_ — Keep the 6 pages but data-drive them from one source to kill copy drift; defer deletion.
- **L2 · solid** _(~3h)_ — Collapse to /pathways/?audience= with filters + 301 the 6 subfolders (Worker Layer 0c).
- **L3 · genius** _(~4h)_ — L2 + audience auto-detect (referrer/UTM) that pre-selects the right pathway on arrival.

### member-surface-ia-cleanup
- **L1 · minimal** _(~1h)_ — Add /members/ to nav + cross-link from portal; rename labels so the four surfaces read distinctly.
- **L2 · solid** _(~3h)_ — L1 + make /vault-portal/ auth-conditional OR 301 it; retire/redirect /member/ after usage check.
- **L3 · genius** _(~5h)_ — L2 + a single coherent member-area sub-nav shared across portal/directory/ranks.

### faq-data-driven-search
- **L1 · minimal** _(~30m)_ — Add category headings + jump links to the existing inline FAQ.
- **L2 · solid** _(~2h)_ — Full JSON-driven render + search + category tabs + schema from JSON.
- **L3 · genius** _(~3h)_ — L2 + 'was this helpful?' micro-feedback per answer feeding the feedback loop.

### merge-membership-value
- **L1 · minimal** _(~1.5h)_ — Cross-link the two + dedupe the most blatant repeated tier table; defer the merge.
- **L2 · solid** _(~4h)_ — Fold unique value content into /membership/#value and 301 /membership-value/.
- **L3 · genius** _(~6h)_ — L2 + an interactive tier comparator (toggle Free/Sparked/Eternal) replacing the static tables.

### merge-brand-into-press
- **L1 · minimal** _(~30m)_ — Cross-link the two pages prominently; remove the duplicate brand link from the Resources nav dropdown.
- **L2 · solid** _(~2h)_ — Merge into /press/ tabs + 301 /brand/ + consolidate CSS.
- **L3 · genius** _(~3h)_ — L2 + a one-click 'download full press+brand kit' zip.

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

- [object Object]
- [object Object]
- [object Object]
- [object Object]

## Skipped (Priority < 1.5)

- rank-orb-on-ranks — PREMISE FALSE — an agent proposed adding rank-orb visualization to /ranks/, but grep confirms ranks/index.html ALREADY references rank-orb/rank-projector/rank-quest. The real gap (flat rank list on the homepage + no personal pace in the portal) is folded into portal-member-first-load-premium (item 2).
- oracle-velocity-chart-build — PARTIALLY DONE — build-velocity-series.mjs (S198) already generates api/velocity-series.json with ISO-week self-tests. The remaining gap (heatmap/constellation/insights still showing 'Loading') is captured in render-dormant-intelligence-visuals (item 1); no separate velocity-build item needed.
- merge-legal-pages — REJECTED BY CANON — privacy/cookies/data-deletion/accessibility must remain individually addressable for app-store + GDPR review (legal-pages-individually-addressable). Correctly kept separate.


---

## Execution Log — Session 200 (/implement full plan, one pass)

| # | Item | Rung | Status | Notes |
|---|---|---|---|---|
| 1 | render-dormant-intelligence-visuals | L2 | ✅ shipped | Root cause found: oracle fetched gitignored `/ignis/output/*` (404 on prod). New `build-oracle-velocity-public.mjs` → public `api/ecosystem-velocity.json`; oracle-extra.js falls back to it. Verified 2 live insight cards + 60-day heatmap. |
| 2 | portal-member-first-load-premium | L1 | ✅ shipped (premise corrected) | Portal already ships gradient card + pace-to-next-tier + streak + founding badge. Added tier-aware header accent for VaultSparked members. |
| 3 | game-cover-art-system | L2 | ✅ shipped | New `build-game-covers.mjs` → 8 bespoke SVG→PNG covers, wired into card CSS; fixes latent missing `.the-exodus` gradient. |
| 4 | universe-depth-map | — | ⏸ deferred | Net-new; requires founder-verified lore edges (canon review). Carry. |
| 5 | hero-immersion-parallax | L1/L2 | ✅ shipped | Scroll parallax on hero vignette (reduced-motion safe). Reveal-timing rework deferred (shell CSS, higher risk). |
| 6 | homepage-live-velocity-meter | L2 | ✅ shipped | Live initiative counts from public-intelligence feed replace static "27 initiatives"; new `home-initiative-counter.js`. |
| 7 | homepage-merge-activity-feeds | L1 | ✅ shipped (partial) | Folded Heartbeat into Recent Ships (adjacent). Vault Signal left (non-adjacent + semantically distinct member activity). |
| 8 | differentiate-intelligence-surfaces | L1 | ✅ shipped | Shared "Studio Intelligence" suite nav on oracle/studio-pulse/nervous-system, each labeling its distinct job. |
| 9 | light-mode-visual-quality | L2 | ✅ shipped | Theme-aware hero glow intensity/blur (light + ambient). |
| 10 | games-grid-honesty-and-cta | L2 | ✅ shipped | Removed dead-end Gridiron-GM-Play card; standardized forge CTAs to "Join Waitlist". Coherence gate green. |
| 11 | merge-pathways-pages | — | ⏸ deferred | Needs Worker Layer 0c 301s + content extraction. Carry. |
| 12 | member-surface-ia-cleanup | L1 | ✅ shipped (partial) | Browse Members link in portal header. Nav-wide add + /member/ retirement deferred (propagation + usage audit). |
| 13 | faq-data-driven-search | — | ⏸ deferred | Medium refactor; better with care. Carry. |
| 14 | merge-membership-value | L1 | ✅ shipped (partial) | Cross-link membership-value→membership. 301 merge deferred. |
| 15 | merge-brand-into-press | L1 | ✅ shipped (partial) | Reverse cross-link added (press→brand already existed). 301 merge + nav dedupe deferred. |

**Shipped: 12/15** (3 deferred with reasons: #4 founder-lore-gated, #11 needs Worker-301 propagation, #13 medium refactor). `npm run build` + `npm run build:check` → EXIT 0. Also fixed pre-existing gate debt (RUM static-list, S199 SIL arithmetic).
