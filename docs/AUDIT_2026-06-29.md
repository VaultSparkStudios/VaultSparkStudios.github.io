<!-- generated-by: render-audit-md.mjs (R-H14) — DERIVED FROM AUDIT_2026-06-29.json; do not hand-edit -->
<!-- generated-at: 2026-06-29 -->
<!-- project: website · type: website -->

# Project Audit — VaultSparkStudios.github.io

> One ranked improvement plan covering 9 axes. Top items first.
> Source of truth: this file is derived from the JSON sidecar. Edit the JSON, then re-render.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 8
- Combined Priority: 130.00
- Top 3: oracle-prebake-answer-api · tier-value-calculator · startup-brief-truth-signals

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov | Priority | Status | Item |
|---|:-:|---|---|:-:|:-:|:-:|:-:|---|
| oracle-prebake-answer-api | FIRE | ai | 5h | 8 | 9 | 22.0 | ✓ | **oracle-prebake-answer-api** — Oracle prebaked answer corpus plus agent-callable lookup endpoint. assets/ignis-answer-engine.js had a keyword-only client answer path and agents.json did not expose a source-backed answer lookup. The site promises human and AI agent usefulness, so the Oracle needs deterministic answers that cost nothing at runtime and cite public sources. **Recipe:** Build scripts/build-oracle-answers.mjs from api/oracle-insights.json and api/membership-tiers.json, commit oracle/answers/index.json, load it before the client fallback, advertise it in agents.json actions/feeds and .well-known/llms.txt, and gate it with self-test plus --check in check-proof-surface. |
| tier-value-calculator | FIRE | engagement | 4h | 7 | 8 | 20.0 | ✓ | **tier-value-calculator** — Interactive membership value calculator on /membership-value/. membership-value/ had static value copy. A calculator lets a visitor select actual intended use, compares it to $4.99/mo from the canonical tier JSON, and emits UX telemetry when computed. **Recipe:** Add assets/membership-value-calculator.js, mount it in membership-value/index.html, source tier facts from /api/membership-tiers.json, and keep the static no-JS fallback. |
| startup-brief-truth-signals | FIRE | security | 2h | 8 | 6 | 18.0 | ✓ | **startup-brief-truth-signals** — Startup brief truth fixes for last-active, revenue signal, and SIL forecast. The /start brief claimed Last active: 20632d, projected SIL 0/1000, and alarmed on a missing local revenue signal. Those are observability lies created by parser/source assumptions, not project reality. **Recipe:** Stop treating numeric silLastSession as a date, make a missing local revenue file neutral rather than stale, and teach the SIL forecaster to parse compact v3 category lines. |
| oracle-answer-quality-gate | SPARK | ai | 1h | 6 | 7 | 16.0 | ✓ | **oracle-answer-quality-gate** — Second-order quality gate for generated Oracle prose. The first generated corpus passed schema checks while producing raw concatenated source prose. The generator needed a quality-oriented root fix so deterministic answers stay readable. **Recipe:** Fix cleanText truncation, filter stopword keywords, keep source snippets in structured citations instead of stuffing them into answer bodies, and assert production corpus safety before writing/checking output. |
| worker-agent-ua-policy-deploy | SPARK | security | 1h | 6 | 5 | 15.0 | · | **worker-agent-ua-policy-deploy** — Deploy the agent-aware Cloudflare Worker user-agent policy. S234 shipped code that allows generic HTTP clients on public reads while still blocking scanner tools. The remaining premise is deployment, not source implementation. **Recipe:** Run the existing wrangler production deploy through the normal approval/network gates, then verify production headers and agent access behavior. |
| inp-root-fix | SPARK | speed | 3h | 7 | 5 | 14.0 | · | **inp-root-fix** — INP root fix remains data-blocked until real interaction samples exist. data/inp-breakdown.json currently has totalSamples: 0 and no route breakdown. Any claimed INP root fix would be fabricated without route/handler evidence. **Recipe:** Keep the telemetry path live, wait for real field samples, then fix the highest route/handler contributor instead of guessing. |
| ops-profile-mismatch-ark | SPARK | tokenCost | 1h | 5 | 6 | 13.0 | · | **ops-profile-mismatch-ark** — Report Studio Ops profile mismatch without editing a sibling repo. studio-ops arc-profile/check-canon-conformance classified this public live website as infrastructure/internal/FORGE even though local PROJECT_STATUS.json says website/public-live/SPARKED. That makes conformance output noisy and can hide the right public-site checks. **Recipe:** Ship Ark cargo to studio-ops with the mismatch evidence; do not directly edit the sibling repo tree from this project. |
| llm-answer-discovery-spine | SPARK | ai | 1h | 6 | 6 | 12.0 | ✓ | **llm-answer-discovery-spine** — Second-order LLM discovery spine for Oracle answers. Adding a JSON endpoint is not enough if agent discovery surfaces do not point to it. The answer corpus should be visible from both agents.json and llms.txt. **Recipe:** Expose oracle/answers/index.json in agents.json feeds/actions and .well-known/llms.txt canonical surfaces. |

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

- oracle-answer-quality-gate
- llm-answer-discovery-spine
- ops-profile-mismatch-ark

## Skipped (Priority < 1.5)

- undefined — undefined

