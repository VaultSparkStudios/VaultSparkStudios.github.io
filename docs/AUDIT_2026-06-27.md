<!-- generated-by: render-audit-md.mjs (R-H14) — DERIVED FROM AUDIT_2026-06-27.json; do not hand-edit -->
<!-- generated-at: 2026-06-27 -->
<!-- project: unknown · type: unknown -->

# Project Audit — Unknown

> One ranked improvement plan covering 9 axes. Top items first.
> Source of truth: this file is derived from the JSON sidecar. Edit the JSON, then re-render.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 10
- Combined Priority: 0.00
- Top 3: —

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov | Priority | Status | Item |
|---|:-:|---|---|:-:|:-:|:-:|:-:|---|
| inp-rum-telemetry | 🔥 | speed | 2h | 9 | 8 | 36.0 | · | **inp-rum-telemetry** — INP breakdown RUM telemetry — diagnose 208ms/224ms homepage + games INP. Field INP for homepage is 208ms (8ms over the 200ms 'Good' budget, 278 samples) and /games/ 224ms (85 samples). The auto-apply system has no recipe because there's no good→bad transition — the pages have been persistently over-budget. Without knowing WHICH interaction triggers the slow response, we're flying blind. Adding INP breakdown events (interaction type, target element, JS handler duration) to the Worker RUM pipeline lets us identify the specific culprit — a dropdown click handler, a search keydown, a nav toggle. Once we know the source, we can defer the handler, add `scheduler.yield()`, or split the long task. This directly unblocks the 'All CWV Good' status for the two highest-traffic pages. **Recipe:** 1) Add `PerformanceObserver` in assets/ambient-bundle.js watching 'event' entries with duration > 150ms. 2) Emit `inp:slow-interaction` RUM event with {element, eventType, duration, route}. 3) Add to Worker RUM_UX_EVENTS. 4) After 24h of field data, identify the top-3 slow interactions and apply fixes. |
| changelog-auto-draft | 🔥 | engagement | 3h | 8 | 9 | 30.4 | · | **changelog-auto-draft** — Changelog auto-draft from session handoffs — 75-day staleness gap. The changelog is 75 days stale (newest entry 2026-04-13, detected by check-content-freshness). Visitors landing on /changelog/ see dead silence — a trust-destroying signal for a SPARKED project that has shipped 40+ sessions of real work. The solution is an AI-assisted auto-draft pipeline: parse LATEST_HANDOFF.md 'Shipped:' items + git log since last changelog entry → generate draft entry with session narrative → commit to a draft file for founder review + one-click publish. This closes the staleness gap structurally rather than requiring manual writing. **Recipe:** 1) scripts/draft-changelog-entry.mjs: reads LATEST_HANDOFF.md shipped items + git log --since=<last-entry-date>. 2) Groups by theme (performance, AI, engagement, infra). 3) Writes to changelog/drafts/<date>.md with honestDark gate. 4) Founder reviews + runs `node scripts/publish-changelog-entry.mjs <date>` to go live. |
| cwv-composite-rum | ⚡ | speed | 2h | 7 | 8 | 28.0 | · | **cwv-composite-rum** — CWV composite pass rate in RUM — track % sessions where all CWV are 'Good'. The RUM pipeline tracks individual CWV metrics (LCP, CLS, INP) per route but never computes the composite 'All Good' rate — the metric Google uses to determine if a page passes Core Web Vitals. Knowing that '73% of homepage visits pass all CWV' vs '68%' gives a single actionable number that correlates directly with search ranking signals. This metric should live in api/rum-summary.json and surface in the /studio-pulse/ dashboard and api/site-health.json so it's visible to the founder and to AI agents scraping the intelligence surface. **Recipe:** 1) In rollup-rum-ux.mjs, add `cwvPassRate` computation: per-sample check LCP<2500, CLS<0.1, INP<200 (when all 3 present). 2) Add to api/rum-summary.json. 3) Surface in build-site-health.mjs as a top-line metric. 4) Add to perf-budget check display. |
| oracle-context-ranking | 🔥 | ai | 4h | 8 | 8 | 27.6 | · | **oracle-context-ranking** — Oracle context-aware answer ranking — use session history to re-rank excerpts. oracle:context_boost is now measured (S228), and the session-context scoring boost is live (+0.15 per matched keyword, capped 2×). But the boost is applied during SCORING not during RETRIEVAL — so a follow-up query about 'Gridiron GM' still pulls the same top-K excerpts from the prefix cache even if the user just asked about football. The next level is context-aware excerpt retrieval: use the session's keyword history to bias the oracle's prefix-cache key selection, surfacing excerpts from pages related to prior queries rather than treating each query independently. This would meaningfully deepen the 'explore more' feeling that differentiates IGNIS from a basic search box. **Recipe:** 1) In ignis-answer-engine.js answer(): extract top-3 keywords from last 2 sessionQueries (already computed for context boost). 2) Compute a secondary prefix set from those keywords (cross-reference oracle-insights.json). 3) Merge top-5 contextual excerpts into the candidate set before scoring. 4) Add emitUx('oracle:context_retrieval_boost') when contextual excerpts appear in top-3 results. |
| build-sha-pre-push | ⚡ | speed | 30m | 6 | 5 | 25.0 | · | **build-sha-pre-push** — Add build-SHA regeneration to pre-push hook — eliminate stale SHA in CI. The compliance job was failing in CI (discovered this session) because api/build-sha.json had the SHA from before the agents.json sitewide commit — the closeout generated the SHA, then a code commit followed without regenerating it. The pre-push hook currently runs build:check but doesn't regenerate build-sha. Adding `node scripts/generate-build-sha.mjs` to the pre-push hook ensures the committed SHA always matches HEAD at push time, eliminating this class of CI failure permanently. **Recipe:** In .husky/pre-push (or .claude/hooks/pre-push), add `node scripts/generate-build-sha.mjs && git add api/build-sha.json` before the build:check step. Must handle the case where this triggers an unstaged change during push (use --amend-if-clean or a git stash dance). |
| lighthouse-staging-warmup | ⚡ | speed | 1h | 6 | 5 | 20.0 | · | **lighthouse-staging-warmup** — Lighthouse staging score — diagnose 0.76 staging vs 0.80 target. Lighthouse CI runs against staging (website.staging.vaultsparkstudios.com) and shows 0.76 homepage performance with LCP 6057ms. Field data shows median LCP of 1108ms (well within budget). The delta is almost certainly staging server cold-start latency — staging on Hetzner is likely sleeping between deploys. Adding a warmup step (a HEAD request to staging before Lighthouse runs) could bring the score to production parity. If staging LCP is genuinely 6s even after warmup, there's a real issue (staging server underprovisioned) that deserves investigation. **Recipe:** In lighthouse.yml, before the treosh/lighthouse-ci-action step, add: `curl -s -o /dev/null -w '%{http_code}' https://website.staging.vaultsparkstudios.com/ || true`. Then run Lighthouse. If scores still ≤0.76, investigate staging server config vs Cloudflare Pages (could be origin response time issue). |
| sibling-ark-compliance | ⚡ | security | 30m | 5 | 3 | 11.4 | · | **sibling-ark-compliance** — Ark cargos for sibling compliance drift — SHADOW/ATLAS prompts + Hashmark TRUTH_AUDIT. Doctor shows 3 sibling compliance failures: SHADOW and ATLAS have start.md/closeout.md at 'vunknown' (not v3.3), and Hashmark is missing the Overall status line in TRUTH_AUDIT.md. These are cross-repo issues that cannot be fixed directly (CANON-018 prohibits direct cross-repo writes). Shipping Ark cargos with the repair instructions ensures the next session in those repos auto-fixes the drift. Each session of unresolved drift reduces portfolio-level compliance velocity from 100% to 92%. **Recipe:** node scripts/ark.mjs ship --type phantom-blocker-fix --to shadow --payload '{"issue":"start.md/closeout.md not at v3.3","fix":"Run ops onboard --repair --write"}'. Repeat for atlas. Ship separate cargo to hashmark for TRUTH_AUDIT. Verify compliance velocity recovers to 100% in next 24h. |
| cls-margin-hardening | 💡 | speed | 2h | 5 | 5 | 12.5 | · | **cls-margin-hardening** — CLS margin hardening — homepage 0.0815 is 18.5% from budget cliff. Field CLS median for the homepage is 0.0815, with a budget of 0.1 — only 18.5% margin. One layout shift (a lazy-loaded image popping in, a webfont FOUT, a dynamic component injecting before the page settles) can push this over the budget. Adding size attributes to any missing `<img>` tags and reserving space for dynamic content (oracle chips, push prompt, returning-visitor chip) would build in buffer. The LQIP system helps but doesn't cover elements without explicit dimensions. **Recipe:** 1) check-cls-reservations.mjs (new gate): scan HTML for img tags without width/height, dynamic injection sites without min-height. 2) Audit oracle chip container (likely expands when loaded). 3) Add `min-height` reservations for IGNIS tray and returning-visitor strip. 4) Wire gate into smoke. |
| push-subscribe-cta | ⚡ | engagement | 2h | 7 | 6 | 21.0 | · | **push-subscribe-cta** — Push notification subscribe CTA — 0 subscribers needs better placement + copy. 0 push subscribers despite the full VAPID + Worker + subscribe UI infrastructure being live since S211. The CTA placement is likely not visible enough — probably buried below the fold or shown to unqualified visitors. High-engagement moments (after clicking Play on a game, after reading a full journal post, after completing the quiz) are the right trigger points. A contextual push prompt after a signal of genuine interest converts far better than a generic 'Get notified' banner. **Recipe:** 1) Audit current CTA placement in push-subscribe.js and game pages. 2) Add triggered prompt after quiz completion (already shows game result). 3) Add post-scroll trigger on journal posts (after 80% read). 4) Test copy variants: 'Get notified when [game they just played] updates' (personalized). 5) Track conversion in RUM as oracle:push_subscribe_attempt. |
| lighthouse-trend-ci-pushback | ⚡ | speed | 2h | 6 | 5 | 15.0 | · | **lighthouse-trend-ci-pushback** — Lighthouse trend CI push-back — commit trend.json from CI after each run. The lighthouse-trend.json grows locally when `npm run build:check` runs, but CI runs Lighthouse against staging and those scores never update the committed ledger. Cross-session trend is invisible — we can't tell if the S228 defer→idle improved staging scores because there's no historical baseline from CI. Adding a push-back step (CI writes updated trend.json + commits via PAT) closes the loop and makes the Lighthouse gate's history meaningful. **Recipe:** 1) In lighthouse.yml, after trend check: run `node scripts/check-lighthouse-trend.mjs --update` to append current run. 2) `git add .cache/lighthouse-trend.json && git commit -m 'chore(lighthouse): update trend [skip ci]' && git push` using GITHUB_TOKEN or PAT. 3) If GITHUB_TOKEN lacks push perms, founder creates a PAT secret (LIGHTHOUSE_PUSH_TOKEN). |

## Depth Ladder (per item: L1 minimal · L2 solid · L3 genius)

> `/implement` selects the depth that fits remaining budget, and climbs the ladder
> on already-shipped items when the session still has budget (S175 saturation).

### inp-rum-telemetry
- **L1 · minimal** _(~30m)_ — Add `passive: true` to all scroll/touchstart/touchmove listeners in ambient-bundle.js and oracle JS — prevents scroll handler from blocking paint.
- **L2 · solid** _(~2h)_ — PerformanceObserver('event') in ambient-bundle tracking interactions > 150ms → Worker RUM emit. Self-test + allowlist entry. Diagnose top culprit.
- **L3 · genius** _(~6h)_ — Full INP attribution: PerformanceObserver + Long Task observer + Main Thread Attribution API. Dashboard in /studio-pulse/ showing INP by interaction type. Apply scheduler.yield() in oracle answer() and nav toggle handlers based on data.

### changelog-auto-draft
- **L1 · minimal** _(~30m)_ — Manually draft 1 changelog entry from S225–S228 ship items and publish via existing changelog pipeline.
- **L2 · solid** _(~3h)_ — Build scripts/draft-changelog-entry.mjs with honestDark gating + changelog/drafts/ folder. Wire to npm run draft:changelog. Founder approves each draft.
- **L3 · genius** _(~6h)_ — Full pipeline: cron workflow drafts weekly from session data, posts Ark cargo to notify founder, founder-twin pre-screens for quality, one-click publish. Changelog becomes a living product signal.

### cwv-composite-rum
- **L1 · minimal** _(~1h)_ — Add cwvPassRate to rollup-rum-ux deriveSummary(). Only requires all-3-present samples. Log to rum-summary.json.
- **L2 · solid** _(~2h)_ — L1 + surface in build-site-health.mjs + check-perf-budget.mjs display header. Self-test.
- **L3 · genius** _(~4h)_ — L2 + historical CWV pass rate trend (weekly rollup) + studio-pulse tile + AI discovery surface in public-intelligence.json.

### oracle-context-ranking
- **L1 · minimal** _(~1h)_ — In answer(), when sessionQueries.length > 1, prioritize excerpt sources that share domain tags with prior query results.
- **L2 · solid** _(~4h)_ — Full context-aware prefix expansion: session keyword → secondary prefix lookup → merged candidates → score. New RUM event. Self-tested.
- **L3 · genius** _(~8h)_ — L2 + session graph: link queries that share keywords, surface 'You explored X, related: Y' chip. Build oracle session replay for internal debugging.

### build-sha-pre-push
- **L1 · minimal** _(~15m)_ — Add `node scripts/generate-build-sha.mjs` to the closeout autopilot's pre-commit checklist so it always runs before the final commit.
- **L2 · solid** _(~30m)_ — Wire into pre-push hook with auto-stage: runs, git-adds build-sha.json, hooks clean. Works even with --no-amend guard.
- **L3 · genius** _(~1h)_ — L2 + gate in build:check warns when SHA is stale by more than 1 commit (not just missing). Closes the full class of post-push code-commit patterns.

### lighthouse-staging-warmup
- **L1 · minimal** _(~15m)_ — Add curl warmup request to lighthouse.yml before Lighthouse runs. Measure score change in next CI run.
- **L2 · solid** _(~1h)_ — L1 + add check-staging-parity assertion that staging LCP within 2× of field median. Flags when staging diverges structurally from prod.
- **L3 · genius** _(~3h)_ — L2 + dedicated staging health probe in uptime-probe.yml reporting staging vs prod LCP ratio. Alert when ratio > 3×.

### sibling-ark-compliance
- **L1 · minimal** _(~30m)_ — Ship 3 Ark cargos (shadow, atlas, hashmark) with repair instructions. Mark carries as Ark-dispatched.
- **L2 · solid** _(~1h)_ — L1 + add to doctor probe: auto-ship Ark cargo when sibling drift > 7 days (not just detect it).
- **L3 · genius** _(~3h)_ — L2 + compliance velocity guard in CI that blocks push when studio-wide compliance < 95% for > 3 consecutive days. Forces immediate drift resolution.

### cls-margin-hardening
- **L1 · minimal** _(~30m)_ — Audit homepage HTML for img without width/height — add missing size attributes.
- **L2 · solid** _(~2h)_ — L1 + check-cls-reservations.mjs gate for dynamic injection zones (oracle, push prompt, returning-visitor chip). Add min-height reservations.
- **L3 · genius** _(~4h)_ — L2 + RUM CLS breakdown telemetry: log which shift (element, delta, reason) contributed most to the 0.0815. Fix top-3 contributors systematically.

### push-subscribe-cta
- **L1 · minimal** _(~30m)_ — Move push subscribe CTA above fold on /membership/ page. Check if it's currently hidden behind a scroll.
- **L2 · solid** _(~2h)_ — Add post-quiz contextual prompt (triggered by quiz completion event) + post-journal-read prompt (80% scroll). Personalized copy using vs_last_game.
- **L3 · genius** _(~4h)_ — L2 + A/B test 3 CTA variants using RUM signal. Auto-optimize winner after 50 shows each. Track full funnel: shown → clicked → subscribed → retained.

### lighthouse-trend-ci-pushback
- **L1 · minimal** _(~30m)_ — Add --update flag to check-lighthouse-trend.mjs that appends current run to the trend file (no commit yet). Verify the data structure is correct.
- **L2 · solid** _(~2h)_ — L1 + CI commit + push step using secrets.LIGHTHOUSE_PUSH_TOKEN. Wire check that token is present before attempting push (exits 0 if missing).
- **L3 · genius** _(~4h)_ — L2 + trend visualization in /studio-pulse/ showing per-page Lighthouse scores over the last 10 CI runs. Flag any page that has trended down for 3+ runs.

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

- changelog-auto-draft
- inp-rum-telemetry
- oracle-context-ranking
- cwv-composite-rum

## Skipped (Priority < 1.5)

- undefined — PHANTOM: check-vocabulary-consistency + check-decision-currency both green (0 deprecated terms, all decisions current). No action needed.
- undefined — PHANTOM: S227 confirmed pattern '(npm|yarn|pnpm|bun)' already covers all 4 managers (12/12 self-tests). No workflows use setup-node cache:. Already green.
- undefined — RESOLVED: e2e job shows 'success' in latest CI run (2026-06-26T20:21). Playwright tests are PASSING. Compliance job failure was LQIP staleness (fixed this session).
- undefined — SHIPPED: Fixed this session — build-lqip-map.mjs now uses git ls-files to only scan committed images (not gitignored docs/mobile-audit/). Local=CI=201 entries.
- undefined — RESOLVED: Lighthouse CI succeeded. E2E succeeded. Compliance was failing due to lqip-map staleness (fixed this session). All gates green after this push.
- undefined — RESOLVED: Lighthouse CI workflow shows 'success' on 2026-06-26. Trend check: ✓ No regressions. Staging score 0.76 is a staging-server-latency artifact (field LCP median=1108ms, well within 2500ms budget).

