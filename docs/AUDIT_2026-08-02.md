<!-- generated-by: render-audit-md.mjs (R-H14) — DERIVED FROM AUDIT_2026-08-02.json; do not hand-edit -->
<!-- generated-at: 2026-08-03 -->
<!-- project: vaultsparkstudios.github.io · type: website -->

# Project Audit — VaultSpark Studios Website

> One ranked improvement plan covering 9 axes. Top items first.
> Source of truth: this file is derived from the JSON sidecar. Edit the JSON, then re-render.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 15
- Combined Priority: 292.00
- Top 3: command-palette · proof-public-verifier · cwv-truth-and-cls-fix
- Sampled: 12/460 files · 29999 tokens

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov | Priority | Status | Item |
|---|:-:|---|---|:-:|:-:|:-:|:-:|---|
| 1 | 🔥 | ux | 6h | 9 | 7 | 52.5 | · | **command-palette** — Universal command palette (Ctrl+K / ⌘K) — any page to any page in two keystrokes. PRE-VERIFIED: the site has 130 sitemap pages across 60+ sections, a standalone /search/ page, and ZERO keyboard access to it — no keydown handler in the shell, no palette. Elite sites of this scale (Linear, Stripe, Vercel) treat the palette as the navigation spine. Outcome: any visitor (or member) reaches games/join/status/proof in 2 keystrokes; mobile gets the same index via the search icon. This is the single highest-leverage premium-feel + navigation item available. **Recipe:** L2 default: build assets/command-palette.js lazy-loaded on first Ctrl+K/⌘K or search-icon tap; index = sitemap.xml routes + page titles baked at build time into api/site-index.json by a new build step (fits existing generator+gate pattern — wire into build:check per feedback_new_asset_contract_gotchas: RUNTIME_TARGET classes not cssText, sitewide ambient block via propagate-nav.mjs, shell-hash awareness). Fuzzy match client-side, no external deps (CANON-039). Sections grouped by vault vocabulary (GAMES · VAULT · INTELLIGENCE · STUDIO). Respect nonce CSP — no inline handlers. |
| 2 | 🔥 | speed | 4h | 8 | 5 | 31.0 | · | **cwv-truth-and-cls-fix** — Re-baseline Core Web Vitals truth post-RUM-restore, then kill the homepage CLS regression. PRE-VERIFIED: data/rum-summary.json shows 7 total samples in 7 days (RUM ingest was 405 for a month until S300 restored it) and homepage CLS p75 = 0.1842 against a 0.1 budget — a FAILING CWV on the front door, low-sample but matching the known post-paint-widget-injection class. Geo LCP is elite (992ms p75 US, 464 samples) so CLS is the one red vital. Outcome: measured CWV pass-rate becomes trustworthy again and the homepage stops shifting under the visitor's first paint — direct conversion-path quality on the page every funnel starts from. **Recipe:** First verify beacon coverage: confirm every shell page carries the RUM beacon and the SW path uses raw fetch (feedback_sw_rum_beacon_raw_fetch); check sample counts rise daily for 2-3 days via data/rum-history.ndjson. In parallel reproduce homepage CLS with the existing bisect harness (feedback_cls_postpaint_injection_bisect): suspect late-injected widgets (status/pulse strips); reserve space with aspect-ratio/min-height or move injection pre-paint. Gate: rum-summary sufficientRoutes > 0 and homepage cwvPass true once samples ≥50. |
| 3 | 🔥 | featureDepth | 6h | 8 | 8 | 32.0 | · | **proof-public-verifier** — /proof — in-browser public verifier that lets any visitor re-verify the studio live. Carry from Wave D, now ranked: the repo's most under-exploited asset is its evidence apparatus — hash-chained deploy ledger, release proof, identity receipt, evidence graph, staging attestations — all already public JSON under /api/. No studio site on the internet lets a visitor click "Verify" and watch their own browser re-fetch the ledger, re-hash the chain, and compare digests. Outcome: trust → join conversion; also the strongest possible AI-agent signal (agents can verify claims mechanically). Innovation 8: not derivable from any template. **Recipe:** Build /proof/ page: client-side JS re-fetches api/staging-deploy-continuity.json + served NDJSON ledger, recomputes the content-addressed chain with WebCrypto SHA-256, verifies head/depth/predecessor links, and renders green/red per check with plain-language explanations (audience voice — no dev jargon, feedback_ai_content_audience_voice). Same pattern for release-proof hold reasons (honest: shows the current hold and why that is a feature). Link from footer + /status + agents.json. All client-side, zero new endpoints, CSP-safe. |
| 4 | ⚡ | ux | 4h | 6 | 4 | 23.3 | · | **theme-ai-image-test** — CANON-047 theme pass: AI image-test every theme, elevate light mode to first-class. PRE-VERIFIED: a theme toggle exists and [data-theme="light"] appears in style.css, but SOUL demands light mode be a first-class experience and CANON-047 requires AI image-testing every theme (screenshot pages/panels/modals; no sub-WCAG-AA pairs) as a blocking release check. That verification pass has no recent evidence. Outcome (info-finding): a screenshot-verified readability matrix across themes × key pages; any visitor on light mode gets the same premium atmosphere instead of a thinned-out dark-theme inversion. **Recipe:** Playwright screenshot sweep (existing harness; snapshotDir explicit per feedback_playwright_snapshotdir_explicit, waitUntil load per polling pages) across home/games/membership/status/login in each theme; AI-review each image for contrast + atmosphere; fix flagged selectors respecting the body-prefix specificity contract (feedback_theme_selector_specificity). Record the matrix in docs/ and wire into /app-release-gate. |
| 5 | ⚡ | immersion | 8h | 8 | 9 | 21.7 | · | **constellation-hero** — Living Constellation hero — the portfolio as a real-data star map. PRE-VERIFIED: api/constellation-activity.json, ecosystem-state.json and ecosystem-velocity.json already publish live public-safe ecosystem data that nothing renders atmospherically. A canvas star map on the homepage — each project a star (SPARKED bright, FORGE forging, sealed vaults as sigil-only dark stars per the sealed-vault pattern), real activity as pulses — is a signature visual moment no template site has, built entirely from data the site already ships. Outcome: homepage → games/portfolio click-through (each star is a link); visitors feel a living studio, not a brochure. **Recipe:** Single dependency-free canvas module, lazy-loaded after LCP (must NOT regress item 2 — reserve the hero box height first), prefers-reduced-motion honored with a static render, DPR-aware, touch + keyboard accessible (stars are real anchor overlays, canvas is decorative). Data from the existing feeds at build + gentle client refresh. Sealed projects render as unnamed sigils only (feedback_sealed_vault_pattern). |
| 6 | ⚡ | ai | 5h | 7 | 8 | 19.9 | · | **ignis-pulse-strip** — IGNIS live-pulse strip — surface real studio intelligence on the public homepage. PRE-VERIFIED: api/ignis-conduit.json, ignis-platform-status.json, ignis-roi.json and studio-pulse are already deployed public-safe feeds, but the homepage shows none of it — IGNIS integration currently ends at dedicated subpages a visitor never finds. A restrained one-line pulse strip ("IGNIS tracking 36 projects · 8 SPARKED · last ship 2h ago") makes the intelligence plane visible where visitors actually land. Outcome: homepage → /ignis and /studio-pulse click-through; positions the studio as AI-native to both humans and crawling agents. Voice-gated: public copy uses audience voice, no internal enums (feedback_voice_leak_patrol). **Recipe:** Build-time generated strip (no client fetch on critical path) injected via the propagate-nav.mjs ambient-block pattern (feedback_sitewide_ambient_pattern — never per-page tags), reserved height (no CLS), click-through to /ignis. Derive every number from the deployed feeds, never hardcode (feedback_derive_dont_hardcode_public_surfaces); cross-surface coherence gate so the strip and /ignis never disagree (feedback_cross_surface_coherence_gate). |
| 7 | ⚡ | security | 2h | 7 | 5 | 17.5 | · | **obelisk-link-failure-receipt** — Structured receipt on Obelisk link FAILURE — diagnosable first logins. Carry (genius list 92): today a failed identity link logs a code and redirects to ?auth_error=bridge_failed; the member sees a generic failure and the studio learns nothing — at exactly the moment the first real members arrive. Outcome: every first-login failure becomes diagnosable within minutes (privacy-safe: code + plane, zero identifiers), protecting the join → member conversion at its most fragile step. **Recipe:** Worker writes a privacy-safe failure receipt (code, plane, timestamp, no identifiers) to the existing KV/telemetry path; login page renders a specific, human recovery message per code family. Tests assert no identifier can enter the receipt (mutation-test the assertion per feedback_receipt_roundtrip_and_selftest_must_call_real_code). |
| 8 | 🔥 | truth | 3h | 9 | 4 | 15.5 | ✓ | **route-provenance-challenge-detection** — Stop /status publishing a false incident — CF bot-challenge detection in worker-route-provenance. Carry [S301→NEXT][OBS/P0], still open and PUBLISHING A FALSE INCIDENT NOW: api/worker-route-provenance.json reads state:mismatch, 0/5 routes, every observation 403 text/html — the signature of a Cloudflare interstitial, while direct probes return healthy 200/302. The history ledger converts the false reading into a false DURATION. Outcome (info-finding + trust): /status shows true state to visitors and agents; the availability history stops accruing fictional downtime. Ranked despite low innovation because a public trust surface is lying today. **Recipe:** Reuse the isChallenged({status, contentType}) primitive already self-tested in scripts/verify-obelisk-edge-deployment.mjs: 403/503 HTML where JSON/redirect was due → challenged → unverified, never mismatch (D-S300.1, feedback_cf_bot_challenge_not_outage). Then verify build-worker-route-history does not accrue incident duration from challenged observations, and regenerate the five consumer feeds in one cascade (feedback_skipci_publisher_must_cascade_derived_artifacts). Sequence whole-cascade in one session — a half-landed cascade is worse than the finding. |
| 9 | ⚡ | ux | 1h | 4 | 2 | 12.7 | · | **mobile-contracts-reverify** — Re-verify mobile parity contracts + tablet breakpoint sweep. PRE-VERIFIED: CANON-041 parity was attested 2026-07-08 (S268) — ~35 sessions of shipped UI ago, including new login/identity surfaces. The attestation is stale as evidence even if the site is fine (recovery is verification, not restoration). Outcome (info-finding): current pass/fail across the seven structural mobile contracts on iPhone/Android/tablet widths; any regression on the join/login path caught before members hit it. **Recipe:** Run node scripts/check-mobile-contracts.mjs (17 detector self-tests + 7 contracts) + Playwright responsive sweep at 390/768/1024 widths on home, games, membership, login, vault-member. Fix any failures; refresh context/MOBILE_PARITY.md date with evidence. |
| 10 | ⚡ | ai | 2h | 6 | 4 | 12.0 | · | **agents-json-cycle-break** — Break the agents.json build cycle with a static proof-surface reference. Carry [S300][AGENT/P1]: agents.json → proof-surface → status-proof → ai-discovery-health → agents.json; no ordering converges (proven, reorder reverted). The site's primary AI-agent discovery surface cannot honestly update. Outcome: agents.json regenerates deterministically every build, keeping the AI-agent front door current (CANON-048 dual-audience). **Recipe:** Replace the mirrored live verdict inside agents.json with a static reference to the proof-surface URL (the agent fetches the verdict live from the feed instead of trusting a baked copy — which is also MORE honest). One-cascade regen + build:check. |
| 11 | ⚡ | seo | 4h | 6 | 5 | 11.6 | · | **aeo-schema-depth** — Answer-engine depth: FAQ/HowTo/speakable schema + sitewide breadcrumb coverage. PRE-VERIFIED: JSON-LD exists (3 blocks on homepage; BreadcrumbList on games/journal indexes) but coverage is partial — /faq has rich Q&A content with no FAQPage schema audit evidence, no speakable markup for answer engines, and breadcrumb coverage beyond games/journal is unverified. GEO/AEO is where AI-first discovery happens (CANON-048). Outcome (info-finding + SEO): AI answer engines and rich results can cite membership/games/faq answers directly; measurable via Search Console rich-result reports. **Recipe:** Generator-owned (feedback_generator_owns_full_head_contract): extend the page generators to emit FAQPage on /faq, BreadcrumbList sitewide, speakable on journal posts + faq; add a build:check contract counting schema types per page class so coverage can never silently regress. Verify with Google Rich Results test on 3 representative pages. |
| 12 | 💡 | process | 3h | 6 | 4 | 10.3 | · | **context-meter-honest-gauge** — context-meter.mjs measures a heuristic, claims a session — make it honest or rename it. Carry (genius #1, 96): it reported 1.5%-used · CONTINUE while the live session was near exhaustion, because it measures fresh-session bootstrap cost, not the session it gauges. Same class as verifying a declaration instead of the condition. Outcome (info-finding): the start/closeout gate stops issuing false greens that can walk a session off a cliff mid-implementation. **Recipe:** Either read a real signal (transcript size when the harness exposes it) or rename output to 'bootstrap-cost estimate' with verdict semantics that never claim session state; update the two consumers (start gate, brief renderer). Honest-dark default when unmeasurable (feedback_honest_dark_status_publishing). |
| 13 | 💡 | process | 2h | 6 | 3 | 9.0 | · | **post-push-ci-confirmation** — Post-push CI confirmation — closeout verifies the remote run, not the exit code. Carry (genius 96): landed ≠ verified — [skip ci] anywhere in a commit body skips ALL workflows, and a push whose CI never ran reads as green locally (feedback_skip_ci_token_in_commit_body_skips_everything, feedback_closeout_push_verification). Outcome (info-finding): every closeout ends with a confirmed remote CI verdict or an honest 'unverified' — no silent skips. **Recipe:** Closeout step: gh run list --commit <sha> after push; assert a workflow actually started and record conclusion (or honest pending) into the handoff. Guard against the [skip ci]-tip stranding CF Pages (add deploy-trigger commit when needed). |
| 14 | 💡 | security | 3h | 7 | 3 | 9.0 | · | **internal-paths-second-origin** — Close the edge + GitHub Pages second origin for internal paths. Carry [S300→NEXT][AGENT/P1]: origin pruning works (CF Pages 404s /logs/, /context/, /scripts/) but the APEX still serves them 200 via stale URL-keyed edge copies + GitHub Pages publishing the branch verbatim as a second, unpruned origin. Internal docs remain publicly fetchable. Outcome: internal operational surface is not served to the public or to crawlers; verified by live 404 probes at the apex. GH-Pages disable/re-point is founder-scoped (it is the documented warm rollback origin, D-S289.8) — agent half is the targeted purge + verification. **Recipe:** Targeted purge-by-URL for the identified stale URLs with post-purge eviction VERIFICATION (never trust the API success flag); document the GH-Pages decision (pruned publish branch vs disable) as a founder escalation with both options costed. |
| 15 | 💡 | security | 5h | 8 | 5 | 14.2 | ⛔ | **identity-link-table** — public.obelisk_identity_link — kill the login scan cliff (founder-gated design, approved direction). Carry [S301→NEXT][SEC/P1] with the corrected design already recorded (D-S301.10): per-login admin scans hit a hard cliff at ~2,000 accounts where EVERY login fails closed; live headroom 1,748. The link table (obelisk_sub PK, user_id unique, inserted before the metadata write) supplies the uniqueness auth denies us and an indexed lookup, killing both full table walks. Outcome: login capacity ceases to be a scaling cliff before real member growth reaches it. Auth-flow change → founder escalation is part of the item, not a reason to skip ranking it. **Recipe:** Escalate with the recorded design for sign-off, then: migration via management API (pre-image captured), worker rewiring with fallback to the existing scan, self-healing orphan-row semantics tested, live verification through verify-supabase-runtime.mjs so evidence is machine-produced. |

## Depth Ladder (per item: L1 minimal · L2 solid · L3 genius)

> `/implement` selects the depth that fits remaining budget, and climbs the ladder
> on already-shipped items when the session still has budget (S175 saturation).

### command-palette
- **L1 · minimal** _(~3h)_ — Palette overlay wired to existing /search index, desktop Ctrl+K only, static route list from sitemap.xml.
- **L2 · solid** _(~6h)_ — Full build-generated api/site-index.json (titles+descriptions), fuzzy match, recent-pages memory (localStorage), mobile entry via nav search icon, theme-safe styling in all themes.
- **L3 · genius** _(~10h)_ — L2 + action verbs ("join", "verify us", "status"), member-aware entries when signed in (rank page, portal), and IGNIS-fed "live now" section from public api feeds.

### cwv-truth-and-cls-fix
- **L1 · minimal** _(~2h)_ — Beacon-coverage check + reserve-space CSS fix for the known injected homepage widgets.
- **L2 · solid** _(~4h)_ — L1 + bisect-harness confirmation, INP hover-pollution guard re-check, and a build:check contract asserting reserved dimensions on injected blocks.
- **L3 · genius** _(~6h)_ — L2 + per-route CWV badge on /status fed by rum-summary once sufficient, closing the loop publicly (Observability Honesty, CANON-031).

### proof-public-verifier
- **L1 · minimal** _(~3h)_ — Static /proof page listing live proof feeds with one-click raw links and explanations; no client-side recompute yet.
- **L2 · solid** _(~6h)_ — L1 + WebCrypto chain re-verification in the visitor's browser with per-check pass/fail UI.
- **L3 · genius** _(~9h)_ — L2 + shareable verification permalink (?verified=<head>) and an agents.json capability entry advertising machine verification steps.

### theme-ai-image-test
- **L1 · minimal** _(~2h)_ — Screenshot sweep + manual review of the 5 key pages in dark and light; fix any contrast failures found.
- **L2 · solid** _(~4h)_ — L1 + modals/drawers/panels states, documented matrix, release-gate wiring.
- **L3 · genius** _(~7h)_ — L2 + a third signature theme (e.g. 'Vault Ember' from the brand kit) shipped only if it passes the same gate.

### constellation-hero
- **L1 · minimal** _(~4h)_ — Static generated SVG constellation from ecosystem-state.json, linked stars, no animation.
- **L2 · solid** _(~8h)_ — Canvas with slow drift + activity pulses, reduced-motion fallback to L1 SVG, mobile-tuned density.
- **L3 · genius** _(~12h)_ — L2 + constellation deep-view on /universe (dir exists) with per-star lore panels fed by the projects catalog.

### ignis-pulse-strip
- **L1 · minimal** _(~2h)_ — Static build-time strip on homepage only, three derived facts, linked.
- **L2 · solid** _(~5h)_ — L1 + sitewide ambient placement, coherence gate, gentle client-side refresh with reserved layout.
- **L3 · genius** _(~8h)_ — L2 + per-page contextual intelligence (games pages show that game's live signals from its project feed).

### obelisk-link-failure-receipt
- **L1 · minimal** _(~1h)_ — Map auth_error codes to specific user-facing recovery copy on /login.
- **L2 · solid** _(~2h)_ — L1 + privacy-safe server-side failure receipt with identifier-free guarantee test.
- **L3 · genius** _(~4h)_ — L2 + failure-rate signal in the identity receipt feed so a spike is visible on the ops side.

### route-provenance-challenge-detection
- **L1 · minimal** _(~3h)_ — Challenge detection in the builder + history-accrual guard + full consumer cascade regen.
- **L2 · solid** _(~4h)_ — L1 + self-test matrix covering challenged/mismatch/ok per route type.
- **L3 · genius** _(~5h)_ — L2 + a shared lib extraction so every prober in the repo classifies challenges identically (Ark pattern-share to studio-ops).

### mobile-contracts-reverify
- **L1 · minimal** _(~1h)_ — Contract run + attestation refresh; file fixes only if red.
- **L2 · solid** _(~3h)_ — L1 + visual screenshot sweep on 3 widths × 2 themes with fixes.
- **L3 · genius** _(~5h)_ — L2 + add a tablet-specific contract (768-1024 range currently untested by the seven).

### agents-json-cycle-break
- **L1 · minimal** _(~2h)_ — Static URL reference swap + cascade regen + gate green.
- **L2 · solid** _(~3h)_ — L1 + agents.json capability entries for /proof verification and the command-palette site index.
- **L3 · genius** _(~5h)_ — L2 + .ai/ companion pages for remaining top-level sections (games already have them).

### aeo-schema-depth
- **L1 · minimal** _(~2h)_ — FAQPage + sitewide BreadcrumbList via generators + coverage gate.
- **L2 · solid** _(~4h)_ — L1 + speakable, VideoGame schema on game pages, Organization sameAs completion.
- **L3 · genius** _(~6h)_ — L2 + llms.txt per-section deep links and freshness stamp wired to the build.

### context-meter-honest-gauge
- **L1 · minimal** _(~1h)_ — Rename + honest verdict semantics + consumer updates.
- **L2 · solid** _(~3h)_ — L1 + attempt real measurement from available session artifacts with unverified fallback.
- **L3 · genius** _(~4h)_ — L2 + Ark pattern-share since every studio repo runs the same meter.

### post-push-ci-confirmation
- **L1 · minimal** _(~1h)_ — gh-run assertion in closeout with honest-pending recording.
- **L2 · solid** _(~2h)_ — L1 + CF Pages deploy-tip check (check-deploy-tip pattern).
- **L3 · genius** _(~3h)_ — L2 + failure triage note auto-drafted into next session's board (new-regression vs pre-existing debt split).

### internal-paths-second-origin
- **L1 · minimal** _(~2h)_ — Targeted purge + verified eviction for known internal paths; probe matrix committed.
- **L2 · solid** _(~3h)_ — L1 + recurring probe in the uptime cron so regression is an alarm, not a discovery.
- **L3 · genius** _(~5h)_ — L2 + pruned publish-branch implementation once founder decides the rollback-origin question.

### identity-link-table
- **L1 · minimal** _(~1h)_ — Founder escalation doc with design, rollback, and blast radius — unblocks the rest.
- **L2 · solid** _(~5h)_ — Full implementation post-approval: table + worker rewiring + fallback + machine-produced evidence.
- **L3 · genius** _(~7h)_ — L2 + daily check-obelisk-link-readiness cron once SUPABASE_ACCESS_TOKEN lands as an Actions secret (separate founder precondition).

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

- constellation-hero
- ignis-pulse-strip
- proof-public-verifier

## Skipped (Priority < 1.5)

- wave-c-page-consolidation — Premise re-verified as still valid (3 membership pages, /leaderboards vs /vault-wall duplication) but the surfaces are SENSITIVE-classified — they cannot ride the content lane and are sequenced AFTER promotion by explicit board decision. Ranking it now would invite a half-landed auth-adjacent change.
- real-provider-e2e — External: blocked on Obelisk shipping /auth/revoke (D-S302.5); our half is shipped. Plus one founder sign-in to prove client registration. Not agent-schedulable.
- lcp-optimization — False premise for further work: geo-vitals shows LCP p75 992ms US at high confidence (464 samples) — well inside the 1.8s bar. Spending here would be waste; CLS is the real red vital (item 2).
- mega-menu-rebuild — Navigation depth is real but the command palette (item 1) addresses it at higher leverage without touching the attested mobile drawer contracts; a nav rebuild would re-open seven structural mobile contracts for marginal gain.

## Execution Log

### route-provenance-challenge-detection
- `2026-08-03T00:04:20.783Z` · **shipped** · L2+. Shared lib vantage-challenge.mjs; provenance state unverified-before-mismatch; history refuses unverified by state; /status renders neutral. Live probe 5/5 matched. Tests 9/9+44/44+19/19. Commit 6c228ad17.

---
*Session 303 full-site premium audit. Weights: ux 2.5 · speed 2 · featureDepth 1.5 (website profile). Every premise pre-verified against live files/feeds this session; three prior-plan premises corrected (LCP already elite; RUM starved not broken; theme toggle exists but unverified against CANON-047). Sequencing: 8 (truth, false incident live) → 2 → 1 → 3 → 6 → 5 → 4 → 9 → 10 → 11 → 7 → 12 → 13 → 14 → 15-escalation.*
