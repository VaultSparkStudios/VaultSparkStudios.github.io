# Audit 2026-07-07 S267 — RUM Field Truth Arc

## Ranked Plan

1. **[OBS/P1] RUM field-vitals visibility contract — shipped**
   - Premise verified: `check-perf-budget --source=rum` treated `/` field p75 LCP as authoritative at 11,592ms while `analyze-home-lcp --check` reported a 192ms local LCP artifact. The raw rollup also counted many all-null or zero-vital rows, creating enough apparent samples to make thin field data look authoritative.
   - Fix: `assets/rum-beacon.js` now sends privacy-safe visibility/navigation context (`startedVisible`, `visibilityState`, `navigationType`, `activationStart`, `pageShowPersisted`, `pageAgeMs`) with the route-level vitals payload.
   - Outcome: future RUM rows carry enough source context to distinguish normal page-load vitals from background-tab, prerender/restored, or otherwise unusable samples.

2. **[OBS/P1] Worker RUM storage preserves the new context — shipped**
   - Premise verified: `cloudflare/security-headers-worker.js` sanitized and stored only connection/save-data/viewport/theme context, so downstream rollups could not reject bad-vitals contexts even if the client knew them.
   - Fix: Worker ingest now stores the new bounded context fields without IDs, query strings, free text, or personal data. Legacy clients store `startedVisible:null` rather than false, avoiding accidental over-filtering while cache rolls forward.
   - Outcome: source-of-truth rows remain public-safe and become filterable by the rollup.

3. **[OBS/P1] RUM rollup excludes unusable vitals samples — shipped**
   - Premise verified: `data/rum-history.ndjson` contained route-day rows with all-null vitals and inflated sample counts. Current 7-day summary dropped from 274 apparent samples / 2 sufficient routes to 27 usable samples / 0 sufficient routes after filtering.
   - Fix: `scripts/rollup-rum.mjs` now excludes rows with no usable vitals, explicit hidden-start context, bfcache restores, nonzero `activationStart`, and `back_forward`/`prerender` navigation types. Self-test covers hidden/restored exclusion.
   - Outcome: `check-perf-budget --source=rum` no longer fabricates field authority from weak samples; it honestly falls back to synthetic/advisory until enough valid field rows accrue.

4. **[PERF/P1] Homepage LCP root-fix — deferred honestly**
   - Premise verified: after the truth fix, there is no longer sufficient field evidence to justify a homepage rendering change. Local LCP proof remains fast, and the old field overage was not trustworthy enough to drive a CSS/asset intervention.
   - Decision: do not ship a cosmetic performance patch. Let the corrected beacon accumulate valid samples, then re-run RUM pull/rollup before claiming a field performance issue or win.

## Execution Log

| Item | Outcome | Evidence |
|---|---|---|
| RUM field-vitals visibility contract | shipped | `assets/rum-beacon.js` emits bounded visibility/navigation context; ambient core bundle regenerated. |
| Worker RUM context storage | shipped | `cloudflare/security-headers-worker.js` stores bounded context fields and preserves unknown legacy state as `null`. |
| RUM rollup usability filter | shipped | `rollup-rum --self-test` covers hidden/restored rows; `data/rum-summary.json` now reports 27 usable samples, 0 sufficient routes. |
| Homepage LCP root-fix | deferred | `check-perf-budget --source=rum` falls back to synthetic with 0 over-budget groups; `analyze-home-lcp --check` remains 192ms. |

## Remaining Gated Work

- Corrected RUM beacon must deploy and accrue enough valid samples before field LCP/INP conclusions are re-opened.
- Football GM INP remains soak/owning-path gated; do not edit a sibling repo from this tree.
- TT enforcement remains amber until warm rows age out or fresh reports prove near-zero readiness plus founder-device verification.
- Founder/content decisions remain gated: forge devlogs and richer public IGNIS exposure.
