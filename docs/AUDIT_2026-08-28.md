<!-- generated-by: render-audit-md.mjs (R-H14) — DERIVED FROM AUDIT_2026-08-28.json; do not hand-edit -->
<!-- generated-at: 2026-08-28 -->
<!-- project: vaultsparkstudios-website · type: website -->

# Project Audit — VaultSpark Studios Website

> One ranked improvement plan covering 9 axes. Top items first.
> Source of truth: this file is derived from the JSON sidecar. Edit the JSON, then re-render.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 3
- Combined Priority: 72.40
- Top 3: external-destination-reachability · attention-pressure-rollup · cta-evidence-age
- Sampled: 11/574 files · 30000 tokens

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov | Priority | Status | Item |
|---|:-:|---|---|:-:|:-:|:-:|:-:|---|
| 1 | 🔥 | ux | 3h | 9 | 6 | 27.2 | ✓ | **external-destination-reachability** — Sample canonical product destinations without letting network uncertainty lie. PRE-VERIFIED: audit-site-links validates host shape and internal existence but never tests whether registry-owned external product destinations answer; S331 fixed several real stale destinations manually. Outcome: visitors reach the intended product in one click, while release reviewers can distinguish a confirmed dead target from an unavailable probe instead of interpreting silence as health. **Recipe:** Extend the import-safe link court with a bounded canonical-destination sampler sourced only from registry-owned external product links; classify each observation as passed, failed, or unknown; require repeated deterministic failure before a dead verdict; make network unavailability explicit and non-passing; write a public-safe aggregate receipt with no visitor data; add self-tests for redirects, timeouts, transient 5xx, and confirmed dead destinations; keep the deterministic build court offline by validating receipt shape and freshness rather than performing live HTTP. |
| 2 | 🔥 | feedback | 3h | 9 | 7 | 25.1 | ✓ | **attention-pressure-rollup** — Publish privacy-thresholded interruption pressure by surface and visit depth. PRE-VERIFIED: VSAttention emits an in-browser vs:attention-claimed event, but no RUM listener consumes it and rollup-rum-ux has no attention family. Outcome: the studio can tell whether the one-surface budget reduces interruptions for new and returning visitors without storing a browser history, while a sparse cohort remains honestly dark. **Recipe:** Emit one allowlisted attention:claimed event through the existing /v/rum path with a bounded label composed from a fixed surface enum and coarse visit-depth bucket; reject arbitrary labels at the Worker; aggregate only day/surface/depth counts from committed RUM history; suppress every cohort below the existing privacy floor; publish a public-safe attention-pressure receipt and status projection; add positive/negative self-tests proving no route, identifier, session key, or free text reaches the artifact. |
| 3 | ⚡ | speed | 1h | 7 | 4 | 20.1 | ✓ | **cta-evidence-age** — Make CTA readiness name the age of the evidence behind it. PRE-VERIFIED: check-cta-readiness reports observedThrough and correctly abstains when no post-epoch span exists, but a frozen source reads like a merely young window rather than stale evidence. Outcome: operators immediately know whether a conversion decision lacks time or lacks fresh observations, reducing false waits and making the next release action findable. **Recipe:** Derive an evidenceAgeDays value from the committed source asOf date, keep wall-clock age out of byte-reproducibility comparisons, render a distinct stale-evidence explanation when the source date falls beyond the declared window, and add self-tests for current, young, stale, and absent evidence. |

## Depth Ladder (per item: L1 minimal · L2 solid · L3 genius)

> `/implement` selects the depth that fits remaining budget, and climbs the ladder
> on already-shipped items when the session still has budget (S175 saturation).

### external-destination-reachability
- **L1 · minimal** _(~1h)_ — Add pure three-state classification helpers and a small explicit destination set.
- **L2 · solid** _(~3h)_ — L1 plus bounded live sampling, public receipt, freshness check, self-tests, and release integration.
- **L3 · genius** _(~6h)_ — L2 plus append-only reachability history and change-risk prioritization.

### attention-pressure-rollup
- **L1 · minimal** _(~1h)_ — Wire the bounded claim event into the existing RUM transport and allowlist.
- **L2 · solid** _(~3h)_ — L1 plus privacy-thresholded rollup, public receipt, status projection, and regression tests.
- **L3 · genius** _(~6h)_ — L2 plus pre/post release pressure comparison and an abstaining trend history.

### cta-evidence-age
- **L1 · minimal** _(~1h)_ — Add the source-derived age and four-state self-test coverage.
- **L2 · solid** _(~2h)_ — L1 plus public CTA surface copy and status projection.
- **L3 · genius** _(~4h)_ — L2 plus age trend history and alert routing.

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

- counterfactual-attention-collision-receipt
- destination-dependency-change-risk
- release-evidence-latency-map

## Skipped (Priority < 1.5)

- production-promotion — HARD SECURITY GATE: check-production-promotion-gate reports real-provider-e2e-pending; obelisk.identity.verify is missing OBELISK_RP_ID, OBELISK_RP_NAME, and OBELISK_RP_ORIGIN; the upstream obelisk-staging-registration receipt is absent; and the remaining journey requires a physical founder passkey. Deployment authorization does not constitute journey evidence.
- security-posture-repair — FALSE PREMISE / WIN: the committed posture remains 8/8 and no source evidence supports widening or replacing the auth flow.
- attention-presentation-redesign — FALSE PREMISE / WIN: S331 rendered 42/42 desktop/mobile/theme cells with zero defects; the open gap is aggregate outcome evidence, not another visual redesign.
- full-network-link-build-gate — REJECTED: making every external response a deterministic build dependency would turn transient network uncertainty into CI noise. The selected design samples only registry-owned destinations and publishes explicit passed/failed/unknown states.

## Execution Log

### external-destination-reachability
- `2026-08-28T05:45:00.000Z` · **shipped** · Shipped a bounded sampler over the public registry projection, an offline freshness/shape court, a four-hour refresh lane, public status projection, and a status-proof/evidence-graph edge. The first live receipt reports 10/12 passed, 0 failed, and 2 unknown; 401 and repeated 503 observations remain unknown rather than green or dead.

### attention-pressure-rollup
- `2026-08-28T05:25:00.000Z` · **shipped** · Shipped fixed-vocabulary post-consent claim telemetry, edge rejection of arbitrary labels, privacy-floor aggregation, a public-safe receipt, status projection, and build/status-proof integration. Cookie consent remains deliberately excluded.

### cta-evidence-age
- `2026-08-28T05:05:00.000Z` · **shipped** · Shipped current/aging/stale/absent evidence-age states in the readiness court and a browser-derived public status tile. Wall-clock age remains outside byte-reproducibility comparisons while the committed observedThrough source stays covered.

---
*S332 nine-axis website audit prioritizes the founder-authorized full deployment without weakening the identity gate. Every emitted item names a conversion or information-finding outcome, reuses existing RUM/link/CTA infrastructure, adds no dependency, and preserves privacy floors. Three novel reserve candidates were generated but not emitted because the carried, premise-verified items compound the current release truth more directly.*
