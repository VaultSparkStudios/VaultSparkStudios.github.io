<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-05-26 -->
<!-- project: vaultsparkstudios-github-io · type: platform · session: S164 -->

# Project Audit — VaultSparkStudios.github.io

> One ranked improvement plan covering 9 axes. Top items first.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: **4**
- Combined Priority: **150.6**
- Top 3: `lazy-command-palette-split` · `nav-sheet-stats-rollup` · `rum-readiness-signal`
- Axis weights (platform): Security 2× · Speed 2× · UX 1.5× · Feedback 1.5×

**Personalization.** S163 closed the RUM → perf-budget loop and produced the first concrete ambient split shortlist. This audit converts yesterday's evidence into runtime and decision-loop wins: lazy-load the largest always-shipped ambient module, create the missing mobile-sheet aggregate, and add guards so the win does not silently regress. Trusted Types enforce and mobile-sheet default remain evidence-gated.

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | 🔥 | speed | 2h | 8 | 8 | 64.0 | **lazy-command-palette-split** — Lazy-load `command-palette.js` behind first search intent instead of parsing it on every page. **Recipe:** add `assets/command-palette-loader.js`, remove the heavy palette from `AMBIENT_SOURCES`, replay open after script load, and guard the split. |
| 2 | ⚡ | ux | 2h | 7 | 7 | 49.0 | **nav-sheet-stats-rollup** — Build `/api/nav-sheet-stats.json` from privacy-minimized RUM `ux` events so the mobile nav default-swap has an aggregate decision signal. **Recipe:** new `scripts/build-nav-sheet-stats.mjs` with `--from`, `--check`, and `--self-test`; wire build gates. |
| 3 | ⚡ | feedback | 1h | 6 | 6 | 45.4 | **rum-readiness-signal** — Expose sample sufficiency and `strictReady` so `/start` and closeout can see whether the field gate is ready to flip. **Recipe:** write a small public-safe readiness block beside the stats artifact. |
| 4 | 💡 | security | 1h | 5 | 5 | 25.3 | **ambient-split-regression-gate** — Fail the build if the heavy palette returns to the ambient critical bundle. **Recipe:** extend coverage/check logic to allow only the loader in ambient sources. |

## Innovation reserve (Innovation ≥ 8 — execute even when busy)

- `lazy-command-palette-split`

## Sequencing note for /implement

Ship `lazy-command-palette-split` and `ambient-split-regression-gate` together because they touch the same builder/check surface. Then ship `nav-sheet-stats-rollup`; fold `rum-readiness-signal` into the same JSON artifact if it stays simpler than a second file.

---
*Generated by `/audit` v1.0 · ready for `/implement`*

## Execution Log (S164 /implement)

Sequenced order shipped. **4 of 4 shipped end-to-end · 0 deferred.** `npm run build:check` exit 0. Focused local browser proof: `npx playwright test tests/ambient-bundle-integrity.spec.js --project=chromium` against `http://127.0.0.1:4173` passed 4/4.

| # | slug | result | note |
|---|---|:-:|---|
| 1 | lazy-command-palette-split | ✅ shipped | `assets/command-palette-loader.js` is now the ambient source; full palette injects only after Cmd/Ctrl+K/mobile search intent. Ambient raw bundle is 124.2 KB after the split. |
| 2 | nav-sheet-stats-rollup | ✅ shipped | `scripts/build-nav-sheet-stats.mjs` writes `api/nav-sheet-stats.json`; self-test 5/5; current artifact has 0 opens and `defaultSwapReady:false` because no raw RUM export is present. |
| 3 | rum-readiness-signal | ✅ shipped | Nav-sheet artifact includes a public-safe `readiness` block with `minOpens`, sufficiency, close rates, and `defaultSwapReady`; RUM summary remains honest at 0 samples. |
| 4 | ambient-split-regression-gate | ✅ shipped | `report-ambient-coverage --check` now fails if `assets/command-palette.js` returns to `AMBIENT_SOURCES`; focused Playwright test verifies lazy injection and visible palette. |
