<!-- generated-by: render-audit-md.mjs (R-H14) — DERIVED FROM AUDIT_2026-06-14.json; do not hand-edit -->
<!-- generated-at: 2026-06-14 -->
<!-- project: unknown · type: unknown -->

# Project Audit — Unknown

> One ranked improvement plan covering 9 axes. Top items first.
> Source of truth: this file is derived from the JSON sidecar. Edit the JSON, then re-render.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 3
- Combined Priority: 0.00
- Top 3: —

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov | Priority | Status | Item |
|---|:-:|---|---|:-:|:-:|:-:|:-:|---|
| 1 | transformative | growth | 3.5h | 9 | 7 | 25.6 | ✓ | **og-bespoke-png-cards** — Bespoke per-title OG cards at build time with the sharp we already own — closing the one S195 deferral by disproving its premise. VaultSpark's entire growth model is that a shared link sells the studio before a human ever clicks. S194 found 73 pages were shipping blank OG images and bandaged it by repointing them at static PNGs — but most pages now share one generic og-image.png, so a link to Call of Doodie looks identical to a link to the privacy policy. S195 deferred the real fix citing a native-dependency / Windows-build risk that simply isn't real: sharp@0.34.5 is already a trusted devDependency and I verified it rasterizes the existing og-image-worker.js SVG template straight to a 1200x630 PNG on this machine. A reject-of-the-deferral is itself the win here — there is no new supply-chain surface to clear. **Recipe:** Extract renderSvg() from cloudflare/og-image-worker.js into a shared pure module (lib/og-template.mjs). New scripts/build-og-cards.mjs: walk public pages, reuse update-og-images.mjs getMeta() to derive {title,eyebrow,status}, render SVG, pipe through sharp(Buffer.from(svg)).png() -> assets/og/<slug>.png; rewrite each page's og:image + twitter:image to its bespoke PNG. THEN neutralize the footgun: the stale update-og-images.mjs still repoints OG at the dead /_og/ SVG worker (running it re-breaks everything S194 fixed) and the worker's source comment still falsely claims 'social platforms all rasterize SVG fine' — guard/retire the script, correct the comment. check-og-images.mjs already gates SVG/missing so it validates the result for free. |
| 2 | high | seo | 1.5h | 7 | 5 | 19.3 | ✓ | **collection-schema-listing** — Blog + ItemList schema on the listing pages — the true remaining JSON-LD rung (Article-on-entries is already done). The TASK_BOARD carry reads 'ARTICLE-SCHEMA-JOURNAL' but ground-truth says all 10 journal entries ALREADY carry Article/BlogPosting schema — so naively 'adding Article schema' would be redundant work, the exact S186 trap. The genuine gap is one level up: journal/, journal/dispatches/, journal/archive/ and changelog/ are collection pages with zero Blog/ItemList/CollectionPage schema, so a crawler or LLM sees a pile of links instead of a structured publication with N dated posts. Wrapping the listings tells Google and answer-engines 'this is a Blog, here are its 10 posts in order' — the structured-understanding rung above the breadcrumbs S195 just completed. **Recipe:** Add a Blog node (with blogPost/itemListElement -> each entry's url+headline+datePublished, derived from the existing journal entries' own Article schema so there's a single source of truth) to journal/index.html; add ItemList to journal/dispatches/ + journal/archive/ + changelog/. Reuse the assets/schema-injector.js named-policy pattern (no inline scripts, TT-safe). Extend the JSON-LD coverage gate already folded into check-proof-surface so a listing page that loses its collection schema fails build:check. |
| 3 | nice-to-have | ecosystem | 24m | 5 | 4 | 15.9 | · | **ark-dead-gtag-pattern-share** — Broadcast the dead-gtag silent-no-op pattern to the CF-Pages fleet via Studio Ark. The same class of bug bit this site TWICE in two sessions: S194's funnel and S195's onboarding tour both fired events through a window.gtag that was removed site-wide at S147 — a permanent silent no-op hidden beside a working beacon. Any sibling repo that also dropped Google Analytics but kept gtag('event',...) call-sites is silently losing the same data right now. CANON-018 says cross-repo intelligence rides the Ark; this is a 20-minute broadcast that could save sibling sessions days of phantom 'why is conversion flat' debugging. **Recipe:** node scripts/ark.mjs ship --type pattern-share --to '*' --payload with the pattern ('events fired through a removed window.gtag are a permanent silent no-op'), the detection recipe (grep gtag( call-sites, verify the global is DEFINED not just called), and the fix recipe (rewire to the /v/rum beacon under a bounded prefix family + add a build-time emit-vs-allowlist gate). Tag with ['analytics','silent-drop','cf-pages','gtag']. |

## Depth Ladder (per item: L1 minimal · L2 solid · L3 genius)

> `/implement` selects the depth that fits remaining budget, and climbs the ladder
> on already-shipped items when the session still has budget (S175 saturation).

### og-bespoke-png-cards
- **L1 · minimal** _(~1.5h)_ — Generate bespoke PNGs for the high-traffic surfaces only (homepage, /membership/, the 2 live games, /studio/) + neutralize the stale update-og-images.mjs footgun and fix the worker's false comment. Everything else keeps its S194 static PNG.
- **L2 · solid** _(~3.5h)_ — Full build-og-cards.mjs across all ~115 public pages, wired into npm run build, idempotent (hash-skip unchanged), with a --check self-test folded into the check-proof-surface orchestrator (zero cmd.exe-chain growth).
- **L3 · genius** _(~8h)_ — Add per-status visual variants + a deploy-time freshness gate that regenerates only pages whose og:title changed, and emit ImageObject/primaryImageOfPage JSON-LD pointing at the bespoke card so the OG image is also a structured-data citation asset for LLMs.

### collection-schema-listing
- **L1 · minimal** _(~30m)_ — Blog + ItemList on journal/index.html only (the canonical hub) — biggest single SEO surface, no gate change.
- **L2 · solid** _(~1.5h)_ — Collection schema on all 4 listing pages + extend the coverage gate to enforce it.
- **L3 · genius** _(~3h)_ — Generate the itemListElement array at build time from the journal entries' front-matter so a new post auto-registers in the Blog node, plus add Speakable SpeakableSpecification to the entry pages for voice-assistant readout.

### ark-dead-gtag-pattern-share
- **L1 · minimal** _(~12m)_ — Ship the pattern-share cargo with the one-line pattern + detection grep.
- **L2 · solid** _(~24m)_ — Full payload with pattern + detection + /v/rum rewire recipe + tags, addressed to the fleet.
- **L3 · genius** _(~1h)_ — Also ship a ready-to-run check-dead-gtag.mjs gate script as cargo so siblings can drop it straight into their build:check.

## Skipped (Priority < 1.5)

- faqpage-schema — Verified ALREADY DONE — FAQPage/Question schema is live on faq/, contact/, and all 8 game pages. A save, not a miss.
- article-schema-journal-entries — Verified ALREADY DONE — all 10 journal entries carry Article/BlogPosting schema. The real gap (collection schema on listings) is promoted to item 2.
- doctor-launch-readiness-red — Verified NON-WEBSITE — the doctor ⛔ is the SPARKED launch-readiness probe failing on 2 SIBLING-scoped blockers (veilos drift + orphaned codex locks in other repos), root-caused S193. CANON-018 forbids the cross-repo writes to fix them here; all 11 website-owned probes are green. Orphan locks auto-clear on those repos' next /start.
- og-rasterizer-via-cf-image-resizing — Considered and rejected — Cloudflare Image Resizing (/cdn-cgi/image/) would rasterize the SVG at the edge with zero deps, but it is a paid/plan-gated feature and CANON-029 forbids variable cost on the free tier. The sharp build-time path (item 1) achieves the same outcome with no runtime cost.

## Execution Log

### og-bespoke-png-cards
- `function at() { [native code] }` · **?** · 

### collection-schema-listing
- `function at() { [native code] }` · **?** · 

### ark-dead-gtag-pattern-share
- `function at() { [native code] }` · **?** · 

