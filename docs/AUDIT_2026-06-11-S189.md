<!-- generated-by: render-audit-md.mjs (R-H14) — DERIVED FROM AUDIT_2026-06-11-S189.json; do not hand-edit -->
<!-- generated-at: 2026-06-12 -->
<!-- project: unknown · type: unknown -->

# Project Audit — Unknown

> One ranked improvement plan covering 9 axes. Top items first.
> Source of truth: this file is derived from the JSON sidecar. Edit the JSON, then re-render.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 5
- Combined Priority: 0.00
- Top 3: —

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov | Priority | Status | Item |
|---|:-:|---|---|:-:|:-:|:-:|:-:|---|
|  | fire | feedback |  | 9 | 9 | 31.3 | · | **funnel-conversion-rollup** — . This is the same silent-drop class as the S186 RUM bug, one layer deeper: the names are allowlisted and emitted, but nothing aggregates them, so the data dies in the rollup. Closing it answers the only question that decides whether S186-S188 paid off: which funnel step leaks. Build it honest-dark (matches the S178 field-win pattern) so it lands now and lights up as CI accrues samples, and prove the aggregation logic this session with a synthetic-fixture self-test even if live counts are sparse. **Recipe:** Add scripts/rollup-rum-ux.mjs (self-test on synthetic samples) that reads the SAME raw pulled sample dir as rollup-rum.mjs, aggregates ux event counts by name per day, writes data/rum-ux-history.ndjson + public-safe api/funnel-summary.json (anonymous aggregate COUNTS only — no email/PII, cost-neutral per CANON-029, honest-dark when total<N). Chain it into the npm run rum:pull sequence after rollup-rum. Add a /status/ 'Conversion funnel' tile rendering visit -> dispatch-shown -> dispatch-subscribe -> proof-line-click -> membership-intent as a step-leak bar. Add scripts/check-funnel-contract.mjs (shape + drift gate) to build + build:check. Outcome: the founder finally sees the funnel's leak points. |
|  | high | ai |  | 7 | 7 | 24.5 | · | **oracle-answer-feedback-loop** — . A retrieval answer with no feedback affordance is a dead-end for both the visitor (no way to say 'that missed') and the studio (no way to improve ranking). A single 1-tap helpful/unhelpful control closes the AI feedback loop, feeds the new funnel-summary as an 'answer helpfulness' info-finding metric, and respects the established pattern: allowlist the beacon, let the S188 check-rum-allowlist gate verify both ends stay in sync. **Recipe:** In assets/ignis-answer-engine.js, render a compact 'Helpful? thumbs up / thumbs down' control under each delivered answer (CSS classes, no inline styles per the strict style gate). On tap, emit allowlisted oracle-answer:{helpful,unhelpful} via the existing emitUx/beacon path and hide the control. Add oracle-answer:helpful + oracle-answer:unhelpful to Worker RUM_UX_EVENTS, then run node scripts/check-rum-allowlist.mjs so emit<->allowlist stays in sync. Surface the helpful-rate in funnel-summary (item #1) so answer quality is measured, not guessed. |
|  | high | ux |  | 7 | 4 | 17.7 | · | **flagship-storytelling-wave2** — . Half-finishing the flagship-storytelling pass means one of the two live titles converts on landing and the other bounces. This is the cheapest high-certainty win on the board: mirror the exact additive pattern S188 proved (promise line under H1, single primary CTA preserved, no risky rebuild of a mature surface per the flag-gate learning). Reversible in one revert. **Recipe:** Open games/vaultspark-football-gm/index.html, locate the hero H1, and add the same additive SOUL-voice promise line + single primary CTA structure used in games/call-of-doodie/ (S188 9d01d298). Keep it reversible and additive — do NOT rebuild the hero. Rebuild the ambient shell if the page is shell-stamped (npm run build), verify the line renders on the pages.dev origin, and confirm a single primary CTA. |
|  | nice | security |  | 5 | 3 | 11.3 | · | **rum-dead-allowlist-sweep** — . An allowlist that drifts from real emit-sites silently rots into a map of intentions instead of reality — the inverse of the S186 bug. Running the gate after the new beacons land keeps RUM_UX_EVENTS truthful and prevents a future agent from trusting a dead name. **Recipe:** Run node scripts/check-rum-allowlist.mjs after items #1-#2 land. For any WARN (allowlisted-but-never-emitted), either wire the missing emit or remove the dead entry from RUM_UX_EVENTS in cloudflare/security-headers-worker.js. Confirm the gate is clean (0 ERROR, 0 WARN) and that build:check still passes. |
|  | nice | process |  | 4 | 2 | 6.0 | · | **ignis-rescore-artifact-settle** — . Stale health signals erode the brief's value as the multi-agent handoff surface — the next session inherits an amber IGNIS and a 'drift' line that are artifacts of staleness, not real problems. A cheap rescore + a clean npm run build settles both so the closeout brief is honest. **Recipe:** Run node ../vaultspark-studio-ops/scripts/ops.mjs rescore (or studio_rescore_ignis) to refresh ignisScore/ignisGrade/ignisLastComputed in context/PROJECT_STATUS.json. Run npm run build to regenerate public-intelligence + llms-full-shards, then rerun npm run build:check to confirm the drift advisories clear. Re-render the startup brief. |

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

- [object Object]

## Skipped (Priority < 1.5)

- wishlist-momentum-proof — BLOCKED on Supabase admin (capability MISSING locally) — needs aggregate count access. Cost-neutral per CANON-029 once unblocked, but not agent-attemptable this session.
- top-verify-carry — RESOLVED as a SAVE, not a skip: live prod probes confirm S188+S187 features deployed (dispatch sitewide, call-of-doodie promise, Discord+Community nav, status-proof 200, deployable tip). Sub-item (c) RUM beacons landing is exactly what item #1 (funnel-rollup) makes visible.
- tt-enforce-reprobe — Evidence-gated: reprobe due ~2026-06-18; pre-S185 samples still aging out. Flip stays SOUL #3 founder-device-gated. Not due this session.

