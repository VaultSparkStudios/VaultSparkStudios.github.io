# AUDIT — S299 · 2026-07-30

**Project:** vaultsparkstudios-website · app · public-live · SPARKED
**Rubric:** product · **Lens:** app-release-gate + web canon (011/041/047/048/045 + legal/IP)
**Baseline:** doctor `blockingFailing: 0` (13/15 pass, 0 fail) · sync 0/0 after rebase (was 27 behind) · staging reachable (receipt 200, history 200)

This is a deeply-saturated infrastructure-heavy site. Every premise below was verified against **live code + live staging bytes** — no phantom items.

## Ranked plan

### SHIP — in-repo, actionable now

**1. [S299][RELEASE/P1] Serve + independently compare the deploy-history ledger; publish a public-safe continuity summary (no receipt/manifest cycle).**
Premise verified live:
- `data/staging-deploy-history.ndjson` **is served** at `https://website.staging.vaultsparkstudios.com/data/staging-deploy-history.ndjson` → HTTP 200, 16016 bytes, **byte-equal** to the local ledger.
- `check-staging-deploy-receipt.mjs --remote` currently fetches **only** the head receipt (`/api/staging-deploy-receipt.json`) and byte-compares; the history NDJSON is validated **locally only** — the served ledger is never independently compared.
- `build-candidate-artifact-manifest.mjs` uses a fixed `CORE_PATHS` allowlist and excludes itself + downstream receipts. A continuity summary at `api/staging-deploy-continuity.json` is **outside `CORE_PATHS`** → cannot enter `candidateRoot` → **no cycle**.

Deliverables (optimal order):
- 1a. Bounded HTTPS fetch of the served history ledger; parse + validate against the same strict lib; require the served chain to **contain the local head** and be byte-continuous (exact-equality of the served bytes to the local attested bytes, since staging serves what we deployed).
- 1b. `api/staging-deploy-continuity.json` — public-safe derived summary: chain depth, head/predecessor lineage integrity, served-vs-local equality verdict, served HTTP status, coverage (served head == receipt head). **Content-stable `generatedAt`** (only advances on a real semantic change — avoids self-invalidating status churn).
- 1c. Import-safe pure lib + self-tests: fail closed on detached / replayed / truncated / superset-mismatch served history.
- 1d. Structural gate: assert the continuity artifact stays **out of `CORE_PATHS`** (no future cycle regression) and that the checker's remote path covers the ledger.
- 1e. Wire the continuity verdict into release-proof lineage as an additive signal (no new cycle).

### DEFER — honest, evidence-backed (recorded, not silently skipped)

**2. [S298→S299][PROCESS/P2][CROSS-REPO] Verify canonical protocol propagation repair.**
Evidence: propagated `docs/SESSION_PROTOCOL.md` still has **no §2B/§2C headings**; no `canon-update` repair cargo in the fresh drain (44 cargo). The repair is **studio-ops-owned and has not propagated**. Acceptance tests already shipped S298 (`01JULCLFE32881AA71DA10278F`). Honest defer — re-verify on the drain that carries the repair.

**3. [S296→NEXT][PROCESS/P2][CROSS-REPO] Skill-trace / session-floor cache contracts.**
Evidence: `../vaultspark-studio-ops/scripts/skill-trace.mjs` **does not exist** in this repo's reach — the fix is control-plane-owned. **12 `repo-question` evidence cargo already outstanding** to studio-ops; re-shipping would duplicate. Honest defer.

**4. [S296→NEXT][OBS/P1][EXTERNAL] Re-evaluate RUM anomaly verdict.**
Evidence: `data/rum-summary.json` `totalSamples: 0`, `sufficientRoutes: 0`; `worker-route-provenance` state `mismatch` (production intentionally held). No fresh coverage has returned. **Do not backfill** — the state machine grades fresh evidence only when production legitimately recovers. Honest defer.

## Verdict
One genuine in-repo item (#1) + its second-order innovations to saturate; three evidence-backed honest defers (cross-repo/external). Ship #1 at best-in-history quality within SOUL non-negotiables; record the defers as wins in DECISIONS.
