<!-- generated-by: render-audit-md.mjs (R-H14) — DERIVED FROM AUDIT_2026-08-20.json; do not hand-edit -->
<!-- generated-at: 2026-08-21 -->
<!-- project: vaultsparkstudios-website · type: website -->

# Project Audit — VaultSpark Studios Website

> One ranked improvement plan covering 9 axes. Top items first.
> Source of truth: this file is derived from the JSON sidecar. Edit the JSON, then re-render.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 3
- Combined Priority: 57.00
- Top 3: verification-reachability-ratchet · closed-day-velocity-drift-proof · publisher-promotion-cadence-contract
- Sampled: 16/556 files · 29904 tokens

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov | Priority | Status | Item |
|---|:-:|---|---|:-:|:-:|:-:|:-:|---|
| 1 | 🔥 | security | 6h | 10 | 7 | 23.3 | ✓ | **verification-reachability-ratchet** — Verification reachability ratchet — every real gate has a measured path. PRE-VERIFIED: check-windows-hide is not reached by build:check and exits 1 on 33 direct child_process imports, so the repo can report 327/327 while violating the machine-safety rule that prevents terminal storms. The broader graph also leaves check-mobile-runtime-contract, check-workflow-git-depth, and generate-pathways --check outside the authoritative run. Outcome: a founder or agent reading the headline test count can trust that these release, mobile, workflow-history, and machine-safety contracts actually ran. **Recipe:** Expand check-build-gate-reachability into a source-declared multi-family ratchet for build/check/generate/derive/enrich scripts. Mark lifecycle-only tools by scope, wire the four verified build/release gates into build:check, route all 33 raw child_process imports through scripts/lib/safe-spawn.mjs, and lock direct, nested, lifecycle-exempt, default-check, and red-gate cases with both-direction self-tests. |
| 2 | 🔥 | feedback | 4h | 8 | 6 | 18.6 | ✓ | **closed-day-velocity-drift-proof** — Closed-day velocity drift proof — verify the 60-day Oracle without crying wolf. PRE-VERIFIED: build-oracle-velocity-public --check is explicitly a dry-run and never reads api/ecosystem-velocity.json. Its moving 60-day edge made byte comparison unsuitable, so the public Oracle can flatten or lag while the check stays green. Outcome: visitors and agents asking how active the forge has been receive a feed whose completed days are cryptographically bound to Git history while today's still-moving count remains honest. **Recipe:** Add closedThrough and a SHA-256 fingerprint over stable date/count pairs to the feed. In --check, rebuild from current Git history, compare only overlapping days strictly before the open day, reject any closed-day mutation or missing stable date, tolerate today's moving count, and self-test open-edge tolerance, closed-day drift, window rollover, and malformed committed feeds. Remove the dry-run declaration and let the reachability ratchet require this gate. |
| 3 | ⚡ | featureDepth | 3h | 7 | 5 | 15.1 | ✓ | **publisher-promotion-cadence-contract** — Publisher promotion cadence contract — say when repository-fresh becomes production-fresh. PRE-VERIFIED: six content-bearing publishers commit with [skip ci], while only refresh-live-data dispatches pages-deploy.yml. This is deliberately bounded by the four-hour coalescer, but neither api/deploy-currency.json nor /status explains that a repository-fresh feed may wait for the next promotion. Outcome: a human or agent checking trust signals can distinguish fresh source data from live production data and knows the maximum scheduled promotion lag. **Recipe:** Add a public-safe publisherPromotion contract to build-deploy-currency with strategy=coalesced, maxLagHours=4, promoterWorkflow=refresh-live-data.yml, and an explicit no-deploy-per-observation rationale. Render the cadence on the Production deploy currency tile, validate the field contract and copy, regenerate dependent proof feeds, and capture desktop/mobile screenshots across every theme for the touched status state. |

## Depth Ladder (per item: L1 minimal · L2 solid · L3 genius)

> `/implement` selects the depth that fits remaining budget, and climbs the ladder
> on already-shipped items when the session still has budget (S175 saturation).

### verification-reachability-ratchet
- **L1 · minimal** _(~3h)_ — Wire the four verified gates and repair the 33 raw child_process imports until check-windows-hide passes.
- **L2 · solid** _(~6h)_ — L1 plus multi-family reachability, explicit lifecycle scopes for the six legitimate non-build scripts, and mutation self-tests.
- **L3 · genius** _(~10h)_ — L2 plus exact-argument reachability receipts, measured gate counts in build-check diagnostics, and a public-safe proof row that cannot outlive the source fingerprint.

### closed-day-velocity-drift-proof
- **L1 · minimal** _(~2h)_ — Compare overlapping closed days and fail on drift while ignoring the current day.
- **L2 · solid** _(~4h)_ — L1 plus committed closedThrough/fingerprint evidence, rollover semantics, malformed-input refusal, and both-direction self-tests.
- **L3 · genius** _(~7h)_ — L2 plus per-day proof diagnostics, source-history completeness checks for shallow clones, and downstream Oracle freshness/abstention wiring.

### publisher-promotion-cadence-contract
- **L1 · minimal** _(~1.5h)_ — Publish maxLagHours/promoterWorkflow in deploy-currency and render one status detail line.
- **L2 · solid** _(~3h)_ — L1 plus schema/consumer contract tests, derived proof regeneration, accessibility-safe copy, and every-theme desktop/mobile visual receipt.
- **L3 · genius** _(~6h)_ — L2 plus next scheduled promotion estimate and a freshness distinction for repository-observed versus production-observed feed timestamps.

## Skipped (Priority < 1.5)

- per-publisher-deploy-dispatch — REJECTED ON VERIFICATION / WIN: five scheduled publishers could dispatch Pages directly, but uptime runs every 30 minutes and a deploy-per-observation design would create needless build churn. The existing four-hour refresh-live-data workflow is the cost-neutral coalescer; the missing product behavior is an explicit, enforced promotion-lag contract.
- runtime-ai-drift-classifier — REJECTED ON INTERNAL-FIRST / WIN: the velocity feed can be proven with a deterministic closed-day fingerprint. A runtime model adds cost and nondeterminism without improving the information-finding outcome.
- new-mobile-or-theme-repair — FALSE PREMISE / WIN: PROJECT_STATUS records a 235/235 runtime contract plus a 63-capture manual review across seven themes and three mobile widths; no live-code premise supports inventing another visual repair.
- new-agent-intent-map — FALSE PREMISE / WIN: api/intent-map.json already provides the outcome-first route/evidence contract and its builder is now reachable from build:check.

## Execution Log

### verification-reachability-ratchet
- `2026-08-21T04:00:00.000Z` · **shipped** · All 33 direct child_process imports now route through safe-spawn. The reachability ratchet measures 233/233 declared gates, exercises 11/11 bidirectional mutation cases, and the Windows storm guard passes.

### closed-day-velocity-drift-proof
- `2026-08-21T04:00:00.000Z` · **shipped** · The public velocity feed now carries closedThrough plus a stable SHA-256 fingerprint. It counts full-log committer instants normalized to UTC, proves 59 completed days while tolerating the still-moving open day, and passes 14/14 drift, rollover, and time-zone self-tests.

### publisher-promotion-cadence-contract
- `2026-08-21T04:00:00.000Z` · **shipped** · Deploy currency publishes the four-hour coalesced promotion contract and /status renders the maximum lag. Field contracts pass, dependent feeds were regenerated, and 28/28 desktop/mobile captures across seven themes were manually reviewed.

---
*S325 full nine-axis website audit. Every emitted item names the public information-finding outcome. The prior S324 same-day Markdown audit was preserved at docs/archive/audits/AUDIT_2026-08-20_S324.md before restoring the JSON-first contract. The 194-script reachability survey was premise-checked down from a noisy 18-candidate scan to four true build/release gates; one is red, three are green-but-unmeasured. Three genuinely novel alternatives were considered and rejected where they would add deploy churn, runtime model cost, or visitor-facing gamification of operator mechanics.*
