<!-- generated-by: render-audit-md.mjs (R-H14) — DERIVED FROM AUDIT_2026-09-11.json; do not hand-edit -->
<!-- generated-at: 2026-09-11 -->
<!-- project: vaultsparkstudios-website · type: website -->

# Project Audit — VaultSparkStudios.github.io

> One ranked improvement plan covering 9 axes. Top items first.
> Source of truth: this file is derived from the JSON sidecar. Edit the JSON, then re-render.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 6
- Combined Priority: 56.50
- Top 3: enable-edge-uptime-sampler · shell-cleanup-regex-root-fix · hidden-tab-poll-gating

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov | Priority | Status | Item |
|---|:-:|---|---|:-:|:-:|:-:|:-:|---|
| 1 | 🔥 | security | 1.5h | 10 | 7 | 20.1 | ⛔ | **enable-edge-uptime-sampler** — Enable the Worker scheduled() uptime sampler so the edge is measured, not merely reported unmeasured. Outcome: a /status/ reader finds a real edge-availability number. Since S349 the public status honestly says edge-unobservable, because every CI probe is bot-challenged. The sampler that can see the edge ships dark. **Recipe:** Create the production UPTIME_SAMPLES KV namespace, uncomment the binding and the 30-minute cron in cloudflare/wrangler.toml, flip UPTIME_SAMPLER_ENABLED to "1", and let the push-triggered Worker deploy run its promotion gate and release ceremony. Confirm with drain-uptime-kv --dry-run once a sample exists. |
| 2 | ⚡ | speed | 30m | 6 | 6 | 14.2 | ✓ | **shell-cleanup-regex-root-fix** — Root-fix the dead cleanup regex that leaves every superseded hashed shell tracked and servable. Outcome: stale CSS and JS stop being publicly servable and precache-reachable, and the repository stops growing by one shell per rotation. Eight stale shells are tracked today: six style.shell copies, one nav-sheet shell and one ambient-feature shell. **Recipe:** cleanupOldFingerprintedFiles built its pattern with a template literal ending `\${ext...}`. The backslash escaped the `$`, so the extension was never interpolated and the regex matched nothing. Escape both parts properly, then rerun build-shell-assets to prune. |
| 3 | ⚡ | speed | 1h | 5 | 5 | 9.3 | ✓ | **hidden-tab-poll-gating** — Stop hidden tabs from polling presence and rotating the Vault Pulse ticker; refresh the moment a reader returns. Outcome: a reader who comes back to the tab sees current presence and ticker rows immediately, and background tabs stop spending requests and battery on things nobody can see. **Recipe:** favicon-pulse tick returns early while document.hidden; vault-pulse skips rotation and pool refresh while hidden; all three scripts refresh once on visibilitychange to visible. desk-presence already skipped its network call while hidden, so it gains only the return-refresh. |
| 4 | ⚡ | ux | 30m | 4 | 3 | 6.8 | ✓ | **heading-level-skips** — Fix h1 to h3 heading skips on Community, Journal and Contact. Outcome: screen-reader and AI-agent outlines of these pages read as a proper hierarchy, so a user navigating by headings finds the Discord invite, the Dispatch signup and the contact channels. **Recipe:** Demote the skipping h3 elements to h2, and scope their existing per-page rules to h2 with font-family: inherit so the rendered look is unchanged. |
| 5 | ⚡ | observability | 30m | 5 | 3 | 6.1 | ✓ | **recover-s349-writeback** — Recover the un-written-back S349 follow-up commit and close the CANON-055 gap on the record. Outcome: the session ledger and the surface follow-through check both tell the truth about 50f13941. **Recipe:** Record 50f13941 in the S350 write-back. Its user-facing effect is a CI probe's exit code and console line, not a site surface, so declare the CANON-055 exemption for it in LATEST_HANDOFF. |
| 6 | ⚡ | ux | 18m | 6 | 4 | 8.2 | ✓ | **contact-input-height** — Contact form inputs collapse to 22px instead of 48px. Outcome: a visitor filling in the studio contact form gets full-height fields with readable placeholders, on the main inbound conversion path. **Recipe:** Scope flex:none to .contact-form .input-field so the column-flex .form-group stops overriding height. |

## Depth Ladder (per item: L1 minimal · L2 solid · L3 genius)

> `/implement` selects the depth that fits remaining budget, and climbs the ladder
> on already-shipped items when the session still has budget (S175 saturation).

### enable-edge-uptime-sampler
- **L1 · minimal** _(~1.5h)_ — Create namespace, bind, enable, deploy.
- **L2 · solid** _(~3h)_ — Also drain samples into the history daily from CI.
- **L3 · genius** _(~6h)_ — Multi-region samples through several cron vantages.

### shell-cleanup-regex-root-fix
- **L1 · minimal** _(~30m)_ — Fix the escape and rerun the build.
- **L2 · solid** _(~1h)_ — Add a self-test asserting the pattern matches a synthetic stale name.
- **L3 · genius** _(~2h)_ — Have the check mode fail on any unmanifested shell.

### hidden-tab-poll-gating
- **L1 · minimal** _(~1h)_ — Visibility guards plus return-refresh.
- **L2 · solid** _(~2h)_ — Move leader election to localStorage with a heartbeat.
- **L3 · genius** _(~4h)_ — One shared presence poller for all widgets.

### heading-level-skips
- **L1 · minimal** _(~30m)_ — Demote and rescope.
- **L2 · solid** _(~1h)_ — Add a sitewide heading-order gate.
- **L3 · genius** _(~2h)_ — Generate a heading outline into agents.json.

### recover-s349-writeback
- **L1 · minimal** _(~30m)_ — Write back and declare.
- **L2 · solid** _(~1h)_ — n/a
- **L3 · genius** _(~1h)_ — n/a

### contact-input-height
- **L1 · minimal** _(~18m)_ — Page-scoped flex:none.
- **L2 · solid** _(~1h)_ — Move the flex:1 onto the horizontal row selectors in style.css.
- **L3 · genius** _(~2h)_ — Add a rendered-height assertion for form fields to the mobile audit.

## Skipped (Priority < 1.5)

- undefined — Gated on a full 31-day sampler window by design; re-scoring them is explicitly forbidden. Item 1 starts that clock.
- undefined — False premise: refreshPresence already returns early when the tab is not visible and focused.

## Execution Log

### enable-edge-uptime-sampler
- `function at() { [native code] }` · **?** · 

### shell-cleanup-regex-root-fix
- `function at() { [native code] }` · **?** · 

### hidden-tab-poll-gating
- `function at() { [native code] }` · **?** · 

### heading-level-skips
- `function at() { [native code] }` · **?** · 

### recover-s349-writeback
- `function at() { [native code] }` · **?** · 

### contact-input-height
- `function at() { [native code] }` · **?** · 

