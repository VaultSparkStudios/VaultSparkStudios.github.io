<!-- generated-by: render-audit-md.mjs (R-H14) — DERIVED FROM AUDIT_2026-06-18.json; do not hand-edit -->
<!-- generated-at: 2026-06-18 -->
<!-- project: vaultsparkstudios-github-io · type: website -->

# Project Audit — VaultSpark Studios Website

> One ranked improvement plan covering 9 axes. Top items first.
> Source of truth: this file is derived from the JSON sidecar. Edit the JSON, then re-render.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 21
- Combined Priority: 631.10
- Top 3: hero-scroll-activation · progressive-membership-reveal · hero-refinement

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov | Priority | Status | Item |
|---|:-:|---|---|:-:|:-:|:-:|:-:|---|
| 1 | 🔥 | ux | 2h | 9 | 8 | 80.0 | · | **hero-scroll-activation** — Hero animation gate: gesture-triggered, not load-triggered. Every hero-reveal element has animation-delay: 1.4s — visitors see invisible sub-content for 1.4s after first paint. The forgeSparkBurst keyframe runs filter:blur() inside the animation (expensive GPU repaint every frame). Triggering the reveal via IntersectionObserver instead of a hard delay eliminates the blank-window, makes the motion feel responsive rather than automatic, and cuts GPU cost during paint. Outcome: faster perceived LCP, cleaner first impression, higher CTA engagement. **Recipe:** Add animation-play-state: paused to .hero-reveal in CSS. A small JS block observes .hero-center and sets animation-play-state: running on first intersection. Reduces default 1.4s blank delay to ~0ms. |
| 2 | 🔥 | ux | 3h | 8 | 7 | 45.2 | · | **progressive-membership-reveal** — Progressive membership reveal: free tier first, paid reveals on scroll. The membership page shows all 6 tiers at once — pricing from $4.99 to $99.99/mo. Visitors who see the highest price first and leave never encounter the free Forge tier. Anchoring with the free value, then letting visitors scroll to discover premium tiers, matches the proven SaaS pricing pattern. Outcome: higher free-tier sign-up rate and fewer visitors bouncing on price shock. **Recipe:** Reorder /membership/ tier cards to show Forge (free) as the hero card. Add a CSS scroll-reveal with IntersectionObserver for premium tier cards and a See all tiers expand link. No backend changes. |
| 3 | 🔥 | ux | 4h | 9 | 5 | 43.5 | · | **hero-refinement** — §3 Homepage hero refinement (flag-gated, founder real-device review required). Three competing radial glow elements (blue 45vw, orange 40vw, gold 30vw), a forgeSparkBurst animation, a hero-chamber vignette, and a 1.3s letter-forge sequence all fight for attention before the visitor reads a single word. The primary CTA is buried below this cascade. This is a mature surface requiring a flag-gate (?hero=v2 or localStorage) and founder real-device visual review before graduating. Outcome: single dominant visual pulls the eye to wordmark and CTA immediately. **Recipe:** Create flag-gated hero variant: ?hero=v2 sets data-hero-v2 on body. CSS: v2 hides gold glow and spark burst, merges blue/orange into a single centered radial-gradient (no filter). JS: letterForge plays at 0.3s instead of 1.3s. |
| 4 | 🔥 | featureDepth | 4h | 9 | 8 | 41.8 | · | **adaptive-homepage-signed-in** — Adaptive homepage: member welcome strip injected above hero when signed in. Signed-in members see the same prove-first homepage as anonymous visitors — zero acknowledgment of their identity or progress. adaptive-cta.js is explicitly disabled on / (prove-first homepage) but body[data-vs-signed-in] is already set by the auth layer. Adding a member welcome strip above the hero is purely additive — anonymous visitors see no change. Outcome: members feel ownership; Continue your vault CTA drives portal re-engagement on every visit. **Recipe:** CSS: body:not([data-vs-signed-in]) .member-welcome-strip { display: none }. HTML: inject .member-welcome-strip above .hero via account-chip post-auth callback. Content: rank badge + Continue to Your Vault link. SOUL-voice copy. |
| 5 | ⚡ | speed | 1h | 8 | 5 | 35.3 | · | **hero-gpu-compositing** — Hero glow: remove filter:blur(80px), use gradient-encoded softness. Three .hero-glow elements each use filter:blur(80px) with no will-change: filter hint — this forces a GPU stacking context AND triggers paint on every scroll frame that intersects the hero. The blur effect can be replicated by encoding softness into the radial-gradient stop positions without any filter at all. The heroParallax animation moves these elements on scroll, compounding the repaint cost. Outcome: 60fps hero scroll on mid-range devices; measurable INP improvement. **Recipe:** Replace filter:blur(80px) on .hero-glow with broader radial-gradient encoding the soft edge: radial-gradient(circle, rgba(R,G,B,0.4) 0%, rgba(R,G,B,0.1) 50%, transparent 70%). Add will-change: transform. Test visual parity across 4 themes. |
| 6 | ⚡ | ux | 6h | 8 | 5 | 33.3 | · | **portal-premium** — §4 Member portal premium + redundancy (carry §4). The vault-portal page was not updated with the S204 premium polish layer. The notification bell is wired but dead, tier-vs-rank confusion exists, and there is no next-achievable progress goal. A member who knows exactly what to do next is a member who returns. Outcome: portal depth drives weekly return visits from active members and establishes a push opt-in touchpoint. **Recipe:** Apply S204 CSS premium custom-prop vars to vault-portal.html. Wire notification bell to existing WebPush service worker (VAPID keys READY). Add next-achievable rank card computed from current points field in Supabase vault_members. |
| 7 | ⚡ | speed | 45m | 7 | 4 | 32.9 | · | **check-mission-coherence-gate** — check-mission-statement-coherence.mjs: WARN gate for retired framing. S204 retired pressure system, moment before ignition, and not a metaphor for storage from all mission surfaces. Without a gate, any future session can reintroduce these phrases without detection until a founder review. A grep-based WARN gate in build:check catches the regression at commit time. Outcome: identity drift prevented before it reaches production; brand consistency maintained across all visitor touchpoints. **Recipe:** scripts/check-mission-statement-coherence.mjs: grep 6 mission surfaces for 3 retired phrases. WARN (not ERROR) to avoid false CI failures. /universe/ path excluded per DECISIONS.md. Fold into check-proof-surface. |
| 8 | ⚡ | featureDepth | 4h | 8 | 8 | 32.5 | · | **command-palette-ignis-terminal** — Cmd+K IGNIS terminal: multi-turn conversation within the palette. The command palette shows a single IGNIS reads: answer then stops — pressing Cmd+K again clears the context. Multi-turn within the palette (answer to follow-up chips to deeper answer) turns a search widget into a zero-chrome research terminal that never disrupts the current page. No equivalent exists on other studio or game portfolio sites. Outcome: info-finding without navigation increases time-on-site and converts curious visitors to members. **Recipe:** Add conversationHistory array to palette state. After first answer, inject follow-up chips from the answer source keys. Each chip re-queries with accumulated context. New topic button clears state. |
| 9 | ⚡ | ux | 3h | 9 | 3 | 29.1 | · | **freshness-sweep** — §6 Audience-correctness + freshness sweep (carry §6). S197 found working game pages showing Demo Coming Soon alongside live Play Now CTAs — a coherence failure that survived 11 sessions because no one walked the actual journey. The S200-S204 wave may have introduced new coherence issues. Walking the full journey (home to play to join to portal, desktop and mobile) verifies every CTA is honest. Outcome: no trust-damaging contradictions anywhere on the conversion path. **Recipe:** Manual + automated pass: check-game-playability-coherence.mjs --verbose; check-content-freshness.mjs; walk / to /games/ to game page to /join/ to /vault-portal/ on pages.dev; sync game/project counts in nav vs homepage vs games page. |
| 10 | ⚡ | ux | 5h | 8 | 4 | 28.5 | · | **membership-consolidation** — §5 Membership cluster consolidation via tabbed hub (carry §5). Four separate URLs for the same decision (/membership/ + /vaultsparked/ + /membership-value/ + /ranks/) create cognitive overhead for a visitor deciding whether to join. A tabbed hub preserves all content while making the membership proposition a single cohesive reading experience. Worker Layer 0c 301s ensure zero SEO disruption. Outcome: membership decision requires one page visit, not four — join conversion improvement. **Recipe:** Build /membership/ tabbed hub (Overview, Tiers, Benefits, Ranks) with CSS :target or JS tab switching. Worker Layer 0c routes: /vaultsparked/ to /membership/#tiers, /membership-value/ to /membership/#benefits, /ranks/ to /membership/#ranks. |
| 11 | ⚡ | newFeatures | 3h | 8 | 9 | 27.1 | · | **vault-momentum-score** — Vault Momentum Score: real-time studio health composite on homepage. The Studio Now strip shows static labels. Three deployed API feeds contain the raw signals: api/velocity-series.json (commits/week), api/funnel-summary.json (CTA clicks and game plays), api/rank-climbers.json (active members). Compositing these into a single animated Vault Momentum chip (like a stock ticker for studio health) gives visitors real-time proof that the studio is actively building. No other indie studio website does this. Outcome: live proof of activity builds trust — first-time to returning visitor conversion. **Recipe:** scripts/build-vault-momentum.mjs: read 3 feeds, compute rolling weighted score, write api/vault-momentum.json. Homepage JS fetches + renders as a chip in Studio Now strip with SOUL-voice labels (SPARKED / FORGING / AT REST). Honest-dark when feeds are empty. |
| 12 | ⚡ | featureDepth | 8h | 7 | 8 | 25.3 | · | **ignis-knowledge-graph** — IGNIS knowledge graph: entity-linked search index. IGNIS uses a flat search index — asking What is DreadSpike returns only that page entry with no connection to the Voidfall universe, the game genre, or related projects. Structuring the index as a knowledge graph (entityType + relatedEntities array) enables answers like DreadSpike is part of the Voidfall universe, which also connects to... revealing the connected portfolio depth. Outcome: richer info-finding answers convert research visitors into multi-page explorers. **Recipe:** Add entityType (game/universe/tool/studio/page) and relatedEntities [{slug, label, type}] to each IGNIS search index entry at build time. ignis-answer-engine.js renders Related: chips below answers from relatedEntities. |
| 13 | ⚡ | ai | 4h | 8 | 8 | 24.8 | · | **personalized-ignis-homepage** — Personalized IGNIS context panel for signed-in members. Signed-in members who open IGNIS see the same anonymous experience as visitors. The auth state is detectable (body[data-vs-signed-in]) and member rank data is available from account-chip. A personalized panel (Your rank: Ember. Next goal: 5 plays on Call of Doodie) converts IGNIS research sessions into vault actions. Outcome: personalized CTAs convert at significantly higher rates than generic ones — portal engagement improvement. **Recipe:** In ignis-answer-engine.js: post-auth callback injects a .ignis-member-context panel above the query input showing rank chip + next milestone + recommended game query pre-fill. Panel hidden for anonymous visitors. Uses existing account-chip data. |
| 14 | ⚡ | feedbackLoop | 2h | 7 | 7 | 24.5 | · | **live-feedback-triage** — Dead-CTA detector: auto-flag 0-click CTAs in startup brief SIGNALS. The RUM funnel-summary.json collects click data for every tracked CTA. A CTA with zero clicks in 7 days is either invisible, misleading, or broken — and the founder discovers this only by manually reading funnel data. A build-time script that reads funnel-summary and surfaces dead CTAs in STARTUP_BRIEF SIGNALS turns invisible revenue loss into a visible session-start alert. Outcome: broken CTAs found and fixed within one session instead of sitting broken for weeks. **Recipe:** scripts/check-dead-ctas.mjs: reads api/funnel-summary.json, flags any funnelCtas entry with 0 clicks in the last 7-day window, exits non-zero with a SIGNAL message. render-startup-brief.mjs includes dead-cta output in SIGNALS block. |
| 15 | ⚡ | gamification | 4h | 7 | 10 | 23.2 | · | **constellation-challenges** — Constellation Challenges: hidden page-sequence badges. The studio has 8+ games, a universe, tools, an oracle, and a portal — but most visitors see only the homepage. A constellation challenge (visit /universe/ then /oracle/ then /games/voidfall/ in sequence to unlock Star Mapper badge) drives exploration purely through JavaScript with no backend. No other indie game studio has this mechanic on their marketing site. Outcome: multi-page exploration doubles; viral word-of-mouth (I found a secret badge) drives organic traffic. **Recipe:** assets/constellation-tracker.js: tracks page-visit sequence in sessionStorage; checks against 5 predefined constellations in data/constellations.json; on match, injects badge reveal animation + RUM emit constellation:unlocked. ambient-loader predicate loads it sitewide. |
| 16 | ⚡ | speed | 1h | 9 | 2 | 22.7 | · | **prod-verify-s204** — Prod-verify S204 wave + clear the 7-item verify backlog. There are 7 open VERIFY/P0 items in the TASK_BOARD (S204 through S198). The S204 premium polish layer, mission rewrite, and toolchain fixes have never been confirmed on production. A CF Pages tip-check plus pages.dev smoke test clears this class of debt in one focused pass. Outcome: confidence that every shipped wave is actually live; prevents the skip-ci tip strandig pattern from leaving work undeployed. **Recipe:** node scripts/check-deploy-tip.mjs && gh run list --limit 5 to confirm tip. Then pages.dev smoke test for S204 checklist: focus rings, press states, scrollbar, mission copy. Close resolved VERIFY items in TASK_BOARD. |
| 17 | ⚡ | feedbackLoop | 2h | 7 | 7 | 21.0 | · | **micro-sentiment-reactions** — Emoji reactions on journal dispatches (KV-backed). Journal dispatches have zero engagement signals beyond page views. Three emoji reactions (Forged / Pondering / Vaulted) require no login, store counts in Cloudflare KV (pattern established by CSP reporting in S199), and give the founder first-party data on which content topics drive interest vs confusion. Aggregated in rollup-rum-ux. Outcome: content strategy guided by real sentiment, not guesses — better dispatches drive subscriber growth. **Recipe:** Adapt assets/changelog-reactions.js for dispatch pages. Worker POST /v/react endpoint: increment KV counter react:{page-slug}:{emoji}. Client: inject 3 buttons below dispatch body via ambient-loader predicate. RUM react:{emoji}:{slug} family. |
| 18 | ⚡ | ai | 4h | 6 | 9 | 20.9 | · | **natural-language-changelog** — SOUL-voice changelog: build-time narrative from commits via Claude API. The changelog shows raw commit-derived entries with technical labels and script names. Members who read it want to feel the story of what is being built. Using Claude API at build time (ANTHROPIC_API READY, CANON-015) to transform each entry into a SOUL-voice narrative converts a dry CI log into a compelling weekly dispatch. Outcome: changelog readers convert to journal subscribers; subscribers convert to members. **Recipe:** scripts/build-changelog-narrative.mjs: reads last N changelog entries, calls Claude API once per entry with SOUL.md voice context, writes narrativeSummary field to changelog data JSON. changelog-live.js renders narrativeSummary as primary display. Cached per commit SHA. |
| 19 | ⚡ | featureDepth | 4h | 6 | 6 | 20.9 | · | **web-push-smart-triggers** — Web push smart triggers: devlog published, rank-up, forge-event. VAPID keys and service worker push infrastructure are fully deployed but no scenarios define when a push fires. The highest-value triggers: new devlog published (reads journal RSS, fires push to subscribers), rank-up congratulation (edge function post-points-update), and forge event (new major deploy notification). Outcome: lapsed members return via push; push opt-in rate becomes a measurable retention metric in funnel-summary.json. **Recipe:** scripts/push-dispatch.mjs: reads journal RSS feed, checks for new entries since last push (state in KV), sends VAPID-signed push to all subscribed endpoints. Wired as a CI step post-deploy. |
| 20 | 💡 | security | 15m | 7 | 2 | 10.6 | · | **tt-enforce-reprobe** — TT-ENFORCE reprobe (due today per S186 carry). The first-party surface is confirmed CLEAN from S185. Pre-S185 tracking technology samples are aging out today (2026-06-18 reprobe date set in S186 TASK_BOARD). A quick reprobe confirms the flip condition has not changed and closes the open carry. Outcome: security posture validated; no third-party tracking leaking visitor data. **Recipe:** node scripts/tt-probe.mjs --surface first-party. Review output. If still CLEAN, document reprobe result in TASK_BOARD and close the S186 carry. |
| 21 | 💡 | security | 2h | 8 | 2 | 8.0 | ⛔ | **staging-box-recovery** — Staging box recovery: HCLOUD_TOKEN needed to restore website.staging (CANON-007). website.staging.vaultsparkstudios.com has been DOWN since S192 (4+ sessions). CANON-007 requires a staging environment before any SPARKED production deploy. The only blocker is HCLOUD_TOKEN missing from the secrets gateway — a founder action to retrieve from Hetzner Cloud Console. Once READY, agent executes hcloud + SSH + Caddy restore autonomously. Outcome: CANON-007 compliance restored; safe deploy path with staging parity. **Recipe:** CANON-019 preflight confirms HCLOUD_TOKEN MISSING. Founder action: retrieve HCLOUD_TOKEN from Hetzner Cloud Console then node scripts/ops.mjs intake-credentials hcloud. Agent then: hcloud server list, SSH, Caddy restore. |

## Depth Ladder (per item: L1 minimal · L2 solid · L3 genius)

> `/implement` selects the depth that fits remaining budget, and climbs the ladder
> on already-shipped items when the session still has budget (S175 saturation).

### hero-scroll-activation
- **L1 · minimal** _(~30m)_ — Add animation-play-state: paused to .hero-reveal; JS IntersectionObserver on .hero-center sets it to running. Removes 1.4s blank delay.
- **L2 · solid** _(~2h)_ — JS orchestration: play hero animations in sequence on viewport entry (wordmark → tagline → CTAs). reduced-motion respected. data-hero-animated attribute for theme gating.
- **L3 · genius** _(~4h)_ — Gesture-reactive hero: glow elements follow cursor position via CSS custom props from mousemove. Spark burst triggers on first CTA hover. Parallax layers respond to scroll velocity.

### progressive-membership-reveal
- **L1 · minimal** _(~1h)_ — Reorder tier cards in HTML: Forge first. Add See all tiers expand link at bottom.
- **L2 · solid** _(~3h)_ — CSS scroll-reveal for premium tier cards. Price comparison table starts collapsed. Stagger-reveal animation on scroll.
- **L3 · genius** _(~6h)_ — Progressive membership wizard: 3 questions (game types, session length, community investment) recommends a specific tier. Driven from data/membership-tiers.json.

### hero-refinement
- **L1 · minimal** _(~1h)_ — ?hero=v2 param hides .hero-glow.gold and .forge-spark-burst. Test on real device. Founder decides.
- **L2 · solid** _(~4h)_ — Full v2 flag-gated hero: single centered glow, 0.4s wordmark reveal, CTAs visible at 0.6s. Founder real-device pass to graduate.
- **L3 · genius** _(~8h)_ — Cinematic depth hero: layered SVG depth planes with CSS perspective-transform parallax. Logo on its own compositor layer. Designed in Figma first.

### adaptive-homepage-signed-in
- **L1 · minimal** _(~1h)_ — CSS-only: body[data-vs-signed-in] .hero-actions::before shows a greeting chip. Zero new JS.
- **L2 · solid** _(~4h)_ — JS welcome strip: rank chip + progress bar + Continue CTA injected above hero after auth resolves. SOUL-voice copy.
- **L3 · genius** _(~8h)_ — Three homepage branches: first-time visitor (prove-first, full hero), known returning anon (streak nudge), signed-in member (rank + goals + personalized next step).

### hero-gpu-compositing
- **L1 · minimal** _(~15m)_ — Add will-change: filter to .hero-glow for immediate GPU hint. 15 min. Minimal visual change.
- **L2 · solid** _(~1h)_ — Remove filter:blur() entirely; encode soft edges in radial-gradient stop positions. Test across dark/light/ambient/warm/cool themes.
- **L3 · genius** _(~3h)_ — Replace glow elements with a single CSS Houdini paint worklet or WebGL canvas for GPU-native blur at zero CPU cost on scroll.

### portal-premium
- **L1 · minimal** _(~1h)_ — Apply S204 premium polish CSS vars to vault-portal.html. Visual parity with homepage.
- **L2 · solid** _(~4h)_ — Next-achievable card + progress bar. Wire notification bell to push subscription flow. Session-expiry modal with Stay signed in action.
- **L3 · genius** _(~8h)_ — Cross-game progress timeline. Referral panel. Personalized Your Vault Story narrative (top game, rank journey, milestone count).

### check-mission-coherence-gate
- **L1 · minimal** _(~45m)_ — Grep gate: 3 phrases x 6 surfaces. Exit 1 with WARN on hit. /universe/ excluded. Fold into check-proof-surface.
- **L2 · solid** _(~2h)_ — Full surface scanner: reads canonical surface list from config, supports phrase allowlist-per-surface. Self-test 4/4.
- **L3 · genius** _(~4h)_ — Semantic drift detector: compares voice against SOUL.md voice fingerprint — catches paraphrase not just literal matches.

### command-palette-ignis-terminal
- **L1 · minimal** _(~1h)_ — Add Ask IGNIS for more link below inline answer that opens /ignis/ with query pre-filled.
- **L2 · solid** _(~4h)_ — Multi-turn within the palette: conversation array, follow-up chips, New topic clear button. History persists 5m via sessionStorage.
- **L3 · genius** _(~8h)_ — Streamed answers, entity-aware follow-ups (chips from knowledge graph), palette pin conversation to sidebar while navigating.

### freshness-sweep
- **L1 · minimal** _(~30m)_ — Run check-game-playability-coherence.mjs + check-content-freshness.mjs. Review output.
- **L2 · solid** _(~3h)_ — Full journey walk desktop + mobile + automated count-sync check + fix any discovered coherence issues.
- **L3 · genius** _(~6h)_ — Automated nightly coherence probe via CI workflow running check-game-playability-coherence + check-content-freshness on schedule.

### membership-consolidation
- **L1 · minimal** _(~1h)_ — Worker 301 routes for /membership-value/ and /vaultsparked/ to /membership/. Tab navigation links added to /membership/.
- **L2 · solid** _(~5h)_ — Full tabbed hub with tab content from each existing page. CSS :target or JS tab switching. Worker Layer 0c 301s for all 3 source pages.
- **L3 · genius** _(~8h)_ — Progressive membership wizard merged with tabbed hub (see progressive-membership-reveal L3).

### vault-momentum-score
- **L1 · minimal** _(~1h)_ — JSON merge script reads 3 feeds, outputs api/vault-momentum.json with {score, label, signals[]}. Build-time.
- **L2 · solid** _(~3h)_ — Animated chip on homepage Studio Now strip. SOUL-voice labels. Honest-dark default.
- **L3 · genius** _(~5h)_ — Historical sparkline (30 days). Per-signal breakdown tooltip. Embeddable widget for studio-ops hub.

### ignis-knowledge-graph
- **L1 · minimal** _(~2h)_ — Add entityType + relatedEntities[] to search index JSON. No engine changes needed.
- **L2 · solid** _(~4h)_ — ignis-answer-engine.js: render Related: chips below each answer from relatedEntities. Chips are clickable follow-ups.
- **L3 · genius** _(~8h)_ — Graph traversal: multi-hop entity walk (DreadSpike to Voidfall universe to other Voidfall games). Narrative answer stitches the path.

### personalized-ignis-homepage
- **L1 · minimal** _(~1h)_ — Simple greeting: Ask me about your rank journey. Signed-in only. Uses existing account-chip data.
- **L2 · solid** _(~4h)_ — Full member context panel: rank chip + next milestone + Ask about your next rank pre-fill + recommended game.
- **L3 · genius** _(~8h)_ — Personalized knowledge index: IGNIS answers weighted toward content relevant to the member game history and rank tier.

### live-feedback-triage
- **L1 · minimal** _(~1h)_ — check-dead-ctas.mjs: reads funnel-summary, prints dead CTAs to stdout. Run manually.
- **L2 · solid** _(~2h)_ — Wire into render-startup-brief.mjs SIGNALS block. Dead CTAs surface as a warning in the startup brief automatically.
- **L3 · genius** _(~4h)_ — CTA performance trend: 7-day rolling window comparison (rising/falling/dead). Integrated with perf-history.ndjson pattern.

### constellation-challenges
- **L1 · minimal** _(~2h)_ — 3-page constellation tracker (JS + sessionStorage). One constellation: /studio/ + /games/ + /oracle/ = The Forge Path badge.
- **L2 · solid** _(~4h)_ — 5 constellations from data/constellations.json. Each with name, badge icon, and flavor text. RUM emit per unlock.
- **L3 · genius** _(~6h)_ — Constellation discovery hints: subtle golden star icons on constellation-member pages. Constellation Guide Easter egg page at /universe/constellations/.

### prod-verify-s204
- **L1 · minimal** _(~15m)_ — node scripts/check-deploy-tip.mjs + gh run list. Confirm tip is deployed.
- **L2 · solid** _(~1h)_ — Full S204 verify checklist on pages.dev + update TASK_BOARD to close resolved items.
- **L3 · genius** _(~3h)_ — Automated prod smoke test Playwright script visits 5 key pages, checks for focus rings, CTA text, mission copy. Wired into CI.

### micro-sentiment-reactions
- **L1 · minimal** _(~30m)_ — Client-only localStorage reaction counts (no server). Pure JS, zero infra. Confirms UX pattern before committing.
- **L2 · solid** _(~2h)_ — KV-backed reactions via Worker /v/react POST. Aggregate in rollup-rum-ux.mjs reactions block.
- **L3 · genius** _(~4h)_ — Reaction analytics dashboard on /oracle/ — which dispatches drove most Forged reactions this month.

### natural-language-changelog
- **L1 · minimal** _(~1h)_ — Build script generates 1-sentence plain-English summary per entry via regex + title transformation. No API cost. Validates pattern.
- **L2 · solid** _(~4h)_ — SOUL-voice narrative via Claude API at build time. Cached per commit SHA (no re-generation). CANON-015 compliant.
- **L3 · genius** _(~6h)_ — Weekly What We Forged auto-dispatch: narrative summary of all changelog entries from past 7 days auto-creates a journal dispatch draft for founder review.

### web-push-smart-triggers
- **L1 · minimal** _(~1h)_ — scripts/push-dispatch.mjs sends a test push to a hardcoded endpoint. Confirms VAPID stack works end-to-end.
- **L2 · solid** _(~4h)_ — New-devlog trigger wired into CI post-deploy. Push to all subscribed endpoints via KV subscription list.
- **L3 · genius** _(~6h)_ — Rank-up push from Supabase edge function. Forge-event push from deploy webhook. Subscriber preference panel on /vault-portal/.

### tt-enforce-reprobe
- **L1 · minimal** _(~15m)_ — Run tt-probe.mjs --surface first-party. Log result. Close or update TASK_BOARD carry.

### staging-box-recovery
- **L1 · minimal** _(~30m)_ — Founder: retrieve HCLOUD_TOKEN from Hetzner console. Agent: hcloud server list + SSH check.
- **L2 · solid** _(~2h)_ — Full Caddy restore + staging parity verification + CANON-007 compliance confirm.

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

- hero-scroll-activation
- adaptive-homepage-signed-in
- command-palette-ignis-terminal
- ignis-knowledge-graph
- personalized-ignis-homepage
- constellation-challenges
- natural-language-changelog
- vault-momentum-score

