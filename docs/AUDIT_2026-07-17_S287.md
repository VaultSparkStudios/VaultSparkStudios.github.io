<!-- generated-by: /audit (continuous /arc mission, Session 287) -->
<!-- generated-at: 2026-07-17 · Session 287 -->
<!-- premise-discipline: every item verified against LIVE code before ranking -->

# AUDIT — Session 287 · VaultSparkStudios.github.io

**Mode:** continuous `/arc` (start → audit → implement → closeout), single terminal.
**Baseline:** SIL 993/1000 · doctor 14/15 (1 warn = 7 stale *sibling* locks, not self-debt) · CI green on `main` (S286 recovery commit: Lighthouse ✅ · Accessibility ✅ · E2E ✅) · context 2%.

## Genius-list posture

Primary Unified Genius List = **2 NOW items**, **8 DEFERRED** (all correctly founder/credential/soak-gated). Deferrals recorded as honest wins:

| Deferred item | Gate | Verdict |
|---|---|---|
| Obelisk Phase-2 IdP migration (~110 call sites, RP creds, JWT/RLS bridge) | FOUNDER + missing credential | Correctly gated — active auth stays Supabase; scaffold not integrated |
| Behavioral Obelisk callback→storage round-trip check | lands with authorized auth repair | blocked on above |
| Multi-sport runway for Franchise Architect (×3) | FOUNDER (domain + product scope) | Correctly gated |
| Forge devlog publishes (×2) | FOUNDER-VOICE (never auto-publish) | Correctly gated |
| TT-ENFORCE-REPROBE | amber-soak (warm rows must age out) | Correctly gated (time, not action) |

→ Primary list thin. Session mass = the one shippable NOW item **plus a LIVE-verified second-order innovation pack** on release-confidence / observability.

---

## Ranked plan

### A1 · [NOW/VERIFY] Post-push CI confirmation — **VERIFIED DONE**
`gh run list`: S286 recovery commit on `main` passed Lighthouse ✅ · Accessibility ✅ · E2E ✅. List failures are on unmerged PR branches (dependabot actions-bump + SEO branch), not `main`. No work — recorded verified.

### A2 · [NOW/PRODUCT] 🔥 FLAGSHIP — Post-promotion receipt (candidate↔production reconciliation)
**Premise (LIVE-verified):** `check-pages-deploy.mjs` reconciles prod SHA vs HEAD but transiently (console advisory, not persisted). `release-proof.json` is entirely pre-promotion (staging/shell/build-sha/worker/favicon). Nothing captures production-observed CSP mode / console-error count / public-signal cardinality. Candidate-green and production-green never reconcile automatically → the "landed ≠ verified" failure class.

**Build:**
1. `scripts/build-promotion-receipt.mjs` → `api/promotion-receipt.json`. Modes: `--emit` (network, gated, honest-dark), `--check` (structure-only), `--self-test` (pure `derivePromotionReceipt`).
   - Prod-served SHA (pages.dev origin, bot-challenge-safe) vs promoted `api/build-sha.json` → `match | ahead | behind | unreachable`.
   - Production CSP mode from live headers → `enforce | report-only | absent` + nonce presence.
   - Console-error count + public-signal request cardinality: browser-observed when Playwright available; else honest-dark `null` + `browserCaptured:false` — never a fabricated zero.
   - Self-grades `receiptState`: `verified | unverified | degraded`.
2. Fold a `production` block + `reconciled` verdict into `release-proof.json` (reads committed receipt as a local file — no network in build:check) → one artifact reconciles candidate↔production.
3. Generate at closeout (post-deploy, honest-dark skip); structure-validate in build:check.

### Second-order innovation pack (LIVE-verified, no founder gate)
- **I1 · CSP-mode production regression guard** — receipt `csp.mode` makes an enforce→report-only prod flip detectable; `--check` fails a committed receipt showing report-only while source policy is enforce. (WORKER_CSP is enforce; no live prod guard today.)
- **I2 · Public reconciliation line on `/status/`** — surface candidate↔production verdict for humans+agents (CANON-048), honest-dark when unverified. (`/status/index.html` + `proof-card.js` consume `status-proof.json`; release/promotion proof absent there.)
- **I3 · Fold receipt into `status-proof` FEEDS** (honest-dark-ok) so a stale/absent reconciliation honestly drags trustScore. (FEEDS omits release/promotion proof today.)

Each innovation premise re-checked at implementation; any false premise deferred honestly (recorded, not skipped).

---

## Outcomes (shipped this session)

| Item | Status | Evidence |
|---|---|---|
| A1 · Post-push CI confirmation | ✅ verified done | S286 recovery commit green on `main` (Lighthouse/A11y/E2E) |
| A2 · Post-promotion receipt (FLAGSHIP) | ✅ shipped | `scripts/build-promotion-receipt.mjs` (15/15 self-test) → `api/promotion-receipt.json`; git-ordered SHA (ahead/behind/match/unknown), live CSP mode, browser console+cardinality, honest-dark; folded into `release-proof.json` (`production` block + `reconciled`); emit wired into closeout step 3d.6; `--check` in build:check |
| I1 · CSP prod regression guard | ✅ shipped | `validateReceiptShape` security invariant — observed report-only/absent enforce CSP → exit 1 |
| I2 · Public `/status/` reconciliation tile | ✅ shipped | honest-dark tile: verified/attention/unverified + streak |
| I3 · Receipt in `status-proof` FEEDS | ✅ shipped | feed #11, freshness-graded (336h), trust 100%, proof-feed-generators green |
| I4 · Reconciliation history ledger + streak | ✅ shipped | `data/promotion-history.ndjson` (tail-safe append, S282-class heal), pure `summarizeHistory`, streak embedded + surfaced; auto-covered by check-ndjson-integrity (10 ledgers clean) |
| Root-fixes (opportunistic) | ✅ | pre-existing derived drift: oracle `ecosystem-state.json` + changelog SSR — regenerated via canonical build order |

**Gate certification:** `npm run build:check` → **218/218 passed, exit 0** (direct exit code). Doctor 14/15 (1 warn = stale sibling locks, not self-debt). All producers exist and run; no fabricated signals; two honest-dark states (unverified when prod on an unpullable commit / no browser) proven, never masked as pass.

## Infrastructure rubric
- Producer exists & runs, or field degrades to UNVERIFIED (no phantom-healthy signals).
- `--check` = structure, never re-fetch volatile prod state, never byte-equality on network data.
- `--self-test` = pure derive, asserts healthy AND degraded branches.
- Honest-dark: network/no-browser → SKIP/degraded + exit 0, never a fabricated pass.
- No sibling-repo writes; cross-repo signal via Ark cargo only.
