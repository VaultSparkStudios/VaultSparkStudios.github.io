# AUDIT — Session 218 · VaultSparkStudios.github.io

Generated: 2026-06-23 · Mode: continuous arc (/start → /audit → /implement → /closeout)
SIL at audit: 947/1000 · Doctor: blockingFailing **0** · 9/13 (3 advisory) · Tests 116/116

> Every item below has had its premise **verified against live code** this session (no phantom items).
> Sibling-repo and founder-voice items are recorded as **honest deferrals** (a deferral with evidence is a WIN, not a skip).

---

## CONTEXT: stranded-work recovery (discovered at /start)

The working tree arrived dirty with **real, uncommitted work from a prior un-closed session** — separated and triaged at start:

- **S187 windows-spawn-hardening codemod** — 60 `scripts/*.mjs` rewired from `child_process` → `./lib/safe-spawn.mjs` (forces `windowsHide:true` on every spawn), a `promisify.custom` bug-fix in `safe-spawn.mjs` (repairs the S188 regression), the `codemod-safe-spawn.mjs` tool, and the runtime `windows-hide-shim.cjs`. Dry-run = 0 (fully consistent). **Recoverable, valuable, fixes a founder-flagged P0 (window-storm steals focus during doctor/test runs).**
- **Build-artifact churn** (ambient-core shell rotation + regenerated api/.cache/context) layered on top — **reverted to HEAD** (regenerates deterministically at build/deploy).
- **Dormant scaffolding** — `context-wipe-guard.mjs` (S179) + `obelisk-broker.mjs` (S183), imported by nothing, stranded ~35 sessions. **Left untracked, not blind-committed or deleted** (not mine to judge).

---

## RANKED PLAN

### TIER 1 — Recover & complete stranded P0 work (highest leverage; mostly built)

**A1 · Recover the S187 windows-spawn-hardening codemod** — score 95
Premise VERIFIED: 60 rewired scripts + safe-spawn promisify fix + shim, dry-run consistent, fixes founder window-storm P0.
Gate: `npm run build:check` green proves the 60-script rewire is sound (it exercises most of them).
Action: verify build:check → create missing `install-windows-hide-shim.mjs` wiring (or confirm not needed) → commit the coherent set.

**A2 · Root-fix Windows command-resolution ENOENT in safe-spawn** — score 92
Premise VERIFIED LIVE: `release-confidence.mjs` crashes `spawn npm ENOENT` (bare `npm`/`npx`/`node`/`git` fail on Windows without `.cmd`/shell). This is the "Launch readiness" advisory doctor item.
Action: extend `safe-spawn.harden()` to resolve known npm-family `.cmd` shims on Windows (no blanket `shell:true` — avoid injection). Re-run release-confidence to confirm.

### TIER 2 — This-repo genius-list items (autonomous, concrete, premise-verified)

**B1 · welcome-back-telemetry** — score 78
Premise VERIFIED: `assets/game-welcome-back.js` has 0 telemetry emits. Add `vs_welcome_back_shown` `/v/rum` beacon + Worker `prefixAllowlist`/`RUM_UX_EVENTS` (instrument both ends — feedback memory).

**B2 · individual-page ecosystem-bridge links → page-specific** — score 68
Premise VERIFIED: bridge markup exists across game/project pages with hardcoded links. Derive related game/project from `public-intelligence.json` instead of 4 hardcoded links (cross-surface leverage).

**B3 · Forge Window naming propagation** — score 76
Premise VERIFIED: 14 raw "Studio Pulse" text occurrences remain (URL `/studio-pulse/` preserved for SEO). Propagate established (S214) "Forge Window" vocab in generated copy with care (keep brand/title where intended).

### TIER 3 — Verify & close stale carries (honest closure)

**C1 · play-next rotation** — VERIFY-AND-CLOSE
Premise CHANGED: `api/dead-ctas.json` = `deadCount:0`. No longer dead → close the carry with evidence; do not rotate.

**C2 · Post-push CI / Lighthouse confirmation** — score 71
Check `gh run list` for recent push health; confirm the runner audits the real artifact. Verify-only.

### TIER 4 — Second-order innovation (generate when primary list thins)
To be authored during /implement once Tiers 1–3 land (goal mandates second-order candidates).

### Optional / investigate (budget-gated)
**D1 · ambient-core shell artifact tracking + Windows↔CI hash non-determinism** — MED
Committed HTML references `ambient-core.shell-<hash>.js` not tracked in git (every *other* shell artifact IS committed); works only because CF rebuilds on deploy. Hash differs Windows vs Linux. Investigate: gitignore all shell artifacts (deploy-built) OR make hash deterministic. Not breaking prod (CF regenerates f15).

---

## HONEST DEFERRALS (evidence-backed — recorded as wins)

- **First real push notification** (96) — sends a real broadcast to live subscribers; outward-facing, founder go-ahead required. Action: verify pipeline ready (`push:count`), do NOT send.
- **Signal Log post / forge devlog** (93/90) — founder-voice publish gate (never auto-publish). Action: may scaffold structure only.
- **studio-ops Ark cargo processing** (84) — sibling repo; not this tree.
- **Sibling CANON-006** (velaxis/syntha/shadow) — sibling repos; ship Ark cargo, never edit sibling trees (compliance-validation advisory).
- **MOBILE-SHEET-DEFAULT-SWAP** — founder real-device verification (flag-gating discipline).

---

## BUILD ORDER
A1 → A2 → C1 (close) → B1 → B2 → B3 → C2 (verify) → Tier 4 (innovation) → closeout.
