# AUDIT — VaultSpark Studios Website Premium Overhaul

**Date:** 2026-06-17 · Session 203
**Directive (founder):** Make the Studio homefront elite, seamless, premium across desktop + mobile (cost increase authorized). Rewrite the "pressure" mission statement. Full redundancy/merge pass landing → user panel. Ensure every surface is fresh + audience-correct. Deliver a token-optimal implementation plan.

**Founder decisions (2026-06-17):** Mission statement = **Purpose/portfolio-first** direction. Consolidation = **Conservative** (membership cluster → tabbed hub + brand+system hub ONLY; leave member/members, vault-portal, feedback as-is). Execution = **full build, all phases → verify → closeout**.

**Scope:** ~133 HTML routes. Static GitHub Pages site (vanilla HTML/CSS/JS, no build step), Supabase auth, Cloudflare Worker headers, Vault Member + Investor portals. Ambient-loader bundle architecture. SIL 982/1000.

---

## 0 · Pre-work (cheap, unblocks tooling) — do FIRST

| ID | Item | Why |
|---|---|---|
| P1 | Fix `scripts/render-startup-brief.mjs` — missing `scripts/lib/turn-classifier.mjs` (imported via `model-router.mjs`). Brief renderer is dead; last brief is 53h stale. | Restores /start + /closeout tooling. ~10 min. |
| P2 | Resolve the 1 failing doctor probe (11/13). Run `node scripts/ops.mjs doctor` to identify. | Unblocks clean health signal before/after the overhaul. |

---

> **PROGRESS LOG**
> - ✅ §0 P1 — startup-brief renderer fixed (7 missing modules from a half-applied refactor recreated: turn-classifier, visual-blocks, doctor-predicates, shared-policies, classify-warning-provenance, sil-forecaster, skill-cost-ledger). Renders EXIT 0, validates conformant.
> - ✅ §0 P2 — doctor 3 failures triaged: all advisory portfolio-rollout drift + known HCLOUD_TOKEN staging human-block. None block this work.
> - ✅ §1 — mission statement rewritten (purpose-first) across studio blockquote, studio why-quote, homepage panel, homepage hero-story, press blockquote, press short-bio. Universe lore left as deliberate fiction. Old "pressure/containment/ignition" phrasing gone from all mission surfaces.
> - ✅ §2 — premium polish layer shipped (additive, `assets/style.css`): motion/elevation/radius/accent-role tokens, site-wide `:focus-visible` ring, button press states, branded selection, refined scrollbar, smoother card lift. Reduced-motion-guarded. Propagated to 104 pages via shell rebuild (`style.shell-a603ec43fc.css`).
> - ✅ **build:check EXIT 0** — completing the WIP also exposed + fixed 3 previously-masked gate failures (ignis-platform-status `schemaVersion`, taskboard runway hygiene, studio "one spark" solo-bet posture). Generated artifacts regenerated; remaining ✗ are warn-only advisories (7 registry on-site dirs, `/` desktop perf, changelog 66d stale).
>
> **CARRIED to next session (need fresh context for the elite/seamless quality bar):**
> - §3 Homepage hero refinement (H1–H6) — flag-gate per the mature-surface rule; founder visual review recommended before swapping the hero.
> - §4 Member portal premium + redundancy (M1–M7).
> - §5 Conservative consolidation — membership cluster → tabbed hub + brand+system hub (needs Worker Layer 0c 301s, not meta-refresh).
> - §6 Freshness + audience-correctness sweep (F1–F4).

## 1 · Mission statement rewrite (small, high-value, clean propagation)

**Current "pressure" copy — TWO quotes, 7 locations:**

- **Quote A (manifesto core):** *"The vault is not a metaphor for storage. It is pressure. It is containment. It is the moment before ignition."*
  - `studio/index.html:320-322` · `index.html:1729-1730` · `press/index.html:193` · `journal/vault-opened/index.html:204-207` · `feed.xml:354-357` · `universe/index.html:465-469`
- **Quote B (pressure system):** *"The vault isn't a brand. It's a pressure system. Seal the idea. Pressurize it. Don't open it until it's ready to spark."*
  - `studio/index.html:570`

**Grounding (from SOUL.md / PROJECT_BRIEF.md):** Independent creative studio forging games, cinematic worlds, creative tools, and AI-native intelligence as ONE connected portfolio. Builds in the open. Real identity — unpredictable, memorable, impossible to ignore. Intentional, atmospheric, premium, earned, studio-owned. Vault/forge/spark lifecycle is load-bearing and stays.

**Plan:** Founder selects a direction (3 drafted, see chat). Replace BOTH quotes consistently across all 7 surfaces + any OG/meta description that echoes them. Verify with a grep that no "pressure system" / "moment before ignition" string survives unless intended.

---

## 2 · Global design layer — HIGHEST LEVERAGE (one edit cascades to 133 pages)

Edit shared `assets/style.css` tokens + shared nav/footer include once; every page inherits.

| ID | Item |
|---|---|
| G1 | **Accent discipline:** gold (#FFC400) is overused → introduce a disciplined secondary (cyan/violet) for hierarchy; define usage rules in tokens. |
| G2 | **Type scale + weight:** headings 600 (not 700 Georgia bold) for modern luxury; reserve 700 for accents; tighten clamp() scale for rhythm. |
| G3 | **Motion system:** standardize easing/durations as tokens; add tasteful scroll-reveal + smooth view transitions; honor `prefers-reduced-motion` (already present). |
| G4 | **Spacing/elevation tokens:** unify radius, shadow ramps, section rhythm so every page breathes consistently. |
| G5 | **Mobile parity (CANON-041):** single responsive nav component; kill desktop/mobile nav duplication; verify safe-area insets, 16px input floor (known iOS traps in memory). |
| G6 | **Portal CSS consolidation:** fold `vault-member/portal.css` + `portal-shell.css` design tokens into the global source of truth (scoped `[data-portal]`) so portal matches homepage polish. |

---

## 3 · Homepage premium pass (single biggest surface — `index.html`)

| ID | Item |
|---|---|
| H1 | **Hero simplification:** 3 glow circles + spark burst + chamber vignette = overload. Unify to one cohesive glow; faster wordmark reveal (<0.6s); modern weight treatment. |
| H2 | **CTA consolidation:** one crystal-clear primary CTA in hero; demote repeated membership pitch to deeper section. |
| H3 | **Milestone cards:** richer live/next/done states (glow "Live Now" badge, progress indicator); currently near-invisible status. |
| H4 | **Recent Ships / heartbeat / momentum:** consolidate 3 overlapping feeds into one "What's Live / What's Next" strip with play-ability. |
| H5 | **Intelligence strips:** cryptic flavor ("Something answered…") → 1–2 compelling, visual lore/intelligence callouts; keep deep IGNIS on its own surface. |
| H6 | **Trust/social proof:** elevate from text dumps to visual proof (member avatars, live leaderboard snippet, real numbers). |

---

## 4 · Member portal premium + redundancy fixes (`vault-member/`, `member/`, `members/`, `vault-portal/`)

| ID | Item |
|---|---|
| M1 | **Entry consolidation:** `/vault-portal/` two-doors intermediary adds a click with no payoff → merge into `/vault-member/` (or make door purpose explicit). |
| M2 | **Profile pages:** `/member/` (lookup) + `/members/` (directory) overlap → `/members/` canonical leaderboard, `/member/:username` detail. |
| M3 | **Tier vs Rank clarity:** subscription tier (SPARKED/ETERNAL) and points-rank (Vault Seeker…) presented inconsistently across chip + profile → clear, separated labeling. |
| M4 | **Motivational dashboard:** add "next achievable" milestones + "Your Games" cross-game progress row (dashboard is currently retrospective only). |
| M5 | **Referral surfacing:** promote referral from buried profile line to first-class dashboard card. |
| M6 | **Session UX:** subtle expiry warning + token-refresh feedback (currently silent logout). |
| M7 | **Notifications:** wire the referenced-but-dead notification bell to a feed (achievement unlocked, friend joined). |

---

## 5 · Information architecture consolidation (landing → panel redundancy)

**Merge candidates (founder to confirm aggressiveness):**

| Candidate | Recommendation |
|---|---|
| `/membership/` + `/vaultsparked/` + `/membership-value/` + `/ranks/` | Consolidate into ONE membership hub with tabs (Overview · Tiers/Pricing · Benefits · Ranks). Highest-value merge. |
| `/vault-wall/` + `/leaderboards/` | Differentiate clearly or merge; nav currently confusing. |
| `/brand/` + `/brand/system/` | One "Brand & Design" hub. |
| `/feedback/` + `/feedback/insights/` | Single feedback dashboard. |
| `/social/` + `/pathways/` | Keep distinct (different intent) — DO NOT merge. |
| Legal pages | **DO NOT MERGE** — must stay individually addressable (app-store + GDPR; memory-confirmed). |

Nav itself (6 dropdowns) is comprehensive but heavy — prune/regroup once merges land.

---

## 6 · Freshness + audience-correctness sweep

| ID | Item |
|---|---|
| F1 | Verify every page's project status labels match real build state (S197 lesson: registry labels lie; walk the journey, click promoted CTAs). |
| F2 | Confirm dev-jargon `currentFocus` etc. uses `publicNote`/`publicNextStep` plain-English on visitor surfaces (memory pattern). |
| F3 | Ensure logged-in-only content isn't leaking to anonymous visitors and vice-versa (account-chip presence-first model). |
| F4 | Sync game/project counts + statuses (Sparked/Forge/Vaulted) across homepage, /games, /projects, nav dropdowns. |

---

## 7 · Verification (last)

- `npm run build` then `npm run build:check` (generators cascade; build first — memory).
- Mind cmd.exe 8191-char `build:check` limit — don't extend the chain naively.
- Walk the real journey: home → hero CTA → play → join → portal, desktop + mobile.
- Prod verify via pages.dev origin + a prod JSON path (CF bot-challenge ≠ outage — memory).
- Ensure pushed tip is NOT a `[skip ci]` commit (CF Pages strands deploys — memory).

---

## Token-optimal execution order (rationale)

1. **§0 pre-work** — cheap, unblocks tooling, no page reads.
2. **§1 mission statement** — 7 known locations, surgical, no exploration cost.
3. **§2 global design layer** — edit shared CSS/nav ONCE → cascades to all 133 pages (max leverage per token).
4. **§3 homepage** — biggest single surface, already mapped.
5. **§4 portal** — second cluster, already mapped.
6. **§5 IA consolidation** — structural; done after nav is understood.
7. **§6 freshness sweep** — touches many files but is verification-grade, cheap per file.
8. **§7 verify** — once.

Grouping by subsystem (not by page) keeps each file opened ~once.
