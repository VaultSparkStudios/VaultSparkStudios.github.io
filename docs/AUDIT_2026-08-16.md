<!-- generated-by: render-audit-md.mjs (R-H14) — DERIVED FROM AUDIT_2026-08-16.json; do not hand-edit -->
<!-- generated-at: 2026-08-17 -->
<!-- project: vaultsparkstudios-website · type: app -->

# Project Audit — VaultSpark Studios Website

> One ranked improvement plan covering 9 axes. Top items first.
> Source of truth: this file is derived from the JSON sidecar. Edit the JSON, then re-render.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 8
- Combined Priority: 221.80
- Top 3: release-capability-slice-gate · mobile-runtime-release-contract · immutable-rollback-origin
- Sampled: undefined/undefined files · undefined tokens

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov | Priority | Status | Item |
|---|:-:|---|---|:-:|:-:|:-:|:-:|---|
| 1 | 🔥 | security | 6h | 10 | 9 | 30.0 | ✓ | **release-capability-slice-gate** — Make every production deploy obey one gate and promote caller plus callee as a capability slice. PRE-VERIFIED P0. npm run deploy calls scripts/deploy-worker.mjs with only --confirm-production; the script never runs check-production-promotion-gate or the release ceremony, unlike CI. Separately, the content lane explicitly allows changed hash-named client JavaScript while blocking cloudflare/**, the exact architecture that shipped Desk callers and stranded their routes in S317. Outcome: a deploy cannot bypass the hold, and a promoted browser capability cannot arrive before the Worker route it calls. **Recipe:** L2: make the local production Worker path enforce the same promotion gate and fresh release ceremony as CI; extract a caller-to-route capability manifest from promoted shell bundles; block content promotion when a changed caller depends on a route not proven live, and cover the S317 desk-reaction shape with mutation tests. |
| 2 | 🔥 | ux | 6h | 10 | 8 | 26.7 | ✓ | **mobile-runtime-release-contract** — Turn 157 recorded mobile defects into a blocking rendered-runtime contract. PRE-VERIFIED P0. tests/mobile-audit.spec.js records findings but contains no expect(), and check-touch-targets fails only under --strict while build:check calls it without that flag. The durable ledger contains five overflows, 74 small targets, 75 small-font findings, and three image-dimension defects, including reproducible Rank Projector overflow at membership routes. The static mobile contract still passes. Outcome: every touched mobile journey is usable at 360–430px and a runtime regression stops release. **Recipe:** L2: repair Rank Projector containment and the high-severity target/font defects; require zero P0/P1 findings and complete route×viewport execution; run touch-target checking in strict mode; bind the visual receipt to current source SHA, routes, themes, and screenshot hashes. |
| 3 | 🔥 | security | 8h | 9 | 8 | 21.7 | ⛔ | **immutable-rollback-origin** — Make rollback restore last-known-good bytes instead of moving main. PRE-VERIFIED P0. GitHub Pages is configured as a legacy main-branch build and the repository describes it as the warm rollback origin. Direct main pushes continue publishing that origin while Cloudflare promotion can be held, so a DNS rollback can restore the very candidate the release gate rejected. Outcome: rollback points at an immutable verified production artifact and is advanced only after post-deploy smoke. **Recipe:** L2: replace the moving main source with a dedicated rollback publication workflow that deploys an immutable verified SHA/artifact; retain current and previous generations; bind the pointer and recovery instructions to the release receipt. |
| 4 | 🔥 | security | 5h | 8 | 6 | 17.1 | ✓ | **push-subscription-enrollment-hardening** — Stop unauthenticated push subscription poisoning and unbounded fan-out. PRE-VERIFIED. The Worker accepts any endpoint up to 512 characters and arbitrary keys, with no origin check or enrollment quota, stores rows for 90 days, and the sender enumerates every row. A bot can create unbounded KV state and force later notification work toward attacker-chosen endpoints. Outcome: only valid same-origin Web Push subscriptions enter a bounded active set. **Recipe:** L2: require HTTPS and allowlisted Web Push hosts, exact p256dh/auth key shapes, same-origin Origin, per-IP/day and global enrollment caps, dedupe, malformed-row quarantine, and hostile-input Worker tests. |
| 5 | ⚡ | intelligence | 2h | 9 | 9 | 40.5 | ✓ | **agent-crawler-policy-coherence** — Let ChatGPT search reach the AI corpus while retaining the training opt-out. PRE-VERIFIED. robots.txt blocks GPTBot, ChatGPT-User, and OAI-SearchBot even though the site publishes agents.json, llms.txt, and agent-readable pages for discovery. The validator parses only User-agent: * and cannot see the contradiction. Official OpenAI publisher guidance distinguishes OAI-SearchBot discovery from GPTBot training. Outcome: the public corpus can be surfaced and cited in ChatGPT search without opting the proprietary site into training. **Recipe:** L2: keep GPTBot disallowed; allow OAI-SearchBot and user-request retrieval on public discovery surfaces; parse every named robots group; classify training/search/user agents; verify live routes and document the proprietary citation policy in agents.json. |
| 6 | 🔥 | featureDepth | 4h | 10 | 8 | 30.9 | ✓ | **desk-freshness-honesty-court** — Make The Desk daily in evidence—or stop claiming it is. PRE-VERIFIED. The public JSON-LD and generated hub say Daily AI news while the newest edition and stats are dated 2026-08-11; on 2026-08-16 the public stats feed already classifies both story and fact metrics stale. news:check proves byte coherence, not editorial liveness. Outcome: visitors see a truthful cadence and the editorial pipeline produces a review-held recovery packet instead of silently aging. **Recipe:** L2: derive a freshness state from the latest edition; render Daily only inside its evidence window and periodic/paused when overdue; add a blocking freshness contract for public claims; use the existing trend radar plus zero-model drafter to create a review-held overdue edition packet, never auto-published commentary. |
| 7 | 🔥 | intelligence | 8h | 9 | 9 | 24.4 | ✓ | **fact-complete-claim-ledger** — Put every sourced fact into the agent claims stream it already promises. PRE-VERIFIED. Public stats count 21 sourced facts, but api/news-desk-claims.ndjson contains only stance and prediction rows. The producer describes one line per claim and the hub advertises the file as the agent-readable claims stream, yet it never iterates story.facts. Outcome: a human or agent can verify and cite an exact factual claim in one hop. **Recipe:** L2: emit type:fact rows with stable IDs/hashes, exact article anchors, fact text, source URL, story/date, and source-health metadata; render copyable claim receipts; enforce exact page-fact to feed-fact parity and reject orphan/duplicate factRefs. |
| 8 | ⚡ | observability | 6h | 9 | 8 | 24.0 | ✓ | **receipt-bound-status-projection** — Make unknown or stale evidence poison green across every public status projection. PRE-VERIFIED. PROJECT_STATUS simultaneously carries stale 295/295 fingerprints, a legacy testsPassed 302 alias, silScore 981 plus legacy sil 993, currentFocus S317, and lastSessionSummary S316. The last-summary checker is not in the build chain. The CI beacon also records unknown scheduled workflows while allGreen/no-dead-cron can remain true. Outcome: one receipt-bound projection owns each claim, and unknown/stale evidence can never render green. **Recipe:** L2: schema-ban canonical-name lookalikes and stale aliases; project tests/SIL/session summary from authoritative receipts; wire last-session coherence; make scheduled unknown/stale a non-green state with coverage counts and age; include field-data observed-through dates so a fresh generatedAt cannot launder July evidence as August. |

## Depth Ladder (per item: L1 minimal · L2 solid · L3 genius)

> `/implement` selects the depth that fits remaining budget, and climbs the ladder
> on already-shipped items when the session still has budget (S175 saturation).

### release-capability-slice-gate
- **L1 · minimal** _(~2h)_ — Call the promotion interlock and release ceremony inside deploy-worker before Wrangler; necessary flag is never sufficient.
- **L2 · solid** _(~6h)_ — L1 plus a deterministic client-capability manifest and a content-lane gate that requires route-provenance proof for every changed caller.
- **L3 · genius** _(~12h)_ — L2 plus a hash-bound CAPABILITY_SLICE receipt pairing Pages bytes, Worker version, route contracts, smoke evidence, and rollback anchors.

### mobile-runtime-release-contract
- **L1 · minimal** _(~2h)_ — Fix current Rank Projector overflow and enable strict touch-target failure.
- **L2 · solid** _(~6h)_ — L1 plus executable mobile-audit assertions, route×viewport completeness, and source-bound rendered evidence.
- **L3 · genius** _(~12h)_ — L2 plus a RUM-weighted responsive sentinel and append-only visual-delta ledger across touched themes and states.

### immutable-rollback-origin
- **L1 · minimal** _(~3h)_ — Stop legacy main publication and publish a dedicated rollback branch/artifact from the current verified production SHA.
- **L2 · solid** _(~8h)_ — L1 plus two retained generations, release-receipt binding, and a post-deploy pointer advance.
- **L3 · genius** _(~16h)_ — L2 plus an automated DNS recovery drill proving Pages bytes, Worker version, CSP, and core routes match the selected receipt.

### push-subscription-enrollment-hardening
- **L1 · minimal** _(~2h)_ — Validate endpoint scheme/host, key encoding/size, same-origin Origin, and add hostile-input tests.
- **L2 · solid** _(~5h)_ — L1 plus bounded quotas, dedupe, quarantine/cleanup, and honest enrollment counters.
- **L3 · genius** _(~10h)_ — L2 plus proof-of-possession enrollment that promotes a quarantined row only after one successful challenge receipt.

### agent-crawler-policy-coherence
- **L1 · minimal** _(~30m)_ — Allow OAI-SearchBot while preserving GPTBot Disallow.
- **L2 · solid** _(~2h)_ — L1 plus named-group validation, live UA probes, and an explicit public citation/training policy in agents.json.
- **L3 · genius** _(~5h)_ — L2 plus a signed agent-access policy with purpose, surface, license, citation, and health fields per agent class.

### desk-freshness-honesty-court
- **L1 · minimal** _(~1.5h)_ — Add a freshness badge and downgrade Daily to periodic whenever the source is overdue.
- **L2 · solid** _(~4h)_ — L1 plus a freshness gate and review-held recovery packet built from existing zero-runtime-cost tools.
- **L3 · genius** _(~10h)_ — L2 plus a format/beat/source/near-term-call flight director with a hash-bound liveness receipt.

### fact-complete-claim-ledger
- **L1 · minimal** _(~3h)_ — Emit stable type:fact rows with story, anchor, text, and source URL.
- **L2 · solid** _(~8h)_ — L1 plus copyable claim receipts, source health, stable hashes, and exact page/feed parity gates.
- **L3 · genius** _(~16h)_ — L2 plus an append-only claim-change/correction graph with observed and rechecked timestamps.

### receipt-bound-status-projection
- **L1 · minimal** _(~2h)_ — Reconcile current status fields, remove or reject legacy aliases, and wire the last-session checker.
- **L2 · solid** _(~6h)_ — L1 plus unknown-schedule poisoning, coverage/age fields, and observed-through field-vitals semantics.
- **L3 · genius** _(~12h)_ — L2 plus a typed projection graph where every public claim carries receipt id, release SHA, confidence, and freshness contract.

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

- desk-topic-dossiers
- prediction-observatory
- reader-signal-decision-receipts

## Skipped (Priority < 1.5)

- undefined — FALSE PREMISE / REFUSED. Worker route history proves reactions and presence are live; the current insufficient state is honest and the floor is the privacy feature.
- undefined — FALSE PREMISE. Current repository gates pass across all four surfaces; no live defect was found.
- undefined — CULLED. Deterministic topic, claim, persona, and source relations achieve the information-finding outcome without uncapped cost or unverifiable ranking.

## Execution Log

### release-capability-slice-gate
- `2026-08-16T21:30:00.000Z` · **shipped** · Local and CI Worker production paths now share the fail-closed promotion ceremony. Content promotion extracts literal /v/* callers, hash-binds them, and requires fresh per-route production proof. Self-tests: capability slice 7/7, deploy entrypoint 4/4, promotion gate 14/14, content preflight 5/5.

### mobile-runtime-release-contract
- `2026-08-16T21:30:00.000Z` · **shipped** · Rank Projector containment and shared mobile targets repaired. Runtime contract is blocking at 47 routes x 5 viewports; 235/235 passed with zero P0/P1. CANON-053 receipt contains 63/63 manually reviewed captures across three routes, seven themes, and three mobile widths.

### immutable-rollback-origin
- `2026-08-16T06:39:58.019Z` · **blocked** · Verified live: GitHub Pages is legacy main/. A prior binding decision (D-S303) classifies changing the public warm-rollback origin as founder-scoped. S318 deploy authorization does not silently override that architecture decision; no external Pages setting was mutated. Exact next action: founder explicitly authorizes switching Pages to an immutable rollback branch/artifact, then run the L2 migration and recovery drill.

### push-subscription-enrollment-hardening
- `2026-08-16T21:30:00.000Z` · **shipped** · Push enrollment now requires same-origin HTTPS, vendor allowlisting, exact key shapes, bounded bodies, per-IP/global quotas, dedupe, corrupt-row quarantine, and bounded validated dispatch. Worker unit suite passed 48/48.

### agent-crawler-policy-coherence
- `2026-08-16T21:30:00.000Z` · **shipped** · Crawler policy now distinguishes training opt-out from public search/user retrieval; all named groups are checked. Crawler self-test 8/8 and local live-coherence check passed.

### desk-freshness-honesty-court
- `2026-08-16T21:30:00.000Z` · **shipped** · Desk cadence is evidence-derived: overdue editions render periodic rather than daily, publish a freshness receipt, and generate a review-held recovery packet. Freshness self-test 5/5; all 10 generated News pages current.

### fact-complete-claim-ledger
- `2026-08-16T21:30:00.000Z` · **shipped** · Claims feed now emits 21 stable sourced fact rows in a 37-row NDJSON ledger with article anchors and receipts. Claim parity self-test 3/3 and exact live parity passed.

### receipt-bound-status-projection
- `2026-08-16T21:30:00.000Z` · **shipped** · Status projection now removes legacy aliases, binds tests/SIL/session fields to authoritative receipts, and makes unknown scheduled CI non-green. Full build passed 309/309; focused status projection and CI beacon tests passed.

---
*S318 /arc audit. Release safety is the first-order theme: the advertised local Worker deploy bypasses the promotion interlock, the content lane can ship a caller while stranding its Worker callee, and the nominal GitHub Pages rollback origin follows rejected main candidates. Product truth is the second: mobile runtime findings cannot fail CI, The Desk promises daily while stale, and its agent claims stream omits every sourced fact.*
