<!-- generated-by: render-audit-md.mjs (R-H14) — DERIVED FROM AUDIT_2026-08-31.json; do not hand-edit -->
<!-- generated-at: 2026-09-01 -->
<!-- project: vaultsparkstudios-website · type: website -->

# Project Audit — VaultSparkStudios.github.io

> One ranked improvement plan covering 9 axes. Top items first.
> Source of truth: this file is derived from the JSON sidecar. Edit the JSON, then re-render.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 14
- Combined Priority: 429.80
- Top 3: pathways-doorway-pages · redirects-splat-301-into-404 · transparency-surface-sprawl
- Sampled: undefined/undefined files · undefined tokens

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov | Priority | Status | Item |
|---|:-:|---|---|:-:|:-:|:-:|:-:|---|
| 1 | 🔥 | ux | 2h | 8 | 5 | 50.0 | · | **redirects-splat-301-into-404** — Four real content pages are 301'd into 404s by _redirects splat rules. PRE-VERIFIED: _redirects maps /solara/* -> /games/solara/:splat and /franchise-architect/* -> /games/franchise-architect/:splat, but solara/archive.html, solara/chronicle.html, solara/sun-widget.html and franchise-architect/game.html (42KB) exist only under the OLD prefix — no counterpart exists under the new one (checked all four destinations). Every one of those URLs therefore returns a 301 into a 404. solara/chronicle.html is 13KB of Solara lore whose only inbound links are its two siblings — an orphaned island the live site cannot reach. Outcome: a bookmark, citation, or old social link to Solara lore or the legacy build lands on a working page instead of a dead end. **Recipe:** Decide per page. Migrate: git mv the three Solara pages under games/solara/ so the existing splat resolves, and relink them from games/solara/index.html. Retire: franchise-architect/game.html is a superseded local build (the live game is at playfranchisearchitect.com) — delete it and add an explicit tombstone rule ABOVE the splat. Then add a build court that resolves every _redirects rule against the built tree and fails when a splat destination cannot exist. |
| 2 | 🔥 | ux | 5h | 9 | 7 | 51.4 | · | **pathways-doorway-pages** — Six Pathway pages ship 23KB each to say 530 bytes — thin doorway pages that dead-end the visitor. PRE-VERIFIED: /pathways/{players,builders,lore,press,investors,supporters}/ are 23.3KB each and the <main> of each is 493-545 bytes — one eyebrow, one headline, one sentence, two buttons. 97.7% of every page is duplicated nav/footer/head chrome. This is the doorway-page shape search engines penalise, and it is a worse experience than the /pathways/ hub that already exists: a visitor who picks 'Players' is rewarded with less information than the page they left. Outcome: the audience-segmentation promise pays off — each pathway either becomes a real guided route worth arriving at, or stops costing a navigation step for nothing. **Recipe:** Choose one end, not the middle. (a) Collapse all six into anchored sections of the existing /pathways/ hub and tombstone the six routes to /pathways/#slug — six fewer pages, zero content lost. (b) Make each pathway substantive: a 4-6 step ordered route naming the specific pages, feeds and receipts that audience needs, each step deep-linked, plus that audience's machine surface so an agent can walk the same route. Recommended: (a) now, (b) for investors and press only, where a dedicated landing genuinely converts. |
| 3 | 🔥 | ux | 10h | 9 | 8 | 46.7 | · | **transparency-surface-sprawl** — Eight competing live-data surfaces with no spine — the studio's best asset is unnavigable. PRE-VERIFIED by title+description extraction: /stats/ (portfolio, audience, editorial, performance and proof statistics), /stats/ecosystem/ (audience coverage and edge infrastructure), /status/ (real-time service status), /proof/ (re-fetches the deploy ledger and re-computes every hash in your browser), /studio-pulse/ (a live window into the forge), /oracle/ (IGNIS's live read of every project), /notebook/ (every commit is an entry), /ignis-health/ (internal). Eight pages answer overlapping forms of one question — 'is this studio real and working?' — and nothing tells a visitor which to open. This radical-transparency layer is the most differentiated thing the site has, and it is diluted by having no front door. Outcome: a first-time visitor, a journalist, and an agent each reach the evidence they came for in one hop instead of guessing among eight. **Recipe:** Build /evidence/ as the one spine: four honest tabs — LIVE (status, uptime), NUMBERS (stats, ecosystem), PROOF (in-browser hash verification, deploy ledger), MOTION (pulse, notebook, oracle commentary). Keep the eight existing pages as deep views and canonicalise each tab to its deep page so no receipt or permalink breaks. Every tab states its own freshness and source feed, and reports UNKNOWN honestly rather than rendering a stale green. Link /evidence/ from the primary nav and from agents.json discovery. |
| 4 | 🔥 | token | 3h | 8 | 7 | 28.0 | · | **desk-topic-novelty-gate** — The Desk can spend three consecutive editions on one story — novelty is enforced after drafting, never before. PRE-VERIFIED in scripts/news-draft-edition.mjs: selectDraftableTopic() walks the ranked queue for REACHABILITY and remembers dead hosts (the S333 fix), but holds no memory of what the Desk already published — nothing in the selector excludes a recently-used slug. On disk, news/2026-08-21, 2026-08-22 and 2026-08-23 all carry the slug from-atari-to-eve-online-building-on-15-years. The 22nd and 23rd are correctly noindexed and canonicalised, so SEO damage was contained — but each still consumed a full LLM draft, an OG image render, and a publish slot, and the human-facing newsroom spent three days of output on one story. Fixing this at selection is strictly cheaper than patching it at publish. MEASUREMENT PLAN: log slug-repeat rate and drafts-per-unique-story per 14-day window before and after; expect drafts-per-unique-story to fall from ~1.4 toward 1.0 with a matching drop in completion and OG-render calls. **Recipe:** Give the selector the same shape of memory it already has for dead hosts: read the published editions index, build a recently-published slug set plus a normalised title-token set to catch reslugged repeats, and skip those topics for free — as a `skipped` reason, not a spent attempt, exactly as the dead-domain path already does. Emit the skip reason into the run receipt so a thin queue is distinguishable from an over-covered one. Self-test three cases: identical slug, reslugged same story, and a genuine follow-up that SHOULD be allowed because it carries new primary sources. |
| 5 | 🔥 | ai | 3h | 8 | 8 | 32.0 | · | **ai-factsheet-layer-undiscoverable** — The 17-page AI fact-sheet layer is excellent and completely undiscoverable. PRE-VERIFIED: 18 tracked files under */.ai/, each an index-follow, self-describing, 'cite this page' canonical fact sheet — the strongest CANON-048 asset on the site. And: zero appear in sitemap.xml (confirmed by diffing the tracked HTML set against it), zero appear in agents.json (0 matches for '.ai/' in a file that lists 19 projects and links each to an llms-full.txt instead), zero carry JSON-LD despite being the machine-canonical surface, and no human page links to one. The layer exists, is served (.nojekyll present), and no crawler or agent will ever find it. Outcome: agent-cited answers about VaultSpark projects resolve to the studio's own canonical sheet rather than a third-party summary. **Recipe:** Three small joins, all generator-side so they cannot drift: (1) emit each .ai/ URL into sitemap.xml from the same registry projection that already writes the project pages; (2) add an aiFactSheet field beside the existing llmsFull on every agents.json project entry; (3) emit JSON-LD (SoftwareApplication/CreativeWork plus isPartOf the studio Organization) into each sheet from the generator that writes its prose, and cross-link each project page to its sheet with rel=alternate. Then extend the standard-files court so a project page without a discoverable fact sheet fails the build. |
| 6 | 🔥 | security | 1h | 7 | 4 | 17.7 | · | **ignis-health-ungated** — /ignis-health/ calls itself internal and is protected only by robots.txt. PRE-VERIFIED: the page title is literally 'IGNIS Health — VaultSpark Studios (internal)' and robots.txt Disallows /ignis-health/ for every crawler group. But GATED_PATH_PATTERNS in cloudflare/security-headers-worker.js contains exactly three entries — /investor-portal, /studio-hub, /vault-member/admin — and /ignis-health is not among them. robots.txt is a request to well-behaved crawlers, not access control: the page is fully readable by anyone with the URL, and it publishes the Supabase project ref and the ask-ignis edge-function contract including its expected auth responses. Outcome: the site's own 'internal' label becomes enforceable, and the operator telemetry surface stops describing the auth boundary to unauthenticated readers. **Recipe:** Pick the honest end. Gate it — add /^\/ignis-health(\/|$)/i to GATED_PATH_PATTERNS so the edge redirects to sign-in without a session cookie, confirming the gate does not emit a cacheable 301 — or make it genuinely public by dropping the '(internal)' label and the function-contract detail. Gating is recommended. Then close the class: a build court asserting every path robots.txt Disallows for the * group is either in GATED_PATH_PATTERNS or on an explicit intentionally-public-but-unindexed list, so the two can never silently disagree again. |
| 7 | ⚡ | organization | 30m | 5 | 3 | 15.0 | · | **orphan-sitemap-html-duplicate** — sitemap.html is a tracked, deployed, drifting duplicate of /sitemap-page/ that nothing links to. PRE-VERIFIED: sitemap.html (34.3KB) and sitemap-page/index.html (36.1KB) carry byte-identical titles and meta descriptions and different content hashes — they have already drifted. git grep finds 0 inbound links to /sitemap.html and inbound links to /sitemap-page/ on essentially every page; sitemap.xml lists /sitemap-page/ only. The file is tracked, shipped, unreachable through the UI, and guaranteed to fall further out of date each time the real one regenerates. Outcome: one human sitemap that is always correct, and one less deployed artifact that can contradict the site. **Recipe:** Delete sitemap.html and add /sitemap.html -> /sitemap-page/ 301 for external bookmarks. Confirm no generator writes it — if one does, remove that output path rather than the file, or it returns on the next build. |
| 8 | 🔥 | speed | 4h | 8 | 6 | 38.4 | · | **homepage-inline-critical-css-66kb** — The homepage inlines 66KB of critical CSS — roughly five times the first-window budget, uncacheable on every visit. PRE-VERIFIED by summing inline blocks: index.html carries 66,473 bytes of inline <style> and 9,471 bytes of inline <script> inside a 159KB document. The async-CSS strategy around it is already correct (preload plus media=print swap plus noscript fallback), which makes the inline block the remaining lever rather than a symptom of a missing one. 66KB of inline critical CSS exceeds the ~14KB first-congestion-window target by roughly 5x, blocks the parser before any paint, and — unlike the hashed shell — is re-sent in full on every navigation because it cannot be cached separately. Outcome: measurably faster LCP on the highest-traffic page and a smaller repeat-visit payload, with no visual change. **Recipe:** Measure first so the change is provable: capture the current homepage LCP/CLS/INP receipt. Then narrow the inline block to what actually paints above the fold — shell tokens, nav, hero — and let the already-async full sheet carry the rest; the generator emitting data-vs-critical-shell should compute the block from above-fold selector coverage rather than shipping a broad slab. Add a hard byte budget for inline critical CSS to build:check (fail over ~20KB) so it cannot creep back. Re-capture and compare; keep the change only if LCP actually improves. |
| 9 | 🔥 | featureDepth | 8h | 8 | 7 | 24.0 | · | **membership-identity-sprawl** — Eight membership surfaces, no spine — a visitor cannot tell where their account actually lives. PRE-VERIFIED by title+description extraction: /membership/ (your identity inside VaultSpark, three tiers), /vault-member/ (create your free account; 133KB, nine dashboard tabs), /members/ (member directory), /member/ (a member public profile), /vault-wall/ (top-ranked members), /invite/ (invite friends), /community/ (Vault Members, Discord, fan art, leaderboards, challenges), plus /leaderboards/ with seven sub-boards. Four of these — /vault-wall/, /members/, /community/, /leaderboards/ — overlap heavily on 'who else is here and how do I rank'. The retired stubs already show the right instinct (/join, /ranks, /vault-portal, /membership-value all now redirect into anchors); the same consolidation simply has not reached the surviving eight. Outcome: one obvious answer to 'what is my account and what do I do with it', which is the site's primary conversion. **Recipe:** Draw the spine explicitly: /membership/ is the PITCH (why an account, what the tiers give), /vault-member/ is the ACCOUNT (everything you do once signed in), /community/ is the PEOPLE (directory, wall, leaderboards, invites). Fold /vault-wall/ and /members/ into /community/ as sections and tombstone them to anchors, exactly as /ranks and /join already are. Move /invite/ inside the account, where the person who can invite actually is. Give each of the three one unambiguous cross-link to the other two, and make the nav expose exactly those three. |
| 10 | ⚡ | organization | 2h | 5 | 5 | 12.5 | · | **dual-redirect-implementations** — Retired routes are implemented twice — meta-refresh stubs AND _redirects 301s — and the two lists can drift. PRE-VERIFIED: 16 stub pages (ranks, join, ip, vault-portal, membership-value, nervous-system, franchise-architect, solara, leaderboards/global, brand/system, security/trusted-types, ignis/roi, vaultsparked, vault-treasury and others) each ship a meta http-equiv=refresh document — and _redirects independently defines a real 301 for the same routes. Whichever wins, the other is dead weight that can silently disagree; a meta-refresh is also slower and worse for assistive tech than a 301. The stubs are not even internally consistent: fifteen use a minimal document while vault-treasury/index.html ships a themed page that loads the nav-sheet bundle to say one sentence. Outcome: one redirect mechanism, one list to maintain, and retired routes that resolve at the edge instead of after a paint. **Recipe:** Make _redirects the single source of truth. Confirm at the edge that each 301 actually fires in production — the Worker sits in front of the origin, so verify against the live host, not pages.dev — then delete the stub files. Where a stub covers a route _redirects does not, add the rule before deleting. Add a build court asserting no tracked HTML contains http-equiv="refresh", so stubs cannot reappear. |
| 11 | 🔥 | ai | 12h | 8 | 9 | 41.1 | · | **unified-grounded-answer-surface** — Three separate answer engines, none grounded in the fact-sheet layer the studio already publishes. PRE-VERIFIED: assets/ignis-answer-engine.js (61KB), assets/vault-oracle.js (32KB) and assets/oracle-extra.js (28KB) are three distinct client bundles behind /oracle/, /ask-founders/ and the in-page answer affordances — 121KB of answer machinery — while the 17 .ai/ canonical fact sheets (item 5) and /.well-known/entity-graph.json (18KB) sit unused as grounding. The studio maintains three answerers and one authoritative corpus, and they are not connected. Outcome: a visitor or agent asking 'what is Atlas / is Voidfall live / what shipped last week' gets one answer citing the studio's own canonical sheet, with a link they can verify — and the studio stops maintaining three engines. MEASUREMENT PLAN: record answered-with-citation rate and client bundle bytes before and after; the citation rate is the quality gate, not the byte count. **Recipe:** Ground before you merge. Step one: make ONE engine read entity-graph.json plus the .ai/ sheets as its retrieval corpus, and require every answer to carry a citation link into that corpus — an answer with no citable source says so rather than improvising. Step two: retire the other two bundles behind the same engine so /oracle/ and /ask-founders/ become two framings of one surface (portfolio-wide read vs bounded founder question), which also cuts the client payload materially. Publish an /api/answer contract so agents query the same grounded corpus humans do. |
| 12 | 💡 | organization | 30m | 4 | 3 | 12.0 | · | **orphan-hashed-shells** — Two superseded 192KB style shells (376KB) ship with every deploy and are referenced by nothing. PRE-VERIFIED by diffing the 25 hashed shells on disk against the 52 hashed names referenced across HTML and JSON: exactly two are orphaned — style.shell-3c2b71ab6a.css and style.shell-cdd5134790.css, 376,248 bytes together. Retaining one prior generation is a deliberate and correct hedge against in-flight cold caches during a hash rotation; retaining two superseded generations past that window is only deploy weight. Outcome: a smaller deploy artifact and a rotation policy that is stated rather than assumed. **Recipe:** Delete the two orphans, then write the retention rule where the rotation happens: keep exactly N-1 (the immediately-previous shell) and prune anything older, enforced by the same script that emits the hashed name so the count cannot creep. Verify no service-worker precache manifest still names them before deleting. |
| 13 | ⚡ | ux | 6h | 7 | 6 | 27.4 | · | **editorial-surface-spine** — Eight editorial surfaces compete for the same reader with no stated difference between them. PRE-VERIFIED by title+description extraction: /news/ (The Desk — AI news with fictional correspondents and a public prediction scorecard), /journal/ (Signal Log — devlog), /journal/dispatches/, /journal/archive/, /changelog/ (every session, every shipped feature), /notebook/ (every commit is an entry), /news/directors-report/, /news/subscribed/. Three of these — /changelog/, /notebook/, /journal/ — are three granularities of one underlying fact stream (session, commit, narrative) presented as three destinations. The Desk is a genuinely distinctive product and is currently one item in an undifferentiated list of eight. Outcome: The Desk reads as the studio's publication rather than one more link, and the build-log granularities stop competing for the same click. **Recipe:** Separate by kind, not by feed. /news/ (The Desk) is the PUBLICATION and belongs in the nav on its own. Everything else is the BUILD RECORD: make /journal/ the single narrative entry point and render commit-level (/notebook/) and session-level (/changelog/) as depth toggles within it rather than sibling destinations, tombstoning the two routes to anchors. Keep the underlying feeds exactly as they are — this is a presentation spine, not a data change, so no receipt or permalink moves. |
| 14 | ⚡ | speed | 6h | 7 | 6 | 33.6 | · | **member-panel-single-document** — The member dashboard ships all nine tabs in one 133KB document before the member has chosen one. PRE-VERIFIED: vault-member/index.html is 133,565 bytes and contains nine dashboard tab buttons — Classified Archive, Vault Challenges, Early Access, Polls, Following, Season Pass, Treasury, Chronicle, Profile & Settings — each with its pane markup present in the document. Only 5.4KB is inline style and 1.6KB inline script, so the weight is genuine panel markup: every signed-in member downloads and parses all nine panes to look at one. This is the most-repeated authenticated view and the heaviest page on the site. Outcome: the account opens noticeably faster on the phone the member actually uses, without removing a single feature. **Recipe:** Render the default pane inline and defer the other eight — separate fragments fetched on first tab activation, or inert templates hydrated on demand — preserving deep links (/vault-member/#chronicle must still open Chronicle directly) and keeping the tabs keyboard- and screen-reader-operable through the swap. Capture before/after weight and an interaction receipt on a mobile viewport; per CANON-053 verify the rendered result in a real browser across themes rather than reviewing the diff. |

## Depth Ladder (per item: L1 minimal · L2 solid · L3 genius)

> `/implement` selects the depth that fits remaining budget, and climbs the ladder
> on already-shipped items when the session still has budget (S175 saturation).

### redirects-splat-301-into-404
- **L1 · minimal** _(~30m)_ — Add explicit pre-splat rules for the four known URLs pointing at live equivalents.
- **L2 · solid** _(~2h)_ — L1 plus migrate/retire the four files for real, relink Solara lore from the game page, and add the redirect-resolution court to build:check.
- **L3 · genius** _(~4h)_ — L2 plus a general orphan detector: any tracked HTML with zero inbound links and no sitemap entry is reported at build time.

### pathways-doorway-pages
- **L1 · minimal** _(~1.5h)_ — Collapse all six to /pathways/#anchor via _redirects; delete the six directories; verify sitemap.xml and nav links follow.
- **L2 · solid** _(~5h)_ — L1 plus build /pathways/investors/ and /pathways/press/ as real guided routes with per-step deep links and a press-kit download.
- **L3 · genius** _(~10h)_ — L2 plus intent-persisted routing — the chosen pathway shapes homepage module order and the nav sheet on return visits.

### transparency-surface-sprawl
- **L1 · minimal** _(~3h)_ — Ship /evidence/ as a routing hub: four cards, each with a one-line 'what this answers', a live freshness stamp pulled from the existing feeds, and a link out.
- **L2 · solid** _(~10h)_ — L1 plus real tabbed embedding of the four highest-value modules, one shared feed-fetch layer, honest UNKNOWN states, and nav promotion.
- **L3 · genius** _(~20h)_ — L2 plus a single /api/evidence.json aggregate so agents get every claim, its source and its age in one fetch — and the eight deep pages render FROM that aggregate, eliminating multi-writer drift entirely.

### desk-topic-novelty-gate
- **L1 · minimal** _(~1h)_ — Exact-slug exclusion over a 14-day window, with the skip surfaced in the run receipt.
- **L2 · solid** _(~3h)_ — L1 plus normalised title-token overlap for reslugged repeats, a follow-up allowance gated on new primary sources, and self-tests for all three cases.
- **L3 · genius** _(~6h)_ — L2 plus a public 'story thread' model — repeats become explicit follow-ups chained to the original with a visible development timeline, turning the failure mode into a feature.

### ai-factsheet-layer-undiscoverable
- **L1 · minimal** _(~1h)_ — Sitemap entries plus the agents.json aiFactSheet field, both from the registry generator.
- **L2 · solid** _(~3h)_ — L1 plus JSON-LD in every sheet, rel=alternate cross-links from the human page, and a build court asserting the join.
- **L3 · genius** _(~6h)_ — L2 plus per-sheet freshness stamps and an /api/factsheets.json index so an agent can enumerate the whole portfolio in one fetch.

### ignis-health-ungated
- **L1 · minimal** _(~15m)_ — Add the /ignis-health pattern to GATED_PATH_PATTERNS and deploy the worker.
- **L2 · solid** _(~1h)_ — L1 plus the robots-vs-gate consistency court in build:check, with the intentional-exception list.
- **L3 · genius** _(~2h)_ — L2 plus a live post-deploy probe asserting an unauthenticated fetch of each gated path returns the gate, not the content.

### orphan-sitemap-html-duplicate
- **L1 · minimal** _(~15m)_ — Delete the file and add the 301.
- **L2 · solid** _(~30m)_ — L1 plus confirm/remove the generator output path so it cannot regenerate.
- **L3 · genius** _(~1.5h)_ — L2 plus generalise: a build court failing any tracked HTML page with zero inbound links and no sitemap entry (shares machinery with item 1 L3).

### homepage-inline-critical-css-66kb
- **L1 · minimal** _(~1h)_ — Add the inline-critical-CSS byte budget to build:check as a warn, and capture the baseline homepage vitals receipt.
- **L2 · solid** _(~4h)_ — L1 plus recompute the critical block from above-fold coverage, flip the budget to blocking, and prove the LCP delta with before/after receipts.
- **L3 · genius** _(~8h)_ — L2 plus extend the budget and recomputation across every template, not just the homepage.

### membership-identity-sprawl
- **L1 · minimal** _(~2h)_ — Write the three-way spine into nav and the cross-links; no page moves yet.
- **L2 · solid** _(~8h)_ — L1 plus fold /vault-wall/ and /members/ into /community/ sections, relocate /invite/ into the account, and tombstone the retired routes to anchors.
- **L3 · genius** _(~14h)_ — L2 plus a state-aware entry: signed-out sees the pitch, signed-in routes straight to the account, and the nav chip reflects presence per the existing presence-first pattern.

### dual-redirect-implementations
- **L1 · minimal** _(~30m)_ — Normalise vault-treasury to the minimal stub shape and diff the stub list against _redirects, reporting any route covered by only one.
- **L2 · solid** _(~2h)_ — L1 plus live-verify each 301 against the production host, delete the redundant stubs, and add the no-meta-refresh court.
- **L3 · genius** _(~3h)_ — L2 plus generate _redirects itself from a single retired-routes manifest that also feeds the sitemap exclusion list.

### unified-grounded-answer-surface
- **L1 · minimal** _(~3h)_ — Point the strongest existing engine at entity-graph.json plus the .ai sheets and render a citation link under every answer; leave the other two surfaces untouched.
- **L2 · solid** _(~12h)_ — L1 plus retire the two redundant bundles onto the shared engine, add the answered-with-citation receipt, and publish the /api/answer contract.
- **L3 · genius** _(~3d)_ — L2 plus grounded answers over the live evidence aggregate from item 3 L3, so 'is the site healthy right now' is answerable from the same engine with the same citation discipline.

### orphan-hashed-shells
- **L1 · minimal** _(~15m)_ — Verify against the SW precache manifest, then delete the two orphans.
- **L2 · solid** _(~30m)_ — L1 plus encode the keep-N-1 rule in the rotation script with a build assertion on the shell count.
- **L3 · genius** _(~1h)_ — L2 plus extend the same policy to every hashed asset family, not just CSS.

### editorial-surface-spine
- **L1 · minimal** _(~1.5h)_ — Add a one-line 'what this is and how it differs' header to each of the eight, and correct the nav grouping.
- **L2 · solid** _(~6h)_ — L1 plus fold /notebook/ and /changelog/ into /journal/ as depth toggles over the existing feeds, with anchor tombstones.
- **L3 · genius** _(~10h)_ — L2 plus one merged reverse-chronological studio timeline with a granularity slider from commit to session to narrative to publication.

### member-panel-single-document
- **L1 · minimal** _(~2h)_ — Defer the three heaviest non-default panes; verify deep links and tab a11y still work.
- **L2 · solid** _(~6h)_ — L1 plus defer all eight non-default panes behind a shared activation path, with the mobile before/after receipt.
- **L3 · genius** _(~12h)_ — L2 plus per-pane data fetching so a pane's data loads with the pane, not with the document.

## Skipped (Priority < 1.5)

- undefined — undefined
- undefined — undefined
- undefined — undefined

---
*Every item was PRE-VERIFIED against live files before inclusion. Three candidate findings were disproved during verification and are recorded in `skipped` — those are wins, not omissions.*
