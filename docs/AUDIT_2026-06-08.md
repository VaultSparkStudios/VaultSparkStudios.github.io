<!-- generated-by: render-audit-md.mjs (R-H14) — DERIVED FROM AUDIT_2026-06-08.json; do not hand-edit -->
<!-- generated-at: 2026-06-08 -->
<!-- project: unknown · type: unknown -->

# Project Audit — Unknown

> One ranked improvement plan covering 9 axes. Top items first.
> Source of truth: this file is derived from the JSON sidecar. Edit the JSON, then re-render.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 6
- Combined Priority: 0.00
- Top 3: —

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov | Priority | Status | Item |
|---|:-:|---|---|:-:|:-:|:-:|:-:|---|
|  | fire | Observability/FeatureDepth |  | 9 | 8 | 36.0 | · | **uptime-publish-loop** — . S176 built a probe to replace the missing uptimerobot credential, and S177 made it measure real availability instead of the CF bot-gauntlet — but its output is alert-only and ephemeral, so the green signal is invisible to everyone. Info-finding outcome: visitors and the founder see a real, self-measured uptime percentage instead of a third-party badge or nothing. This also closes the open UPTIME-PROBE-GREEN-CONFIRM carry by making greenness publicly legible. **Recipe:** Make uptime-probe.yml commit a rolling api/uptime.json (latest summary) + append to data/uptime-history.ndjson (cap ~30d); guard the commit so a failed probe still alerts but does not poison history. Render a live uptime % + last-N sparkline into the existing /status/ 'Uptime · History' tile (status/index.html:325) from api/uptime.json. Add a check-uptime-contract.mjs gate to build:check. |
|  | fire | Speed/Data |  | 8 | 8 | 32.0 | · | **field-win-auto-publish** — . S174 made the RUM loop self-feeding and S175 moved the origin to the edge; the measurement machine now grades deploys, but the wins stay buried in a JSON file. Conversion/transparency outcome: when a verdict is CONFIRMED, the site auto-surfaces 'homepage got N% faster for real visitors, measured from M field samples' as proof-as-marketing — extending the existing 'measured from real visits' strip. The machine should publish its own wins, not depend on session cadence. **Recipe:** Add scripts/build-field-win-proof.mjs reading data/field-verdicts.json, emitting api/field-win.json containing ONLY verdicts whose status is improved/regressed with confidence (never pending). Render a 'Biggest measured win' proof tile on /studio-pulse/ (or the /status/ Live Signals strip) gated to show only confirmed deltas with sample count. Wire into npm run build + a freshness gate. |
|  | fire | UX/Conversion |  | 9 | 8 | 31.0 | · | **returning-visitor-digest** — . The studio's whole posture is momentum and transparency, yet a returning prospective member sees no evidence that anything moved since their last visit. Conversion/retention outcome: a small, dismissible 'N things shipped since you were here' strip seeded from ship-receipts filtered against a localStorage last-visit timestamp gives returning visitors a concrete reason to re-engage. Cost-neutral (localStorage + already-generated static JSON; no per-user studio cost), public-safe (ship receipts are already public). **Recipe:** Add assets/returning-visitor-digest.js: on load, read vs_last_visit_ts from localStorage; fetch api/ship-receipts.json; if 2+ receipts are newer than last visit AND visit_count>1, render a compact dismissible strip ('Since your last visit: 3 ships') linking to /studio-pulse/. Stamp vs_last_visit_ts on dismiss/navigate. Wire behind the ambient predicate loader (assets/ambient-loader.js), not base ambient. Add a focused Playwright smoke. |
|  | high | Observability/Security |  | 7 | 6 | 26.5 | · | **uptime-alert-path-proof** — . A monitor whose alert path is unproven is only half a monitor; S176's probe earned trust on the up path but the down path is untested. Info-finding outcome: the founder gets evidence the alarm works without a false page. Adds a --simulate-failure --dry-run mode that runs the full alert-format + dedup decision against a synthetic 'down' summary and prints the exact email it would send, plus a self-test assertion that a down signal triggers the send branch. **Recipe:** Extend scripts/probe-uptime.mjs with --simulate-failure (injects an origin-down signal) and --dry-run (formats + logs the alert, skips Resend send + skips writing the dedup ledger). Add a self-test case asserting shouldAlert===true on the synthetic down. Document the one-liner in the uptime-probe.yml comment block. |
|  | high | TokenCost/Efficiency |  | 6 | 5 | 18.9 | · | **taskboard-archive-rotation** — . The board's live signal (Now/Next + the current 1-2 Done sets) is buried under ~15 sessions of completed work. Token-cost outcome: a one-time rotation + a closeout rule keeps the live board lean so every future /start and on-demand read is cheaper. Measurement: TASK_BOARD.md 356KB -> target <50KB; verified via file size pre/post and a board-size gate. **Recipe:** Add scripts/rotate-taskboard.mjs that moves Done sections older than the last 2 sessions into context/archive/TASK_BOARD_ARCHIVE.md (append, dated), preserving Now/Next/current Done. Run once now; add an advisory board-size check to build:check (warn >80KB). Same pattern optional for CURRENT_STATE snapshot tail. |
|  | high | Speed |  | 6 | 4 | 15.1 | · | **ambient-genome-strip-split** — . On a mature codebase subtraction compounds, and the ambient bundle is the one asset every first paint pays for. Speed outcome: moving the genome strip behind the existing predicate loader trims the cold-cache ambient payload, improving the metric the origin migration already targets (first-paint TTFB->LCP). Lowest-risk perf win on the board. **Recipe:** Move assets/vault-genome-strip.js out of AMBIENT_SOURCES and load it via assets/ambient-loader.js on its existing capability predicate. Add a focused Playwright smoke proving the strip still renders when its trigger is present and is absent on first load. Let report-ambient-coverage --check enforce it does not return to base ambient. |

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

- uptime-publish-loop
- field-win-auto-publish
- returning-visitor-digest

## Skipped (Priority < 1.5)

- undefined — undefined
- undefined — undefined
- undefined — undefined
- undefined — undefined

