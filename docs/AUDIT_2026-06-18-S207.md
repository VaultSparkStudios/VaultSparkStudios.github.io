<!-- generated-by: render-audit-md.mjs (R-H14) — DERIVED FROM AUDIT_2026-06-18-S207.json; do not hand-edit -->
<!-- generated-at: 2026-06-18 -->
<!-- project: vaultsparkstudios-website · type: website -->

# Project Audit — VaultSpark Studios Website

> One ranked improvement plan covering 9 axes. Top items first.
> Source of truth: this file is derived from the JSON sidecar. Edit the JSON, then re-render.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 9
- Combined Priority: 0.00
- Top 3: —

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov | Priority | Status | Item |
|---|:-:|---|---|:-:|:-:|:-:|:-:|---|
| 1 | 🔥 | ux | 2h | 9 | 7 | 78.8 | ✓ | **play-next-intent-retiming** — Retime the dead cross-game play-next card from pre-engagement to post-engagement. We REDESIGNED this card last session (S206 #2) and moved it above the fold — and it now has 18 impressions and 0 clicks (api/dead-ctas.json, the single flagged dead CTA). The redesign made it MORE banner-blind, not less: a visitor lands on a game page to play THAT game, and a thin gold divider strip saying 'Haven't tried [other game] yet?' fires before they've engaged, fighting the page's own primary Play CTA. The fix is timing + framing, not pixels. Outcome: recover a cross-catalog routing path that currently converts a measured 0% of 18 impressions — the studio's audience-compounding thesis depends on this card working. **Recipe:** In assets/cross-game-play-next.js: (1) defer render() until an engagement signal — IntersectionObserver on the game's own play area reaching >=60% scroll OR a 45s dwell OR exit-intent (mouseleave toward top), whichever fires first; (2) reframe copy from pre-engagement 'Haven't tried X yet?' to completion-framed 'Done here? [Name] is live too - play free' so it reads as a next-step not an interruption; (3) make it a real card (vertical, larger cover, own visual weight) instead of a divider strip between hero and content; (4) keep honest-dark + TT-safe DOM building. Re-emit play-next:shown only on actual reveal so the dead-CTA gate measures the new timing fairly. |
| 2 | 🔥 | ux | 1.5h | 9 | 6 | 74.7 | ✓ | **trial-offer-promo-acknowledgment** — Honor the 50%-off trial offer on the /join/ page (dangling promise today). Last session shipped smart-trial-offer.js (S206 #7) - a panel that says 'Claim 50% off' and links to /join/?promo=TRIAL50. But /join/ does NOT read the promo param at all (grep: zero handling), so a high-intent visitor who clicks the discount lands on a page that never acknowledges or applies it. That is a broken conversion path AND a public-promise integrity problem (CANON-031 - no lying surfaces). Outcome: every trial-offer click lands on a page that confirms the discount, raising the highest-intent funnel segment's conversion instead of silently dropping it. **Recipe:** On /join/: read ?promo from the URL; if it matches a known promo (TRIAL50), inject a dismissible acknowledgment banner above the tier cards ('50% off your first 3 months - applied at checkout') and persist promo to sessionStorage so it survives the auth round-trip. Pass the promo through to the create-checkout edge call (Stripe promotion_code). L1 = honest acknowledgment + pass-through; the actual Stripe coupon object is the escalation rung (pricing = founder gate). Emit funnel:promo_landed RUM. A promo link with no matching server-side coupon shows 'offer expired' rather than a silent no-op. |
| 3 | 🔥 | ux | 2h | 8 | 7 | 70.0 | ✓ | **passport-share-inbound-conversion** — Turn shared Vault Passports into a signup loop (inbound CTA is missing). Last session shipped the Vault Passport (S206 #11) with Web Share so members can broadcast their rank - a viral gesture. But /vault-member/passport/ has NO inbound CTA: someone who opens a shared passport URL (the entire point of sharing) sees an auth-gated card and zero prompt to make their own. The viral loop dead-ends at the moment of highest social proof. Outcome: convert each shared-passport open into a 'Forge your own Vault Passport - join free' path, closing a referral loop the share button currently opens but never lands. **Recipe:** On /vault-member/passport/, detect the non-owner / anonymous viewer state (no session OR ?u=<handle> differs from signed-in user). For that state, render a prominent 'Forge your own Vault Passport' card under the shared card: rank ladder, one-line value prop, join CTA deep-linked to /join/ with funnel:passport_inbound RUM. Owner view unchanged. Honest-dark when the shared handle can't be resolved. TT-safe DOM. Optionally read ?u= to greet 'You're viewing [handle]'s passport'. |
| 4 | 🔥 | speed | 2h | 8 | 8 | 64.0 | ✓ | **prod-wave-verify-automation** — Automate prod wave verification - kill the 7-deep [VERIFY/P0] backlog. Every session for the last 8 (S199->S206) ships a wave and files a manual '[VERIFY/P0] Prod-verify the S{N} wave on a real browser' task - and almost none get checked, so the TASK_BOARD carries a 7-deep verification debt. This is the literal answer to 'what did we miss': not features, but confirmation that shipped features actually deployed. Most of each checklist (does the artifact/page exist on prod?) is machine-checkable via the pages.dev origin (CF bot-challenge != outage, per S177). Outcome: every wave is auto-verified at closeout, the manual backlog disappears, and a genuinely-broken deploy is caught the same session instead of 8 sessions later. **Recipe:** Write scripts/prod-verify-wave.mjs: read the current wave's expected surfaces from a small manifest (api JSON paths + page routes), fetch each from https://vaultsparkstudios-website.pages.dev/ (origin bypasses the bot-challenge), assert 200 + (JSON) schemaVersion present + (pages) a sentinel string. Emit a pass/fail report; honest-dark on network failure (don't false-fail a CI runner). Wire as an advisory closeout step. Truly-visual checks stay human; presence/liveness is automated. |
| 5 | ⚡ | speed | 2h | 6 | 4 | 24.0 | ✓ | **ambient-bundle-reaudit** — Re-audit the ambient bundle weight after 5+ assets added in S205-S206. S205-S206 added several predicate-loaded client assets (smart-trial-offer.js, cross-game-play-next redesign, ignis-prefix-cache, oracle-feedback-close, passport). The ambient loader predicate-loads features, but each addition risks creeping the core bundle or the cold-cache request count that S136 fought down to 1. Speed is weighted 2x and LCP history is sensitive (S153/S160 regressions). Outcome: confirm the recent wave didn't regress cold-cache requests or core bundle size, or split what crept - protecting the field LCP that drives bounce. **Recipe:** Run the ambient-coverage / bundle audit and diff core-bundle byte size + cold-cache request count vs the S204 baseline. Confirm the 5 new assets are predicate-loaded (not in core). If any leaked into core, move to predicate load. Capture before/after request count. Re-run a synthetic LCP trace on / and /games/ to confirm no regression. |
| 6 | ⚡ | ai | 1h | 6 | 6 | 22.7 | ✓ | **constellation-sequence-analytics** — Per-step constellation progress events to see where sequences drop off (S206 carry). Constellation challenges (S205) emit constellation:unlock:<id> only on completion - so we see who finished a hidden page-sequence but are blind to WHERE the rest abandoned it. Carried from S206. Outcome: per-step drop-off visibility lets us shorten or re-route the sequences that bleed visitors, turning a gamification feature we can't tune into one we can. **Recipe:** In assets/constellation-tracker.js, emit constellation:progress:<id>:<step> as each step in a sequence is reached (not just the terminal unlock). Add the matching prefix to the Worker RUM allowlist (RUM_UX_DYNAMIC) and to check-rum-allowlist so the gate stays green. Add a 'constellations' drop-off block to rollup-rum-ux.mjs aggregating step reach counts per sequence. |
| 7 | ⚡ | ai | 4h | 7 | 8 | 21.6 | ✓ | **ignis-graph-depth-l3** — IGNIS cross-entity traversal: 'Related' chips open a mini-catalog sub-panel (S206 carry). S205 shipped related-entity chips at the bottom of IGNIS answers, but clicking one just re-queries. Carried from S206. Genius depth: clicking a Related chip should expand a sub-panel of 3 matching cards drawn from api/public-intelligence.json - a mini-catalog traversal - so the Oracle becomes a graph you browse, not a single-shot Q&A. Outcome: deeper info-finding sessions (more queries per visit), the leading indicator of membership consideration. **Recipe:** In assets/ignis-answer-engine.js, add a 'mini-catalog mode': on related-chip click, fetch (cached) api/public-intelligence.json, match the chip's entity to up to 3 related entities, render a sub-panel of compact cards (title + one-line + link) below the answer instead of re-running the query. Emit oracle:graph_traverse RUM (allowlist it). Keep the existing re-query as a fallback link. Intelligence-style-contract compliant - CSS classes via ensureStyles(), never inline styles. |
| 8 | 💡 | feedback | 2h | 6 | 7 | 21.0 | ✓ | **oracle-feedback-themes-loop** — Close the loop on the Oracle thumbs-down feedback form - aggregate into a themes feed. S206 shipped the oracle-feedback-close form (thumbs-down -> text input -> oracle:feedback_submitted RUM), but the captured text currently just lands in the RUM stream with no aggregation or action - an open loop. Outcome: aggregate negative-feedback themes into api/oracle-feedback-themes.json so 'what visitors wish the Oracle answered' becomes a ranked content backlog, turning raw signal into the next wave's IGNIS depth priorities. **Recipe:** Add scripts/build-oracle-feedback-themes.mjs: read oracle:feedback_submitted events from the RUM rollup, cluster by simple keyword/topic, emit api/oracle-feedback-themes.json (top requested topics, counts, honest-dark when <5 submissions). Add an advisory gate in check-proof-surface.mjs. Optionally surface top unmet topics on an internal/ops view (not public). |
| 9 | 💡 | feedback | 3h | 6 | 8 | 20.7 | ✓ | **dead-cta-rotation-loop** — Structural dead-CTA recovery: rotate copy variants when a CTA flatlines. check-dead-ctas.mjs (S205) FLAGS dead CTAs but nothing ACTS on them - the play-next card sat at 18/0 until this audit looked. Genius second-order fix: when a CTA crosses the dead threshold (shown >=N, click 0), the page automatically rotates to the next copy variant from a small registry and re-measures, so a dead CTA self-heals across deploys instead of waiting for a human to notice. Outcome: every conversion surface becomes a standing A/B that escapes local minima on its own. **Recipe:** Define data/cta-variants.json (CTA id -> ordered copy variants). In the relevant client modules, read api/dead-ctas.json at build time (or a derived data/cta-state.json) and select the next variant for any CTA currently flagged dead. Emit cta:variant:<id>:<n> RUM so rollup can attribute clicks to the active variant. Start with play-next (the one real dead CTA) as the pilot. Structural sibling of item #1's L3. |

## Depth Ladder (per item: L1 minimal · L2 solid · L3 genius)

> `/implement` selects the depth that fits remaining budget, and climbs the ladder
> on already-shipped items when the session still has budget (S175 saturation).

### play-next-intent-retiming
- **L1 · minimal** _(~30m)_ — Reframe copy to completion-framed 'Done here? [Name] is live too - play free' and keep current mount. Cheapest honest test of the framing hypothesis.
- **L2 · solid** _(~2h)_ — Engagement-gated reveal (scroll >=60% OR 45s dwell OR exit-intent) + completion copy + real vertical card. Re-emit shown on reveal.
- **L3 · genius** _(~5h)_ — Client A/B: 50% post-engagement timing, 50% above-fold; both emit a variant tag to /v/rum so rollup-rum-ux proves which timing wins, feeding dead-cta-rotation-loop.

### trial-offer-promo-acknowledgment
- **L1 · minimal** _(~30m)_ — Read ?promo on /join/, show acknowledgment banner + sessionStorage persist. No checkout wiring - closes the lying-surface gap immediately.
- **L2 · solid** _(~1.5h)_ — L1 + pass promo through to create-checkout as promotion_code; funnel:promo_landed RUM; expired-promo fallback copy.
- **L3 · genius** _(~3h)_ — Create the Stripe promotion_code object via the gateway (founder pricing approval), validate in stripe-webhook, reconcile the discount in the member record so the 50% is real end-to-end.

### passport-share-inbound-conversion
- **L1 · minimal** _(~30m)_ — Static 'Forge your own Vault Passport - join free' CTA shown to anonymous viewers under the card. No handle resolution.
- **L2 · solid** _(~2h)_ — Viewer-state detection + rank-ladder teaser + funnel:passport_inbound RUM + ?u= greeting when present.
- **L3 · genius** _(~4h)_ — Render a real read-only public passport for ?u=<handle> from a public profile feed (public_profile=eq.true) so shared links show the actual person's rank/tenure to anonymous viewers - maximum social proof before the join CTA.

### prod-wave-verify-automation
- **L1 · minimal** _(~1h)_ — Script fetches a hardcoded list of the latest wave's artifacts from pages.dev origin, asserts 200, prints a report. Manual invocation.
- **L2 · solid** _(~2h)_ — Manifest-driven (reads wave surfaces from a small JSON) + JSON schemaVersion + page sentinel assertions + honest-dark. Wired as advisory closeout step.
- **L3 · genius** _(~4h)_ — Closeout auto-emits the wave manifest from the session's changed api/ + page files, runs the verifier post-push with retry/backoff for CF Pages propagation, writes a verify badge into PROJECT_STATUS.json for the startup brief.

### ambient-bundle-reaudit
- **L1 · minimal** _(~30m)_ — Diff core bundle size + cold-cache request count vs baseline; report only.
- **L2 · solid** _(~2h)_ — Full audit + move any leaked assets to predicate load + synthetic LCP re-trace on / and /games/.
- **L3 · genius** _(~4h)_ — Add check-bundle-budget.mjs gate that fails build:check if core bundle bytes or cold-cache request count exceeds a committed budget.

### constellation-sequence-analytics
- **L1 · minimal** _(~1h)_ — Emit constellation:progress:<id>:<step> + allowlist the prefix + check-rum-allowlist update. No rollup block yet.
- **L2 · solid** _(~2h)_ — L1 + rollup-rum-ux 'constellations' drop-off aggregation block.
- **L3 · genius** _(~3h)_ — Surface the funnel in api/constellation-activity.json (drop-off per step) and render a step-by-step completion bar in the public constellation feed, honest-dark under threshold.

### ignis-graph-depth-l3
- **L1 · minimal** _(~1.5h)_ — Related chip click renders up to 3 related entity links inline (text only) from public-intelligence.json. No card UI.
- **L2 · solid** _(~4h)_ — Full mini-catalog sub-panel with compact cards + oracle:graph_traverse RUM + ensureStyles() CSS classes.
- **L3 · genius** _(~8h)_ — Multi-hop breadcrumb traversal (chip -> sub-panel -> chip) with a back stack, so a visitor browses the entity graph several hops deep without leaving the answer view.

### oracle-feedback-themes-loop
- **L1 · minimal** _(~1h)_ — Script tallies feedback submission counts into api/oracle-feedback-themes.json. No clustering.
- **L2 · solid** _(~2h)_ — Keyword clustering + ranked topics + honest-dark + advisory gate in check-proof-surface.
- **L3 · genius** _(~4h)_ — Feed the top unmet topics back into the IGNIS search index so the Oracle proactively suggests 'others asked about X' - a self-improving content loop.

### dead-cta-rotation-loop
- **L1 · minimal** _(~1h)_ — data/cta-variants.json + manual variant pick for play-next only. No auto-rotation.
- **L2 · solid** _(~3h)_ — Build-time variant selection driven by dead-cta state + cta:variant RUM attribution, piloted on play-next.
- **L3 · genius** _(~6h)_ — Generalize to all tracked CTAs with a rotation ledger recording which variant beat which, so the registry converges on the winning copy per surface.

## Skipped (Priority < 1.5)

- check-mission-statement-coherence-gate — VERIFIED ALREADY DONE - scripts/check-mission-statement-coherence.mjs exists and is wired into check-proof-surface.mjs (S204 carry complete). A reject-on-verification win.

## Execution Log

### play-next-intent-retiming
- `session 207` · **shipped** · shipped

### trial-offer-promo-acknowledgment
- `session 207` · **shipped** · shipped

### passport-share-inbound-conversion
- `session 207` · **shipped** · shipped

### prod-wave-verify-automation
- `session 207` · **shipped** · shipped

### ambient-bundle-reaudit
- `session 207` · **shipped** · shipped

### constellation-sequence-analytics
- `session 207` · **shipped** · shipped

### ignis-graph-depth-l3
- `session 207` · **shipped** · shipped

### oracle-feedback-themes-loop
- `session 207` · **shipped** · shipped

### dead-cta-rotation-loop
- `session 207` · **shipped** · shipped

