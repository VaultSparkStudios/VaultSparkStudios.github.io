# Audit — Session 278 · 2026-07-13

**Mode:** full arc (start → audit → implement → closeout), continuous.
**Method:** every genius-list item premise-verified against LIVE code before ranking — no phantom items. Honest deferral of a bad/ungated item recorded as a WIN, not a silent skip.

## Verified findings

| # | Item | Premise verdict | Disposition |
|---|------|-----------------|-------------|
| 1 | Lighthouse gate RED on the S277 push | ✅ REAL — CI on `c9a3ff4b3`: e2e ✓, playwright-axe ✓, axe-cli ✓, compliance ✓, **lighthouse ✗**. Failure = `/community/` 0.81<0.82 (core) + `/ranks/` 0.81<0.82 (trust). Homepage was NOT in the failure set. | Root-fix `/ranks/` (concrete lever); honest-defer `/community/`. |
| 2 | "Post-push CI confirmation" (genius #2) | ✅ RESOLVED with evidence — the browser gates DID run and are green except Lighthouse (→ #1). | Closed. |
| 3 | `/ranks/` eager render-blocking `supabase-client.js` | ✅ REAL — end-of-body but synchronous; inline consumer null-guards on `window.VSSupabase` so a naive `defer` would silently kill the leaderboard. | **Shipped:** `defer` + consumer gated on `DOMContentLoaded`. |
| 4 | Same eager-script pattern on `/join/`, `/vault-wall/` | ✅ REAL, provably safe (globals set synchronously; consumers external-deferred or DOMContentLoaded-gatable). | **Shipped:** `/join/` defer; `/vault-wall/` defer + `loadWall` gated on DOMContentLoaded. |
| 5 | SSR + client-skip/hydrate convention doc (genius #3) | ✅ REAL — doc absent; 2 S277 libs real (`assets/lib/*-render.mjs`). | **Shipped:** `docs/SSR_ZERO_CLS_CONVENTION.md` (Patterns A & B, real markers/lines). |
| 6 | 2nd-order: byte-budget gate lets small eager scripts through | ✅ REAL structural hole — `check-js-budget` is 80KB byte budget; a 2KB eager script (the exact `/ranks/` regression) stays green. | **Shipped:** `check-render-blocking-routes.mjs` — zero eager render-blocking scripts on strict-floor (core/trust/catalog) routes, derived from `lighthouse-route-tiers.json`; `/vaultsparked/` documented-exempt. Wired into build:check with `--self-test`. |
| 7 | SIL score self-inconsistency Σcategories=998 ≠ silScore=999 (CANON-005 / observability honesty) | ✅ REAL | Fixed at closeout (canonical place the SIL score is written). |
| 8 | Homepage LCP measured pass (genius #1/#5) | ⚠️ Homepage NOT the current red (passed this run; longtail 0.76 floor). Only lever remains the FOUC-risky 47KB inline-CSS split. | **Honest deferral** — needs throttled Lighthouse before/after + multi-viewport FOUC on a preview deploy; unverified FOUC-risky change to the brand-anchor site is not shippable headlessly (CANON-031). |
| 9 | `/community/` 0.81<0.82 (core) | ✅ REAL but no safe structural lever — text `<h1>` LCP in system-serif Georgia, critical CSS already inlined, all scripts deferred; 0.01 is lab-volatile. | **Honest deferral** — needs throttled-Lighthouse forensics for a measured lever. |
| 10 | `/universe/` never loads public-intelligence.js (genius #4) | ❌ PHANTOM — public-intelligence.js loads via the sitewide ambient-core bundle; the line-181 when-clause is `intent-flight-director`, not this. | Dropped. |
| 11 | Forge devlog · TT-enforce reprobe · wishlist momentum · IGNIS public-safe exposure | ✅ correctly gated (founder-voice / founder-device / missing Supabase admin credential / founder decision). | **Honest deferrals** — remain in the gated ledger. |

## Shipped this session
- `/ranks/`, `/vault-wall/`: render-blocking supabase client → deferred + DOMContentLoaded-gated consumer.
- `/join/`: render-blocking supabase client → deferred.
- `docs/SSR_ZERO_CLS_CONVENTION.md`: the standing zero-CLS SSR/hydrate convention.
- `scripts/check-render-blocking-routes.mjs`: structural gate closing the byte-budget blind spot.
- SIL score honesty reconciliation (closeout).

## Honest deferrals (WINS, not skips)
Homepage inline-CSS split · `/community/` 0.01 perf · forge devlog · TT-enforce · wishlist momentum · IGNIS public-safe exposure. Each carries a precise next-step and a real gate (visual/founder/credential/throttled-measurement) that cannot be honestly cleared in a headless agent session.

## Next-session lever (perf)
Both remaining perf near-misses need the same missing capability: a **throttled local Lighthouse before/after harness** with multi-viewport FOUC capture. That harness would unlock `/community/`, the homepage inline-CSS split, and confirm the S278 `/ranks/` fix flips 0.81→0.82. Highest-leverage next build.
