<!-- generated-by: render-audit-md.mjs (R-H14) — DERIVED FROM AUDIT_2026-06-14-S198.json; do not hand-edit -->
<!-- generated-at: 2026-06-14 -->
<!-- project: unknown · type: unknown -->

# Project Audit — Unknown

> One ranked improvement plan covering 9 axes. Top items first.
> Source of truth: this file is derived from the JSON sidecar. Edit the JSON, then re-render.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 12
- Combined Priority: 0.00
- Top 3: —

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov | Priority | Status | Item |
|---|:-:|---|---|:-:|:-:|:-:|:-:|---|
| 1 | transformative | ux | 2h | 8 | 7 | 70.0 | · | **membership-value-clarity** — Membership page: day-in-the-life narrative strip. The /membership/ page surfaces tiers and features but never answers 'what will I actually DO differently?' A visitor sees prices and checkmarks but not the felt experience of being a FORGE Member. A 3-panel 'Vault Journey' strip — Day 1 (join as FORGE), Day 30 (First Climb complete), Day 90 (known in community by rank) — translates abstract features into lived moments. This is the conversion call the membership-unlock.js already tracks (4 stages) but never surfaces at the decision point. **Recipe:** Insert a 3-panel .vault-journey-strip section above the tier cards in membership/index.html. Each panel: icon + heading + 2-sentence scenario copy. Link panel 2 to /ranks/ (First Climb quest). Mirror membership-unlock.js stage language for consistency. |
| 2 | transformative | newFeatures | 2h | 8 | 9 | 36.0 | · | **visit-streak-retention** — Daily visit streak mechanic on game pages. The site has play→join instrumentation but no retention loop — a visitor who plays Call of Doodie today has zero ambient reason to return tomorrow. A localStorage-based streak mechanic surfaced near the join CTA creates urgency without backend infrastructure: 'Day 3 streak — your rank grows with consistency.' This is the single cheapest compulsion lever available to a traffic-starved site without a registered user base, and it dovetails directly into the existing rank narrative. **Recipe:** New assets/visit-streak.js: read vs_last_visit_date + vs_streak_count from localStorage. If last visit was yesterday → increment streak. On game pages with a join CTA, inject a .streak-badge div above the CTA showing 'Day N' when streak ≥ 2. Add bounded 'streak:' prefix to Worker RUM_UX_DYNAMIC. Load via ambient-loader predicate on .play-panel pages. |
| 3 | transformative | gamification | 1h | 7 | 8 | 35.4 | · | **vault-rank-hook-play-pages** — Rank preview card below join CTA on SPARKED game pages. The post-play join CTA asks visitors to commit without showing what they're committing to. The 9-tier rank system is the studio's primary community hook but it's buried on /ranks/. A compact 'Your Rank Preview' mini-card directly below the join CTA — showing the FORGE badge, 'Start here. Climb to [next tier]', and one concrete benefit — makes the rank system tangible at the moment of maximum intent. **Recipe:** Add a .rank-preview-card below .game-join-cta in games/call-of-doodie/index.html and games/vaultspark-football-gm/index.html. Shows FORGE badge + 'Start here' + 'First Climb quest: play both SPARKED games → earn your first badge' + link to /ranks/. body[data-vs-signed-in] hides it for members. |
| 4 | transformative | ai | 1h | 7 | 8 | 35.4 | · | **oracle-velocity-git-series** — Oracle velocity chart from git history (393 commits, public-safe). The Oracle's velocity chart has been honest-dark since S193 because 'no public-safe velocity series exists.' This premise is wrong — the git log has 393 signal-rich commits since 2026-01-01 in a public repo. Build-time aggregation of weekly commit counts over 24 weeks gives a true 'studio cadence' sparkline with zero new data exposure and zero honesty compromise. The commits are already on GitHub for anyone to count; rendering them is a display gap, not a data-safety gap. **Recipe:** Create scripts/build-velocity-series.mjs: runs 'git log --format="%cd" --date=short' piped through ISO-week grouping, outputs api/velocity-series.json with {week, commitCount} for last 24 weeks. Add to npm run build + build:check. In oracle-extra.js, velocity panel reads /api/velocity-series.json and renders a 24-bar inline SVG sparkline. |
| 5 | transformative | ai | 2h | 7 | 8 | 28.0 | · | **ignis-local-session-memory** — IGNIS cross-page recent-queries chips (localStorage thread). Every IGNIS thread resets on navigation — a visitor who researches 'membership' on /ignis/ then navigates to /membership/ arrives with no memory of what they learned. S195 shipped conversational multi-turn within a page but the context dies on navigation. A localStorage 'vs_ignis_history' (last 3 query slugs) surfaced as chips on next load turns IGNIS from a page-scoped search into a cross-session research companion — the most defensible AI differentiation on the site. **Recipe:** In ignis-answer-engine.js: after resolving an answer, push query slug to vs_ignis_history (max 3, newest first). On load if history exists and input is empty, render .ignis-history-chip elements above input. Click re-runs query. Emit oracle-followup:history (allowlisted). Slugs only, no PII. |
| 6 | high | speed | 1h | 5 | 4 | 25.3 | · | **ignis-build-incremental** — Content-hash skip for expensive build scripts. build-ignis-search-index.mjs, build-entity-graph.mjs, and build-oracle-query-clusters.mjs run on every npm run build even when source data is unchanged. A content-hash check (SHA-256 of inputs vs .cache/[script]-hash, skip if match) cuts build time for the common case of 'changed only an HTML file.' The S195 ambient bundle already has a --check mode; this is the same pattern for the 3 most expensive data-indexing scripts. **Recipe:** Create scripts/lib/build-cache.mjs: exports checkHash(inputFiles) and saveHash(key, hash) using Node crypto. Wrap build-ignis-search-index, build-entity-graph, build-oracle-query-clusters. Print 'SKIP (unchanged, saved ~Xs)' on cache hit. .cache/ entries are gitignored. |
| 7 | high | gamification | 2h | 7 | 7 | 24.5 | · | **first-climb-cross-surface** — First Climb quest: cross-surface on game pages. The First Climb quest (play both SPARKED games → earn your first badge) exists on /ranks/ but a visitor who plays Call of Doodie never sees that playing Football GM next would complete a quest. Cross-surfacing the quest at the play-next moment turns the affinity routing already in game-affinity.json into a quest-completion hook. rank-quest.js already tracks state; this is a rendering gap, not an engine gap. **Recipe:** In both SPARKED game pages: below the play-next affinity card, add a .first-climb-hook div: 'First Climb quest: play both SPARKED games → earn your first badge → [link to /ranks/]'. rank-quest.js checks localStorage for the other game's play click; if done, text changes to '1 of 2 complete.' |
| 8 | high | featureDepth | 6h | 8 | 5 | 20.0 | · | **game-registry-single-source** — GAME-REGISTRY single source of truth (carry from S197 L3). Game status currently lives in 4 disconnected places: nav dropdown, games-index card, data/game-affinity.json, and the game page body. S197 found this disconnection was what let SPARKED titles show 'Demo Coming Soon' for 11 sessions. The coherence gate is the interim guard, but the structural fix requires a canonical data/game-registry.json that makes future game launches and status changes single-edit operations. **Recipe:** Create data/game-registry.json: { slug: { name, status, playUrl, embeddable, mediaReady } } for all 10 games. Wire check-game-playability-coherence.mjs to validate against it. Create derive-game-nav.mjs + derive-game-index.mjs to generate HTML fragments from the registry at build time. |
| 9 | high | featureDepth | 2h | 7 | 3 | 15.8 | · | **ambient-shell-dead-sink-rewire** — Rewire dead-gtag engagement events in ambient shell to /v/rum. scroll-depth.js fires scroll_milestone via 'if (typeof gtag === function)' — confirmed dead since S147 (gtag removed). exit-intent.js fires exit_intent_shown and exit_intent_answered via the same dead path (lines 135, 156 of assets/exit-intent.js). These are the studio's primary engagement signals yet zero data has landed from them in 50+ sessions. The S194 rewire fixed funnel-tracking.js; this is the same proven pattern for 2 remaining dead-sink scripts. **Recipe:** In assets/scroll-depth.js: replace the gtag fireEvent block with sendBeacon to /v/rum posting {ux:'engagement:scroll_milestone'}. In assets/exit-intent.js: replace lines 135 and 156 with /v/rum beacons under engagement:exit_intent_shown/answered. Add 'engagement' prefixAllowlist to Worker RUM_UX_DYNAMIC. Rebuild ambient bundle. |
| 10 | high | feedbackLoop | 1h | 6 | 4 | 15.2 | · | **acquisition-source-rum-complete** — Complete referrer → /v/rum beacon wire in analytics.js. analytics.js classifies visitor referrers into buckets but the classified source is NOT included in the /v/rum beacon body — confirmed open TASK_BOARD item. The Worker 'source:' prefix family (shipped S194) is ready and waiting. Without this wire, acquisition channel data in funnel-summary.json stays permanently empty even as visits accrue. Knowing which channel drives traffic is the first strategic data point worth acting on. **Recipe:** In assets/analytics.js: after bucket = classify(referrer), add sendBeacon('/v/rum', JSON.stringify({ux: 'source:' + bucket})) inside the existing once-per-session guard. The Worker 'source:' prefixAllowlist already admits this format. rollup-rum-ux.mjs 'sources' aggregation is already wired. |
| 11 | high | security | 30m | 7 | 2 | 10.6 | · | **tt-enforce-reprobe** — TrustedTypes enforce reprobe (due 2026-06-18, 4 days). The TrustedTypes enforce flip is gated on a clean reprobe — pre-S185 samples are aging out by 2026-06-18 (4 days). The first-party surface has been CLEAN since S185 but the flip requires fresh samples. Running the reprobe now means the flip can happen in the next session rather than slipping another sprint. **Recipe:** Run 'node scripts/lint-tt-policies.mjs --self-test' then 'node scripts/capture-home-first-viewport-proof.mjs --refresh' to get fresh TT coverage samples. If coverage holds, update the TT status in security-posture. Actual enforce flip is founder-gated (real-device verify per S195 DECISIONS.md). |
| 12 | high | security | 1h | 7 | 2 | 8.9 | · | **staging-box-recovery** — Hetzner staging box recovery (CANON-007 violation, agent-attemptable). website.staging.vaultsparkstudios.com has been DOWN since ~S192 and CANON-007 (staging before prod) is unenforceable in practice. Per CANON-019, this must be agent-attempted before labeling founder-blocked: probe hcloud capability, then attempt server start or SSH recovery. **Recipe:** Step 1: node ../vaultspark-studio-ops/scripts/check-secrets.mjs --for hcloud. If READY: hcloud server list → if stopped, start it → SSH ping → nginx check → check-staging-parity.mjs --refresh. If MISSING: document + add scoped founder-action to TASK_BOARD. |

## Depth Ladder (per item: L1 minimal · L2 solid · L3 genius)

> `/implement` selects the depth that fits remaining budget, and climbs the ladder
> on already-shipped items when the session still has budget (S175 saturation).

### membership-value-clarity
- **L1 · minimal** _(~30m)_ — Add a 3-sentence callout <p class='vault-journey-intro'> at the top of /membership/: 'Day 1: you\'re FORGE — the first rank. Day 30: you\'ve cleared the First Climb quest. Day 90: you\'re known in the community by your rank.'
- **L2 · solid** _(~2h)_ — A styled 3-panel .vault-journey strip (flexbox cards) with before/after framing: free visitor vs FORGE member at the play moment, the dispatch moment, the community moment. Each panel has icon, heading, 2-sentence scenario, and CTA. Cross-links to /ranks/. Mirrors membership-unlock.js stage language.
- **L3 · genius** _(~8h)_ — Animated rank-climb visualization: 9 tiers as a horizontal progress path with hover-to-reveal per-tier unlocks. Non-members see FORGE highlighted with a pulsing 'start here' marker. Signed-in members see their current rank position. Derives tier names and benefits from /ranks/ content.

### visit-streak-retention
- **L1 · minimal** _(~30m)_ — Read vs_visit_count + vs_last_visit_date from localStorage. If count ≥ 2 and last date != today, show a 1-line 'Returning player' note near the join CTA. No beacon — 10 lines added to membership-unlock.js as a game-page guard.
- **L2 · solid** _(~2h)_ — Full assets/visit-streak.js: daily streak tracking, 'Day N streak' badge on .play-panel pages, rank-voice copy. Bounded 'streak:' RUM family in Worker + rollup-rum-ux. Load via ambient-loader predicate. Self-test 5/5.
- **L3 · genius** _(~8h)_ — Streak → rank XP system: daily streaks grant local XP (localStorage, no backend). Non-members see 'Join to save your XP — you\'ve earned N pts this week.' Members see XP on /vault-portal/. Creates a parallel motivation track: consistency earns XP, XP earns rank perks.

### vault-rank-hook-play-pages
- **L1 · minimal** _(~30m)_ — One line after the join CTA: 'Start as a FORGE Member — the first rank in the Vault. <a href="/ranks/">See all 9 ranks →</a>' Static, no component.
- **L2 · solid** _(~1h)_ — Styled .rank-preview-card: FORGE badge icon + 'Your starting rank' heading + 1-line benefit + 'See all 9 ranks' link. body[data-vs-signed-in] hides it. Added to both SPARKED game pages.
- **L3 · genius** _(~4h)_ — Full rank teaser panel: FORGE + next 2 tier badges (greyed out), 'unlock with First Climb' hint, per-visible-tier specific benefit. Members see their current rank position instead. Derives from rank data in /ranks/ via a build-time JSON export.

### oracle-velocity-git-series
- **L1 · minimal** _(~30m)_ — scripts/build-velocity-series.mjs reads git log, groups by week, writes api/velocity-series.json. Add to npm run build. Confirm oracle-extra.js velocity panel unhides by fixing its data source path.
- **L2 · solid** _(~1h)_ — Full script with --check (validates schema + non-empty), --self-test (5/5), folded into check-proof-surface as a feed. Oracle panel renders 24-week bar chart via inline SVG. Labeled 'Studio Cadence — commits per week (public git)' for honesty.
- **L3 · genius** _(~4h)_ — Classify commit types by week (feat/fix/chore). Stacked sparkline: green=feat, yellow=fix, grey=chore. Overlay markers for high-output sessions. The Oracle becomes a live studio health visualization unique to VaultSpark.

### ignis-local-session-memory
- **L1 · minimal** _(~30m)_ — Push last query slug to localStorage vs_ignis_history (max 3) after answering. On page load, if history exists, render 1-3 chips above IGNIS input. No beacon, no re-run — just restore input value on click.
- **L2 · solid** _(~2h)_ — Full implementation: history chips, click re-runs query, 'Continue your research' label, clear (×) per chip. Emit oracle-followup:history to /v/rum. Works across pages — persistent cross-navigation thread.
- **L3 · genius** _(~8h)_ — Cross-page IGNIS assistant ribbon: last answered question + summary snippet persists as a collapsed 'Continue' ribbon at the bottom of every page. Click expands inline answer panel. IGNIS becomes a site-wide research co-pilot.

### ignis-build-incremental
- **L1 · minimal** _(~30m)_ — Add inline content-hash check in build-ignis-search-index.mjs: hash public-intelligence.json + ecosystem-state.json, compare to .cache/ignis-index-hash, skip if match.
- **L2 · solid** _(~1h)_ — Shared scripts/lib/build-cache.mjs. Apply to build-ignis-search-index + build-entity-graph + build-oracle-query-clusters. --force flag bypasses skip (for CI).
- **L3 · genius** _(~4h)_ — Full build cache registry: scripts register their inputs + outputs. build-cache-report.mjs shows per-script cache hit rate. Target: 30-50% build time reduction for typical session work.

### first-climb-cross-surface
- **L1 · minimal** _(~30m)_ — Static inline text below the affinity card on each SPARKED game page: 'First Climb quest: play both SPARKED games → earn your first badge. <a href="/ranks/">See ranks →</a>'
- **L2 · solid** _(~2h)_ — rank-quest.js reads localStorage funnel events to check if other SPARKED game was played. Shows '1 of 2 complete' vs '0 of 2'. Emits quest:first_climb_progress (bounded, allowlisted). Cross-surfaced on homepage Studio Now strip.
- **L3 · genius** _(~8h)_ — Full quest system: 3 starter quests (First Climb, First Dispatch, First Proof) with localStorage-checkable conditions. Persistent progress ribbon at bottom of relevant pages. Completion upgrades membership-unlock.js stage.

### game-registry-single-source
- **L1 · minimal** _(~1h)_ — Create data/game-registry.json with slug→{status, playUrl} for 10 games. Update check-game-playability-coherence.mjs to cross-check against it.
- **L2 · solid** _(~4h)_ — Full registry + build scripts that derive nav dropdown and games-index card CTAs from it. One registry edit propagates to 3 of 4 surfaces at next build.
- **L3 · genius** _(~6h)_ — Full single-source: all 4 surfaces derived at build. scripts/sync-game-page-headers.mjs updates SPARKED/FORGE/VAULTED banners from registry. Any status change is a 1-line registry edit.

### ambient-shell-dead-sink-rewire
- **L1 · minimal** _(~30m)_ — In assets/scroll-depth.js: replace gtag call with sendBeacon('/v/rum', JSON.stringify({ux:'engagement:scroll_milestone', pct:percent})). Add 4 exact-match 'engagement:scroll_milestone_N' entries to RUM_UX_EVENTS. Rebuild ambient bundle.
- **L2 · solid** _(~2h)_ — Full rewire: scroll-depth.js + exit-intent.js → /v/rum under 'engagement:' prefix family. rollup-rum-ux.mjs aggregates engagement: rows into funnel-summary.json 'engagement' object. Rebuild ambient bundle + self-test.
- **L3 · genius** _(~4h)_ — Add ignis_lens_opened and visit_depth_upsell_shown to engagement: family. Full engagement panel on /status/ showing scroll-depth distribution and exit-intent conversion rate.

### acquisition-source-rum-complete
- **L1 · minimal** _(~30m)_ — In assets/analytics.js: after the bucket classification, add sendBeacon('/v/rum', JSON.stringify({ux: 'source:' + bucket})) inside the once-per-session guard. That's the entire fix.
- **L2 · solid** _(~1h)_ — L1 fix + verify with check-rum-allowlist.mjs that both ends are registered. Run rollup-rum-ux.mjs --self-test to confirm 'sources' aggregation is exercised.
- **L3 · genius** _(~2h)_ — Add 'Sources' breakdown tile to /status/ Conversion funnel: shows search/social/direct/referral counts in honest-dark format (<20 threshold).

### tt-enforce-reprobe
- **L1 · minimal** _(~30m)_ — Run lint-tt-policies.mjs + capture-home-first-viewport-proof.mjs --refresh. Log result. If first-party surface still clean, update reprobe timestamp in security-posture.
- **L2 · solid** _(~1h)_ — Full reprobe: both scripts, check ttPolicy in captured proof, confirm no violations. Update security-posture.json tt-enforce control with fresh evidence + verified: true.
- **L3 · genius** — N/A — actual enforce flip is founder-gated.

### staging-box-recovery
- **L1 · minimal** _(~30m)_ — Run check-secrets.mjs --for hcloud. If READY, run 'hcloud server list' to see server state.
- **L2 · solid** _(~1h)_ — Full recovery attempt: hcloud server list → start if stopped → SSH ping → nginx check → check-staging-parity.mjs --refresh to confirm green.
- **L3 · genius** — N/A — box re-provisioning is a founder-action.

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

- visit-streak-retention
- vault-rank-hook-play-pages
- oracle-velocity-git-series
- ignis-local-session-memory

## Skipped (Priority < 1.5)

- games-index-play-first — PREMISE DISPROVED — games/index.html already has play-first primaries on all SPARKED cards: 'Play Now — It\'s Free' (line 659, call-of-doodie) and 'Play Beta' (line 726, vaultspark-football-gm).

