<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-06-04 -->
<!-- project: vaultsparkstudios-website · type: website · session: 173 -->

# Project Audit — VaultSparkStudios.github.io

> One ranked improvement plan covering 9 axes. Top items first.
> Run `/implement` to execute in optimal order.
> **Personalized to S172 closeout:** RUM export is agent-owned, TT soak is real, Ark is restored, membership orphans are down to one yes/no, and homepage field LCP is now an evidence-backed P1.

## Top-line

- Total items: 14
- Combined Priority: 344.1
- Expected post-closeout impact score if implemented: 94/100
- Top 3: `homepage-critical-css-de-dupe` · `field-lcp-autopsy-lab` · `rum-strict-flip-ladder`
- Headline: S172 turned the site from synthetic guessing to field truth. S173 should spend that truth aggressively: remove duplicated homepage critical CSS, explain the real visitor LCP path, and make the RUM gate graduate itself when enough samples arrive.

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | 🔥 | Speed/UX | 2h | 10 | 8 | 40.0 | **homepage-critical-css-de-dupe** — Collapse the homepage's two competing critical CSS systems into one measured first-viewport contract. `index.html` currently ships a large page-local `#critical-css` block followed by the generated `data-vs-critical-shell` block, and both define hero/header/root primitives. The RUM profile says FCP is near LCP, so the first render path is the battlefield; duplicated critical CSS is an obvious, evidence-backed place to cut parse and cascade work before touching riskier product features. **Recipe:** Create `scripts/check-home-critical-css-contract.mjs` to fail if `index.html` contains both `#critical-css` and `data-vs-critical-shell`; move the homepage-only first-viewport rules into the generated shell or an allowlisted home shell segment; run warm and cold `measure-page-performance` before/after and record field-target hypothesis in DECISIONS. |
| 2 | 🔥 | AI/Intelligence + Speed | 2h | 9 | 8 | 36.0 | **field-lcp-autopsy-lab** — Build a local LCP autopsy artifact that names the actual homepage LCP candidate, render phase, CSS ancestry, and blocking resources. S172 corrected the story: `/` is genuinely slow for field visitors. The current recipes still speak in broad classes like infra/cache-state or ambient cold cost; the next leap is a forensic HTML report with the exact LCP element and why it paints late under cold-cache/4g conditions. **Recipe:** Extend `scripts/measure-page-performance.mjs` or add `scripts/analyze-home-lcp.mjs` using Playwright trace + PerformanceObserver injection; output `docs/PERF_HOME_LCP_AUTOPSY_2026-06-04.json/html` with candidate selector, text/image dimensions, computed style ancestry, request waterfall, long tasks, and a ranked fix recipe. |
| 3 | 🔥 | Speed/Data Truth | 1h | 9 | 6 | 34.1 | **rum-strict-flip-ladder** — Replace the binary RUM strict flip with a staged ladder: accumulating, warning, route-strict, site-strict. `data/rum-summary.json` has 42 total samples and 18 on `/`, while the strict threshold is 50. The site should not wait passively; it should make the next state visible and automatically tighten from advisory to warning to blocking as confidence rises. **Recipe:** Add `scripts/check-rum-strict-ladder.mjs`: <25 samples = accumulating, 25-49 = warning with projected flip date, >=50 = strict for that route, >=50 on top routes = site-strict candidate. Wire non-blocking into `build:check`, and log the state in `api/site-health.json`. |
| 4 | ⚡ | Speed/Resilience | 1h | 8 | 5 | 25.2 | **sw-shell-precache-coherency-gate** — Guard service-worker shell precache against stale fingerprint drift. The service worker's `FINGERPRINTED_SHELL_ASSETS` references the current ambient shell, while `STATIC_ASSETS` still includes an older ambient shell path. Even if the install remains tolerant, stale precache entries make cold-cache field evidence harder to trust and can silently preserve old shell behavior. **Recipe:** Add a check inside `build-shell-assets.mjs --check` or a new `scripts/check-sw-shell-coherency.mjs` that parses `sw.js`, compares `FINGERPRINTED_SHELL_ASSETS` against `STATIC_ASSETS`, and fails on old `style/theme/nav/shell-health/ambient.shell-*` entries. |
| 5 | ⚡ | Speed/Token Cost | 4h | 8 | 8 | 24.8 | **ambient-loader-graph-split** — Turn the ambient bundle from one guarded blob into a dependency graph with route, viewport, session, and intent loaders. The current ambient report shows 31 sources, 134.3KB raw, with 22 guarded modules and 94.3KB of conditional code still shipped to every page. S164 proved intent-paid loading works with command palette; now apply the pattern to nav-sheet, exit-intent, visit-depth, presence, favicon pulse, and rank-only surfaces without losing the single-shell discipline. **Recipe:** Create `assets/ambient-loader.js` plus a generated manifest from `AMBIENT_SOURCES`; keep always-on primitives in the base bundle and lazy-load guarded modules by predicates. First split targets: `nav-sheet.js`, `exit-intent.js`, `visit-depth.js`, `presence-badge.js`, `favicon-pulse.js`; prove absent-by-default and behavior-on-trigger with Playwright. |
| 6 | ⚡ | UX/Visual Quality | 2h | 8 | 6 | 24.0 | **home-first-viewport-freeze-frame** — Add a deterministic first-viewport screenshot and layout contract specifically for `/` under slow-network simulation. Long-tail visual proof is now strong, but the brand anchor's first viewport is where field LCP is failing. A frozen first-viewport proof turns "homepage feels slow" into a visual diff: what is visible at 500ms, 1500ms, 2500ms, and after idle. **Recipe:** Build `scripts/capture-home-first-viewport-proof.mjs` with Playwright network/CPU throttling and four timed screenshots; fail if hero wordmark, tagline, primary CTA, and nav are not visible by the target frame. Publish under `docs/visual-proof/home-lcp-s173/` and link from the gallery. |
| 7 | ⚡ | Security | 2h | 8 | 6 | 24.0 | **tt-enforce-route-ladder** — Convert Trusted Types from one future cliff into a per-route enforce ladder with rollback receipts. S172 made the TT soak real and fixed the first high-volume sink. The current next step still says re-probe in about a week, then maybe enforce `/privacy/`; a route ladder makes this safer and faster by enforcing only routes with zero soak hits and a named rollback path. **Recipe:** Extend `probe-tt-soak.mjs` to emit route-level readiness; add Worker config support for `TT_ENFORCE_ROUTES=/privacy/,/terms/`; require an intent receipt and `docs/TT_ENFORCE_ROLLBACK.md` before changing headers. |
| 8 | ⚡ | Feature Depth/Gamification | 2h | 8 | 6 | 24.0 | **membership-proof-loop** — Turn the membership interview and rank economy into a closed proof loop: intent, simulated rank path, next challenge, and post-signup continuity. S172 rewired the membership interview and kept the rank economy simulator, but the user still experiences them as separate surfaces. Membership must feel real; the best conversion path is not another CTA, it is a visible personal trajectory that survives signup. **Recipe:** Have the interview write a local `vs_membership_intent` profile, feed it into `rank-economy-simulator.js`, and render a "first three sparks" plan on `/membership/` and `/vault-member/` after signup. Keep it local-only until Obelisk/Vault SSO posture allows server continuity. |
| 9 | ⚡ | User Feedback Loop | 2h | 7 | 6 | 21.0 | **feedback-to-ship-receipts** — Upgrade the feedback decision board into public-safe ship receipts that connect themes to shipped pages, commits, and before/after proof. The repo already has feedback provenance, commit maps, visual proof, and a decision board. The next differentiator is closing the loop in public: "you asked for trust/depth/mobile clarity; here is the shipped receipt." **Recipe:** Create `scripts/build-ship-receipts.mjs` joining `api/feedback-provenance.json`, `api/commit-map.json`, `docs/visual-proof/**/manifest.json`, and TASK_BOARD done items; render a receipts lane on `/feedback/insights/` with no raw feedback text. |
| 10 | ⚡ | Organization/Ecosystem | 1h | 7 | 4 | 17.7 | **ark-signature-failure-dossier** — Convert the three Ark signature failures from upstream noise into a precise ecosystem repair packet. Ark is restored, but the first drain found three signature failures. If the studio-wide transport is how Obelisk, Hub, and canon replies move, signature failures are not harmless; they are silent cross-repo drift until diagnosed. **Recipe:** Write `docs/ARK_SIGNATURE_FAILURE_DOSSIER_2026-06-04.md` from `.cache/ark-inbox.json`/drain logs with producer, cargo type, signature status, expected key, and recommended studio-ops fix; ship a `repo-question` or `content-acked` cargo back once repaired. |
| 11 | 💡 | Token/API Cost | 2h | 6 | 7 | 21.0 | **public-intelligence-budget-ledger** — Publish a local build-time budget ledger for every public intelligence surface: bytes, requests, cached freshness, AI cost posture, and free-tier status. CANON-029 requires cost-neutral free tiers, and this repo now has many intelligence surfaces. A public-safe ledger keeps the site honest: which widgets are static, which are cached, which are local-only, and which must never drift into studio-paid per-user AI. **Recipe:** Generate `api/intelligence-budget.json` and `docs/INTELLIGENCE_BUDGET_LEDGER.md` from package scripts and known data files; include measurement plan: bytes from build artifacts, request count from crawl, AI cost posture from `context/PROJECT_STATUS.json.freeTierCostStatus` and disclosure gates. |
| 12 | 💡 | UX/Feedback Loop | 1h | 6 | 5 | 18.9 | **nav-sheet-decision-accelerator** — Accelerate the mobile-sheet default decision without cheating the data gate. `api/nav-sheet-stats.json` currently has too little signal, so the default swap remains correctly parked. The project can still learn faster by showing the canary more deliberately to mobile visitors who signal navigation intent. **Recipe:** Increase exposure only for eligible mobile sessions after first nav open or search tap, keep deterministic assignment, and add a `decisionETA` field to `api/nav-sheet-stats.json`. Do not flip default until the existing thresholds pass. |
| 13 | 💡 | Efficiency/Organization | 30m | 5 | 3 | 11.4 | **founder-yesno-retire-vaultsparked-proof** — Remove the last membership orphan only after converting the existing dossier into a one-line yes/no gate with rollback. S172 already proved `assets/vaultsparked-proof.js` is superseded by `live-proof.js`. The only remaining risk is deleting a feature-bearing-looking file without a founder-readable receipt. **Recipe:** Add a short `docs/MEMBERSHIP_ORPHAN_DECISION.md` with delete/keep consequence and rollback command; after approval, delete `assets/vaultsparked-proof.js`, rerun `check-orphan-assets`, and update TASK_BOARD. |
| 14 | 💡 | Security/Release Ops | 1h | 5 | 3 | 9.5 | **staging-parity-health-tile** — Promote staging from a static URL field to a checked parity tile in the public-safe ops surface. CANON-007 is satisfied, but `PROJECT_STATUS.json` still marks staging status as unknown. A SPARKED brand anchor should know whether staging serves the same shell, headers, and key pages as production before public deploy decisions. **Recipe:** Add `scripts/check-staging-parity.mjs` comparing status, shell manifest hash, CSP/security headers, and three key pages between production and `website.staging.vaultsparkstudios.com`; write `api/staging-health.json` and update testingSurfaces status. |

## Innovation Reserve

- `homepage-critical-css-de-dupe` — high-confidence, field-evidence-backed front-door speed win.
- `field-lcp-autopsy-lab` — turns performance debugging into a durable intelligence artifact.
- `ambient-loader-graph-split` — upgrades the site from bundled guarded code to a predicate-driven loader graph.

## Skipped

- `edge-account-personalization` — still gated by the S167 Obelisk Phase 2 decision; do not bypass the auth/capability contract.
- `trusted-types-global-enforce` — too broad for the current evidence; route ladder is safer.
- `public-raw-feedback-digest` — violates public-safe constraints; use aggregate ship receipts instead.

---
*Generated by `/audit` v1.0 · ready for `/implement`*

## Execution Log

| # | Slug | Result |
|---|---|---|
| 1 | homepage-critical-css-de-dupe | ✅ SHIPPED — Removed the page-local homepage `#critical-css` block and added `scripts/check-home-critical-css-contract.mjs`; final gate proves `generatedShell=true, pageLocal=false`. |
| 2 | field-lcp-autopsy-lab | ✅ SHIPPED — Added `scripts/analyze-home-lcp.mjs` and generated `docs/PERF_HOME_LCP_AUTOPSY_2026-06-04.{json,html}`; refreshed proof on current shell shows local LCP 324ms on `span.forge-letter.vsx-2c82527298`. |
| 3 | rum-strict-flip-ladder | ✅ SHIPPED — Added `.cache/rum-strict-ladder.json` via `scripts/check-rum-strict-ladder.mjs`; current state `accumulating`, 33 total samples, `/` needs 37 more samples before route-strict evaluation. |
| 4 | sw-shell-precache-coherency-gate | ✅ SHIPPED — Fixed `build-shell-assets.mjs` so old `*.shell-*` entries in `STATIC_ASSETS` rotate with the manifest; added `scripts/check-sw-shell-coherency.mjs`. |
| 5 | ambient-loader-graph-split | ✅ SHIPPED — Added `assets/ambient-loader.js` and moved `nav-sheet`, `exit-intent`, `visit-depth`, `presence-badge`, and `favicon-pulse` out of the base ambient bundle. Bundle dropped from 31 sources / 134.3KB raw to 27 sources / 104.5KB raw. |
| 6 | home-first-viewport-freeze-frame | ✅ SHIPPED — Added `scripts/capture-home-first-viewport-proof.mjs`, generated `docs/visual-proof/home-lcp-s173/` with 4 timed desktop frames, and refreshed `docs/visual-proof/index.html`. |
| 7 | tt-enforce-route-ladder | ✅ SHIPPED (evidence says hold enforce) — Extended `probe-tt-soak.mjs` with route readiness and rollback rows. Fresh read found 81 violations across 3 counter days, so enforce remains correctly parked. Evidence: `docs/TT_SOAK_EVIDENCE_2026-06-05.md`. |
| 8 | membership-proof-loop | ✅ SHIPPED — Added local-only `assets/membership-proof-loop.js`; interview now stores `vs_membership_intent`, rank simulator prefills from it, membership auto-renders a plan after interview, and portal has a proof-loop mount. |
| 9 | feedback-to-ship-receipts | ✅ SHIPPED — Added `scripts/build-ship-receipts.mjs`, `api/ship-receipts.json`, and `docs/SHIP_RECEIPTS.md` joining feedback themes, commit map, and visual-proof manifests without raw feedback. |
| 10 | ark-signature-failure-dossier | ✅ SHIPPED — Added `scripts/build-ark-signature-dossier.mjs` and `docs/ARK_SIGNATURE_FAILURE_DOSSIER_2026-06-04.md`; current packet records 3 signature failures. |
| 11 | public-intelligence-budget-ledger | ✅ SHIPPED — Added `scripts/build-intelligence-budget.mjs`, `api/intelligence-budget.json`, and `docs/INTELLIGENCE_BUDGET_LEDGER.md`; 7 intelligence surfaces, 52.1KB generated, runtime AI cost `none-runtime`. |
| 12 | nav-sheet-decision-accelerator | ✅ SHIPPED — `build-nav-sheet-stats.mjs` now emits `remainingOpens` and `decisionETA`; current default remains unflipped with 0 opens and 50 remaining. |
| 13 | founder-yesno-retire-vaultsparked-proof | ✅ SHIPPED — Added `docs/MEMBERSHIP_ORPHAN_DECISION.md` with delete/keep consequence and rollback command; file is still retained pending founder yes/no. |
| 14 | staging-parity-health-tile | ✅ SHIPPED — Added `scripts/check-staging-parity.mjs` and `api/staging-health.json`; current status `yellow` because live staging/prod differ on shell/header parity for sampled routes. |

**14/14 shipped · `npm run build` green · `npm run build:check` green end-to-end (108-page crawl, 0 status failures, 0 blocking-script findings).**
