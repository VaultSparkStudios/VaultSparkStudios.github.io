<!-- generated-by: render-audit-md.mjs (R-H14) — DERIVED FROM AUDIT_2026-06-14-S197.json; do not hand-edit -->
<!-- generated-at: 2026-06-14 -->
<!-- project: unknown · type: unknown -->

# Project Audit — Unknown

> One ranked improvement plan covering 9 axes. Top items first.
> Source of truth: this file is derived from the JSON sidecar. Edit the JSON, then re-render.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 3
- Combined Priority: 0.00
- Top 3: —

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov | Priority | Status | Item |
|---|:-:|---|---|:-:|:-:|:-:|:-:|---|
| 1 | transformative | ux | 2.5h | 10 | 7 | 32.3 | ✓ | **game-play-dead-end-fix** — The studio's #1 CTA dead-ends on a 'Demo Coming Soon' placeholder for a game that is LIVE — wire the play path and gate the SPARKED↔playable coherence. VaultSpark's whole growth thesis is 'a shared link sells the studio,' and S193 made the homepage hero's single primary CTA '▶ Play Free — No Download' → /games/call-of-doodie/. But that page renders a 'Demo Coming Soon — the playable build is in active development' placeholder (the real iframe is commented out with a literal [GAME_EMBED_URL]), while the games index and a lower homepage section correctly link 'Play Now — It's Free' to the actual live build at https://callofdoodie.wtf/. So a visitor who clicks the studio's most-promoted action lands on 'coming soon' for a game that is already live — and the page's meta description, OG description, VideoGame schema, and FAQ all assert it is playable now (CANON-031 no-lying-surfaces + CANON-008). This is a pure conversion leak from a stale placeholder, not an availability problem; 8+ sessions missed it because the audits trusted the registry's 'SPARKED — Live' label instead of walking to the play surface. The CTA TEXT stays true (the game IS free, no download) — fixing the destination makes an existing promise true rather than changing a promise, so it ships without founder escalation. vaultspark-football-gm is the harder case: labeled 'SPARKED — Live & Playable' but its page also shows 'Demo Coming Soon' with no discoverable external build — its label may itself be the lie and needs reconciliation (wire the build if one exists, else honestly drop it to FORGE). **Recipe:** L1 first: on games/call-of-doodie/index.html replace the .demo-placeholder 'Demo Coming Soon' block (keep the markup pattern from the games-index card, line ~659) with a prominent primary '▶ Play Now — It's Free →' button to https://callofdoodie.wtf/ (target=_blank rel=noreferrer), preserving the 'Join The Vault for early access / save progress' as the secondary; verify the homepage hero (index.html:1061) now reaches a playable surface in one click. Then align the page's VideoGame schema url/offers + FAQ to reflect the live build (no surface should claim a coming-soon game is playable, or vice-versa). |
| 2 | high | featureDepth | 2h | 7 | 6 | 21.0 | ✓ | **post-play-membership-bridge** — Turn the play moment into the membership funnel's top intake — the games are the traffic magnet but they hand visitors to an external domain with no capture. Once item 1 wires the play path, the dominant visitor action becomes a click out to callofdoodie.wtf — an external domain where the studio loses the visitor with no return affordance and no membership capture, on a site whose SOUL says membership is load-bearing. The game page already carries a Vault Points callout (it asserts 'create a free Vault Member account to save your progress and earn Vault Points'), but it sits BELOW a 'coming soon' fold and is disconnected from the play action. The strategic move is to fuse the games funnel into the membership funnel at the exact moment of intent: lead the launch pad with Play, and frame the membership ask around the thing a player wants the instant they finish a run — saved progress, points, leaderboards. This is on-thesis (the studio's monetization is membership, not the free games) and ground-truthed (the callout copy + register path already exist; this elevates and re-sequences them rather than inventing a feature). **Recipe:** On the SPARKED game pages, place the Vault Points / 'save your progress' membership callout immediately adjacent to the new Play CTA (above the fold), with a one-line value frame tied to play ('Earn Vault Points and save your run — join free'). Reuse the existing /vault-member/#register path and the already-loaded funnel beacon so the play→join intent is measurable; do not add a new tier or change pricing (escalation). |
| 3 | high | ux | 1h | 7 | 4 | 17.7 | ✓ | **game-snippet-truncation-fix** — Stop truncating the studio's prime share surface — 7 game pages + the games index ship 201–258-char meta descriptions that cut mid-sentence in Google, Discord, and AI snippets. check-meta-descriptions reports 10 length warnings; 7 of them are game pages (call-of-doodie 254, gridiron-gm 232, mindframe 219, the-exodus 223, football-gm 220, vaultfront 201, plus games index 208 and promogrind 258). Search engines truncate at ~155–160 chars and social/LLM unfurls clip similarly, so the description for the studio's prime conversion-and-share surface — the exact thing the 'a shared link sells the studio' thesis depends on — reads as a sentence cut off mid-word. data-deletion (69) sits just under the 70 floor (trivially extend). The front-loaded hooks are already good (call-of-doodie opens 'Play Call of Doodie free in your browser'); the fix is to keep the hook and cut the redundant tail to land a complete, compelling sentence on every discovery surface. **Recipe:** Rewrite the 9 over-long descriptions to SERP-safe ≤155 chars, front-loading the play hook and dropping redundancy (e.g. 'No download required' is already implied once 'in your browser' is said); extend data-deletion past 70. Keep SOUL voice. Re-run check-meta-descriptions to clear all 10 warns. |

## Depth Ladder (per item: L1 minimal · L2 solid · L3 genius)

> `/implement` selects the depth that fits remaining budget, and climbs the ladder
> on already-shipped items when the session still has budget (S175 saturation).

### game-play-dead-end-fix
- **L1 · minimal** _(~45m)_ — call-of-doodie page: swap the 'Demo Coming Soon' placeholder for a real 'Play Now — It's Free →' CTA to callofdoodie.wtf; confirm homepage hero delivers play. Single highest-leverage fix — unblocks the #1 CTA.
- **L2 · solid** _(~2.5h)_ — L1 + reconcile vaultspark-football-gm's SPARKED label with reality (find/wire its live build, or honestly relabel FORGE across nav + games-index card + game-affinity.json) + align VideoGame schema + FAQ on both SPARKED pages + ship scripts/check-game-playability-coherence.mjs (folded into check-proof-surface, no new build:check segment): a page with data-status='sparked' must contain a real play link and MUST NOT contain 'Demo Coming Soon' / [GAME_EMBED_URL]; a non-SPARKED game must NOT assert playable-now in meta/schema/FAQ.
- **L3 · genius** _(~6h)_ — Root cause is that game status lives in 4 disconnected places (nav dropdown, games-index card, data/game-affinity.json, the game page body) with no source of truth, so a game can be 'live' in 3 and 'coming soon' in the 4th. Create data/game-registry.json (slug → {status, playUrl, embeddable}) as the single source; derive the nav/index/affinity/page status + play links from it at build time; the coherence gate cross-checks all 4 surfaces against the registry so this class is permanently impossible.

### post-play-membership-bridge
- **L1 · minimal** _(~30m)_ — call-of-doodie only: move the existing Vault Points callout up next to the Play CTA + add the play-framed one-liner.
- **L2 · solid** _(~2h)_ — Both SPARKED game pages + emit a bounded funnel: event ('game_play_click' / 'game_join_from_play') through the live /v/rum funnel family so the play→join bridge is measurable; honest-dark until samples accrue.
- **L3 · genius** _(~4h)_ — A returning-player recognition: on a 2nd+ visit to a game page (localStorage signal), swap the generic join ask for 'Welcome back — save the progress you've been making' (reuse the membership-unlock.js stage classifier, no new tier logic), so the membership ask escalates with demonstrated play intent.

### game-snippet-truncation-fix
- **L1 · minimal** _(~30m)_ — Fix the 4 worst (promogrind 258, call-of-doodie 254, gridiron-gm 232, the-exodus 223) + extend data-deletion past the 70 floor.
- **L2 · solid** _(~1h)_ — Fix all 10 flagged pages — clear the check-meta-descriptions advisory to 0 warnings.
- **L3 · genius** _(~2h)_ — All 10 + tighten the gate: add a per-page-type ceiling in check-meta-descriptions so SPARKED game pages (the conversion surface) hard-cap at ≤160 chars, preventing regression on the surface that matters most.

## Skipped (Priority < 1.5)

- journal-looks-abandoned-fix — DISPROVEN on verification: journal/index.html already renders a dynamic journal-feed.js living feed beside the curated entries, so the surface is not visually abandoned. The 84d freshness warn is a founder-gated content-publish decision (SOUL: never auto-publish curated essays), not a code gap.
- aeo-qa-content-page — SPECULATIVE on a data-starved site: api/oracle-queries.json is curated seed chips, not captured intent, and S196 already shipped FAQPage schema sitewide. A net-new crawlable Q&A content surface is low-confidence ROI until real query data exists.
- more-funnel-instrumentation — Rejected by the standing verdict: S186–S196 over-built the measurement apparatus on a funnel at ~1 event/30d. Adding more measurement before fixing the broken conversion path (item 1) and the thin traffic would compound the existing imbalance.

## Execution Log

### game-play-dead-end-fix
- `2026-06-14` · **shipped** · Ground-truth corrected the framing: both SPARKED pages already linked their live builds in the hero; the bug was a stale, redundant "Demo Coming Soon" section contradicting them (CANON-031). Replaced both with a live Play panel; removed gridiron (VAULTED) embed-stub debris; shipped check-game-playability-coherence.mjs (7/7) folded into check-proof-surface. L3 single-source game-registry.json DEFERRED (6h structural refactor of 4 surfaces — too risky late-session; the gate already blocks the contradiction class).

### post-play-membership-bridge
- `2026-06-14` · **shipped** · Merged into item 1 section rewrite: each live Play panel pairs the play CTA (game_play_click) with the membership capture (game_join_from_play), emitted as bounded funnel:* to /v/rum; loaded funnel-tracking.js on both SPARKED game pages so the play->join bridge is measurable. L3 returning-player recognition deferred (nice-to-have).

### game-snippet-truncation-fix
- `2026-06-14` · **shipped** · Rewrote all 13 flagged game/listing pages to SERP-safe ~136-159 chars (front-loaded hooks, SOUL voice); the tighter 160 game-page ceiling then surfaced + fixed 3 more (gridiron-gm-play, project-unknown, solara). check-meta-descriptions now holds game pages to 160 (WARN-only per contract), self-test 10/10. 0 length warnings.

