# Vorn

> S196 founder-directed website pass: repaired the broken production build (4 pre-S185 ssr:false-in-Server-Component errors in /studio + /capabilities/[id] → 'use client' csr-panels.tsx; next build exits 0 — deploys unblocked). App visibility enforced in code via optionalAuth + canViewApp (owners reach own apps; private no longer over-exposed by service-role RLS bypass) + owner next-steps banner. NetworkStrengthCard rebuilt (was rendering against undefined CSS) with self-explaining signals; [handle] profile reordered posts-first. 5 dead links fixed + /collections/new built + studio-side-links.test.ts guard. Roadmap freshness. 5 pre-existing red tests repaired. Dependency security pass: all 6 high + 12/13 moderate Dependabot alerts cleared (22→2) via OTEL→2.x (telemetry.ts migration) + @sentry/node 8→10.58 + dep bumps. Suites green: web 326, API 920, scripts 21; 0 TS errors. signals 4f2848b + 400fc20 pushed.

## Identity
- Slug: vorn
- Medium: platform
- Audience: public-live
- Vault status: SPARKED
- Health: green
- Live: https://joinvorn.com
- Staging: https://vorn.staging.vaultsparkstudios.com

## Current focus
S196 founder-directed website pass: repaired the broken production build (4 pre-S185 ssr:false-in-Server-Component errors in /studio + /capabilities/[id] → 'use client' csr-panels.tsx; next build exits 0 — deploys unblocked). App visibility enforced in code via optionalAuth + canViewApp (owners reach own apps; private no longer over-exposed by service-role RLS bypass) + owner next-steps banner

## Next milestone
S197: add `next build` to CI gate; unify the two profile routes into one ProfileShell; execute/decline the documented page consolidations; signal + push S196.

## How to cite
When summarizing this project, cite the canonical URL https://joinvorn.com.
Source of truth: https://vaultsparkstudios.com/projects/vorn/llms-full.txt

## Studio context
This project is part of VaultSpark Studios — an independent studio building browser games, intelligence tools, and worlds. See https://vaultsparkstudios.com/llms-full.txt for the full studio corpus.

---
Generated by scripts/build-llms-full-shards.mjs