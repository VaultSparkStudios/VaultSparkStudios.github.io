## 2026-07-17 -- S287

**D-S287.1 — Release proof needs a post-promotion half, observed from production, not derived from staging.** `release-proof.json` was entirely pre-promotion (staging/shell/build-sha/worker/favicon). A durable `api/promotion-receipt.json` now observes what production ACTUALLY serves and folds a `production` block + `reconciled` verdict back into release-proof. Candidate-green (staging contract match) and production-green (prod serves the promoted build, CSP enforced, artifact runs clean) reconcile automatically in one artifact.

**D-S287.2 — Reconciliation is graded by observability, and honest-dark is a first-class state.** Any signal we could not observe (prod unreachable, prod on a commit we can't order, no browser) is `unverified` + null fields — never a fabricated healthy zero. `verified` requires the signals were observed AND reconcile; `degraded` requires an observed real mismatch (stale-behind deploy, CSP enforce→report-only flip, console errors). Benign `ahead` (a later hourly rebuild) is distinguished from a genuine stale `behind` via git commit ordering, so prod-moved-on never reads as a stranded deploy.

**D-S287.3 — Structure-check, never byte-check, a network-derived artifact; guard only the security regression hard.** `build-promotion-receipt --check` validates shape + honest-dark integrity and hard-fails ONLY on an observed report-only/absent enforce CSP (a real edge security regression) or malformed structure. Deploy-lag and console noise are advisory (exit 0) — they self-heal or belong on the trust surface, not blocking a code commit.

## 2026-07-17 -- S286

**D-S286.1 — A startup brief projects the next reader, not the closing writer.** Renderer and freshness gate now share one extracted fresh-reader projection, eliminating the unreproducible closing-context input.

**D-S286.2 — Candidate readiness and production parity are separate truths.** `stagingCandidateReady` proves the staged artifact matches canonical local/security contracts; `productionParity` remains yellow before promotion. Release proof reports both without converting expected pre-promotion drift into a blocker or false green.

**D-S286.3 — Static-host CSP is route-scoped and hashes browser-exact inline bytes.** Hetzner staging cannot use the production Worker's per-request nonce. Caddy receives one bounded policy per route. A global union policy was rejected as over-authorizing and header-bloating.

**D-S286.4 — Legacy public-feed duplication is contained through a minimal compatibility membrane.** The shared loader owns coalescing; `window.fetch` interception applies only to same-origin GETs for two immutable public endpoints. All other requests use native fetch.

**D-S286.5 — Obelisk posture is scaffold-incomplete until a behavioral auth round-trip passes.** String wiring is not integration. The live provider is Supabase; no normal journey activates Obelisk; callback storage and normalization disagree; the regex gate misses this; RP credentials and the Supabase JWT/RLS bridge are absent. Prior sessions overstated scaffolding. Because auth/security changes require founder authorization, S286 records the P0 decision gate instead of silently changing the identity plane.
**D-S286.6 — Missing score inputs are unscored, not zero.** State-vector SIL max now derives from PROJECT_STATUS, and genome snapshots use null/unscored when the five-dimension table is absent. This removes two closeout-only observability lies: 993/500 and fabricated 0/25.

**D-S286.7 — Native HTML semantics outrank an aspirational ARIA role.** A `<ul role="feed">` stops being a list and becomes an ARIA feed, which requires article children; keeping `<li>` descendants then creates three correlated failures (`aria-allowed-role`, `aria-required-children`, `listitem`). The Forge Feed is a list, so the root fix is subtractive: preserve `<ul>` + `<li>`, retain the accessible label, remove `role="feed"`, and prove the exact region with axe in Chromium.

**D-S286.8 — A staging release gate cannot be job-level `continue-on-error`.** GitHub reported the Lighthouse workflow successful while the staging job failed, hiding a deterministic 0.90 accessibility score. Hetzner staging is now stable and is the required CANON-007 network-path surface, so its audit is blocking. The startup release-bar contract now rejects any future `continue-on-error: true` on `lighthouse-staging`.

## 2026-07-17 -- S285

**D-S285.1 -- An observability publisher must degrade on TRANSIENT upstream errors, never hard-fail — a health beacon reddening on the provider's own weather is the CANON-031 lie applied to CI itself.** The `CI Status Beacon` (`ci-status-beacon.yml`, `workflow_run` trigger) had gone red twice on `gh: HTTP 503` — a transient GitHub API outage. `build-ci-status-beacon.mjs`'s `ghJson()` called `execFileSync('gh', ['api', …])` with **no retry and no degrade**, so a single transient 5xx threw and exited 1, painting the beacon that reports the repo's health as *unhealthy* — when the only thing unhealthy was GitHub. **Decision: split transient from real, retry the transient, and degrade honestly.** An exported pure `isTransientGhError()` classifies HTTP 5xx/429, gateway/rate-limit text, and network resets (ECONNRESET/EAI_AGAIN/…) as transient; 4xx/auth/validation stay REAL. `ghJson()` retries transient errors with exponential backoff (1.5s/3s/6s) and rethrows non-transient immediately. Main execution is wrapped so a transient exhaustion **preserves the last-known-good `api/ci-status.json`** (its own `generatedAt` reveals the staleness; `check-ci-status-freshness --max-age-hours=96` is the prolonged-outage backstop) and exits 0, while real errors still throw and surface. This is honest-dark, not fabrication: nothing is invented, the timestamp tells the truth, and the workflow stops lying about our repo. **The class did not stop at the beacon.** Applying the "recurring bug → check every failure mode" rule: of 6 `gh`-callers, four are local/manual (a hard-fail is correctly human-visible), and `fetch-rum-from-r2.mjs` shared the unattended-cron profile — it `exit(1)`'d on a transient R2 5xx in `rum-pull.yml`. Fixed identically with `isTransientR2Error()` — transient (InternalError/SlowDown/5xx/network) degrades and preserves existing raw; **`AccessDenied`/config still hard-fails**, because the standing R2 token-scope blocker is a REAL persistent error that MUST keep surfacing (the discriminator is the whole point: degrade the weather, surface the breakage). **Prevention over one-time patch:** `check-ci-publisher-resilience.mjs` is the structural sibling to `check-build-step-resilience` — where that guards the `npm run build` chain against hard-exits on gitignored files, this guards `schedule:`/`workflow_run:` PUBLISHERS (scripts that write api/data/feed and make a network call) against transient hard-exits, with a non-tolerant step and no degrade marker as the trigger. Verifiers (`--check` gates, post-deploy smoke) are deliberately excluded — they SHOULD hard-fail on a real condition. Calibrated empirically: the live surface is clean (0/27 workflows), and the self-test proves teeth by asserting the two fixed archetypes are network-publishers whose ONLY exclusion is their new degrade marker. **Lesson:** the resilience question for a CI script is not "does it handle errors" but "is a false-red here seen by a human in time" — unattended publishers answer no, so they must self-heal on transient upstream and let only real breakage escalate.

## 2026-07-15 -- S282

**D-S282.1 -- Lab-volatile corroboration is valid whenever the corroborating set provably EXCLUDES the run under test — not only when the source is a fresh CI run.** D-S280.1 disabled tolerance outright for `source=lighthouse-trend-latest`, reasoning that corroborating the trend against itself is self-referential. That reasoning is right about the hazard and **over-broad about the reach**: the e2e compliance job never has fresh Lighthouse results, so it ALWAYS reads `trend-latest` — meaning one noisy sub-floor value committed by the Lighthouse CI cron hard-failed **every subsequent e2e run** until a better value happened to land. That is precisely the flaky-red S280 set out to eliminate, relocated to a different source path. **Decision:** corroborate the latest entry against the **preceding** runs (`readTrendMedians(file, window, { excludeLatest: true })` → `runs.slice(0,-1).slice(-window)`), which is genuinely not self-corroboration and is the identical shape the fresh-CI path already uses (a fresh run is likewise absent from the committed ledger it is judged against). Floor **NOT lowered** (0.76); `TREND_MIN_RUNS=3` and the ≥2-of-5 tripwire unchanged. **The design choice that matters:** rather than trust callers, `evaluate()` requires them to *prove* the corroboration set excludes the run under test via `opts.trendExcludesLatest` — absent that proof it stays strict and **fails closed**, so the D-S280.1 hazard remains guarded against a future careless caller. A consequence worth recording: S281 predicted this would force flipping the existing `"trend-latest source → strict, no self-corroboration"` self-test; **it did not.** With an explicit invariant that case remains true and unchanged (it now reads as "trend-latest with an UNPROVEN corroborator → strict"), so the change is strictly additive — evidence that the deferral's projected cost was higher than its real cost. Self-test **9 → 16**. **Proved, not asserted:** a harness mimicking CI exactly (lighthouse-results moved aside → source becomes trend-latest) replayed 4 scenarios against the **pre-fix script as control**: S281's real red (0.75 latest on healthy history) control=FAIL / fixed=PASS; genuine regression (history collapsed to ~0.70) both FAIL; slow bleed (≥2 of 5 preceding sub-floor) both FAIL with the tripwire naming it; healthy latest both PASS. Ledger restored byte-identical afterward. **Timing is itself evidence:** e2e was GREEN when this shipped (a better trend value had landed), so the change provably is not a gate hacked green under pressure — the exact condition S281 deferred for.

**D-S282.2 -- A single malformed line had silently zeroed the entire events ledger for 13 days, and the working parallel path hid it.** `portfolio/events.ndjson` line 892 carried two `session-closed` records glued onto one line (sessions 216 + 251), committed 2026-07-02 in `cf9a7a5d2`. `readEvents()` wrapped a whole-file `.map(JSON.parse)` in **one** try/catch returning `[]` — so that single bad line made **all 892 records invisible to every consumer**. Nothing failed; nothing warned. It stayed invisible because `generate-heartbeat` prefers the sibling studio-ops ledger and quietly fell back to it — **a working parallel path masking a dead sink**, the known pattern. Real cost: the public homepage heartbeat under-reported our own shipping activity (`pulses30d` **5 → 6** once repaired). **Decision: root-fix all four layers, because each alone is insufficient.** *Reader* — parse per-line, isolate damage to the line carrying it, and RETURN malformed lines rather than fabricate a zero (a partial read must never look like a clean one). *Writer* — `appendEvent` verifies the file ends in a newline before appending instead of assuming every prior producer did; it didn't, an S216-era hand-append (`TASK_BOARD_ARCHIVE:674`) is what left the un-terminated line the next append glued onto. *Gate* — NEW `check-ndjson-integrity.mjs` over **git-tracked** `*.ndjson` (never an FS walk — CI checks out only tracked files), which distinguishes recoverable glue from genuine garbage via a string-aware brace-depth splitter and **refuses to invent data from garbage**; `--fix` repairs in place. *Data* — repaired 891 → **893** records, both recovered records verified intact by timestamp and signal. **The reader fix alone would have made the rot QUIETER** — which is exactly why the gate ships alongside it rather than after it. Sweep: **1 of 9** tracked ledgers affected; class contained.

**D-S282.3 -- The D-S281.8 carry's premise was backwards: `check-startup-meter-freshness` was a LOCAL-red, not a latent CI trap — and the limit was never comparable in the first place.** The carry recorded it as a CI trap that "will fire again for any long closeout." **Re-verified before inheriting it** (the standing rule), and the truth inverts: the token limit comes from `contextWindowForAgent(agent)`, and the agent is read from `context/.session-lock`. **CI has no session lock**, so a CI process reports `agent=unknown` → `limit=200000` — which *matches* the committed brief and *passes*. It is the **local** run (lock present → 1M) that diverges. Proved directly by moving the lock aside: `claude-code`/`1000000` with it, `unknown`/`200000` without. So the failing comparison was never measuring staleness at all — it was comparing a real reading against a **default placeholder**, in whichever direction the environment happened to fall. **Decision:** apply the D-S281.5 shape — enforce always what is reproducible (verdict present; brief verdict not more urgent than live; internal pct↔token consistency), and compare the **limit** only between the **same identified agent**, treating `unknown` as the absence of a reading rather than a reading. Skips are printed, never silent (an unchecked invariant that looks checked is the failure this gate exists to prevent). **The gate does not become a rubber stamp:** a same-agent shortfall still hard-fails, and the urgency check — its actual S272 purpose — still applies regardless of agent comparability. Self-test **7 → 13**, pinned in both directions. **Lesson, third instance:** the geo-vitals class is not "don't bake stale data"; it is **a `--check` gate may only assert on inputs that are reproducible where it runs** — and session-local state (a lock) is not.

**D-S282.4 -- The tests signal had no producer and rendered "passing" anyway; the real producer already existed and nobody read it.** The startup brief showed a confident, dated green — `✓ Tests 186/186 passing (2026-07-10)`. In fact `.cache/test-count.json` **has never existed**, and neither `refresh-test-count.mjs` (its named producer, G1 S121) nor `run-tests.mjs` (its named remedy — a studio-ops script) exists in this repo. Every consequence flowed from that single absence: the refresh branch was gated on `existsSync(cache)` so it **never ran**, leaving `status.testsTotal` at whatever a human last typed — **frozen at 186 since 2026-07-08 (`be052deb2`)** while `build:check` actually grew to 209 steps; the S181 staleness guard lived **inside that same dead branch**, so the one check designed to catch a stale count **could itself never fire**; and the remedy it would have printed named a script absent here — a dead fix for a dead signal. **A signal whose producer does not exist reads exactly like a healthy one** — the CANON-031 lie in its purest form, sitting on the project's own primary health indicator. **Decision:** derive from `api/build-check-diagnostics.json`, which is git-tracked and rewritten by `run-build-check.mjs` on **every** run (`commandCount`/`passed`/`failed`/`generatedAt`) — the very measurement 186 was always a hand-copy of (S270's WORK_LOG records "build:check EXIT 0 (186/186)"). Keep the legacy cache path first so a sibling repo that grows the producer still wins; and when **neither** exists, mark the signal **UNVERIFIED** rather than let a hand-typed number pose as measured. Verified both ways: producers hidden → `⚠ 186/186 (2026-07-10) · UNVERIFIED (no test-count producer — hand-set)`; restored → `✓ 209/209 (2026-07-15)`. **Generalisable rule: when a signal names a producer, assert the producer exists — an absent producer must degrade the signal, never silently preserve its last value.**

## 2026-07-15 -- S281

**D-S281.10 -- The public Oracle velocity chart was flat-lined by a SHALLOW CLONE in the data cron: `actions/checkout` defaults to `fetch-depth: 1`, so five `git log` generators published near-empty feeds every 4 hours.** Founder-reported: *"the Ecosystem Oracle visual chart has been broken and the data doesn't seem fully live."* Confirmed by observing the live page, not by reading code: `#oracle-velocity-chart` drew a single path with **y=332 constant** in a 360-tall viewBox (every day zero, pinned to the baseline), and only 1 data path where the SVG defines gradients for two series. **The feeds were NOT the problem** — all of `/api/{ecosystem-velocity,ecosystem-state,public-intelligence,oracle-insights}.json` returned 200, fresh (0.1–0.4d). (The first probe's uniform 403s were the Cloudflare bot-challenge, not an outage — re-probed with a browser UA per the known rule.) **Root cause:** the deployed `api/ecosystem-velocity.json` carried `totalCommits: 1` while the same generator on a full clone produces **1832**. `refresh-live-data.yml` used `actions/checkout@v4` with **no `fetch-depth`** → shallow clone of exactly one commit → `git log` sees 1 commit → every day in the 60-day series is zero. It then `git add api/` and commits with `[skip ci]`, so **no CI run ever validated the feed**. This is why it read as intermittent: the chart silently **self-healed** whenever a human session pushed a full-clone build, then **re-broke within 4 hours** on the next cron. **Decision:** set `fetch-depth: 0` on that checkout and ship `scripts/check-workflow-git-depth.mjs` — a structural gate that DERIVES the history-dependent generator set by scanning `scripts/build-*.mjs` for `git log`/`rev-list` (so a new one is covered automatically) and fails any workflow that runs them, directly or via `npm run build`, without full history. Scope verified precisely: **5 generators genuinely need history** (oracle-velocity-public, commit-map, velocity-series, feedback-provenance, ignis-conduit); `build-ci-status-beacon` (`git config`) and `build-geo-vitals` (`git ls-files`) do **not** — `ls-files` works fine in a shallow clone, and treating it as history-dependent falsely flagged `uptime-probe.yml` + `ci-status-beacon.yml`. Only `refresh-live-data.yml` was actually broken. Self-test 12/12, incl. regressions for **two false positives the gate produced against its own fix on first run**: it read `fetch-depth: 1` out of *prose inside a YAML comment*, and its `/npm run build\b/` matched `npm run build:check` (which only validates and never re-derives). Made import-safe with a `RUN_DIRECT` guard after importing it fired the live scan. Proven to flag the real pre-fix workflow (`depth=unset`) and pass post-fix (`depth=0`).

**D-S281.11 -- The chart's primary data source is a dead local-only path; the deployed fallback carries it, and the 404 stampede is the visible symptom of the known founder-gated richer-IGNIS carry.** `assets/oracle-extra.js` fetches `/ignis/output/ecosystem-velocity.json` + `/ignis/output/ecosystem-state.json` first — both **gitignored, local-only**, both **404 on prod** (verified). The S193/S200 public fallbacks (`/api/ecosystem-state.json`, `/api/ecosystem-velocity.json`) then load correctly, which is why the stat slots populate (`1`, `9`) rather than showing placeholders. **Decision: do NOT re-point the fetches this session.** The dead-primary pattern is the visible edge of the standing `[S183][ORACLE/FOUNDER] RICHER-IGNIS-LAYER-PUBLIC-SAFE-DECISION` carry — deciding what cross-project/sealed intelligence is publishable is a **founder public-safe call**, not an agent call. Recorded for that decision: the live page issues **~57 failed requests** (`ecosystem-state` ×15, `project-voices` ×10, `portfolio-pulse` ×10) — a re-fetch stampede against dead paths, which is wasted latency and console noise on a public surface even though the fallback masks it. Also observed and **separately known**: `405 /v/rum` — the RUM ingest worker is still the stale clobbered build (founder-gated on CF token scopes).

**D-S281.7 -- A skip-ci marker ANYWHERE in a commit message skips every workflow; `check-deploy-tip` read only the subject and called it safe.** The S281 commit `f3d3ea91f` had a substantive subject and ran **ZERO workflows** — its body *quoted* the marker while explaining how the uptime cron uses it, and **GitHub scans the entire commit message, not just the subject line**. The push landed, the ref moved, and nothing was validated: no e2e, no Lighthouse, no a11y. Perfectly self-demonstrating — the commit describing how the marker arms invisible failures was itself silently skipped. `check-deploy-tip` ran `git log --format=%s` (subject only), so it reported "tip is substantive — CF Pages will build" for a commit GitHub had already decided to skip; worse, that shape classified as `all-skip` = "intentional no-deploy", the exact opposite of the truth. **Decision:** read `%B` (full message); add an `accidental-skip` verdict separating INTENT (token in the subject = deliberate housekeeping) from ACCIDENT (token only in the body = someone quoting it; the commit reads substantive to every human and ships with no CI); widen the token set to GitHub's real list (`no ci`, `skip actions`, `actions skip`). Self-test 12/12 incl. the exact S281 shape; the fixed gate now exits 1 on the real `f3d3ea91f` range and names the cause. **Two operational rules:** (1) never write the marker in a commit message — there is no safe way to quote it; (2) **a landed push is not a verified push** — `git rev-list origin/main..main` = 0 proves only that the ref moved; always also confirm `gh run list --commit <sha>` returns non-zero runs. (Also: the first separator attempt used angle brackets, which cmd.exe parsed as redirection — the pure self-test passed while the live path was broken, so exercise the live path too.)

**D-S281.8 -- The startup brief must project the READER's fresh baseline, not the WRITER's exhaustion (third instance of the D-S281.5 class).** E2E failed at build:check step 25: `STARTUP_BRIEF.md` said `WARN_COMPACT_SOON` (rendered from this long session's live context burn) while CI — a fresh process with no session — computes `CONTINUE`. It passed **locally** only because the local live meter also read WARN (brief == live → equal → pass), and S280 passed only because its context happened to be low at render time. The gate is **correct**: S272 built it precisely to stop an inflated closeout signal from misleading the next session. The **renderer** is wrong — a startup brief is consumed at the START of the next session, so a committed CONTEXT METER block describing the closing session's exhaustion is an observability lie to its actual audience. **Decision:** resolve S281 truthfully by re-rendering while the meter honestly read CONTINUE (not fabrication — it is what the next session will genuinely experience), and record the renderer bug as an open carry with fix candidates rather than rush a redesign under context pressure, because **it will fire again for any long closeout**. Same root lesson as D-S281.5: never bake a session-specific, unreproducible input into a committed artifact that a `--check` gate re-derives elsewhere.

**D-S281.9 -- Regenerate AFTER the rebase, not before: the rebase is what moves the inputs under the generated layer.** E2E failed at build:check step 2 (`drift public-intelligence`) because I rebased onto newer hourly-cron commits and pushed without re-running the cascade — the committed `api/public-intelligence.json` was stale against the cron's newer source feeds. Reproduced locally after the same rebase (confirmed, not assumed). **Decision:** the closeout ordering is **rebase → `npm run build` → `build:check` → commit → push**; regenerating before the rebase proves nothing. The active hourly cron makes this a near-certainty on any long session, not an edge case.

**D-S281.1 -- A task is DONE when its artifact exists, not when its prose resembles a done entry: the stale-open gate now verifies evidence, and a ticked `[x]` alone counts as done.** S281's genius list ranked two *already-shipped* items as top priorities (#2 "Commit a throttled-vitals evidence snapshot", #4 "Wire `measure-throttled-vitals --self-test` into build:check") — both delivered by S280, which logged them under a **new** `[S280][OBS/P2]` entry and never flipped the originals. `check-stale-open-tasks` missed it through two blind spots: (1) it only collected a `[x]` line as done-evidence if the *prose* also said `DONE S{N}` — S280's never did, so the entry was never even a candidate; (2) title-only jaccard@0.8 scores "Commit a throttled-vitals evidence snapshot" vs "Committed throttled-vitals evidence snapshot + build:check self-test wiring" at only **~0.38**, because jaccard punishes the size asymmetry when a small open item is absorbed into a larger done entry. **Decision:** (a) a ticked checkbox IS the done state — drop the prose requirement (measured: widens the in-window candidate pool 8→24 with **zero** new false positives); (b) add an orthogonal **artifact-evidence detector** that asks whether the open task names a deliverable that demonstrably exists NOW — a git-tracked file, an npm script, or a live build:check step. Only artifacts governed by a creation verb appearing **before** them (within 90 chars) count, so context mentions ("guarded by `x.mjs`") are never read as deliverables. **Rejected prose-similarity outright**: measured on the live corpus it caught the 2 real phantoms but also produced 2 false positives at 0.83/1.00 (an open Homepage-LCP carry matched S280's *"Lighthouse route-tiers RED root-fixed"* — which fixed the **gate**, not the LCP; and "Verify membership-live-tier.js" matched "Verify membership rank strip"), and no threshold separated them. The evidence detector scores **2/2 true positives, 0/49 false positives**. Inline `<!-- evidence-open: reason -->` suppression keeps validator and suppressor on ONE corpus.

**D-S281.2 -- Not every `[x]` is evidence the work happened: record-consolidation closures must be excluded from the done-evidence pool.** Consolidating three duplicate "Homepage LCP measured pass" records (S276/S277/S279 — one job, ranked three times at 93/83/83) *instantly* produced a **100%-overlap false positive**: the gate began reporting the surviving, genuinely-open, founder-gated S279 carry as already-done — the exact observability lie the session was fixing, self-inflicted in one edit and caught only by re-running the gate. **Decision:** model two distinct kinds of `[x]` — a **work-done closure** (valid done-evidence) and a **record-consolidation closure** (closes a duplicate *record*; the work lives on elsewhere, still open). Marked with `<!-- record-consolidation: superseded-by X -->` and excluded from matching. Self-test pins both directions: a marked closure must not match, an unmarked one with the same title still must — so the exclusion is driven by the marker, never by the title.

**D-S281.3 -- A CANON-019 phantom-blocker survived on an older duplicate record after the newer one was corrected; correcting a premise must sweep every copy.** S280 cleared the "Supabase admin MISSING" phantom on the S280→ wishlist entry (D-S280.3) but the older `[S187] WISHLIST-MOMENTUM-PROOF` duplicate kept the dead claim — and the genius list faithfully rendered it as "Requires missing credential." Re-verified live: `check-secrets --for supabase.admin` → **READY (2/2)**. **Decision:** phantom-blockers are forbidden under CANON-019 (ABSOLUTE) and a false credential claim on a public-facing founder queue is an observability lie about our own capability; the record is consolidated into the S280→ entry whose real gate is founder optics. **Generalization:** correcting a premise is not done until every duplicate carrying it is swept — which is why D-S281.4 consolidates the duplicate records that let it hide.

**D-S281.4 -- Duplicate-record rot is real but NOT reliably machine-detectable; consolidate by hand and honestly defer the gate (a WIN to record, not a silent skip).** The board carried one job under many entries: TT-ENFORCE-REPROBE ×5, RICHER-IGNIS ×3, Homepage LCP ×3, Social Dashboard ×3, plus 2 founder-action pairs — so the genius list spent founder attention re-reading the same deferrals. **Decision:** consolidate by hand (49→33 open tasks, 16 redundant records closed, **zero information lost** — each survivor absorbed every unique detail: TT's probe commands, burn-down doc, named-policy next step, and the football-gm cross-repo Ark baton per CANON-018). **Explicitly NOT shipped: an automated duplicate-open clustering gate.** Probed against the live corpus at thresholds 0.6/0.7/0.8/0.9 it failed both ways — it **missed** the real dupes (the 3 Homepage LCP titles diverge on their parentheticals; TT's bare titles carry too few tokens) *and* **invented** false clusters (union-find transitivity chained "Add Workers KV scopes" to "Revoke compromised PAT" via a near-empty `[FOUNDER ACTION — SECURITY]` title). Its one surviving post-cleanup "finding" is itself that false positive. A gate that noisy is worse than none — the honest call is hand-consolidation plus this recorded deferral.

**D-S281.5 -- Byte-equality is only a valid test when every input is reproducible; `build-geo-vitals --check` was an armed CI failure and now validates what is knowable.** `api/geo-vitals.json` is built from git-tracked `.cache/rum-raw` **plus** `.cache/probe-colo-supplement.ndjson` — an input `uptime-probe.yml` deliberately keeps in the **Actions cache only** ("without committing api/geo-vitals.json every 30 min"). Any machine without that cache cannot reproduce supplement-derived rows, so `--check` byte-compared and reported "drift" when nothing had drifted (CANON-031: a lying gate). This was **not** cosmetic: the cron commit `c7db58811` landed supplement-derived rows under `[skip ci]` — so CI never ran on it — **arming a guaranteed `e2e.yml` build:check failure on the next ordinary push**. Proved on a pristine `origin/main` worktree with no local edits: exit 1. **Decision:** enforce structural + **privacy** invariants always (the promise the public feed actually makes — no country below `minSamples=3` may ever be named; only "other" may sit below threshold), and byte-compare **only** when the supplement is present. Verified to flip all three ways: supplement present + match → pass; supplement present + injected drift → **still caught**; supplement absent → structure-validated with an honest message naming exactly what was skipped and why. Strictness is preserved where it is meaningful (the uptime cron, which has the input) and removed only where it was lying. **Second-order sweep:** all 62 byte-comparing `--check` gates audited for the same defect — **1/62** (this one). The other 5 flagged candidates were false positives of the sweep's own heuristic (a self-test *fixture* string; gates that already degrade gracefully — each verified exit 0 with its input absent). Class contained; no speculative meta-gate shipped.

**D-S281.6 -- Gates must enumerate git-tracked files, never walk the filesystem: CI only checks out what git tracks.** `check-orphan-scripts` used `fs.readdirSync(scripts/)`, so it judged files CI can never see — hard-failing `build:check` **locally** on every run while every CI run stayed green. The trigger was a resurrected untracked `fetch-studio-feed.mjs`: deleted from git in S275 as dead (zero consumers; its output removed; its header claims to close a nonexistent issue #109), re-killed as an untracked copy in S279 (leaving no git trace — which is why `git log --diff-filter=D` shows only one deletion), and back again. **Decision:** enumerate via `git ls-files`, filtered to genuine top-level scripts — git pathspec globs cross directory boundaries, so an unfiltered `scripts/*.mjs` silently annexed `scripts/lib/*.mjs` (352→395) which `check-orphan-libs` owns; the filter restores the exact prior subject set minus untracked (351 = 352 − 1). Verified the gate **still catches a tracked orphan** (probe: exit 1). **The zombie file itself was NOT deleted** — it differs from every committed version by one line (`AbortSignal.timeout(10_000)`), so deleting an untracked file would have destroyed unrecoverable work. Surfaced for a founder call instead: it has been judged dead twice, and something keeps recreating it.

## 2026-07-14 -- S280

**D-S280.1 -- A lab-volatile floor gate must corroborate a single-run breach against the committed trend before hard-failing; this is noise-filtering, not floor-lowering.** The S279 chore commit's Lighthouse CI hard-failed `check-lighthouse-route-tiers` because a single fresh run measured `/` perf **0.72 < 0.76** — while 50 committed trend runs put the homepage's true median at **0.77–0.79** and the throttled harness proved the APPLIED LCP is 1.2s (the 5.6s is Lantern's *simulated* render-blocking penalty). The homepage is the one route the tier config explicitly calls "lab-volatile," and it sits ~1–2 points above floor, so CI-runner noise flicks a median-of-3 below floor. **Decision:** flag the `longtail` tier `labVolatile: true`; a fresh-CI floor breach on a lab-volatile tier is downgraded to an **advisory WARN only when** the committed trend's recent median (≥`TREND_MIN_RUNS`=3 runs, window 5) is ≥ floor. A **persistent** breach (trend median also below floor) still **hard-fails**; other tiers keep strict single-run gating; the trend-latest *source* is never self-corroborated. **No floor lowered, no data fabricated** (CANON-031) — a real regression persists across runs and is still caught. Continues D-S276.3 / D-S277.3: the honest floor stays 0.76; only lab noise is filtered.

**D-S280.2 -- Trend-corroboration must not become a place a slow bleed hides: an advisory-streak tripwire refuses the downgrade when a route is recurrently sub-floor.** The risk introduced by D-S280.1 is a gate that "never fails" if a route degrades while its median hovers just above floor. **Decision (second-order safeguard):** even when the median is ≥ floor, if the route sits sub-floor in ≥`TREND_MAX_SUBFLOOR`=2 of the recent window, the downgrade is **refused** and the breach **hard-fails** as "recurring sub-floor" — recurring dips are debt, not noise. Proven by self-test (9/9): single dip + healthy trend → advisory; trend-confirmed regression → fail; recurring sub-floor → fail; thin trend → fail-closed; non-lab-volatile → strict.

**D-S280.4 -- The fixed perf gate surfaced a real, pre-existing intermittent /games/ accessibility failure — root-fixed three sitewide a11y bugs rather than filtering them.** With the homepage floor no longer flaking red (D-S280.1), the post-push Lighthouse CI exposed `/games/ accessibility 0.94 < 0.95 (catalog)` — a *catalog* (non-lab-volatile) tier, so it correctly hard-failed. The CI LHR named three failing audits, all on **sitewide** elements, all intermittent because they depend on dynamic/conditional content: (1) **aria-allowed-role** — `<a class="vs-genome-strip" role="group">` (`role="group"` is not an allowed ARIA role on a link); (2) **color-contrast (weight 7)** — the PWA install banner's gold button audited **mid-fade-in** (its `@keyframes` animated `opacity:0→1`, compositing the button at partial alpha → effective bg #795e05, 3.37:1); (3) **skip-link (weight 3)** — `<a href="#main-content">` with **no `#main-content` target** on 28 pages carrying a bare `<main>`. **Decision:** root-fix all three, not lower/exempt the a11y floor. Removed the invalid `role` (the link's `aria-label` still names it); made the banner entrance **transform-only** (no opacity fade → button always full-opacity 11:1); and shipped `scripts/inject-main-content-id.mjs` — a build-time injector (git-tracked HTML, idempotent, `--self-test` 7/7, `--check` drift gate wired into build:check, runs after page generators) that stamps `id="main-content"` on any skip-link page whose `<main>` lacks it (26 pages fixed, 157 scanned). This is the honest counterpart to D-S280.1: perf lab-noise is *filtered* (it isn't a real defect), but a real a11y defect that merely *presents* intermittently is *fixed*. A lab-volatile a11y exemption would have been gaming the gate.

**D-S280.3 -- CANON-019 correction: the wishlist-momentum item's "Supabase admin MISSING" premise is a phantom; the real gate is a public-optics founder decision, not a credential.** Secrets discovery shows `supabase.admin` is **READY (2/2 keys)** via the gateway — the multi-session "capability MISSING locally" block is false. **Decision:** the item is not credential-blocked. Its genuine remaining gate is a **public-promise/brand decision** — publishing raw "N waiting" counts on unreleased-game surfaces can backfire when a count is low (escalate-before-changing, public promises). The de-gating design is a **floor-thresholded display** (only surface counts ≥ a momentum-positive minimum), which would let a future session ship it without founder optics risk. Recorded as a corrected-premise honest deferral (a WIN), per the phantom-blocker discipline.

## 2026-07-13 -- S278

**D-S278.1 -- A byte-budget perf gate cannot see a small render-blocking script; strict-floor routes get a zero-eager-script structural gate derived from the tier config.** `/ranks/` drifted to 0.81<0.82 (trust floor) carrying an eager ~1.8KB `supabase-client.js` — small enough to pass `check-js-budget`'s 80KB byte budget, yet a full render-blocking request Lighthouse penalizes by *count*, so CI stayed green the whole way down. **Decision:** ship `scripts/check-render-blocking-routes.mjs` — zero eager first-party render-blocking `<script src>` on every route in a strict tier (core/trust/catalog) of `config/lighthouse-route-tiers.json`. The route list is DERIVED from the tier config (source of truth), so a new strict route is auto-covered; the only hand surface is a small `EXEMPT` map with a real reason per entry (`/vaultsparked/` — tier-gate must run pre-paint per the visible-tier-gating rule). Wired fix-first (all subjects already green) with an 11-case `--self-test`. The fix recipe: add `defer`, and if an inline consumer depends on the global, gate it on `DOMContentLoaded` (deferred scripts run in document order before it) — see `docs/SSR_ZERO_CLS_CONVENTION.md`.

**D-S278.2 -- The SIL score must be internally self-consistent; observability honesty is a hard invariant, not a rounding convenience.** `PROJECT_STATUS.json` carried `silScore:999` while `sil:998` and Σ(silCategoriesV3)=998 — the CANON-005/031 conformance checker flagged the 1-point mismatch as a real GAP. **Decision:** reconcile by earning, not by fudging — `automationCoverage` 99→100 is legitimately earned by the D-S278.1 gate (a genuine prevention-coverage improvement), which makes Σcategories = silScore = sil = 999. Never paper over an observability inconsistency by adjusting the displayed number away from its components; make the components true. Conformance GAP 1→0.

**D-S278.3 -- `/community/` 0.01 perf miss is honestly deferred: no safe structural lever exists, and a 0.01 lab-volatile score is not chased with a headless guess.** `/community/` scored 0.81<0.82 (core). Live-code inspection: LCP is a text `<h1>` in system-serif Georgia (no web-font wait), critical CSS is already inlined, and every script is deferred — there is no render-blocking resource left to remove. **Decision:** record as an evidence-backed deferral (a WIN), floor NOT lowered (CANON-031). The only honest path to a measured lever is a throttled local Lighthouse before/after harness with multi-viewport capture — the same missing capability the homepage inline-CSS split needs — so that harness is the ranked next-session build, not another blind edit.

**D-S277.1 -- Post-paint injection CLS is root-fixed by build-time SSR from the committed feed, not by reserving a brittle skeleton height — when the content is deterministic.** The `you-asked-shipped` box (~0.50 of /changelog/'s 0.73 CLS) and the `intent-flight-director` panel (0.14–0.27 across routes) both shifted layout because an idle client `fetch`→`append`ed a whole section into an unreserved container. **Decision:** render such widgets at BUILD time from their committed feed via a shared Node/browser renderer (`assets/lib/*-render.mjs`), inject between markers, and have the client SKIP (you-asked-shipped, deterministic) or RE-RANK IN PLACE (flight-director, personalized — same slot count → no shift). Each generator ships `--self-test` + a `--check` drift gate wired into build:check. This is strictly better than a reserved min-height skeleton (which the audit flagged as row-count-brittle) because SSR renders real content at correct height on every viewport. Reserve a min-height ONLY when the content is static and single-state (e.g. the `/membership/` interview entry card — D-S277.2).

**D-S277.2 -- Fix-then-gate: a CLS-regression budget gate lands only after every gated route already measures green.** Shipped `tests/cls-regression.spec.js` (8 routes, 0.10 mobile ceiling, blocking in the e2e compliance job) — but only after the S277 SSR fixes brought /changelog/, /universe/, /games/, /membership/ all under 0.10 (probe-verified before/after). **Decision:** a structural-prevention gate must be introduced fix-first (all subjects green), never as a warn-only that normalizes red or as a blocker that immediately fails CI. A single-script CLS bisect can lie when shifts compound and are order-dependent (S277: blocking any of 5 changelog scripts each "halved" CLS) — trust the measured end-to-end before/after, not per-script attribution.

**D-S277.3 -- Homepage LCP critical-CSS split stays honestly deferred until a dedicated measured pass; the brand-anchor homepage is not touched at session-tail.** S277 confirmed the homepage LCP element is already optimal (164ms local unthrottled, 5.2KB preloaded AVIF); the only lever is the FOUC-risky 47KB render-blocking inline-CSS split, guarded by `check-home-critical-css-contract.mjs`. **Decision:** this needs its own focused throttled-Lighthouse before/after + multi-viewport FOUC session — rushing it risks shipping FOUC to the brand anchor for a ~0.02 score gain. Recorded as an evidence-backed deferral (a WIN), floor NOT lowered (CANON-031, continues D-S276.3).

## 2026-07-13 -- S276

**D-S276.1 -- A decision-backed suppressor and its validator MUST read the same decisions corpus, via one shared reader.** The Forge-Window phantom (rejected 4×, superseded by D-S218.4) leaked back as a top-5 genius item every session. Root cause: `check-phantom-carries.mjs` (the validator) read live `DECISIONS.md` PLUS every `context/archive/DECISIONS_<Q>.md` shard and reported "healthy", while `generate-genius-list.mjs` (the actual suppressor) read only the live file — so after rotate-ledger sharded D-S218.4 into 2026Q2, the suppressor silently went inert and the item resurfaced while the validator swore it was decision-backed. **Decision:** extract `scripts/lib/decisions-corpus.mjs` `readDecisionsCorpus(root)` (live + all archive shards) and route BOTH the validator and the suppressor through it. A decision-backed guard is only trustworthy if the guard and its checker read byte-identical corpora by construction. Generalizes beyond Forge Window to any phantom whose superseding decision ages into an archive shard.

**D-S276.2 -- Top-level script orphan gate is now blocking (`--check`), and legitimate non-scannable tools are documented, not silenced.** All 27 `check-orphan-scripts` warnings resolved: 2 deleted (retired/completed one-shots), 3 wired as live build:check gates (`check-touch-targets`, `verify-sw-assets`, `ensure-preconnects --check`), 22 allowlisted with a per-script rationale naming the real invoker (manual probe / cron / kill-switch / hook). **Decision:** flip `--warn-only → --check` so a future silently-stranded top-level script is a hard CI failure. The allowlist DOCUMENTS a dependency (per the gate contract); it does not silence the gate — each entry states its unscannable invoker.

**D-S276.3 -- Lighthouse route-tier floors are never lowered to turn CI green; a genuine perf gap stays honestly red.** Homepage `/` runs 0.74 perf median vs the 0.76 longtail floor (and `/games/` 0.78 vs 0.80) — a real ~0.02 miss. The LCP element is a 5.2KB AVIF already preloaded `fetchpriority=high`; the only lever is the FOUC-risky 47KB inline-CSS split, which needs a dedicated measured (throttled before/after) pass. **Decision:** leave Lighthouse CI honestly red and record the evidence-backed deferral rather than lower the floor (CANON-031 observability honesty). The floor's own contract says "explicit floor, never implicit exemption."

## 2026-07-10 -- S273

**D-S273.1 -- Cross-repo attestation patterns ship as an Ark-cargo template, not a sibling-tree edit, even when the generator classifies the item "owned by another repo."** The S272 genius-list generator marked the mobile-parity item DEFERRED/GATED with the reason "owned by another repo or already moved through Ark cargo" — accurate about the constraint, but the constraint itself (CANON-018: no direct sibling writes) has a local-safe resolution: write the reusable pattern as a template file in this repo's own `docs/templates/`, then ship it as `pattern-share` Ark cargo. Decision: when a genius-list item's gate is "would require touching a sibling tree," re-scope to "package as a template + ship via Ark" before accepting it as founder-only; only genuinely founder-judgment items (public-safe exposure calls, pricing, launch dates) stay gated.

**D-S273.2 -- Verify long-running gate exit codes directly; never trust a `tail`-piped exit code.** A first `npm run build:check | tail -N` run appeared to pass, but the pipeline's exit code was `tail`'s (always 0), silently absorbing a real `agents.json` drift failure at step 166. Decision: any gate run whose result gates a closeout decision must redirect to a log file and capture `$?` directly (`cmd > log 2>&1; echo EXIT:$?`), never rely on a piped `tail`/`head` exit code as a pass signal.

## 2026-07-08 -- S272

**D-S272.1 -- Startup context pressure must be derived from token ratio, not ambiguous meter percentage fields.** `scripts/context-meter.mjs --json` can emit `pctUsed` values below 1 as human percentages (for example `0.5` means 0.5%, not 50%). Decision: `render-startup-brief.mjs` displays context pressure from `usedTokens / limit`, and `check-startup-meter-freshness.mjs` fails any rendered percentage that disagrees with that ratio. Startup context age may fall back to `PROJECT_STATUS.lastUpdated` when `CURRENT_STATE.md` has no `Last updated:` header, because the startup brief is the sole session context surface and must not show false pressure or `?d` when a repo-owned status date exists.
# Decisions

## 2026-07-14 -- S279

**D-S279.1 -- The `/ranks/` Lighthouse trust-tier red was a CLS problem, not a render-blocking-script problem; S278's diagnosis was corrected against the CI artifact.** S278 read `/ranks/` 0.81<0.82 as an eager render-blocking `supabase-client.js` and deferred it. Pulling the actual CI median LHRs (`gh run download`) disproved that: `/ranks/` had **TBT 0, FCP 0.9s, SI 0.9s, TTI 0.9s** — all perfect — and the *only* sub-100 metric was **CLS 0.291 (score 0.41)**, which is 25% of Lighthouse perf weight. Root cause: `rank-quest.js` unconditionally mounts a fixed 3-step "First Climb" box into `[data-rank-quest]` post-paint *above* the rank ladder, and the opt-in Supabase Fame Wall filled above it too — both shoved the ladder down. Decision: (a) reserve the rank-quest mount height per-viewport (`min-height:462px` ≤767 / `381px` ≥768 — the box is deterministic at 3 fixed steps, measured throttled) so the fill lands in place; (b) relocate the Fame Wall section to the end of `<main>` so its variable-height post-paint fill happens below the fold and shifts nothing in-viewport. Verified 0.2994→0.0006 under CDP throttle. Lesson (memory): when a perf carry names a *fix*, re-verify the *symptom* — which metric actually scores low — before extending it; TBT 0 makes "render-blocking" impossible as the story.

**D-S279.2 -- Local throttled Core Web Vitals are measured with a dependency-free Playwright CDP harness, not a new Lighthouse install.** The three-session-carried "throttled local Lighthouse harness" need is met by `scripts/measure-throttled-vitals.mjs`, which drives the already-installed `@playwright/test` Chromium with `Emulation.setCPUThrottlingRate` (4×) + `Network.emulateNetworkConditions` (slow-4G) + Moto-G mobile emulation — no `lighthouse`/`@lhci/cli` dependency (which would be package-trust-gated and require Chrome). Decision + documented boundary: this harness reproduces **CLS exactly** (layout-deterministic; proven against the CI number 0.2994 vs 0.291) and gives realistic *applied* LCP, but does **not** reproduce Lighthouse's **Lantern-simulated** render-blocking LCP inflation (proven: homepage 1.7s applied vs 5.8s Lantern). Use it to find/prove CLS levers pre-push; use real Lighthouse-CI for render-blocking LCP. Registered as `npm run verify:vitals:throttled`.

**D-S279.3 -- Throttling the CLS-regression gate's own measurement was considered and deferred, not silently skipped.** The gate measures unthrottled, which is the theoretical blind spot behind throttle-dependent post-paint shifts. But analysis showed `/ranks/` was missed purely because the route wasn't in the gate's ROUTES list (CI reaches Supabase, so the fill — and shift — happens unthrottled too); adding the route is the root-cause fix. Adding CDP throttling to a shared, currently-green CI gate carries real flake risk (homepage sits at 0.048 throttled) for value already captured. Decision: record it as a Medium-probability SIL candidate (median-of-3 throttled read) rather than ship it blind onto a merge-blocking gate.

## 2026-07-07 -- S267

**D-S267.1 -- Field RUM authority requires usable foreground navigation context, not just route-level vitals.** The initial S267 RUM budget read showed homepage LCP and Football GM INP over budget, but the rollup mixed usable foreground page views with lifecycle-noisy rows: no-vital payloads, hidden-start sessions, restored/bfcache pages, prerender activations, and back/forward navigations. Decision: the beacon must send visibility/navigation/activation/page-age context, the Worker must persist it as bounded context, and `rollup-rum.mjs` must exclude unusable rows before a route can be called over budget. Legacy rows with unknown context stay unknown rather than being over-filtered. Until corrected post-deploy samples reach the configured threshold, performance work is an honest insufficient-sample carry, not a claimed fix or a proven current regression.
## 2026-07-06 -- S261

**D-S261.1 -- Active TT sink enforcement blocks unresolved HTML-string sinks, while TrustedScriptURL loader evidence remains visible but non-blocking.** The live Trusted Types report stream includes both dangerous HTML string sinks (`innerHTML`/`insertAdjacentHTML`) and dynamic script-loader `.src` rows. Decision: `scripts/analyze-tt-violations.mjs` maps all freshness-ranked rows back to local source for visibility, but `scripts/check-active-tt-sinks.mjs` fails only rows whose current source still contains an unresolved HTML-string sink near the reported location. Dynamic loader rows remain in TT burndown evidence and must be handled by policy/soak work, but they are not counted as unresolved active HTML sink debt.

## 2026-07-04 -- S255

**D-S255.1 -- build:check uses a Node runner because the suite is now too long for Windows shell command lines.** The full check chain hit a Windows command-length failure even though the underlying checks were valid. Decision: keep the canonical ordered chain in `package.json` as `build:check:steps`, but make `npm run build:check` call `scripts/run-build-check.mjs`, which tokenizes and runs each `&&` step with `shell:false` and `windowsHide:true`. This preserves direct exit-code truth and makes individual step failures visible without shortening the quality gate.

**D-S255.2 -- Page generators own the complete social/discovery head contract.** S254 found a generator-level `og:image` omission that rendered-page checks caught only after regeneration. Decision: page-owning generators must carry canonical URL, meta description, `og:image`, and `twitter:image` in their templates; `scripts/check-generator-head-contracts.mjs` is now the regression gate. Non-page generators are explicitly excluded rather than silently skipped.

**D-S255.3 -- The local project status wins over a stale control-plane profile until Studio Ops fixes the mapper.** `arc-profile.mjs` still misclassifies this repo as infrastructure/internal/FORGE even though `PROJECT_STATUS.json`, AGENTS.md, and the public site identify it as website/public-live/SPARKED. Decision: execute this repo from local source-of-truth, ship Ark cargo for the Studio Ops root-fix, and never edit the sibling Studio Ops tree to make a local gate green. S255 cargo: `01JSLS5C7NE4AE9D044420DEDA`.
## 2026-07-04 -- S254

**D-S254.1 -- generate-pathways.mjs must preserve og:image meta tags through every regeneration.** After migrating pathways sub-pages, check-og-images reported all 6 as missing og:image. Root cause: buildPage() never included og:image; the template silently dropped the cards on regeneration. Decision: the generator must derive and include the canonical OG card path from the known pattern assets/og/og-pathways-{slug}.png. Any generator owning a page template owns the full head contract including og:image.

**D-S254.2 -- The getPolicy-before-createPolicy guard is mandatory for any TT policy that may load twice.** schema-injector.js createPolicy threw InvalidStateError on a second load, setting _ttScript=null and causing 122 TT violations via the createTextNode fallback. Decision: all TrustedTypes policy creations must use (getPolicy(name) || createPolicy(name, ...)) pattern. The try/catch must still wrap both calls for runtime safety.

**D-S254.3 -- rotate-taskboard --apply must consolidate session-tagged stale runway headings.** The S183 implementation renamed only bare ## Now / ## Next headings; session-tagged ## Now (Session 249 runway) survived indefinitely. Decision: extend --apply with consolidateStaleRunwayHeadings() to rename ## Now|Next|Runway|Human Action Required|Founder Action (Session N ...) headings older than KEEP_RECENT sessions to ## Historical Runway (Session N). Apply at every closeout.

## 2026-07-02 — S249

**D-S249.1 — The doctor must classify compliance/launch debt by OWNER; a repo may never hard-fail its board on a sibling's debt.** Three doctor probes read FAIL (Compliance validation, Compliance velocity 32/36, Launch readiness 2 blockers), but every violation was sibling-owned (MindFrame/Hashmark/SHADOW/ATLAS TRUTH_AUDIT gaps; `veilos` liveUrl/Stripe) — this repo passes clean. The local `validate-compliance.mjs` + `run-doctor.mjs` had drifted behind the canonical **S181 self-vs-sibling exit contract** (0 clean / 1 sibling-WARN / 2 self-FAIL) and hard-failed on any violation, so observability lied (blocking-red for debt this repo must NOT fix — no-sibling-tree-edits). **Decision:** port the S181 owner classification into the local validator and sync the `validate` probe (`pass: code===0||code===1, warn: code===1`); make `compliance-velocity` (`self 100% · portfolio 89%`) and `launch` (`self clear · N sibling blockers`) probes self-aware. `isSelfRepo` compares `path.resolve(localPath)` vs `path.resolve(root)` **case-insensitively on win32** — a robustness improvement over the canonical's exact `===` (the registry stores `VaultSparkStudios.github.io` while cwd is lowercased; exact match mis-classes self as sibling on Windows). Shared back to studio-ops as Ark pattern-share `01JSI460VB…`.

**D-S249.2 — A decided phantom is a distinct suppression class from a resolved-DONE carry, and the suppressor must be DECISION-BACKED.** "Forge Window naming propagation" (genius score 86) kept burning a top-5 slot every session — S185 renamed the public label to "Studio Pulse"; re-rejected S218/S221/S222 — because the generator scans TASK_BOARD/PROJECT_STATUS text, blind to the superseding DECISIONS entry (the old guard was a brittle TASK_BOARD string-match that leaked when the exact phrase was absent). **Decision:** a phantom-carry registry (`context/PHANTOM_CARRIES.json`) drives suppression in `generate-genius-list.mjs`, but a phantom is honored **only while its `supersededBy` decision id is present in DECISIONS.md** — so the suppressor can never silently bury a live item (reverse/remove the decision and the carry automatically returns). Validator `check-phantom-carries.mjs` (self-test 6/6) fails when a `supersededBy` id is missing from DECISIONS.md; folded into `check-proof-surface` (not the cmd.exe-bounded build:check chain). This is the correct home for the recurring Forge-Window rejection — the 4th rejection is recorded here as the phantom's superseding basis. Shared to studio-ops as Ark pattern-share `01JSI43U26…`.

**D-S249.3 — A CTA impression must be a true viewport view, not an engagement-trigger fire.** The play-next widget emitted `play-next:shown` inside `reveal()`, which fires on scroll≥60% / 45s dwell / exit-intent — but the card mounts at the bottom of `<main>`, so a dwell/exit-intent trigger counted an "impression" for a card the visitor never scrolled to. The 37-shown / 0-click denominator was therefore uninterpretable. **Decision:** decouple the engagement-gated mount (visual) from the impression emit; `play-next:shown` + the variant impression fire only when an `IntersectionObserver` reports the card ≥50% in the viewport. The rollup epoch is bumped to 2026-07-02 (the S209 epoch mechanism) so the honest window never mixes with the old dishonest denominator. Do NOT redesign the CTA until ~1 week of honest data accrues — instrument-then-decide, per the audit-live-conversion-paths discipline.

## 2026-07-02 — S248

**D-S248.1 — The homepage hero is editorially curated, not purely auto-ranked.** Every SPARKED project ties at progress 85, so the old live-first/progress-desc auto-rank surfaced whichever items happened to sort first — which put market/betting-adjacent utilities (Velaxis on `velaxis.markets`, PromoGrind on `promogrind.bet`) as the first thing every human and agent sees. **Decision:** the studio deliberately curates its hero via a `HERO_SPOTLIGHT` list in `generate-public-intelligence.mjs` (the catalog source of truth), which stamps an integer `spotlight` rank onto catalog items; `build-hero-portfolio.mjs planPortfolio` orders tiles by that rank and backfills free tiles with the old auto-rank. This is a curation of *order + membership* only — catalog-wide live/forge/total counts stay truthful, and non-spotlit projects still appear everywhere else on the site. A FORGE flagship (MindFrame) may be spotlit; VAULTED projects may never be. Curated hero: Call of Doodie · MindFrame · VEILOS · Vorn · VaultSpark Football GM.

**D-S248.2 — A source-curated showcase needs a coherence gate, because it is hand-editable.** Unlike pure auto-rank, an editorial list can be typo'd, duplicated, point at a VAULTED project, or silently fail to render. **Decision:** `check-hero-spotlight-coherence.mjs` (in `check-proof-surface`/build:check) proves the curation end-to-end against source-of-truth — unique+contiguous ranks, no VAULTED flagship, and the rendered `index.html` hero order actually matching the spotlight. Build the gate for the class in the same pass that introduces the class.

**D-S248.3 — Registry id ≠ page directory is resolved by an explicit alias, not a fallback.** `football-gm`'s page lives at `vaultspark-football-gm/`; the generic-landing fallback masked the mismatch until the tile was featured. **Decision:** carry a small `PAGE_ALIAS` id→page map in the hero builder so a known slug/dir mismatch resolves to the real page (self-test enforces it) rather than degrading to a section landing.

## 2026-07-01 — S246

**D-S246.1 — Homepage fallback copy is public content, not an implementation placeholder.** The external audit correctly flagged dash counters and crawlable loading language because crawlers/no-JavaScript visitors treat it as page copy. **Decision:** homepage fallbacks must be truthful, compact, and non-numeric unless backed by a live source; loaders may hydrate silently but may not be the crawler-visible claim. `scripts/check-home-audit-regressions.mjs` guards this class.

**D-S246.2 — A public label may describe only one destination.** `Vault Pipeline` described both the project page and the roadmap. **Decision:** keep `Vault Pipeline` for `/projects/vault-pipeline/`; label `/roadmap/` as `Studio Roadmap` in the nav/footer generator.

**D-S246.3 — Build scripts must generate every artifact that build-check requires.** `check-proof-surface` expected project schema from `enrich-projects-schema.mjs`, but `npm run build` did not call the generator. **Decision:** if a gate checks generated state, the generator belongs in `npm run build` before the gate; manual one-off patching is not acceptable.
## 2026-07-01 — S245

**D-S245.1 — The local project status wins over a stale cross-repo profiler until the owner repo fixes the mapper.** `arc-profile.mjs` currently reports this repo as infrastructure/internal/FORGE, while `AGENTS.md`, `context/PROJECT_STATUS.json`, and the public site posture identify it as website/public-live/SPARKED. **Decision:** local execution should follow the repo-owned source of truth and ship an Ark cargo to Studio Ops for the profiler root fix; do not edit the sibling Studio Ops tree from this repo.

**D-S245.2 — Homepage proof copy can include diagnostic status-proof detail when it stays source-derived and compact.** The Studio Signal proof line may show oldest-feed age and seed-risk posture from `/api/status-proof.json` because those fields are generated proof metadata, not new claims. **Decision:** keep the homepage line concise, guard the wiring in S98 smoke, and move any deeper recovery details to a future detail surface rather than crowding the first viewport.
## 2026-07-01 — S243

**D-S243.1 — Homepage status claims should show proof provenance, not just counts.** The Studio Signal count is useful only if visitors and agents can see it is backed by fresh public proof. **Decision:** homepage spine copy may derive portfolio counts from the catalog, but must surface status-proof freshness/trust from `/api/status-proof.json` so the first-impression signal has visible provenance.

**D-S243.2 — Raw field verdict ledgers are not public trust feeds.** `api/field-verdicts.json` is a raw grading ledger and can be stale without meaning the public proof is stale. `api/field-win.json` is the distilled, generated, public-facing proof. **Decision:** status-proof includes `field-win`, not raw `field-verdicts`, to avoid depressing public trust with an internal ledger that is not the source of visitor truth.

**D-S243.3 — Lighthouse regression baselines should be recent rolling medians, not all-time lucky highs.** An all-time best synthetic run can create permanent false regressions after normal runner variance. **Decision:** `check-lighthouse-trend` compares current medians against the rolling median of recent prior runs, while still catching sustained drops.

Public-safe decisions retained in this repo:

## 2026-07-01 — S242

**D-S242.1 — Obelisk is phase-0 ready, not active; do not fake the provider flip.** The public site had an Obelisk login/callback scaffold but no Worker verifier route, no available verifier secret/capability, and no Supabase JWT/RLS bridge. `assets/identity.js` correctly kept Obelisk not ready. **Decision:** the website may expose a fail-closed `/api/obelisk-verify` route so the callback is diagnosable, but full `VSIdentity.useProvider('obelisk')` remains gated until a real verifier contract, gateway secret, Supabase bridge RPC/JWT flow, founder enrollment, and soak migration exist.

**D-S242.2 — Empty Studio Pulse graph edges should render honest public nodes, not placeholder nothing.** Founder-confirmed graph edges are currently empty by design; fabricating edges would violate observability honesty. **Decision:** when `projectGraph` has no nodes/edges but the public catalog exists, Studio Pulse may render catalog nodes only and label the absence of founder-confirmed edges explicitly.

**D-S242.3 — Public intelligence hydration needs a static regression gate.** The Oracle failure was a parse-time inline-script error that existing feed/schema gates could not see. **Decision:** intelligence proof surfaces need a gate that parses executable inline scripts and verifies public fallback paths, not only JSON feed shape.
## 2026-06-30 — S241

**D-S241.1 — Remove an inaccurate public proof surface rather than polish it.** The homepage Portfolio Heartbeat was still inaccurate, and its source feed was not authoritative enough for a public first-impression truth claim. **Decision:** the homepage must not render heartbeat cadence until the data can be derived from a self-validating, authoritative source with clear provenance. The generated `/api/heartbeat.json` endpoint can continue for internal/status consumers, but it is no longer a homepage proof surface.

**D-S241.2 — Homepage Studio Signal uses registry counts, not event pulse counts.** The replacement spine card now reads SPARKED/FORGE counts from `/api/public-intelligence.json`, which is catalog-derived and build-checked. **Decision:** public homepage portfolio scale/status signals should prefer catalog/source-of-truth counts over session/event cadence unless cadence is independently verified.

**D-S241.3 — The canonical Studio Discord invite is `https://discord.gg/rKG9GGaSdu`.** All website Discord URLs, source contracts, nav/footer templates, and generated public feeds should use that invite. **Decision:** do not point public Studio community links at old invite codes or Discord user-profile URLs.
## 2026-06-30 — S240

**D-S240.1 — Public repos may read the canonical Studio Ops capability map, but must not mutate sibling secrets state.** S240 found a false `0/0 capabilities ready` signal: env files could merge from Studio Ops, but `loadCapMap()` only checked this repo's absent `secrets/CAPABILITY_MAP.json`. **Decision:** `scripts/lib/secrets.mjs` and `scripts/probe-capability.mjs` may discover and read the sibling Studio Ops `CAPABILITY_MAP.json` so capability readiness/probes reflect real credentials. Write-back is local-only: `probe-capability` stamps `lastProbeAt/lastProbeStatus` only when the map is local, and otherwise records probe telemetry in this repo's ops ledger. This preserves autonomy without cross-repo secrets mutation.

**D-S240.2 — Generic HTML must be buffered before multi-clone cache writes, not only HTMLRewriter nonce output.** S239 fixed the nonce HTMLRewriter deadlock, but S240 verified that plain HTML outside nonce mode could still flow into primary cache + DR cache clone writes as a live stream. **Decision:** any HTML response that may be read by multiple asynchronous consumers (`caches.default.put`, DR cache, response return, logging) is materialised first with `arrayBuffer()` or equivalent. `check-worker-rewriter-safety.mjs` now gates both unsafe transform streams and missing generic `isHtml` buffering.

**D-S240.3 — The genius list must prefer fresh source-of-truth feeds and suppress later-proven historical carries.** The hit list kept promoting items already completed in later sessions because it read old open rows without checking done evidence or artifact truth. It also treated stale `api/public-intelligence.json` CI health as current even when `api/ci-status.json` was fresh and all-green. **Decision:** `generate-genius-list.mjs` uses fresh `api/ci-status.json` for CI posture and suppresses open rows with later `[x]` evidence or live artifact proof. True deferrals stay visible; stale phantoms do not crowd out real work.
## 2026-06-30 — S239

**D-S239.1 — HTMLRewriter must materialise the response body before any subsequent `Response.clone()` call; cloning a streaming HTMLRewriter output twice creates a deadlock.** `rewriter.transform(upstream)` returns a `ReadableStream`-backed `Response`. Calling `finalResponse.clone()` twice (once for `caches.default.put()` in the nonce-window path and once for the Durable-Retry DR cache via `ctx.waitUntil`) creates two simultaneous tee-readers on the same stream. Both readers need the other to drain before they receive bytes — a true deadlock. The symptom is that HTML requests hang indefinitely with no error; the only observable signal is a ≥12s timeout. The root-cause was introduced in S176 (DR cache) and first exposed in S238 when `purge_everything: true` in `pages-deploy.yml` cleared the Worker's `caches.default` after every deploy, forcing every HTML request through the uncached (and therefore deadlocking) path. **Fix:** `const htmlBody = await rewriter.transform(upstream).arrayBuffer()` — materialise the body into an ArrayBuffer once; all subsequent `clone()` calls copy the buffer reference (O(0) cost, no stream tee), safe to call arbitrarily many times. Deployed as Worker commit `c2bbcc7a`. smoke-live confirmed: edge / — HTTP 200 in 93ms (was 12s+ hang). **Rule:** never call `.clone()` more than once on any `Response` whose body is a live `ReadableStream`; always buffer first with `.arrayBuffer()` or `.text()` if multiple readers are needed.

**D-S239.2 — A cache purge that removes a Worker's own cache exposes any latent streaming bug; post-purge liveness is a required deploy gate.** The S238 `pages-deploy.yml` `purge_everything: true` step is correct and necessary (ensures visitors get fresh HTML after a Pages deploy). But it also clears `caches.default` — the Worker's nonce-window and DR-cache entries — so the very next real-visitor HTML request must go through the Worker's full rewrite path uncached. Any streaming bug in that path is only triggered on the cache-miss path. **Decision:** add `smoke-live --edge-only` (5s timeout × 2 retries) to `pages-deploy.yml` after the purge step. It runs a fast edge liveness check that catches the hang class in ≤15s, before any visitor sees it. The check exits non-zero on any 5xx or hang (0 status), keeping CI RED until the Worker is confirmed healthy. This is now the standard pattern for any workflow that includes a full-cache purge.

**D-S239.3 — OG-coverage metrics should persist as a tracked feed, not just a build-log line.** Prior to S239, the OG-coverage gate reported current state (carded/dark/untriaged) as console output only — the triage state was visible but not trackable over time. `scripts/build-og-coverage.mjs` (S238 brainstorm item #1) converts this: it writes `api/og-coverage.json` on every `npm run build`, registered in `SURFACES` with maxDays:2/blockDays:4 (a dead generator blocks the next deploy), gated in `check-proof-surface.mjs`, and includes a 100%-coverage floor warning when `coverageRatio < 1`. Current state: 108/108 public pages carded, 42 intentionally dark, 0 untriaged. **Rule:** any build-time count that is reviewed each session should be persisted as a JSON feed so it is comparable over time and not just ambient log noise.

## 2026-06-30 — S238

**D-S238.1 — A no-og:image WARN is ambient noise; classify every card-less page (dark vs untriaged) so the signal is precise and a new public page surfaces as fresh work.** For 40+ sessions `check-og-images` emitted "54 no-og:image warning" as one number nobody escalated. The fix is not "card everything" — many pages (auth flows, `.ai` agent surfaces, gated investor portal, internal dashboards, error/offline) legitimately have no share card. S238 added `OG_INTENTIONALLY_DARK` (exact paths + patterns, each with a rationale) and `PUBLIC_NO_OG` (genuinely-public pages that get bespoke cards). The gate now reports "N intentionally dark · M untriaged" and ERRORS on any card-less page that is neither carded nor allowlisted. **Rule:** convert any warn-only count that has persisted unactioned into a precise allowlist-vs-untriaged split that errors on the untriaged remainder, with a self-test proving it flips both ways.

**D-S238.2 — Every public trust feed must name how it is regenerated; a stale feed is a dead end without a recovery path.** The trust-feed freshness gate could BLOCK on a dead cron but only said "regenerate api/X.json". S238 added `gen`/`recover`/`wf` to each `SURFACES` entry (single source of truth, shared with the freshness gate) and `check-feed-publisher-manifest.mjs` to gate parity (every feed names a real generator + recovery command + workflow), detect dead recovery paths, and emit the public `api/feed-publishers.json` inventory. A `--recover-stale` mode executes the declared command. **Rule:** observability that can block must also name (and ideally execute) its own recovery; a freshness ceiling without a recovery path just moves the dead end.

**D-S238.3 — `journal` stays warn-only on purpose; not every public-content surface should get a hard blockDays ceiling.** The S237 carry to "generalize blockDays to surfaces that only warn" named status-proof + uptime — both of which already gained hard ceilings in S231. The only remaining warn-only content surface is the founder-voice `journal` (Signal Log). A hard block there would force a hand-written devlog post to unblock *unrelated* deploys — a perverse incentive. `changelog` is auto-drafted so its blockDays:60 is safe; `journal` is not. **Rule:** a hard freshness ceiling is appropriate only where the surface can be regenerated without forcing unrelated human authorship.

## 2026-06-29 — S236

**D-S236.1 — Entity schema dead zones are a CLASS; close the class with a gate, not a page-by-page fix.** S236 found that 10 high-traffic public pages (membership, oracle, nervous-system, pathways, press, community, membership-value, and 4 project sub-pages) had zero entity-schema JSON-LD — only BreadcrumbList at best. These are structured-data dead zones: no rich results, no AI-typed context, no potentialAction for search engines. Rather than patching pages one-by-one forever, `check-schema-coverage.mjs` gates 16 must-have pages with an expected entity-type list; any page that loses its schema is caught on the next `build:check`. **Rule:** after hand-fixing any multi-page structural gap, build a gate against the class so the fix doesn't silently regress.

**D-S236.2 — `@graph` arrays require explicit unwrapping in schema parsers; a top-level `@type` lookup misses all nested entities.** The `games/index.html` page wraps its VideoGame, FAQPage, and BreadcrumbList entities inside a single `{"@context":"...","@graph":[...]}` object — the outer wrapper has no `@type`. A naive `d['@type']` check on each JSON-LD block would find nothing and incorrectly flag the page as a dead zone. Fixed by adding `if (Array.isArray(d['@graph'])) d['@graph'].forEach(node => types.add(node['@type']))` inside the parser. **Rule:** any schema parser, gate, or linter that reads JSON-LD must unwrap both top-level `@type` and `@graph` arrays; real-world pages use both patterns.
## 2026-07-20 -- S288

**D-S288.1 -- A production deployment is stranded only after two consecutive behind receipts; one behind observation is settling.** Candidate-to-production propagation is asynchronous, so a single fresh `behind` sample is insufficient evidence of failure. `summarizeHistory()` owns the consecutive streak and the CI beacon exposes `settling`, `clear`, or `stranded`; no timestamp or synthetic pass is invented.

**D-S288.2 -- Explicitly authorized auth/security/provider migrations are authorization-gated work, not autonomous NOW tasks.** The Genius List may rank ordinary security fixes, but language requiring founder authorization, provider migration, or auth-flow approval is classified separately. This preserves CANON-019 try-first discipline without violating the project's explicit escalation boundary.

**D-S288.3 -- A Cloudflare deploy capability is accepted only when the token can reach the resources the Worker actually binds.** Token verification and Workers Scripts listing are necessary but insufficient for this project because the Worker binds `vaultspark-rum`. The live probe must also read that R2 bucket; HTTP 403 yields `scope-error` and blocks a doomed deploy without relabeling the credential missing.

**D-S288.4 -- The latest completed SIL ledger entry is the canonical session-score source.** `context/PROJECT_STATUS.json`, startup briefing, state vector, and derived IGNIS surfaces must match its session number, total, and ten-category vector. The shared parser and integrity gate reject partial in-progress headings and cross-surface drift.

**D-S288.5 -- Intellectual-property disclosure is a first-class universal route with proprietary-first language.** `/ip/` is a distinct public doorway, not an alias of Terms or provenance documentation. It uses unique metadata and brand voice while retaining CANON-008's all-rights-reserved posture; universal sitemap coverage now includes privacy, terms, contact, and IP.

**D-S288.6 -- A second-order innovation pack is a deterministic artifact, not transient session prose.** `scripts/generate-innovation-pack.mjs` renders the source-backed candidate ledger and supports `--check`; the ops command registry exposes it agent-neutrally. Overlapping primary/secondary outcomes stay explicit instead of inflating the shipped count.

**D-S236.3 — A Product schema with a multi-tier Offer array is more search-actionable than separate ItemList + Product entries.** For the membership page, the most AI- and search-readable shape is a single Product with three nested Offer items (each with @type, name, price, priceCurrency, availability, url). This lets price-comparison engines show all three tiers under one entity and allows `potentialAction: BuyAction` to be added later. ItemList is the right shape for a page whose primary content IS a list (vaultsparked/index.html); CollectionPage with hasPart is the right shape for a pathway index. **Rule:** choose schema @type based on the page's primary editorial purpose, not structural convenience.

**D-S236.4 — The membership value calculator warrants a full UI layer (tier bars + trajectory chart + recommendation chip), not just a checkbox list.** The original calculator was a static checklist with a running total. V2 adds: (1) tier bar fills (animated via CSS, via `.style.width`) showing how each tier's value compares to its price, (2) a 12-month polyline SVG trajectory chart (solid value, dashed cost), (3) a recommendation chip with tier-specific color and a "Find your plan" CTA, and (4) a perk-group organizer that visually separates Free/Sparked/Eternal perks. The RUM event `value-calc:compute` is emitted on every recalculation. **Rule:** a value calculator that can't show the recommendation outcome visually is not doing its job; the chart closes the trust loop for visitors who need to see compounding over time.

## 2026-06-29 — S235

**D-S235.1 — The Oracle answer layer is deterministic, public-source-backed, and zero runtime cost by default.** `scripts/build-oracle-answers.mjs` prebakes answers from public feeds into `oracle/answers/index.json`; the browser uses that committed corpus before falling back to keyword matching. No hidden private context, no live model call, and no per-query cost are required for the public answer surface. **Rule:** Oracle answer improvements should first enrich public source feeds and the deterministic builder; live model expansion is optional, gated, and never required to make the public surface truthful.

**D-S235.2 — New RUM events must be Worker-allowlisted in the same change that emits them.** The membership value calculator initially emitted `value-calc:compute` while the Worker allowlist rejected it, recreating the silent-telemetry-drop class. Fixed by adding the static event to `RUM_UX_EVENTS` and proving `check-rum-allowlist` green. **Rule:** any asset emitting `/v/rum` telemetry must land with the allowlist update and the scanner output in the same session.

**D-S235.3 — INP root-fix remains evidence-gated, not ambition-gated.** `data/inp-breakdown.json` still has zero route samples. A claimed INP root fix before real field samples would be fabricated. **Rule:** do not ship INP optimization as a root-cause fix until `inp-breakdown` identifies the route/target/phase; generic performance work can ship, but must not be described as the root fix.
## 2026-06-28 — S234

**D-S234.1 — A site with 233 sessions of per-surface gates can still ship cross-surface lies; the missing gate compares surfaces to each other.** The full-website pass found four live contradictions (wrong auth-return domain, `sealed:7` after the vocab was retired, gold-vs-blue tier theme, 277-day-stale `393` days-since-launch) that every existing gate missed because each gate validated ONE surface in isolation. `check-content-coherence.mjs` is the structural fix: it diffs shared facts (vocab labels, tier-theme color, vaulted count, launch-age) ACROSS surfaces and blocks build:check on disagreement. **Rule:** when the same fact appears on N surfaces, the gate that matters is the one asserting the N copies agree — not N gates each checking its copy is well-formed. Derive from one source where possible (`api/membership-tiers.json`); gate agreement where you can't.

**D-S234.2 — A dual-audience site (CANON-048) must not hard-block at the edge the HTTP clients its agent audience uses.** The Worker's `BLOCKED_UA_PATTERNS` lumped curl/wget/python-requests/go-http-client in with real attack tooling (sqlmap/nuclei/nmap), so the site that brands itself "built for AI agents" 403'd the most common agent HTTP libraries. Fix: split scanner-tools (always block, every method/path) from generic-HTTP-clients (may READ public content; still blocked on gated paths + non-safe methods). **Rule:** edge UA policy on a dual-audience surface is scoped by intent — block scan/exploit tooling outright, but let generic clients read public content (rate-limited), or the agent-friendliness is marketing-only.

**D-S234.3 — Diff-scoped gate selection cuts the per-session agent-token floor without losing coverage — the full sweep belongs in CI, not the inner loop.** build:check runs ~159 cold node spawns whose interleaved stdout the agent re-reads every closeout. `gate-scope.mjs` maps `git diff --name-only` to only the gate CLASSES covering changed files and runs just those locally; CI keeps the full build:check. **Rule:** local dev-loop gating should be proportional to the diff; exhaustive gating is a CI responsibility. Coverage is preserved because the authoritative full run still gates the push.

## 2026-06-28 — S233

**D-S233.1 — A Worker that reads `raw?.ux` will silently drop any client that sends `raw.event` instead — always check both keys, or the data-loss is invisible.** `inp-telemetry.js` sends `{ event: 'inp:slow_interaction', route, element, ... }` via `navigator.sendBeacon` (bare JSON, no `ux` key), but the Worker's `handleRumIngest` read `raw?.ux ?? undefined` — so every inp:slow_interaction beacon was stored with `ux: null` and the `inpPhase` block never written. The only observable symptom was `0 inp:slow_interaction samples` in the RUM summary, which looked like "no slow interactions" rather than "drop". **Fix:** `const uxRaw = raw?.ux ?? raw?.event` — fallback to `raw.event` for the direct-sendBeacon form. **Rule:** any Worker ingest that accepts multiple client beacon shapes must coalesce all key aliases before dispatching on the value; a `?? undefined` fallback produces a silent-drop class that only surfaces as mysteriously-zero counters.

**D-S233.2 — An absolute floor gate complements the regression gate; the two are not substitutes.** `check-lighthouse-trend` catches score drops (Δ ≥ 0.05 from prior run) but not pages that hover at a consistently bad level (no single-run regression to detect). The homepage has been 0.76–0.78 for ≥4 CI runs with no regression alert — because 0.77 → 0.78 → 0.76 has no ≥0.05 step. `check-lighthouse-floor.mjs` closes this: it analyses median perf across the last N runs and warns (≥0.78 floor miss) or errors (≥0.74 floor miss). **Rule:** run both gates — the regression gate for fresh drops, the floor gate for chronic underperformance. The floor gate is advisory-with-error-exit-1, not warn-only, so it blocks on genuinely bad sustained scores rather than being ignored.

## 2026-06-29 — S237

**D-S237.1 — Empty INP route samples are a hard evidence gate, not a prompt to guess.** `data/inp-breakdown.json` still reports `totalSamples: 0`, so no route/phase/handler root-cause claim is valid. The correct closeout state is an honest deferral with the evidence path preserved, not a speculative passive-listener/layout patch that would make observability lie.

**D-S237.2 — VideoGame metadata can use notional free offers only when the product state supports it.** Free web/playable surfaces use a `0 USD` `Offer`; preview/forge surfaces use `PreOrder`; vaulted surfaces use `Discontinued`. This keeps structured data useful without inventing availability, ratings, or paid products.

**D-S237.3 — Duplicate social-card fixes belong in the generator, with explicit overrides for secondary pages.** Pages that are intentionally secondary or skipped by default may still need unique cards when an advisory gate proves they collide on social surfaces. The override list is the honest contract: it preserves hand-made primary art and names the pages where generated text cards are the right fallback.


## 2026-07-02 — S247

**D-S247.1 — A long-lived "data-blocked" deferral must be re-verified against the RAW upstream store, not the derived artifact.** The INP carry was honestly deferred for ~14 sessions on `data/inp-breakdown.json` `totalSamples: 0` — but the rollup read `data/rum-raw.ndjson`, a path no pipeline step ever writes, and silently fell back to phase-less aggregates while 217 real `inp:slow_interaction` rows sat in `.cache/rum-raw/dt=*`. The artifact's own `source` field was the tell. Rule: when a deferral survives more than ~3 sessions, grep the raw store for the event name; make the rollup's `--check` FAIL when it built from a fallback source while the real store exists; wire every rollup into the chain that refreshes its input (`rum:pull`).

**D-S247.2 — Event Timing telemetry must filter `entry.interactionId` or it measures hover jank, not INP.** ~90% of the field phase samples were `pointerenter`/`mouseover` paints (real rendering cost, but INP counts only actual interactions). The client now drops entries without an `interactionId`. Consequence recorded honestly: existing phase data is polluted — the actual INP performance fix waits for ~7 days of clean post-filter samples rather than being fitted to noise.

**D-S247.3 — A page's hardcoded status badge must agree with the registry-derived nav that links to it; the class is now gated, not hand-maintained.** Four project pages said "⚒️ Forge" while the nav promoted them under "🔥 Sparked" (registry truth: sparked, live URLs verified). Fixed at the pages, and `check-project-status-coherence.mjs` (blocking, in `check-proof-surface`) parses the NAV arrays and fails on any future disagreement — the S197 walk-the-journey lesson applied to the status axis with a gate this time.

**D-S247.4 — Atlas stays off the public site until its canonical registry entry carries a real description.** Listing a project whose canonical description is empty would ship a hollow tile below the CANON-011 quality bar. Enrichment requested via Ark (`01JSGDF4CF77DF6878E0E7D88A`); the local sync is an honest deferral recorded as a WIN, not a gap.


## 2026-07-02 — S250

**D-S250.1 — A closeout's "build:check green" is not "CI green"; the deploy gate is the REMOTE run, and it must be confirmed, not assumed.** The `E2E Test Suite` workflow had been RED across the last 4 runs (since `bce31505`) and survived two prior closeouts that reported local `build:check` green. Root cause: S249 authored `assets/covers/veilos.png` + `vorn.png` but never committed the regenerated `data/lqip-map.json`; local `build:check` passed only because `npm run build` transiently regenerated the map in-tree (never staged), while CI ran `build-lqip-map --check` against the committed-stale map and failed. This is the S231 local-green/CI-red trap recurring through generator-output that lands in the working tree but not in the commit. Rule: (a) when a generator (`build-lqip-map`, hero portfolio, feeds) writes a tracked artifact, the closeout MUST `git add` the regenerated output, not just re-run the generator; (b) the "[VERIFY] post-push CI confirmation" step reads the actual CI beacon / `gh run` status, never the local wrapper exit — a green wrapper over a red remote is the exact lie CANON-031/CANON-036 forbid.

**D-S250.2 — Read the failing CI *job*, not the workflow name, and capture the real exit code (PIPESTATUS), not the pipeline's.** The RED "E2E Test Suite" run had a *passing* `e2e` job and a *failing* `compliance` job; the actual failure line (`build-lqip-map --check: coverage drift`) was the last output before `##[error] exit code 1`, buried under hundreds of passing self-test lines. Diagnosing required `gh run view --json jobs` to find the failing job and `--log-failed | tail` to the real cause — and locally, running `node script --check` without a `| tail` pipe (which masks the real exit as the tail's 0). The arc's verification discipline (exit codes lie under indirection) is what surfaced it.

## 2026-07-02 — S251

**D-S251.1 — "Resolved-DONE" carries rot inside archived TASK_BOARD history the same way "decided-phantom" carries do, and `check-stale-open-tasks` doesn't see them.** That gate only scans the last 3 sessions' active runway, so an unchecked `[ ]` line sitting in an S80/S94/S185 historical section — describing work a *later* session actually shipped without ever flipping the box — survives indefinitely and keeps re-scoring high in `genius-list --refresh` (deterministic text-scan, no live-code verification). A full-board agent sweep (S251) found and closed 9 such items with direct code evidence: PROOF-LINE-TELEMETRY, IGNIS-HINT-CONVERSION-TRACKING, CLOSEOUT-BUILD-ORDER-MODULE, SearchAction `/search/`, CSP nonce migration, rate-limit+CSRF (partial — investor-doc signed-URL sub-clause still open), "Ask IGNIS" concierge (4 duplicate listings), unified cross-portal shell (3 duplicate listings), and ETERNAL tier vocabulary. Rule: when a genius-list item's premise looks stale or suspiciously large for its score, grep the codebase for the described artifact/function BEFORE spending session time rebuilding it — the S196 "disprove-deferral" habit applies equally to items that read as open due to bookkeeping lag, not just items marked deferred. No new automated gate was built for this class (fuzzy-matching "is this already done" risks false positives); a periodic full-board sweep is the interim mitigation.

**D-S251.2 — A precise (non-fuzzy) exact-title-duplicate gate is a safe subset of D-S251.1's rejected "already-done" heuristic — built and shipped it.** D-S251.1 explicitly declined a fuzzy "is this already done" auto-gate over false-positive risk. But an EXACT match of the bolded item title, where one copy is `[x]` and another is `[ ]`, is a much narrower and safer signal — no semantic guessing, just literal string identity. Shipped `scripts/check-taskboard-duplicate-titles.mjs` (self-test 6/6): exact-title grouping, always advisory (exit 0, never blocks), wired into `check-proof-surface.mjs` as a warn-only report (self-test guarded in the blocking `STEPS` chain, live report outside it — same pattern as `check-registry-freshness`/`check-lighthouse-trend`). First live run immediately found 5 MORE genuine phantom duplicates this session (ORIGIN-MIGRATION-FIELD-VERDICT ×3, STATUS-PROOF-INDEX, TASK_BOARD-size-strategy, RUM-DEAD-ALLOWLIST-SWEEP, EDGE-GATE-PRIVATE-PORTALS) — all closed with evidence. Precision over recall: the gate will miss near-duplicates with different phrasing (by design, per its own self-test) — it is a lead-generator for a human/agent to verify, never a verdict.

**D-S251.3 — Layering a game's abstract branded title-card as a blurred hero backdrop is not a "screenshot"; reverted rather than shipped.** Investigating FLAGSHIP-PRODUCT-STORYTELLING's "screenshot" sub-item, `assets/covers/{doodie,footballgm}.png` turned out to be generated branded title cards (baked-in game name + status badge + genre label + gradient/grid texture) — the same abstraction language already live in the page's own CSS gradient hero theme, not gameplay art. A CSS `image-set()` layer was built, verified applying correctly via Playwright (`CSS.supports` + computed `background-image` both confirmed), screenshotted, and judged: blurred, it reads as a redundant color wash carrying illegible duplicate text, adding no real information. Reverted before commit. A genuine screenshot needs founder-provided gameplay captures or a real image-generation pipeline — neither exists; the sub-item stays an honest, evidenced deferral (TASK_BOARD), not a cosmetic-but-hollow "done".

## 2026-07-04 -- S256

**D-S256.1 -- Tracked CTA impressions must be viewport impressions across families, not only play-next.** S249 fixed play-next, but S256 verified proof-line still emitted `proof-line:shown` immediately after render. Decision: any tracked CTA family used as a click-through denominator must emit `*:shown` only from a viewport observer (>=50% visibility) or an explicitly observer-gated helper. `scripts/check-cta-impression-contracts.mjs` now guards play-next and proof-line together.

**D-S256.2 -- Build-check timing is a source-of-truth artifact, not console trivia.** The suite is now long enough that slow/flaky gates must be trendable. Decision: `scripts/run-build-check.mjs` writes public-safe per-step status/duration diagnostics after each run, excluding command output, so slow gates and failures are inspectable from `api/build-check-diagnostics.json` and `docs/BUILD_CHECK_DIAGNOSTICS.md`.

## 2026-07-04 — S257

**D-S257.1 — CTA families need a declarative contract registry once more than one denominator is guarded.** S256 generalized viewport-impression checks across play-next and proof-line, but the checker still carried bespoke family arrays. `scripts/lib/cta-contract-registry.mjs` is now the source for family id, source file, shown/click events, rollup family, clean epoch, and gated-helper expectations. Rule: future tracked CTA families join the registry first, then the gate proves the source and rollup contract.

**D-S257.2 — Proof-surface health is a committed diagnostic artifact, not a console scrape.** `check-proof-surface.mjs` is the slowest build-check block and wraps many substeps. It now writes public-safe substep status/duration artifacts to `api/proof-surface-diagnostics.json` and `docs/PROOF_SURFACE_DIAGNOSTICS.md`. The contract is status/timing only; command output stays out of public artifacts.

**D-S257.3 — A nonzero TT window can still contain a current local root-fix.** The 2026-07-04 Trusted Types reprobe remains AMBER, so enforcement must not flip. But the July 3 `/leaderboards/` fallback/skeleton `innerHTML` sink was fresh and local, so it was fixed with DOM row helpers at `leaderboards/index.html` and propagated through `build-leaderboard-subpages.mjs`. Rule: burn down fresh local sinks while recording enforcement as honestly gated until post-fix soak proof is near-zero plus founder-device verification.
## D-S258.1 — CTA registry owns rollup family metadata

- **Date:** 2026-07-04
- **Decision:** Tracked CTA funnel family definitions now live in `scripts/lib/cta-contract-registry.mjs`, including rollup `parts`, `rate`, `label`, and optional `epoch`. `scripts/rollup-rum-ux.mjs` derives tracked CTA families from the registry; checker scripts must accept registry-backed rollup ownership instead of requiring duplicated literal family entries.
- **Reason:** CTA denominator honesty should have one source of truth. Duplicating family/epoch config in the rollup and checker makes future conversion surfaces drift-prone and can break gates when a legitimate registry refactor removes hardcoded literals.
- **Scope:** Website repo only. Non-CTA UX families (`oracle-chip`, `ignis-hint`, `oracle-answer`, terminal conversions) remain local to `rollup-rum-ux.mjs` until they get equivalent contract registries.

## D-S259.1 — Obelisk Passport bridge is real integration; full provider truth stays gated until RP/session contract exists

- **Date:** 2026-07-05
- **Decision:** The website can honestly ship a Phase 1 Obelisk Passport bridge now: browser identity state is normalized through `assets/identity.js`, callbacks persist verified `/api/obelisk-verify` payloads, and the Worker verifier remains fail-closed. It must not claim a full Obelisk data-plane or default-provider migration until `obelisk.identity.verify` has relying-party config (`OBELISK_RP_ID`, `OBELISK_RP_NAME`, `OBELISK_RP_ORIGIN`) and a Supabase JWT/RLS bridge is implemented and soaked.
- **Reason:** CANON-031 observability honesty applies to auth posture. A visible Passport bridge is valuable and testable, but replacing Supabase/session truth without the RP contract would be a fabricated readiness claim.

## D-S259.2 — TT enforcement decisions rank by current activity before 30-day volume

- **Date:** 2026-07-05
- **Decision:** Trusted Types burndown now reports freshness buckets and a freshness-ranked table before the volume-ranked table. Active/warm clusters are the remediation priority; stale 30-day volume remains useful context but cannot dominate enforcement readiness.
- **Reason:** The old burndown repeatedly let pre-deploy or already-fixed high-volume clusters outrank currently active sinks. Enforcement gates need current-risk ordering first, long-window cleanup second.

## D-S259.3 — Fingerprinted shell hashes must normalize text before hashing

- **Date:** 2026-07-06
- **Decision:** `scripts/build-shell-assets.mjs` hashes and writes text shell assets after LF normalization instead of hashing raw working-tree bytes.
- **Reason:** Git checkout line endings differ between local Windows and GitHub Ubuntu. Raw-byte hashes made CI regenerate a different shell stylesheet and rewrite every page even when source semantics were identical. Fingerprinted public assets must be content-semantic and platform-stable.
- **Scope:** Website shell assets only; binary assets still hash raw bytes.

## D-S260.1 — Active Trusted Types burn-down must pair DOM fixes with a regression guard

- **Date:** 2026-07-06
- **Decision:** Freshness-ranked local Trusted Types sinks are not complete when the visible renderer is merely changed; the same session must add a narrow regression gate for the active sink class. `scripts/check-active-tt-sinks.mjs` now guards the S260 local rows and is wired into `npm run build:check` after the Trusted Types analyzer.
- **Reason:** The burndown report can contain stale live evidence next to still-active local code. S260 verified each premise against source, rejected the already-fixed `home-dynamic-hero` row, fixed the still-active hero ticker/Gridiron/leaderboard rows, and made the current-risk set self-checking so future sessions do not re-open the same class from memory.
- **Scope:** Website repo only. Cross-repo Trusted Types rows still move by Ark/owning repo, not sibling-tree edits.
**D-S261.2 -- Lighthouse trend warnings are advisory; only error-level trend drops or floor failures hard-fail CI.** The S261 recovery push proved the old `--check` behavior was too brittle: a single `/leaderboards/` performance warning at `0.89` vs rolling baseline `0.94` failed CI even though the absolute floor gate stayed green and local reruns showed no sustained regression. Decision: `check-lighthouse-trend.mjs --check` still prints warning-level deltas (`>=0.05`) but exits nonzero only for error-level deltas (`>=0.10`). Sustained low performance remains guarded by `check-lighthouse-floor.mjs`.

## 2026-07-06 — S263

**D-S263.1 — Latest-session completion is artifact-verified, not inferred from partial write-back.** Recovery proved a session can update handoff/work log while still missing the closeout cache/brief. Decision: `scripts/check-closeout-boundary.mjs` verifies latest-session handoff, work log, closeout brief, and closeout cache coherence and is wired into `build:check`. A future interrupted closeout must surface as an incomplete boundary before the session is called green.

**D-S263.2 — Data-gated conversion/performance work should become readiness artifacts, not recurring open work.** play-next redesign remains unready at `0/20` true-viewport post-2026-07-02 impressions, and Football GM INP remains pending until post-mitigation field soak exists. Decision: readiness artifacts (`.cache/cta-readiness.json`, `api/inp-soak-verdicts.json`, `api/tt-readiness.json`) are the source of truth for whether those items can move from waiting to implementation.

## 2026-07-07 — S264 — Genius list actionability gates are source-of-truth, not a hiding place

**Decision:** The Unified Genius List must rank only work that can be implemented from local source truth in the current session. Items that require founder/content decisions, source-of-truth thresholds, field-soak windows, credentials/provider dashboards, real devices, inbox receipt, logged-in account state, Stripe/payment/portal traversal, sibling repos, or founder-device flips stay visible in DEFERRED / GATED but are excluded from actionable rankings.

**Reason:** Ranking gated items as local work creates phantom blockers and encourages fabricated verification. An empty actionable list with a clear gated ledger is a valid win condition.

**Verification:** `node scripts/smoke-startup-scripts.mjs` now covers genius-list gate integrity; `docs/GENIUS_LIST.md` currently reports `0/100` opportunity pressure with only deferred/gated work remaining.

## 2026-07-07 — S266

**D-S266.1 — A browser asset orphan is no longer an advisory if it is shipped to a public feature mount.** S266 found `/membership-value/` still declaring the calculator mount while the runtime script was absent, leaving a blank interactive card even though `build:check` passed. Decision: mount-point runtimes get explicit required-script contracts, and `check-orphan-assets` now runs `--strict` for browser-shipped `assets/*.js` because the asset-orphan baseline is zero. Script/tool orphans stay advisory because many are manual or cross-repo invoked.

**Reason:** A public page can be structurally reachable and still fail if a feature runtime silently falls out of the page. The correct fix is not deleting feature code or refreshing a report; it is restoring the runtime and making the missing-runtime class self-checking.

## 2026-07-08 — S268

**D-S268.1 — Worker deploy token documentation must derive from bound resources, not generic Wrangler permissions.** Production `cloudflare/wrangler.toml` binds `RUM_BUCKET` to the `vaultspark-rum` R2 bucket, so a deploy token documented as only Workers Scripts: Edit + Zone: Read is incomplete. `scripts/check-worker-deploy-token-scope.mjs` now fails when a production R2 binding exists but the Worker deploy workflow omits R2 Bucket Read/Edit scope language. This does not repair the provider token itself; it prevents the repo from carrying an ambiguous deploy-red cause.

**D-S268.2 — Mobile parity attestation is a source-of-truth artifact.** Passing mobile contracts is not enough for portfolio canon scanners unless the repo records the result. Public-web repos attest CANON-041 with `context/MOBILE_PARITY.md` or `PROJECT_STATUS.mobileParity=true`; this repo now does both after local contract verification.

## 2026-07-08 -- S270

**D-S270.1 -- Lighthouse release gates are route-tiered, and the global floor is the minimum safety net, not the prestige target.** S269's global `0.85` Performance error floor made the Lighthouse workflow red for routes whose current committed evidence does not meet that score, especially `/` around `0.76` and `/ranks/` around `0.82`. Decision: `.lighthouserc.json` enforces the evidence-backed global minimum, while `config/lighthouse-route-tiers.json` and `scripts/check-lighthouse-route-tiers.mjs` enforce explicit route-class floors in CI and build-check. Do not claim homepage `0.85` until a trace-backed performance pass proves it; the route-tier gate is honesty, not a cosmetic downgrade.

**D-S270.2 -- CI status must separate known terminal blockers from in-progress or unexpected failures.** The Worker deploy red is a known provider token-scope issue (`CF_WORKER_API_TOKEN` missing R2 Bucket Read/Edit for `vaultspark-rum`), while Lighthouse red is a separate browser/release gate. Decision: `api/ci-status.json` now carries `terminalState`, `browserGatesGreen`, and `knownTerminalBlockers`; the beacon workflow delegates to `scripts/build-ci-status-beacon.mjs` so local self-tests cover the state machine.

## 2026-07-08 -- S271

**D-S271.1 -- Browser-gate green is a distinct CI state from all-green when the only red is a known provider token-scope blocker.** The website's browser/release gates are green on `be052deb241a6c37484971499aa524fd5ecaa7fb`, while Deploy Cloudflare Worker remains known-blocked on `CF_WORKER_API_TOKEN` R2 Bucket Read/Edit for `vaultspark-rum`. Decision: `api/ci-status.json` may report `browserGatesGreen:true` and `terminalState:"known_blocked"` at the same time. Public surfaces and the Genius List must not treat that as active browser CI red, and they must not claim Worker deploy is fixed.

**D-S271.2 -- Stricter Lighthouse targets stay evidence-gated until a trace-backed pass proves them.** The homepage 0.85 target is a future performance objective, not a verification task. Decision: wording such as "do not claim until a focused trace-backed performance pass proves it" is an evidence gate; `generate-genius-list.mjs` must keep that work in DEFERRED/GATED rather than ranking it as NOW. Local LCP can be fast while broader Lighthouse/CLS work remains open.

## 2026-07-10 -- S274

**D-S274.1 -- Package-trust BLOCK is honored even when the only reason is missing precedent; precedent requests go through Ark.** `@fontsource/fraunces` scored BLOCK 52/100 solely on "no existing Studio precedent" while every other trust signal was clean (OFL-1.1, official fontsource/font-files repo, 10-month release age, stable maintainers). Decision: the elite-typography item is deferred rather than the gate bypassed, and precedent review was requested from studio-ops via Ark repo-question `01JT54BDHQ1A69BFA307974C0D` (CANON-018/023). No font files or dependencies were installed this session.

**D-S274.2 -- Both mobile nav cohorts are first-class parity surfaces.** The nav-sheet 50% canary means half of mobile visitors never see the classic drawer, so any drawer-only fix (theme pills, CTA reachability) silently misses half the audience. Decision: CANON-041/047 mobile parity work must ship to the drawer AND the sheet; `window.VSTheme` is the shared theme API so alternate nav surfaces never duplicate theme state.

**D-S274.3 -- Overlay chrome must yield to the open nav drawer.** The cookie banner (z 9000) sat above the drawer (z 200) and hid its bottom CTAs on 390x844. Decision: while the drawer is open, lower-priority fixed chrome slides out (`body:has(.nav-center.open) .vs-cookie-banner`); raising drawer z-index above every banner was rejected because the genome strip and command palette legitimately sit higher.

## 2026-07-12 -- S275

**D-S275.1 -- Content routes load the stylesheet render-blocking; the async media=print swap is homepage-only.** The rAF swap painted every route before the full stylesheet applied; only the homepage has a fold-complete critical-CSS contract, so content routes painted semi-unstyled and snapped (field CLS p75 0.24-0.64 across /games/ /oracle/ /changelog/ /studio-pulse/). Decision: `build-shell-assets.mjs` ASYNC_CSS_PAGES allowlist = index.html; every other route gets a plain blocking link (SW-precached on repeat visits). Rationale: zero CLS by construction beats a marginal first-paint gain on routes whose LCP is text.

**D-S275.2 -- Intelligence widgets must not construct their surface after paint; static mounts with reserved height are the pattern.** ignis-answer-engine created and inserted its whole section post-paint (oracle CLS 0.86, bisect-proven). Decision: pages own a static mount (`#ask-ignis`, reserved height, class-based release); runtime stylesheets for such widgets ship in static CSS. The remaining post-paint injectors (nav-sheet, vault-genome-strip, intent-flight-director, rate-page, vault-kinesis on /studio-pulse/) are a recorded carry with a reproducible bisect (`scripts/probe-cls-bisect.mjs`).

**D-S275.3 -- atlas + scriptorium teaser pages shipped agent-authored, flagged for founder voice review.** The registry flipped both to public audiences, which fails the portfolio-coherence gate without on-site pages. Teasers follow the sanitized one-liner pattern (D-S208.5): ATLAS "The foundation that carries the ecosystem." (from CANON-050 language), Scriptorium "Where the studio's worlds get written." Founder may recurate the copy; pages are generator-owned (`build-forge-project-pages.mjs`).

**D-S275.4 -- Ledger rotation is structural (rotate-ledger.mjs), and archived decisions remain first-class.** Five append-only ledgers (2.88MB) rotated into verbatim quarter shards under context/archive/; caps gated in build:check. Consequence handled: any gate that resolves decision IDs must search the archive shards too (check-phantom-carries fixed the same session). Append-only contract preserved -- nothing was edited or deleted, only moved verbatim.

**D-S275.5 -- The out-of-band worker deploy class is a studio-level incident, not website debt.** Something outside this repo deployed a stale build onto this repo's production worker route on 2026-07-03 (void/syntha workers modified the same hour). The website cannot guard other repos' wrangler usage; the guard request (deploy-target allowlist) went to studio-ops as incident cargo `01JTC1CP1E02EB47D7444FBB7A`. Locally, probe-uptime's worker-ingest signal makes any recurrence visible within one probe cycle instead of 9 days.

## 2026-07-16 -- S283 (shipped by a cut-off codex arc, verified + landed by S283-recovery)

**D-S283.1 -- Public AI-discovery manifests derive from the committed public source, never gitignored IGNIS state.** `build-agents-json.mjs` and `build-llms-full-shards.mjs` now read `api/ecosystem-state.json` (committed, `publicSafe:true`) and fail closed if it is absent or malformed. Rationale: the prior sources under `ignis/output/` are gitignored, so local checks could disagree with the committed site while CI silently skipped generation -- the dual-audience discovery layer must reproduce the same safe answer in every environment. A startup-smoke contract now forbids the public generators from referencing `ignis/output`.

**D-S283.2 -- A ranking system may suppress a task only on carry METADATA, never on prose that happens to contain the word.** The Genius List was deleting the highest-value verified task because one explanatory sentence contained "carry". `scripts/lib/genius-task-classifier.mjs` recognises only consolidated-carry tags/titles; `generate-genius-list.mjs` consumes it; four behavioral cases (true meta-carry vs ordinary prose, both directions) guard it. A queue that deletes its best item on a word coincidence cannot be trusted, and founder-visible priority truth is load-bearing.

**D-S283.3 -- Oracle reads deployed public feeds once; production requests to gitignored paths are structurally forbidden.** Both Oracle runtimes now default to `/api/*` behind a shared promise cache (repeated consumers coalesce), with an explicit localhost-only private-preview override instead of probing dead `/ignis/output/*` URLs that 404 in production. A browser/static contract fails if any production Oracle request targets `/ignis/output`. This de-noises the public surface (removes the ~57-failed-request stampede from D-S281.11) WITHOUT expanding public data exposure -- the richer-IGNIS exposure decision remains an explicit founder call.

**D-S283.4 -- A skip-CI publisher must prove the exact artifacts it stages before it commits.** The half-hour uptime publisher could land generated data that no workflow validates (because the commit carries `[skip ci]`), arming a failure for the next ordinary push. It now runs focused `--check` contracts before `git add`; any red aborts before commit, and a workflow structural test fails if generation can reach a skip-CI commit without validation. A publication boundary owns validating its own output.

**D-S283.5 -- Two gates measuring the same signal must share one volatility policy, or they will disagree on byte-identical code.** `check-lighthouse-trend.mjs` and `check-lighthouse-route-tiers.mjs` both judge homepage performance; one corroborated known lab noise while the other hard-failed a single runner dip. Extracted `scripts/lib/lighthouse-volatility-policy.mjs` (pure); both gates consume it. Floor 0.76 and the >=2-of-5 slow-bleed tripwire are preserved -- no threshold lowered (CANON-031). Resolves the S282 #1 carry using the re-run evidence S282 already gathered (same commit, byte-for-byte, scored 0.67-fail then success), so the fix shipped with proof rather than needing re-reproduction.

**D-S283.6 -- Closeout must not claim a cross-repo mirror it does not perform; the local event ledger is the project's own CI-readable truth.** `closeout-autopilot` was `copyFileSync`-ing `portfolio/events.ndjson` onto itself while logging that it mirrored the sibling studio-ops ledger -- a false message that manufactured a bogus 893-vs-1278 "divergence" blocker and invited a CANON-018-violating cross-repo write "fix" that would have been strictly worse. Removed both false self-mirrors, made closeout validate/count the local NDJSON, kept `appendEvent` local, and added a dry-run/self-test asserting no write resolves outside `PROJECT_ROOT` (`check-closeout-boundary.mjs`). The 893-record local ledger is authoritative for this repo; the sibling is a separate portfolio-wide file, not a mirror target.

**D-S283.7 (recovery) -- A cut-off session's shipped-but-uncommitted work is verified against reality before the boundary is trusted, and one regression its own gates would have caught is fixed at recovery.** S283 (codex) completed /audit + /implement but died during /closeout with 0 commits. Recovery did not trust the audit's "shipped" log: it ran the integrity sweep (0 bad JSON/ndjson), fixed a networkidle regression S283 left in `tests/oracle-extra.spec.js` (the `check-e2e-networkidle` gate would have blocked the very closeout that never ran), rebuilt, and confirmed `build:check` 213/213 EXIT 0 before committing the recovered work as its own labelled boundary. Restraint preserved: no reset-hard, no force-push; the local ledger and generated feeds were regenerated via `npm run build`, not hand-edited.

**D-S283.8 (recovery, second-order) -- A ranked priority surface resolves GENERIC post-push CI-verify carries on committed evidence, not a hand-maintained phrasing allowlist.** The genius-list generator's `isResolvedCarryForward` had grown a ~30-entry table pairing each resolved verify's open-phrasing with its done-phrasing; any post-push "confirm the push went green" carry that nobody added a bespoke pair for re-ranked NOW every session until hand-flipped (three such carries were sitting at 98/96/90 this session). Extracted `scripts/lib/verify-carry-evidence.mjs`: a generic post-push verify (names push + green + CI, no specific deliverable) is resolved iff the committed CI beacon `api/ci-status.json` proves the browser gates green on a verified head. Fails safe -- absent/red/unknown beacon leaves the verify ranked NOW -- and never auto-resolves a carry that names independently-gated work (annual checkout, worker deploy, real-device render, etc.). This is the VERIFY analog of D-S281.1 (evidence-based done-detection) and the same evidence-over-phrasing discipline as D-S283.2; beacon freshness stays owned by check-ci-status-freshness so the two gates compose. Self-test: 6 behavioral cases, both directions, in smoke-startup-scripts. Confirmed live: the stale S282 "Confirm the S282 push went green" carry dropped from the NOW list while the genuine synthetic post-push confirmation (ciGreen conservatively false pending the current tip's beacon refresh) correctly persisted.

## 2026-07-16 -- S284 (recovery of S283 → changelog/rebrand feature session)

**D-S284.1 -- VaultSpark Football GM is rebranded to "Franchise Architect"; name and slug were decoupled for safety.** Founder creative direction: "Franchise Architect" is the clean umbrella brand (sport becomes a facet; playfranchisearchitect.com envisioned as the multi-sport home). Executed in two phases so the URL change could never 404 an inbound link: Phase 1 changed the DISPLAY name only (323 instances across ~150 source files, slug/CSS untouched, historical/tombstone records preserved) — fully deployable, zero link-rot. Phase 2 changed the slug (`/vaultspark-football-gm/` → `/franchise-architect/`, dirs git-mv'd, 421 refs updated) with 301 redirects. Redirect mechanism: a Cloudflare Pages native `_redirects` file (production origin is CF Pages since S175) — deploys with every push, NO Worker needed, sidestepping the founder-gated Worker token; the Worker's `createOriginFetch` uses `redirect:follow`, so no 404 is possible even before the Worker's canonical Layer-0c 301s deploy. A rebrand tombstone (old name → Franchise Architect successor) mirrors the CryptoMatrix→Velaxis precedent. Post-deploy verify: `curl -sI https://vaultsparkstudios.com/games/vaultspark-football-gm/` should 301.

**D-S284.2 -- The changelog "Time Machine" scrubber is fixed and augmented, not removed.** Its Older/Newer buttons were inverted relative to the slider and it offered no search. Kept the gate-verified scrubber (corrected: left=Newer, right=Older, Newest→Oldest scale) and added a real search box (highlight + empty state + count), year filter chips, stable per-entry anchors, per-entry permalinks, deep-link (`#cl-latest`/`#<id>` → scroll+flash), and URL-synced search/filter state (`?q=&year=`, shareable + back-button). The homepage hero ticker now deep-links to `/changelog/#cl-latest`.

**D-S284.3 -- The public homepage hero must never admit raw commit text.** The IGNIS "reading the studio" ticker wrapped raw commit subjects in a template, leaking conventional-commit prefixes, dev parentheticals, arrows, and mangled casing onto the brand's front door. Hardened `build-ignis-conduit.mjs` with a sanitizer (strip prefixes/asides/`+`-clauses/arrows, drop leading imperatives, preserve proper-noun casing) and a DEVISH reject guard that DROPS any still-dev-facing subject (paths, S###/D-S/CANON refs, ratios, CI/ops jargon) rather than narrate it — ticker falls silently to the next candidate. --self-test wired into build:check.

**D-S284.4 -- The public changelog is data-driven with a founder-approved draft→publish flow; it never admits raw commit text.** `CONSUMER_CHANGELOG` (hardcoded array, newest entry frozen at 2026-05-14) is extracted to `data/consumer-changelog.json` (source of truth; array kept as seed). `generate-public-intelligence.mjs` merges seed+file, deduped, newest-first — new entries need no code change. `scripts/publish-changelog-draft.mjs` promotes an approved draft (`approved: true`, founder gate — nothing auto-approves) into the feed through the SAME public-safe validator as the ticker guard, so the changelog can stay current without leaking dev voice. First current entry (2026-07-16) published through the flow.
