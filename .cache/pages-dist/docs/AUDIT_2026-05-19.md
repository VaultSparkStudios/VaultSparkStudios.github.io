<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-05-19 -->
<!-- project: vaultsparkstudios-website · type: platform (Security 2x · Speed 2x · UX 1.5x · Feedback 1.5x) -->

# Project Audit — VaultSpark Studios Website (Session 137)

> One ranked improvement plan covering 9 axes. Top items first.
> Project type: **platform** (Security 2x · Speed 2x · UX 1.5x · Feedback 1.5x).
> Supersedes the open S136 test/perf/security carry list and incorporates the same-day MCP recovery.
> Run `/implement` to execute in optimal-efficiency order.

## Top-line

- Total items: **6** (all bounded for this session)
- Combined Priority: **170.6**
- Top 3: `oracle-public-language-contract` · `all-page-crawl-build-gate` · `forge-forecast-unit-contract`
- **Strategic frame:** S136 made the site faster and more public-facing, then the same-day verification pass added a full-route crawler and fixed the Codex MCP config split (`CODEX_HOME` real vs sandbox). The remaining risk is not lack of ambition; it is **new capabilities without permanent contracts**. This audit converts those discoveries into gates so the website cannot quietly drift back to broken routes, parser-blocking script creep, or public Oracle labels leaking dev vocabulary.

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | FIRE | UX · Security · Process | 30m | 9 | 7 | 38.6 | **oracle-public-language-contract** — `/oracle/` now uses public vocabulary, but one inline hover handler still emits "commits / active repos / IGNIS" while `assets/oracle-extra.js` emits "signals / worlds / cognition". A test also expects the old dev label. **Recipe:** update inline hover copy, update the Playwright assertion, and treat public-facing labels as a contract. |
| 2 | FIRE | Speed · Process | 20m | 9 | 6 | 34.3 | **all-page-crawl-build-gate** — `scripts/crawl-all-pages.mjs` found 144 HTML routes, 0 status failures, and 0 script-blocking budget failures, but it is only a manual script. **Recipe:** wire `npm run verify:all-pages` into `build:check` so every closeout catches route rot and parser-blocking local script creep. |
| 3 | FIRE | Innovation · Process | 45m | 8 | 9 | 32.0 | **forge-forecast-unit-contract** — Forge Forecast is the freshest proprietary module, but `computeForecasts()` has no dedicated deterministic tests. **Recipe:** extend `tests/oracle-insights.spec.js` with ship-soon, climbing, awakening, and vaulted/red exclusion cases. |
| 4 | STRONG | Security · Speed | 30m | 7 | 6 | 24.0 | **mcp-codex-home-runbook** — Codex MCP appeared broken because bare subprocesses used `C:/Users/CodexSandboxOffline\.codex` while real MCP config lives in `<user-home>\.codex`. **Recipe:** add the exact verification recipe to local verification docs; do not make future sessions re-diagnose this as missing MCP servers. |
| 5 | STRONG | A11y · UX | 45m | 7 | 5 | 22.4 | **oracle-panel-browser-smoke** — Run the focused Oracle/browser specs after the contract fixes. They exercise Smart Insights, heatmap, lifecycle donut, Top Movers, chart hover, and nav coverage without running the whole slow matrix. |
| 6 | STRONG | Security · Process | 20m | 7 | 4 | 19.3 | **pre-closeout-secret-and-header-proof** — The earlier same-day sweep was clean, but implementation creates new staged artifacts. **Recipe:** run staged/all secret scans, `build:check`, all-page crawl, focused browser specs, and live header verification before closeout. |

## Innovation reserve

- `forge-forecast-unit-contract` — keeps the public prediction module honest while preserving the unique Oracle positioning.
- `all-page-crawl-build-gate` — route + blocking-script budget becomes a machine contract across every HTML page.

## Skipped

- **Critical CSS extraction** remains the biggest raw LCP lever, but it is intentionally deferred. The site already has hashed shell CSS; extracting critical CSS safely across 140+ pages should be a dedicated pass with screenshot comparison, not a late closeout patch.
- **Feedback identity linkage** remains privacy-gated by prior decision. No schema/auth promise changes without the explicit privacy decision.

## Bounded `/implement` order

1. Repair Oracle public-language contract.
2. Add Forge Forecast unit coverage.
3. Wire the all-page crawler into `build:check`.
4. Update local verification docs with the MCP `CODEX_HOME` recipe.
5. Run focused tests and closeout proof.

---

*Generated by `/audit` v1.0 · ready for `/implement`*

---

## Execution Log (S137 — /implement pass 1)

| Status | # | Slug | Note |
|---|---:|---|---|
| DONE | 1 | oracle-public-language-contract | Updated the inline `/oracle/` hover label from `commit/active repo/IGNIS` to public-facing `signal/world/cognition`, aligned the `assets/oracle-extra.js` header comment, and updated `tests/oracle-extra.spec.js` to assert public vocabulary. |
| DONE | 2 | all-page-crawl-build-gate | Wired `node scripts/crawl-all-pages.mjs` into `npm run build:check`. First run proved 144 HTML files, 0 status failures, 0 parser-blocking local-script findings. |
| DONE | 3 | forge-forecast-unit-contract | Added deterministic `computeForecasts()` tests for likely-to-ship, climbing-fast, awakening-from-rest, and vaulted/red exclusion behavior. |
| DONE | 4 | mcp-codex-home-runbook | Added `CODEX_HOME='<user-home>\.codex'` MCP verification recipe to `docs/LOCAL_VERIFY.md` so the sandbox-home split is no longer rediscovered as a missing-server problem. |
| DONE | 5 | oracle-panel-browser-smoke | `node scripts/run-local-browser-verify.mjs tests/oracle-extra.spec.js --workers=1 --timeout=90000` passed 5/5 after stabilizing the chart-hover test by dispatching a pointer event against the SVG bounds. |
| DONE | 6 | pre-closeout-secret-and-header-proof | `npm run build:check` passed. The optional external `claude.api` gateway readiness probe now skips when a sibling Studio Ops `CAPABILITY_MAP.json` is invalid JSON; after final rebase the external map was valid again and the readiness probe passed. |

## Verification Evidence

- `npx playwright test tests/oracle-insights.spec.js --project=chromium --workers=1 --timeout=60000` — 14 passed.
- `node scripts/run-local-browser-verify.mjs tests/oracle-extra.spec.js --workers=1 --timeout=90000` — 5 passed.
- `npm run build:check` — passed, including the new all-page crawl gate.
