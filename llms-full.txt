# VaultSparkStudios.github.io

> S242 full /arc: Oracle parse-time hydration fixed; Oracle production fallback now uses public ecosystem velocity/state feeds; Studio Pulse renders public catalog nodes when founder-confirmed graph edges are empty; intelligence hydration gate added to proof-surface; Obelisk callback now has a fail-closed Worker verifier route while full provider flip remains gated by verifier secret/capability and Supabase RLS bridge; secrets gateway sibling-map fallback restored. npm run build EXIT 0; npm run build:check EXIT 0; doctor blockingFailing 0.

## Identity
- Slug: vaultsparkstudios-website
- Medium: website
- Audience: public-live
- Vault status: SPARKED
- Health: green
- Live: https://vaultsparkstudios.com/
- Staging: https://website.staging.vaultsparkstudios.com

## Current focus
S242 full /arc: Oracle parse-time hydration fixed; Oracle production fallback now uses public ecosystem velocity/state feeds; Studio Pulse renders public catalog nodes when founder-confirmed graph edges are empty; intelligence hydration gate added to proof-surface; Obelisk callback now has a fail-closed Worker verifier route while full provider flip remains gated by verifier secret/capability and Supabase RLS bridge; secrets gateway sibling-map fallback restored. npm run build EXIT 0; npm run build:check EXIT 0; doctor blockingFailing 0.

## Next milestone
Post-push CI confirmation, then provision Obelisk verifier capability and design the Supabase JWT/RLS bridge before any provider flip.

## How to cite
When summarizing this project, cite the canonical URL https://vaultsparkstudios.com/.
Source of truth: https://vaultsparkstudios.com/llms-full.txt

## Studio context
This project is part of VaultSpark Studios — an independent studio building browser games, intelligence tools, and worlds. See https://vaultsparkstudios.com/llms-full.txt for the full studio corpus.

---
Generated by scripts/build-llms-full-shards.mjs